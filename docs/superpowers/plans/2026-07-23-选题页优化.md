# 选题页(第2步)优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复选题页的字号越界、收藏/归档无反馈、rank 行拥挤三个问题，使其符合项目事实字号刻度并提供清晰的收藏/归档反馈。

**Architecture:** 纯前端改动，锁定两个文件——`apps/web/src/styles.css`(字号 token + rank 两层布局 + 卡片视觉态)和 `apps/web/src/pages/IntelligentSimpleFlow.tsx`(toggleCollection 成功 toast、rank 行信息/操作两层 DOM、顶部计数、默认视图过滤归档)。所有状态由单一数据源 `opportunities` state 派生，不引入新状态。不碰后端、不碰数据层。

**Tech Stack:** React + TypeScript + Vite，纯 CSS(无 CSS-in-JS)，lucide-react 图标，项目自有 `useToast`/`Badge`/`Button` 组件。

## Global Constraints

- 工作仓库：`/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent`，分支 `feat/topic-diversity`(不是 content-agent-p0 worktree)。
- ToastKind 仅 `'success' | 'error' | 'info'` 三种，无 "default"；取消动作用 `'info'`。
- 事实字号刻度(CDP 实测)：`11 / 12 / 13 / 14 / 16`，h1=29。只固化这套已存在的刻度，不发明新档位。
- 只修选题页 <11px 的越界字号；不全站扫改。
- dev server 已在跑：web=5173(cwd `content-agent/apps/web`)，api=8780。修改 CSS/TSX 会经 Vite HMR 生效，无需重启。
- 无自动化测试框架用于该前端页面——每个任务的"测试"是 CDP 计算值断言 + 截图人工核验(harness 在 `/Users/a1234/.claude/jobs/14e7481a/tmp/`)。
- 提交粒度：每个 Task 末尾单独 commit。

---

## Task 1: 固化字号 Token 到 :root

**Files:**
- Modify: `apps/web/src/styles.css:7-31`(`:root` 块，在 `--shadow-md` 后追加字号 token)

**Interfaces:**
- Produces: CSS 变量 `--font-2xs: 11px`、`--font-xs: 12px`、`--font-sm: 13px`、`--font-md: 14px`、`--font-lg: 16px`、`--font-h1: 29px`。后续 Task 3/4 的选题页字号引用这些变量。

- [ ] **Step 1: 在 :root 末尾追加字号 token**

在 `apps/web/src/styles.css` 第 31 行 `  --shadow-md: 0 16px 45px rgb(25 38 31 / 10%);` 之后、`}` 之前插入：

```css
  --font-2xs: 11px;
  --font-xs: 12px;
  --font-sm: 13px;
  --font-md: 14px;
  --font-lg: 16px;
  --font-h1: 29px;
```

- [ ] **Step 2: 验证 CSS 语法未破坏**

Run: `cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent && npx --yes vite build --mode development 2>&1 | tail -5 || echo "build skipped"`
Expected: 无 CSS 解析报错(若 build 太慢可跳过，靠 HMR 无红屏即通过)。

- [ ] **Step 3: Commit**

```bash
cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent
git add apps/web/src/styles.css
git commit -m "feat(web): add font-size design tokens to :root"
```

---

## Task 2: 收藏/归档成功 toast + 顶部计数 + 计数联动筛选器

**Files:**
- Modify: `apps/web/src/pages/IntelligentSimpleFlow.tsx:449-457`(`toggleCollection` 补成功 toast)
- Modify: `apps/web/src/pages/IntelligentSimpleFlow.tsx:780-787`(panel header 加计数摘要)

**Interfaces:**
- Consumes: 现有 `toast`(useToast)、`opportunities` state、`collectionFilter` state + `setCollectionFilter`。
- Produces: `collectionCounts` 派生值 `{ collected: number; archived: number }`，供 header 摘要渲染。

- [ ] **Step 1: 给 toggleCollection 成功路径补 toast**

将 `apps/web/src/pages/IntelligentSimpleFlow.tsx:449-457` 的 `toggleCollection` 整体替换为：

```tsx
  const toggleCollection = async (item: TopicOpportunity, target: "collected" | "archived") => {
    const next = (item.collectionStatus ?? "active") === target ? "active" : target;
    try {
      const updated = await api.opportunities.setCollection(projectId, item.id, next);
      setOpportunities((current) => current.map((opp) => opp.id === updated.id ? updated : opp));
      const feedback: Record<"collected" | "archived" | "active", { text: string; kind: "success" | "info" }> = {
        collected: { text: "已收藏", kind: "success" },
        archived: { text: "已归档", kind: "success" },
        active: { text: target === "collected" ? "已取消收藏" : "已取消归档", kind: "info" },
      };
      toast.push(feedback[next].text, feedback[next].kind);
    } catch (error) {
      toast.push(error instanceof Error ? error.message : "操作失败", "error");
    }
  };
```

- [ ] **Step 2: 添加 collectionCounts 派生值**

在 `apps/web/src/pages/IntelligentSimpleFlow.tsx` 的 `visibleOpportunities`(约 377-380 行)之后追加：

```tsx
  const collectionCounts = useMemo(() => {
    let collected = 0;
    let archived = 0;
    for (const item of opportunities) {
      const status = item.collectionStatus ?? "active";
      if (status === "collected") collected += 1;
      else if (status === "archived") archived += 1;
    }
    return { collected, archived };
  }, [opportunities]);
```

- [ ] **Step 3: 在 panel header 渲染计数摘要（点击切筛选器）**

在 `apps/web/src/pages/IntelligentSimpleFlow.tsx:781` 的 `</p></div>` 里、最后一个 `<p className="opportunity-refresh-note">...</p>` 之后、闭合 `</div>` 之前，插入计数摘要（仅当有收藏或归档时显示）：

```tsx
{(collectionCounts.collected > 0 || collectionCounts.archived > 0) && (
  <p className="opportunity-collection-summary">
    <button type="button" onClick={() => setCollectionFilter("collected")}>★ 已收藏 {collectionCounts.collected}</button>
    <span aria-hidden="true">·</span>
    <button type="button" onClick={() => setCollectionFilter("archived")}>已归档 {collectionCounts.archived}</button>
  </p>
)}
```

- [ ] **Step 4: 加计数摘要样式**

在 `apps/web/src/styles.css` 的 `.opportunity-refresh-note strong`(991 行)之后追加：

```css
.opportunity-collection-summary { display: flex; align-items: center; gap: 8px; margin: 8px 0 0; font-size: var(--font-xs); color: var(--muted); }
.opportunity-collection-summary button { padding: 2px 8px; border: 1px solid var(--line); border-radius: 999px; background: var(--surface); color: var(--ink-2); font-size: var(--font-2xs); cursor: pointer; }
.opportunity-collection-summary button:hover { border-color: color-mix(in srgb, var(--brand) 45%, var(--line)); }
```

- [ ] **Step 5: CDP 验证 toast + 计数**

Run: `cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent && node /Users/a1234/.claude/jobs/14e7481a/tmp/collection-e2e.mjs`（该脚本在 Task 2 执行时新建：登录→打开 /generate→点第一张卡的收藏→断言出现 `.toast` 且文本含"已收藏"、header 出现 `.opportunity-collection-summary`）。
Expected: 输出 `{ toastText: "已收藏", summaryVisible: true, collectedCount: 1 }`。

- [ ] **Step 6: Commit**

```bash
cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent
git add apps/web/src/pages/IntelligentSimpleFlow.tsx apps/web/src/styles.css
git commit -m "feat(web): add collection/archive toast feedback and clickable count summary"
```

---

## Task 3: rank 行两层重排 + 字号重映射 + 卡片视觉态

**Files:**
- Modify: `apps/web/src/pages/IntelligentSimpleFlow.tsx:830`(rank 行 DOM 拆成信息层 + 操作层)
- Modify: `apps/web/src/styles.css:1510-1513`(rank 两层布局)
- Modify: `apps/web/src/styles.css:1497-1512`(卡片视觉态 + 越界字号)

**Interfaces:**
- Consumes: 现有 `rankView`(resolveOpportunityRankView)、`collectionStatus`、`toggleCollection`、`setEditingOpportunity`、`deleteOpportunity`；lucide 图标 `Star`/`Archive`/`Pencil`/`Trash2`(已 import)。
- Produces: DOM 类名 `.opportunity-card__rank-info`、`.opportunity-card__rank-actions`；卡片修饰类 `is-collected`/`is-archived`。

- [ ] **Step 1: 拆 rank 行 DOM 为信息层 + 操作层**

将 `apps/web/src/pages/IntelligentSimpleFlow.tsx:830` 那一整个 `<div className={`opportunity-card__rank ...`}>...</div>` 替换为：

```tsx
<div className={`opportunity-card__rank ${rankView.sortable ? "is-sortable" : "needs-review"}`}>
  <div className="opportunity-card__rank-info">
    <strong>{rankView.title}</strong>
    <Badge tone={rankView.sortable ? "positive" : "warning"}>{rankView.valueLabel}</Badge>
    {collectionStatus === "collected" && <span className="opportunity-card__state opportunity-card__state--collected"><Star size={11} className="filled" /> 已收藏</span>}
    {collectionStatus === "archived" && <span className="opportunity-card__state opportunity-card__state--archived"><Archive size={11} /> 已归档</span>}
    {rankView.unknownMetrics.length > 0 && <small>unknown：{rankView.unknownMetrics.map((metric) => metric).join("、")}</small>}
  </div>
  <div className="opportunity-card__rank-actions">
    <span className="opportunity-card__edit" role="button" tabIndex={0} title={collectionStatus === "collected" ? "取消收藏" : "收藏"} onClick={(event) => { event.stopPropagation(); void toggleCollection(item, "collected"); }} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.stopPropagation(); void toggleCollection(item, "collected"); } }}><Star size={13} className={collectionStatus === "collected" ? "filled" : ""} /> {collectionStatus === "collected" ? "取消收藏" : "收藏"}</span>
    <span className="opportunity-card__edit" role="button" tabIndex={0} title={collectionStatus === "archived" ? "取消归档" : "归档"} onClick={(event) => { event.stopPropagation(); void toggleCollection(item, "archived"); }} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.stopPropagation(); void toggleCollection(item, "archived"); } }}><Archive size={13} /> {collectionStatus === "archived" ? "取消归档" : "归档"}</span>
    <span className="opportunity-card__edit" role="button" tabIndex={0} onClick={(event) => { event.stopPropagation(); setEditingOpportunity(item); }} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.stopPropagation(); setEditingOpportunity(item); } }}><Pencil size={13} /> 编辑度量</span>
    <span className="opportunity-card__edit opportunity-card__edit--danger" role="button" tabIndex={0} onClick={(event) => { event.stopPropagation(); void deleteOpportunity(item); }} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.stopPropagation(); void deleteOpportunity(item); } }}><Trash2 size={13} /> 删除</span>
  </div>
</div>
```

- [ ] **Step 2: 给卡片加视觉态修饰类**

将 `apps/web/src/pages/IntelligentSimpleFlow.tsx:822` 的卡片 className 从：

```tsx
className={`opportunity-card ${selectedOpportunityId === item.id ? "selected" : ""}`}
```

改为：

```tsx
className={`opportunity-card ${selectedOpportunityId === item.id ? "selected" : ""} ${collectionStatus === "collected" ? "is-collected" : ""} ${collectionStatus === "archived" ? "is-archived" : ""}`}
```

- [ ] **Step 3: 重写 rank 两层布局 CSS**

将 `apps/web/src/styles.css:1510-1513` 这 4 行：

```css
.opportunity-card > .opportunity-card__rank { display: flex; align-items: center; gap: 6px; width: 100%; padding: 7px 8px; border-radius: 8px; background: var(--surface-soft); }
.opportunity-card__rank strong { font-size: 9px; color: var(--muted); }
.opportunity-card__rank small { flex-basis: 100%; color: var(--amber); font-size: 8px; }
.opportunity-card__rank.is-sortable strong { color: var(--green); }
```

替换为：

```css
.opportunity-card > .opportunity-card__rank { display: flex; flex-direction: column; gap: 6px; width: 100%; padding: 8px 10px; border-radius: 8px; background: var(--surface-soft); }
.opportunity-card__rank-info { display: flex; align-items: center; flex-wrap: wrap; gap: 6px; }
.opportunity-card__rank-info strong { font-size: var(--font-sm); color: var(--muted); }
.opportunity-card__rank.is-sortable .opportunity-card__rank-info strong { color: var(--green); }
.opportunity-card__rank-info small { flex-basis: 100%; color: var(--amber); font-size: var(--font-2xs); }
.opportunity-card__rank-actions { display: flex; flex-wrap: wrap; align-items: center; gap: 10px; padding-top: 6px; border-top: 1px solid var(--line); }
.opportunity-card__edit { display: inline-flex; align-items: center; gap: 3px; font-size: var(--font-2xs); color: var(--muted); cursor: pointer; }
.opportunity-card__edit:hover { color: var(--ink-2); }
.opportunity-card__edit--danger:hover { color: var(--red); }
.opportunity-card__state { display: inline-flex; align-items: center; gap: 3px; padding: 1px 7px; border-radius: 999px; font-size: var(--font-2xs); }
.opportunity-card__state--collected { color: var(--green); background: var(--green-soft); }
.opportunity-card__state--archived { color: var(--muted); background: var(--surface-soft); border: 1px solid var(--line); }
```

- [ ] **Step 4: 加卡片视觉态 + 修 ranking-boundary 越界字号**

将 `apps/web/src/styles.css:1509` 的 `.opportunity-ranking-boundary p { ... font-size: 10px; ... }` 中的 `font-size: 10px` 改为 `font-size: var(--font-xs)`。

在 `apps/web/src/styles.css:1499`(`.opportunity-card.selected {...}`)之后追加卡片视觉态：

```css
.opportunity-card.is-collected { border-color: color-mix(in srgb, var(--green) 45%, var(--line)); }
.opportunity-card.is-archived { opacity: .62; }
.opportunity-card.is-archived:hover { opacity: 1; }
```

- [ ] **Step 5: CDP 验证 rank 计算字号 + 行数**

Run: `cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent && node /Users/a1234/.claude/jobs/14e7481a/tmp/layout-audit.mjs 1024`
Expected: 输出中 `rank.rows` ≤ 2(信息层 + 操作层，不再是 4 行挤压)；无 `.opportunity-card__rank-info strong` 命中 <11px（改跑 font-audit 复核 rankStrong=13）。

- [ ] **Step 6: 截图人工核验（1280 + 1024）**

Run: `cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent && node /Users/a1234/.claude/jobs/14e7481a/tmp/shot-step2.mjs 1280 && node /Users/a1234/.claude/jobs/14e7481a/tmp/shot-step2.mjs 1024`
Expected: 生成 `step2-1280.png`/`step2-1024.png`，Read 查看 rank 行为清晰两层、标题不再 9px 蝇头字、收藏/归档态可见。

- [ ] **Step 7: Commit**

```bash
cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent
git add apps/web/src/pages/IntelligentSimpleFlow.tsx apps/web/src/styles.css
git commit -m "feat(web): restructure opportunity rank row into info/action layers with legal font sizes and collection visual states"
```

---

## Task 4: 默认视图过滤归档

**Files:**
- Modify: `apps/web/src/pages/IntelligentSimpleFlow.tsx:377-380`(`visibleOpportunities` 过滤逻辑)

**Interfaces:**
- Consumes: `opportunities` state、`collectionFilter` state(值域 `"all" | "active" | "collected" | "archived"`，定义在 :314)。
- Produces: 修改后的 `visibleOpportunities` — `"all"` 不再包含 archived。

- [ ] **Step 1: 修改 visibleOpportunities，让 "all" 排除已归档**

将 `apps/web/src/pages/IntelligentSimpleFlow.tsx:377-380`：

```tsx
const visibleOpportunities = useMemo(
  () => opportunities.filter((item) => collectionFilter === "all" || (item.collectionStatus ?? "active") === collectionFilter),
  [opportunities, collectionFilter],
);
```

替换为：

```tsx
const visibleOpportunities = useMemo(
  () => opportunities.filter((item) => {
    const status = item.collectionStatus ?? "active";
    if (collectionFilter === "all") return status !== "archived";
    return status === collectionFilter;
  }),
  [opportunities, collectionFilter],
);
```

- [ ] **Step 2: CDP 验证归档项从默认视图消失**

Run:
```bash
cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent
node -e '
const API="http://127.0.0.1:8780";
(async()=>{
  const lg=await fetch(API+"/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:"admin",password:"Admin-change-me-2026!"})});
  const sc=lg.headers.get("set-cookie");const s=/ca_session=([^;]+)/.exec(sc)[1];const c=/ca_csrf=([^;]+)/.exec(sc)[1];
  const H={cookie:`ca_session=${s}`};
  const pl=await (await fetch(API+"/api/projects",{headers:H})).json();const pid=(Array.isArray(pl)?pl:pl.data)[0].id;
  const op=await (await fetch(`${API}/api/projects/${pid}/topic-opportunities`,{headers:H})).json();
  const items=Array.isArray(op)?op:op.items||op.data;
  const oid=items[0].id;
  // archive it
  await fetch(`${API}/api/projects/${pid}/topic-opportunities/${oid}/collection`,{method:"POST",headers:{...H,"content-type":"application/json","x-csrf-token":c},body:JSON.stringify({status:"archived"})});
  const after=await (await fetch(`${API}/api/projects/${pid}/topic-opportunities`,{headers:H})).json();
  const arr=Array.isArray(after)?after:after.items||after.data;
  const target=arr.find(x=>x.id===oid);
  console.log("archived status persisted:", target.collectionStatus);
  // restore for idempotency
  await fetch(`${API}/api/projects/${pid}/topic-opportunities/${oid}/collection`,{method:"POST",headers:{...H,"content-type":"application/json","x-csrf-token":c},body:JSON.stringify({status:"active"})});
  console.log("restored to active");
})();
'
```
Expected: `archived status persisted: archived` then `restored to active`。前端过滤逻辑纯客户端，此步确认后端状态可持久化；默认视图排除靠 Step 1 的 filter，用 Task 3 的截图复核归档卡不在 "全部" 视图。

- [ ] **Step 3: 截图复核默认视图 + 已归档视图**

Run: `cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent && node /Users/a1234/.claude/jobs/14e7481a/tmp/shot-step2.mjs 1280`
Expected: Read `step2-1280.png` — "全部" 筛选下无「已归档」角标的卡片；点「已归档」筛选(需扩展 harness 或人工点)可见归档卡。

- [ ] **Step 4: Commit**

```bash
cd /Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent
git add apps/web/src/pages/IntelligentSimpleFlow.tsx
git commit -m "feat(web): exclude archived opportunities from default topic view"
```

---

## Self-Review

**Spec coverage:**
- 设计第 1 节(字号 token) → Task 1 + Task 3 Step 3/4(重映射) ✓
- 设计第 2 节(toast + 视觉态 + 计数) → Task 2(toast+计数) + Task 3 Step 1/2/4(视觉态) ✓
- 设计第 3 节(rank 两层重排) → Task 3 Step 1/3 ✓
- 设计第 4 节(归档语义/默认过滤) → Task 4 ✓

**Placeholder scan:** 无 TBD/TODO；每个改代码的 step 都给出完整前后代码块。✓

**Type consistency:**
- `toggleCollection(item, "collected" | "archived")` 签名在 Task 2/3 一致 ✓
- ToastKind 只用 `success`/`info`/`error`，无 "default" ✓
- 类名 `.opportunity-card__rank-info`/`-actions`/`__state`/`is-collected`/`is-archived` 在 TSX(Task 3 Step1/2)与 CSS(Task 3 Step3/4)两侧一致 ✓
- `collectionFilter` 值域 `all|active|collected|archived` 在 Task 4 与 :314 定义一致 ✓
- 字号变量 `--font-2xs/xs/sm` 在 Task 1 定义、Task 3 引用，名称一致 ✓

计划完整，无缺口。
