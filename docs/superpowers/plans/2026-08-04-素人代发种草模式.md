# Agent 创作素人代发种草模式 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Agent 创作通道产出贴合 67 篇真实对标语料的素人代发种草内容,而不是机构客服口吻。

**Architecture:** 新增 `HarnessSeedingMode` 模式开关(默认 `peer_seeding`),沿 `validateHarnessCandidates` 的 `constraints` 参数传入,校验按模式分叉。`brand_voice` 保留现有全部严格行为。改动全部限定在 harness 通道内,主线 `agent-core` 一个字不动。

**Tech Stack:** TypeScript;测试框架分裂——`packages/agent-harness-core` 与 `agent-core` 用 **vitest**,`apps/api`、`apps/web` 用 `node:test`。本计划的测试全在 harness-core,一律 vitest。npm workspaces(`@content-agent/agent-harness-core`、`@content-agent/api`、`@content-agent/web`)

## Global Constraints

**红线——只允许改这 7 个文件:**

```
packages/agent-harness-core/src/validation.ts
packages/agent-harness-core/src/types.ts
packages/agent-harness-core/src/runner.ts
packages/agent-harness-core/src/methods.ts
apps/api/src/agent-harness.service.ts
apps/web/src/pages/AgentHarnessPage.tsx
apps/web/src/types.ts        ← 只碰 harness 那一段
```

新建测试文件 `packages/agent-harness-core/src/validation.test.ts` 也允许。

另外允许改 `packages/agent-harness-core/src/runner.test.ts`:它的候选 fixture 在 `:59` 设了 `disclaimer`,删字段后该行会因 TS 多余属性报错(该文件在 `src/` 下,进 typecheck 范围)。只删那一处赋值,不动其余断言。

**绝对不碰:**

```
packages/agent-core/**                        主线引擎
apps/api/src/generation.service.ts
apps/api/src/export.service.ts
apps/api/src/intelligence.service.ts
apps/web/src/pages/IntelligentSimpleFlow.tsx
apps/web/src/pages/GenerationResultPage.tsx
apps/web/src/components/quick/**
apps/web/src/lib/utils.ts
apps/web/src/lib/validation-labels.ts
apps/api/src/generation-reader-view.ts
```

**主线有同名的 `Cref.disclaimer` 和自己的证据边界(`packages/agent-core/src/engine.ts`),同名不同文件,不许顺手一起改。**

- 默认模式 `peer_seeding`,不传 `seedingMode` 时走它。
- `brand_voice` 下所有既有校验行为逐字不变。
- 每个任务提交前跑 `git show --stat`,改动文件超出上述清单即违规。
- 提交只暂存本任务文件,禁止 `git add -A`(工作树有另一会话的未提交改动)。
- 注释与文案用中文,与仓库现有风格一致。
- 测试一律用 **vitest**(`import { test } from "vitest"`)——harness-core 的 `package.json` 是 `vitest run`,写成 `node:test` 会让 `npm test` 直接报 `No test suite found`。断言可继续用 `node:assert/strict`(已实测在 vitest 下正常工作,失败能被捕获)。不引入新依赖。
- 注释要写清「为什么」,不只写「做了什么」——放宽诚实校验必须留下可审计的理由。

---

### Task 1: 模式开关的类型与默认值

**Files:**
- Modify: `packages/agent-harness-core/src/methods.ts`(类型与常量**都**放这里,紧邻 `HarnessBodyLength` / `HARNESS_BODY_LENGTH_TARGETS` 那一对)
- Test: `packages/agent-harness-core/src/validation.test.ts`(新建)

`types.ts` **不改**。原计划让类型住 `types.ts`,但 `types.ts:1` 已经 `import type ... from "./methods.js"`,再让 `methods.ts` 反向导入就成了源码级循环;而 `HarnessBodyLength`(类型)与 `HARNESS_BODY_LENGTH_TARGETS`(常量)同处 `methods.ts` 正是现成先例。

**Interfaces:**
- Produces: `type HarnessSeedingMode = 'peer_seeding' | 'brand_voice'`、`const DEFAULT_HARNESS_SEEDING_MODE: HarnessSeedingMode = 'peer_seeding'` —— **两者都从 `./methods.js` 导出**。后续任务全部依赖这两个名字。

**为什么都放 `methods.ts`**:`methods.ts` 已经放着 `DEFAULT_HARNESS_METHOD_ID`、`HARNESS_BODY_LENGTH_TARGETS` 这类运行时常量,以及与它们配对的类型别名(`HarnessBodyLength`)。把新的类型+常量放在一起既有先例,也避免与 `types.ts` 形成互相导入。

`index.ts` 已经 `export * from` 这两个文件,不用改导出。

- [ ] **Step 1: 写失败测试**

新建 `packages/agent-harness-core/src/validation.test.ts`:

```ts
import assert from 'node:assert/strict';
import { test } from 'vitest';
import { DEFAULT_HARNESS_SEEDING_MODE, type HarnessSeedingMode } from './methods.js';

/*
  素人代发种草模式。默认开(peer_seeding),因为这个通道的实际用途就是给真人素人
  账号起草代发内容;brand_voice 保留原有的机构口吻严格校验。
*/
test('默认模式是素人代发', () => {
  const mode: HarnessSeedingMode = DEFAULT_HARNESS_SEEDING_MODE;
  assert.equal(mode, 'peer_seeding');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent" && npm test -w @content-agent/agent-harness-core -- src/validation.test.ts`

Expected: FAIL，报找不到 `DEFAULT_HARNESS_SEEDING_MODE` 导出。

- [ ] **Step 3: 加类型**

`packages/agent-harness-core/src/types.ts`,紧跟在 `export type HarnessThreadKind = ...` 那一行之后:

```ts
/**
 * 内容形态模式。
 *
 * `peer_seeding`(默认):素人代发种草。内容由真人素人账号发布、经历真实,AI 只是
 * 代笔起草,所以允许第一人称时间线叙述,评论区以博主本人回复为主。
 * `brand_voice`:机构口吻。保留原有全部严格校验——机构不能假装自己是顾客。
 */
export type HarnessSeedingMode = "peer_seeding" | "brand_voice";
```

- [ ] **Step 4: 加默认值常量**

`packages/agent-harness-core/src/methods.ts`,紧跟在 `export const DEFAULT_HARNESS_METHOD_ID = ...` 那一行之后:

```ts
/**
 * 默认走素人代发。
 *
 * 这个通道的实际用途是给真人素人账号起草代发内容,默认关掉会让每次运行都要手动
 * 勾选。默认开,但保留 brand_voice 这条路——通道里有 brand 发布身份和偏机构口吻
 * 的写法,那些用法不该被强制伪造成素人。
 */
export const DEFAULT_HARNESS_SEEDING_MODE: HarnessSeedingMode = "peer_seeding";
```

**实施更正(Task 1 已按此完成)**:类型和常量**都放 `methods.ts`**,`types.ts` 不动,也不加任何 import。

原因:`types.ts:1` 已经 `import type { ... } from "./methods.js"`,而 `methods.ts` 没有任何 import。把类型放 `types.ts` 再让 `methods.ts` 导回来会形成 `methods.ts ↔ types.ts` 源码级循环(类型-only,tsc 会擦除,但仍是本不该有的循环)。同文件先例是精确的:`HarnessBodyLength`(类型)与 `HARNESS_BODY_LENGTH_TARGETS`(常量)就相邻住在 `methods.ts`。

**后续任务注意**:`HarnessSeedingMode` 从 `./methods.js` 导入,不是 `./types.js`。

- [ ] **Step 5: 跑测试与 typecheck**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
npm run typecheck -w @content-agent/agent-harness-core
```
Expected: 测试 1/1 通过，typecheck 干净。

- [ ] **Step 6: 变异验证**

把 `DEFAULT_HARNESS_SEEDING_MODE` 改成 `"brand_voice"`,重跑测试——**必须变红**。改回来再跑一次确认恢复绿。

- [ ] **Step 7: 红线核对并提交**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
git add packages/agent-harness-core/src/types.ts packages/agent-harness-core/src/methods.ts packages/agent-harness-core/src/validation.test.ts
git diff --cached --name-only
git commit -m "feat(harness): 素人代发种草模式的类型与默认值"
git show --stat HEAD
```

`git diff --cached --name-only` 必须只列这三个文件。禁止 `git add -A`——工作树里有另一会话的未提交改动。

---

### Task 2: 评论区结构按模式分叉

**Files:**
- Modify: `packages/agent-harness-core/src/validation.ts`（拓扑要求 `:232`、身份字段要求 `:244-249`）
- Modify: `packages/agent-harness-core/src/runner.ts`（`THREAD_SCHEMA.required`，`:71`）
- Test: `packages/agent-harness-core/src/validation.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `HarnessSeedingMode`、`DEFAULT_HARNESS_SEEDING_MODE`
- Produces: `validateHarnessCandidates` 的 `constraints.seedingMode?: HarnessSeedingMode`

**背景（实施者必读）：** 这是整个计划的核心。现在 `validation.ts:232` 强制每套评论区至少 2 条 `org_answer`（机构答疑），且每条必须带 `clarification` / `nextStep` / `boundary` / `stopReason` 四个字段。实测 67 篇真实语料里博主回复 187 行、带机构口吻的只有 1 行（0%），所以这条强制要求就是产出像客服话术的直接原因。

- [ ] **Step 1: 写失败测试（6 条）**

追加到 `packages/agent-harness-core/src/validation.test.ts`。先在文件顶部补一个可复用的 fixture 工厂（放在已有 import 之后）：

```ts
import { validateHarnessCandidates } from './validation.js';
import type { HarnessCandidate, HarnessCommentThread } from './types.js';

/**
 * 只为评论区拓扑测试服务的最小候选。
 *
 * 不复用 runner.test.ts 那个完整 fixture:那份为了跑通全链路把每个字段都填满了,
 * 在这里会让「哪个字段导致报错」变得看不出来。这里只填拓扑相关字段,其余给空值,
 * 断言时按 issue code 过滤,不看总数。
 */
function thread(overrides: Partial<HarnessCommentThread> & { id: string }): HarnessCommentThread {
  return {
    threadKind: 'org_answer', displayName: '路人甲', replyDisplayName: '',
    question: '这个多久能好', answer: '大概两周',
    followUps: [], clarification: '', nextStep: '', boundary: '',
    stopReason: 'answered', postingIdentity: 'publisher', evidenceIds: [],
    ...overrides,
  };
}

/** 一条结构完整的机构答疑:四个字段齐全。 */
function orgThread(id: string, identity: HarnessCommentThread['postingIdentity'] = 'staff'): HarnessCommentThread {
  return thread({
    id, postingIdentity: identity,
    clarification: '个体差异不能一概而论', nextStep: '到院面诊评估', boundary: '不构成医疗建议',
    stopReason: 'professional_review',
  });
}

/** 博主本人以素人身份回复:不带那四个字段。 */
function authorThread(id: string, question: string, answer: string): HarnessCommentThread {
  return thread({ id, postingIdentity: 'author', question, answer });
}

function readerExchange(id: string): HarnessCommentThread {
  return thread({
    id, threadKind: 'reader_exchange', replyDisplayName: '路人乙',
    question: '我也卡在这一步', answer: '对,我准备先去问问',
    stopReason: 'no_new_gap',
  });
}

function organicReaction(id: string): HarnessCommentThread {
  return thread({
    id, threadKind: 'organic_reaction', question: '先码住',
    answer: '', stopReason: undefined as unknown as HarnessCommentThread['stopReason'],
  });
}

/** 只取评论拓扑相关的 issue code,避免被无关必填项的报错干扰。 */
function codesOf(threads: HarnessCommentThread[], seedingMode?: 'peer_seeding' | 'brand_voice'): string[] {
  const candidate = { candidateIndex: 0, content: { Cref: { threads } } } as unknown as HarnessCandidate;
  return validateHarnessCandidates([candidate], [], new Set<string>(), {
    ...(seedingMode ? { seedingMode } : {}),
  }).filter((issue) => issue.candidateIndex === 0).map((issue) => issue.code);
}
```

然后五条用例：

```ts
test('peer_seeding:2 条博主回复 + 1 接话 + 1 短反应 → 拓扑合格', () => {
  const codes = codesOf([
    authorThread('t1', '是哪个白白哦？', '老朱，朱冠锋呀'),
    authorThread('t2', '价格多少', '看方案，5k到1w'),
    readerExchange('t3'),
    organicReaction('t4'),
  ], 'peer_seeding');
  assert.ok(!codes.includes('comment_topology'), `不该报拓扑错误：${codes.join(',')}`);
});

test('peer_seeding:0 条博主回复 → comment_topology 报错', () => {
  const codes = codesOf([
    orgThread('t1'), orgThread('t2'), readerExchange('t3'), organicReaction('t4'),
  ], 'peer_seeding');
  assert.ok(codes.includes('comment_topology'), '素人模式下没有博主回复必须报错');
});

test('peer_seeding:博主回复缺 boundary/nextStep 不报错', () => {
  const codes = codesOf([
    authorThread('t1', '是哪个白白哦？', '老朱，朱冠锋呀'),
    authorThread('t2', '价格多少', '看方案，5k到1w'),
    readerExchange('t3'), organicReaction('t4'),
  ], 'peer_seeding');
  for (const code of ['missing_thread_boundary', 'missing_thread_next_step', 'missing_thread_clarification', 'missing_thread_stop_reason']) {
    assert.ok(!codes.includes(code), `博主素人回复不该要求 ${code}`);
  }
});

test('peer_seeding:机构身份线程仍必须带边界与下一步', () => {
  /*
   * 这条是防止「素人模式」被当成万能借口:机构一开口就得带边界,
   * 否则模型会用 staff 身份说机构话、又省掉所有约束。
   */
  const bare = thread({ id: 't2', postingIdentity: 'staff' });
  const codes = codesOf([
    authorThread('t1', '是哪个白白哦？', '老朱，朱冠锋呀'), bare,
    readerExchange('t3'), organicReaction('t4'),
  ], 'peer_seeding');
  assert.ok(codes.includes('missing_thread_boundary'), '机构身份缺边界必须报错');
  assert.ok(codes.includes('missing_thread_next_step'), '机构身份缺下一步必须报错');
});

test('brand_voice:现状不变,2 条机构答疑合格且博主回复不计入', () => {
  const ok = codesOf([orgThread('t1'), orgThread('t2'), readerExchange('t3'), organicReaction('t4')], 'brand_voice');
  assert.ok(!ok.includes('comment_topology'), `品牌模式下 2 条机构答疑应合格：${ok.join(',')}`);
  const authorOnly = codesOf([
    authorThread('t1', '是哪个白白哦？', '老朱，朱冠锋呀'),
    authorThread('t2', '价格多少', '看方案，5k到1w'),
    readerExchange('t3'), organicReaction('t4'),
  ], 'brand_voice');
  assert.ok(authorOnly.includes('comment_topology'), '品牌模式下博主回复不能替代机构答疑');
});

test('THREAD_SCHEMA 不再把四个机构字段列为必填', () => {
  /*
   * 只放宽 validation.ts 不够。runner.ts 的 THREAD_SCHEMA.required 里含这四个键时,
   * 模型在结构上就被逼着为每条博主回复编出「可核验的下一步」和「停止原因」——
   * 客服味正是这么来的,而上面那条「不报错」的测试会假绿(校验不查了,但模型仍在输出)。
   *
   * 用源码断言而非行为断言:schema 是喂给模型的常量,单测里没有模型可跑。
   */
  const source = readFileSync(new URL('./runner.ts', import.meta.url), 'utf8');
  const match = source.match(/const THREAD_SCHEMA = \{[\s\S]*?required: \[([^\]]+)\]/u);
  assert.ok(match, '没解析到 THREAD_SCHEMA 的 required 数组');
  const required = match![1];
  for (const key of ['clarification', 'nextStep', 'stopReason', 'boundary']) {
    assert.ok(!required.includes(`"${key}"`), `${key} 仍在 THREAD_SCHEMA.required 里,模型仍会被逼着输出它`);
  }
  // 仍须保留在 properties 里:允许不填，不等于不许填
  for (const key of ['clarification', 'nextStep', 'stopReason', 'boundary']) {
    assert.ok(source.includes(`${key}:`), `${key} 不该从 properties 里删掉`);
  }
});
```

这条测试要在文件顶部加一行 `import { readFileSync } from 'node:fs';`。

- [ ] **Step 2: 跑测试确认失败**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
```
Expected: 新增 5 条里至少 3 条红（第 1、3 条因拓扑与字段要求而红；第 2 条此时可能已绿，因为它期望的报错恰好是现状行为——这不算问题，Step 4 之后它必须仍绿）。

- [ ] **Step 3: 给 constraints 加 seedingMode**

`validation.ts` 的 `validateHarnessCandidates` 签名（当前 `:70-76`）里，在 `bodyLength` 后面追加一个字段：

```ts
    revisionInstruction?: string; selectedImages?: readonly HarnessImageSource[]; bodyLength?: "short" | "medium" | "long";
    /** 素人代发种草模式。缺省走 DEFAULT_HARNESS_SEEDING_MODE(peer_seeding)。 */
    seedingMode?: HarnessSeedingMode;
```

顶部 import 补上类型与默认值：

```ts
import { DEFAULT_HARNESS_SEEDING_MODE, HARNESS_BODY_LENGTH_TARGETS } from "./methods.js";
import type { HarnessSeedingMode } from "./methods.js";
```

函数体开头（`const issues` 那一行之后）解析出模式：

```ts
  const seedingMode = constraints.seedingMode ?? DEFAULT_HARNESS_SEEDING_MODE;
  const peerSeeding = seedingMode === "peer_seeding";
```

- [ ] **Step 4: 拓扑要求按模式分叉**

把现在的拓扑判断（`validation.ts:232-234`，`kindCount("org_answer") < 2` 那一段）替换成：

```ts
      /*
       * 拓扑要求按模式分叉。
       *
       * brand_voice 保持原样:至少 2 条机构答疑。
       * peer_seeding 改成至少 2 条**博主本人回复**(org_answer + postingIdentity==='author'),
       * 机构答疑 0 条也合格。依据:67 篇真实语料里博主回复 187 行,带机构口吻的只有 1 行,
       * 而原规则强制 ≥2 条机构答疑,正是产出像客服话术的直接原因。
       */
      const authorReplies = typedThreads.filter((item) => item.threadKind === "org_answer" && item.postingIdentity === "author").length;
      const accountableAnswers = typedThreads.filter((item) => item.threadKind === "org_answer" && item.postingIdentity !== "author").length;
      const answerShortfall = peerSeeding ? authorReplies < 2 : accountableAnswers < 2;
      if (typedThreads.length !== candidate.content.Cref.threads.length || answerShortfall || kindCount("reader_exchange") < 1 || kindCount("organic_reaction") < 1) {
        add(index, "comment_topology", "error", peerSeeding
          ? "素人种草评论区必须混排至少 2 条博主本人回复、1 条读者接话和 1 条短反应。"
          : "新评论关系网必须混排至少 2 条机构答疑、1 条读者接话和 1 条短反应。");
      }
```

注意 `kindCount("org_answer")` 在 `brand_voice` 下的语义变化：原来它把 `author` 身份也算进机构答疑，现在 `accountableAnswers` 明确排除了 `author`。这是有意收紧——`brand_voice` 的本意就是「机构口吻答疑」，`author` 身份不该充数。

- [ ] **Step 5: 四个字段要求按身份放宽**

把 `validation.ts:244-249`（`if (threadKind === "org_answer") { ... }` 那一段）替换成：

```ts
      if (threadKind === "org_answer") {
        if (!thread.answer.trim()) add(index, "empty_thread", "error", "机构答疑必须包含直接回答。");
        /*
         * 澄清/下一步/边界/停止原因只对**可追责的机构身份**强制。
         *
         * peer_seeding 下博主以 author 身份回复自己的帖子,那是朋友聊天的口气
         * (「是哪个白白哦？」→「老朱，朱冠锋呀」),硬塞这四样就会写成客服话术。
         * 但机构一开口仍然全部要求 —— 换模式不该让机构失去边界。
         */
        const accountable = !(peerSeeding && thread.postingIdentity === "author");
        if (accountable) {
          if (!thread.clarification?.trim()) add(index, "missing_thread_clarification", "error", "机构答疑必须说明澄清内容或不可判断范围。");
          if (!thread.nextStep?.trim()) add(index, "missing_thread_next_step", "error", "机构答疑必须给出可核验的下一步。");
          if (!thread.boundary?.trim()) add(index, "missing_thread_boundary", "error", "机构答疑必须在相关结论附近显示边界。");
          if (!thread.stopReason) add(index, "missing_thread_stop_reason", "error", "机构答疑必须说明停止原因，避免机械追加追问。");
        }
      } else if (threadKind === "reader_exchange") {
```

**同一步还要改 JSON schema，只改校验会假绿。** `runner.ts:71` 的 `THREAD_SCHEMA.required` 里含这四个键——schema 层面就是必填，模型仍会被逼着为每条博主回复编出「可核验的下一步」和「停止原因」，客服味就是这么来的。

把 `runner.ts:71` 那行 `required` 改成（只去掉这四个键，其余顺序不动；`properties` 保持不变，仍允许输出）：

```ts
  required: ["id", "threadKind", "displayName", "replyDisplayName", "question", "answer", "followUps", "postingIdentity", "evidenceIds"],
```

这是**两种模式共用的一份 schema**，所以 `brand_voice` 的强制性由上面 `validation.ts` 的 `accountable` 继续保证——那边四个字段缺失照旧 ERROR，行为不变。schema 只负责「允许不填」，校验负责「该填时必须填」。

补一条源码断言测试（追加到 `validation.test.ts`，第 6 条）：

```ts
test('THREAD_SCHEMA 不再强制四个机构字段:只改校验会被 schema 假绿', () => {
  /*
   * 这四个键留在 required 里的话,模型必须为每条博主回复编出「可核验的下一步」
   * 和「停止原因」——校验放宽了也没用,产出照旧是客服话术。
   * brand_voice 的强制性由 validation.ts 的 accountable 分支保证,不靠 schema。
   */
  const source = readFileSync(new URL('./runner.ts', import.meta.url), 'utf8');
  const required = source.match(/required: \[([^\]]*"postingIdentity"[^\]]*)\]/u);
  assert.ok(required, '没找到 THREAD_SCHEMA 的 required 数组');
  for (const key of ['clarification', 'nextStep', 'stopReason', 'boundary']) {
    assert.doesNotMatch(required[1], new RegExp(`"${key}"`, 'u'), `${key} 仍在 THREAD_SCHEMA.required 里`);
  }
  // properties 里必须保留,否则模型连想填都填不了
  for (const key of ['clarification', 'nextStep', 'stopReason', 'boundary']) {
    assert.match(source, new RegExp(`${key}: \\{`, 'u'), `${key} 从 properties 里被误删`);
  }
});
```

该测试需要在文件顶部补 `import { readFileSync } from 'node:fs';`（若尚未导入）。

- [ ] **Step 6: 跑测试确认全绿**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
npm run typecheck -w @content-agent/agent-harness-core
npm test -w @content-agent/agent-harness-core
```
Expected: `validation.test.ts` 全绿；typecheck 干净；**`runner.test.ts` 也必须仍全绿**——它那份 fixture 的 `org_answer` 线程用的是 `postingIdentity: 'publisher'`，在 `peer_seeding` 默认下会因「0 条博主回复」而报 `comment_topology`。若它红了，说明它需要显式传 `seedingMode: 'brand_voice'`，或把 fixture 的两条机构答疑改成 `author` 身份——**选前者**，因为那份 fixture 的本意是测完整机构答疑契约，不该被本次改动改变语义。

- [ ] **Step 7: 变异验证**

逐个做，每次改完跑 `npm test -w @content-agent/agent-harness-core -- src/validation.test.ts`，确认指定用例变红后立刻改回：

| 变异 | 应变红 |
|---|---|
| 去掉 `postingIdentity === "author"` 判断（`authorReplies` 改成统计所有 org_answer） | 第 2 条 |
| 把 `accountable` 恒为 `true` | 第 3 条 |
| 把 `answerShortfall` 恒用 `peerSeeding` 分支（忽略 brand_voice） | 第 5 条 |
| 把 `"clarification"`、`"nextStep"`、`"stopReason"`、`"boundary"` 放回 `THREAD_SCHEMA.required` | 第 6 条 |

每次改回后用 `git diff packages/agent-harness-core/src/validation.ts` 确认已恢复（应无输出）。

- [ ] **Step 8: 红线核对并提交**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
git add packages/agent-harness-core/src/validation.ts packages/agent-harness-core/src/runner.ts packages/agent-harness-core/src/validation.test.ts
git diff --cached --name-only
git commit -m "feat(harness): 素人模式下评论区改成博主回复为主"
git show --stat HEAD
```

若 Step 6 需要改 `runner.test.ts`，把它一起 `git add`（它在允许清单里）。

---

### Task 3: 免责声明移出交付字段

**Files:**
- Modify: `packages/agent-harness-core/src/types.ts:141`（删 `disclaimer`）
- Modify: `packages/agent-harness-core/src/runner.ts`（schema `:127-129`、长度表 `:289`、组装 `:380`、提示词）
- Modify: `packages/agent-harness-core/src/validation.ts`（删 `:157` 的 ERROR、`:37` 的阻断码）
- Modify: `packages/agent-harness-core/src/runner.test.ts:59`（fixture 去掉该字段）
- Modify: `apps/api/src/agent-harness.service.ts:1233`（导出改常量）
- Modify: `apps/web/src/pages/AgentHarnessPage.tsx:215,307`（界面固定提示条）
- Modify: `apps/web/src/types.ts`（**只改 harness 那一段**）
- Test: `packages/agent-harness-core/src/validation.test.ts`

**Interfaces:**
- Produces: `HARNESS_SIMULATION_NOTICE` 常量（`methods.ts` 导出），供 API 导出与前端提示条复用

**背景：** `Cref.disclaimer` 现在是交付字段，模型每次生成「以下为多角色评论情景演练…不代表真实用户发言」这句话，而它会直接出现在用户要粘贴到小红书的评论区里。这句话的读者是操盘手，不是小红书用户。

**⚠️ 红线警告：** `apps/web/src/types.ts` 里有**两个** `disclaimer` 字段——`:1776` 附近是 harness 的，另一处属于主线 `ContentPackage.Cref`。只改 harness 那一段。改之前先用 `grep -n "disclaimer" apps/web/src/types.ts` 看清两处的上下文。

- [ ] **Step 1: 写失败测试**

追加到 `validation.test.ts`：

```ts
test('交付结构不再有 disclaimer 字段,校验不因此报错', () => {
  /*
   * disclaimer 已从交付字段移除:那句话是给操盘手看的标注,
   * 不该混进用户要粘贴出去的评论区。旧的 comment_disclaimer ERROR 一并删除。
   */
  const codes = codesOf([
    authorThread('t1', '是哪个白白哦？', '老朱，朱冠锋呀'),
    authorThread('t2', '价格多少', '看方案，5k到1w'),
    readerExchange('t3'), organicReaction('t4'),
  ], 'peer_seeding');
  assert.ok(!codes.includes('comment_disclaimer'), 'comment_disclaimer 应已删除');
});

test('模拟提示语作为常量存在,不由模型生成', () => {
  assert.match(HARNESS_SIMULATION_NOTICE, /模拟/u);
  assert.match(HARNESS_SIMULATION_NOTICE, /不是|不代表/u);
});
```

顶部 import 补 `HARNESS_SIMULATION_NOTICE`：

```ts
import { DEFAULT_HARNESS_SEEDING_MODE, HARNESS_SIMULATION_NOTICE } from './methods.js';
```

- [ ] **Step 2: 跑测试确认失败**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
```
Expected: FAIL —— `HARNESS_SIMULATION_NOTICE` 未导出。

- [ ] **Step 3: 加常量**

`methods.ts` 末尾追加：

```ts
/**
 * 模拟问答的操盘标注。
 *
 * 这句话的读者是操盘手,不是小红书用户,所以它**不进交付字段**:原先它住在
 * Cref.disclaimer 里,由模型每次生成,结果会直接出现在用户要粘贴出去的评论区。
 * 改成常量后界面固定显示、导出固定携带,且不会因模型漏写而消失。
 */
export const HARNESS_SIMULATION_NOTICE = "这些是模拟问答参考，用来安排评论区节奏，不是已经发生的真实互动。";
```

- [ ] **Step 4: 删交付字段与校验**

1. `types.ts:141` —— 从 `Cref` 里删掉 `disclaimer: string;` 那一行。
2. `runner.ts:127-129` —— `required` 数组去掉 `"disclaimer"`，`properties` 去掉 `disclaimer: { type: "string" },`。
3. `runner.ts:289` —— 长度上限表删掉 `["disclaimer", cref.disclaimer, 1_000],` 那一项。
4. `runner.ts:380` —— 组装 `Cref` 时删掉 `disclaimer: String(cref.disclaimer ?? "").trim(),`。
5. `validation.ts:156-157` —— 删掉读 `disclaimer` 的那两行（`const disclaimer = ...` 与紧随的 `if (!/(参考|模板)/u...)`）。
6. `validation.ts:37` —— `simulationBlocked` 的码数组里去掉 `"comment_disclaimer",`。
7. `runner.ts` 提示词里若有要求模型输出 disclaimer 的句子，删掉；用 `grep -n "disclaimer" packages/agent-harness-core/src/runner.ts` 确认无残留。

- [ ] **Step 5: 修 fixture 与下游**

1. `runner.test.ts:59` —— fixture 的 `Cref` 去掉 `disclaimer: '以下为模拟问答参考模板，不代表真实互动。',`。
2. `apps/api/src/agent-harness.service.ts:1233` —— `Cref.disclaimer` 改成 `HARNESS_SIMULATION_NOTICE`，并在该文件顶部已有的 `@content-agent/agent-harness-core` import 里补上这个名字。
3. `apps/web/src/types.ts` —— harness 那一段的 `Cref` 去掉 `disclaimer: string;`。**先 grep 确认改的是 harness 那一处，不是主线 ContentPackage。**
4. `apps/web/src/pages/AgentHarnessPage.tsx:307` —— `<p className="harness-disclaimer">{Cref.disclaimer}</p>` 改成写死的提示条：

```tsx
      {/*
        模拟提示语写死在前端:它是给操盘手看的标注,不是交付内容的一部分。
        写死还有一个好处——不会因为模型这次漏写就消失。
      */}
      <p className="harness-disclaimer">这些是模拟问答参考，用来安排评论区节奏，不是已经发生的真实互动。</p>
```

5. `apps/web/src/pages/AgentHarnessPage.tsx:215` —— 复制到剪贴板的 Markdown 里 `Cref.disclaimer` 改成同一句字面量。

- [ ] **Step 6: 跑全量与 typecheck**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core
npm run typecheck -w @content-agent/agent-harness-core
npm run typecheck -w @content-agent/api
npm run typecheck -w @content-agent/web
npm test -w @content-agent/web
npm run build -w @content-agent/web
```
Expected: 全部干净。**typecheck 是这一步的主要保护**——删字段后所有残留引用都会编译报错，逐个修掉即为完整。

- [ ] **Step 7: 变异验证**

把 `HARNESS_SIMULATION_NOTICE` 改成 `"参考"`（去掉「模拟」二字），重跑 `validation.test.ts` —— 第 2 条测试必须变红。改回。

- [ ] **Step 8: 红线核对并提交**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
git add packages/agent-harness-core/src/types.ts packages/agent-harness-core/src/methods.ts \
        packages/agent-harness-core/src/runner.ts packages/agent-harness-core/src/validation.ts \
        packages/agent-harness-core/src/runner.test.ts packages/agent-harness-core/src/validation.test.ts \
        apps/api/src/agent-harness.service.ts apps/web/src/pages/AgentHarnessPage.tsx apps/web/src/types.ts
git diff --cached --name-only
git commit -m "feat(harness): 模拟提示语移出交付字段,改为界面固定提示"
git show --stat HEAD
```

`git diff --cached --name-only` 必须只列上面 9 个文件。特别核对 `apps/web/src/types.ts` 的 diff **只含 harness 那一段**：

```bash
git diff --cached apps/web/src/types.ts
```

---

### Task 4: 第一人称拆两支

**Files:**
- Modify: `packages/agent-harness-core/src/validation.ts:6`（拆正则）、命中处
- Test: `packages/agent-harness-core/src/validation.test.ts`

**Interfaces:**
- Consumes: Task 2 的 `peerSeeding` 布尔量

**背景：** 现在一条正则同时管两类东西。实测 67 篇语料（扫正文**加评论区**，即 `visibleCandidateText` 的真实范围）：

```
亲测 / 我做过 / 我用过 / 我体验过 / 朋友做过 / 闺蜜做过 / 真实顾客 / 真实客户   0 篇
我(或朋友/闺蜜/同事/姐妹/家人)…刚做|做完|做了|做的|术后|恢复                  13 篇
```

命中全部集中在后一支，也就是朴素的时间线叙述。前一支是营销化的口碑话术，两种模式都该禁。

- [ ] **Step 1: 写失败测试**

`validation.test.ts` 追加。这两条要看整篇可见文本，所以需要一个能设正文的 fixture：

```ts
/** 只为正文措辞测试服务:把待测句子放进正文,评论区给一套合格拓扑。 */
function codesForBody(body: string, seedingMode: 'peer_seeding' | 'brand_voice'): string[] {
  const candidate = {
    candidateIndex: 0,
    content: {
      N: { coverHeadline: '', coverSubheadline: '', imageSequence: [], title: '', body, callToAction: '' },
      H: { hashtags: [] },
      Cref: {
        ownedFirstComment: '',
        threads: [
          authorThread('t1', '是哪个白白哦？', '老朱，朱冠锋呀'),
          authorThread('t2', '价格多少', '看方案，5k到1w'),
          readerExchange('t3'), organicReaction('t4'),
        ],
      },
    },
  } as unknown as HarnessCandidate;
  return validateHarnessCandidates([candidate], [], new Set<string>(), { seedingMode })
    .filter((issue) => issue.candidateIndex === 0).map((issue) => issue.code);
}

test('peer_seeding:朴素时间线叙述允许', () => {
  /*
   * 「我做完两天了」是真人自然叙述,不是营销话术。内容由真人素人账号发布、
   * 经历真实,AI 只是代笔起草,所以素人模式放开这一支。
   * 实测 67 篇语料里这一支命中 13 篇(19%),原规则会把它们全判死。
   */
  const codes = codesForBody('我做完两天了，确实不肿。', 'peer_seeding');
  assert.ok(!codes.includes('fabricated_experience'), `素人模式不该拦时间线叙述：${codes.join(',')}`);
});

test('brand_voice:时间线叙述仍然阻断', () => {
  const codes = codesForBody('我做完两天了，确实不肿。', 'brand_voice');
  assert.ok(codes.includes('fabricated_experience'), '品牌口吻下机构不能假装自己是顾客');
});

test('营销口碑话术两种模式都阻断', () => {
  for (const mode of ['peer_seeding', 'brand_voice'] as const) {
    const codes = codesForBody('亲测有效，真实顾客都说好。', mode);
    assert.ok(codes.includes('fabricated_experience'), `${mode} 下「亲测」必须阻断`);
  }
});
```

- [ ] **Step 2: 跑测试确认失败**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
```
Expected: 第 1 条红（现状会拦时间线叙述），另两条绿。

- [ ] **Step 3: 拆正则**

`validation.ts:6` 那一行替换成两条：

```ts
/*
 * 伪造口碑的营销话术。两种模式都是 ERROR。
 *
 * 实测 67 篇真实对标语料里这一支命中 0 篇 —— 真人不这么说话,
 * 「亲测」「真实顾客」是广告文案的措辞,该一直禁。
 */
const FABRICATED_TESTIMONIAL = /(亲测|我做过|我用过|我体验过|朋友做过|闺蜜做过|真实顾客|真实客户)/u;
/*
 * 第一人称时间线叙述。peer_seeding 放开,brand_voice 仍 ERROR。
 *
 * 「我做完两天了」这类朴素叙述在 67 篇语料里命中 13 篇(19%),是这批内容的常态。
 * 放开的依据:内容由真人素人账号发布、经历真实,AI 只是代笔起草。
 * brand_voice 下保持阻断 —— 机构口吻不能假装自己是顾客。
 */
const FIRST_PERSON_TIMELINE = /(?:我|朋友|闺蜜|同事|姐妹|家人).{0,12}(?:刚做|做完|做了|做的|术后|恢复)/u;
```

- [ ] **Step 4: 命中处按模式分叉**

找到 `if (EXPERIENCE_CLAIM.test(visible)) add(index, "fabricated_experience", ...)` 那一行（在 `validation.ts` 的 `HARD_SELL_LANGUAGE` 检查附近），替换成：

```ts
    if (FABRICATED_TESTIMONIAL.test(visible) || (!peerSeeding && FIRST_PERSON_TIMELINE.test(visible))) {
      add(index, "fabricated_experience", "error", peerSeeding
        ? "可见内容出现「亲测/真实顾客」这类伪造口碑的营销措辞。"
        : "可见内容包含可能伪装真实经历或口碑的措辞。");
    }
```

用 `grep -n "EXPERIENCE_CLAIM" packages/agent-harness-core/src/validation.ts` 确认无残留引用。

- [ ] **Step 5: 跑测试与 typecheck**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
npm test -w @content-agent/agent-harness-core
npm run typecheck -w @content-agent/agent-harness-core
```
Expected: 全绿。

- [ ] **Step 6: 变异验证**

| 变异 | 应变红 |
|---|---|
| 把两条正则合回一条（`FABRICATED_TESTIMONIAL` 里塞回时间线分支） | 第 1 条 |
| 去掉 `!peerSeeding &&` 前置条件（改成始终检查时间线） | 第 1 条 |
| 把 `FABRICATED_TESTIMONIAL` 检查也加上 `!peerSeeding &&` | 第 3 条 |

每次改回后 `git diff packages/agent-harness-core/src/validation.ts` 确认恢复。

- [ ] **Step 7: 红线核对并提交**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
git add packages/agent-harness-core/src/validation.ts packages/agent-harness-core/src/validation.test.ts
git diff --cached --name-only
git commit -m "feat(harness): 第一人称拆成口碑话术与时间线两支"
git show --stat HEAD
```

---

### Task 5: 标签禁品牌词

**Files:**
- Modify: `packages/agent-harness-core/src/validation.ts`
- Test: `packages/agent-harness-core/src/validation.test.ts`

**Interfaces:**
- Consumes: `seedingMode` from Task 1, `peerSeeding` local from Task 2.
- Produces: `constraints.projectName?: string` — a new optional constraint field. Task 6 wires the two call sites to pass it.

**背景（实施者必读）：** 语料里 67 篇无一篇标签带品牌名，全是 `#成都眼袋#` `#眼袋泪沟#` 这类品类词加城市词。品牌词一出现，帖子立刻不像素人发的。

判据用「本轮项目名」做子串比对，**不维护关键词表**。这是 WARNING 而非 ERROR：项目名可能恰好含通用词（比如项目就叫「眼袋」），硬拦会误伤。给提示、不阻断。

- [ ] **Step 1: 写失败测试**

追加到 `packages/agent-harness-core/src/validation.test.ts`：

```ts
test('peer_seeding:标签含项目名给 WARNING,不阻断导出', () => {
  const candidate = peerCandidate();
  candidate.content.H.hashtags = ['#成都眼袋', '#星零感微孔去眼袋'];
  const issues = validateHarnessCandidates([candidate], EVIDENCE, DISCLOSED, {
    ...BASE, expectedCandidateCount: 1, projectName: '星零感微孔去眼袋',
  });
  const brandIssues = issues.filter((issue) => issue.code === 'brand_hashtag');
  assert.equal(brandIssues.length, 1, JSON.stringify(issues));
  assert.equal(brandIssues[0]?.severity, 'warning', '品牌词必须是 WARNING,项目名可能含通用词,硬拦会误伤');
  // 不阻断:导出只看 error
  assert.ok(!issues.some((issue) => issue.severity === 'error'), JSON.stringify(issues));
});

test('peer_seeding:标签只有品类词与城市词不报品牌词', () => {
  const candidate = peerCandidate();
  candidate.content.H.hashtags = ['#成都眼袋', '#眼袋泪沟', '#变美日记'];
  const issues = validateHarnessCandidates([candidate], EVIDENCE, DISCLOSED, {
    ...BASE, expectedCandidateCount: 1, projectName: '星零感微孔去眼袋',
  });
  assert.ok(!issues.some((issue) => issue.code === 'brand_hashtag'), JSON.stringify(issues));
});

test('brand_voice:标签含项目名不报警,机构自己发就该带品牌', () => {
  const candidate = brandCandidate();
  candidate.content.H.hashtags = ['#星零感微孔去眼袋'];
  const issues = validateHarnessCandidates([candidate], EVIDENCE, DISCLOSED, {
    ...BASE, expectedCandidateCount: 1, seedingMode: 'brand_voice', projectName: '星零感微孔去眼袋',
  });
  assert.ok(!issues.some((issue) => issue.code === 'brand_hashtag'), JSON.stringify(issues));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
```
Expected: 前两条里第一条 FAIL（`brandIssues.length` 是 0，期望 1）。`projectName` 是新字段，运行时被忽略，所以断言失败。

**注意（计划自审更正）**：`packages/agent-harness-core/tsconfig.json` 明确写了 `"exclude": ["src/**/*.test.ts"]`，所以**测试文件不被 typecheck 覆盖**。本计划早前写「漏改会被编译挡住」是错的，那层保护网不存在：`projectName` 不加进 `constraints` 类型时，测试里传它**不会**报编译错，只会在运行时被静默忽略。因此这一步必须靠上面那条运行时断言把关，不能指望 typecheck。

- [ ] **Step 3: 加 projectName 到 constraints 类型**

`packages/agent-harness-core/src/validation.ts` 的 `constraints` 参数（Task 2 已加过 `seedingMode`，这里再加一个）：

```ts
    revisionInstruction?: string; selectedImages?: readonly HarnessImageSource[]; bodyLength?: "short" | "medium" | "long";
    seedingMode?: HarnessSeedingMode;
    /** 本轮项目名。只用于 peer_seeding 下提示标签堆了品牌词;缺省则跳过该检查。 */
    projectName?: string;
```

- [ ] **Step 4: 实现品牌词检查**

在 `validation.ts` 的候选循环里，紧接 Task 4 那段第一人称检查之后插入：

```ts
    /*
     * 标签堆品牌词只给 WARNING,不阻断。
     *
     * 判据是拿本轮项目名做子串比对,不维护关键词表——表会过期,而项目名一定是当前的。
     * 用 WARNING 的理由:项目名可能恰好含通用品类词(项目就叫「眼袋」的情况),
     * 硬拦会把正常的品类标签误判成品牌堆砌。给提示,让人自己看。
     *
     * brand_voice 不查:机构自己发的内容带品牌名是正常的。
     */
    if (peerSeeding && constraints.projectName?.trim()) {
      const projectName = constraints.projectName.trim();
      const branded = candidate.content.H.hashtags.filter((tag) => tag.includes(projectName));
      if (branded.length) {
        add(index, "brand_hashtag", "warning", `标签里出现了项目名（${branded.join("、")}）。素人代发的标签更接近品类词和城市词，品牌词会让帖子显得像官方账号发的。`);
      }
    }
```

- [ ] **Step 5: 跑测试与 typecheck**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
npm run typecheck -w @content-agent/agent-harness-core
```
Expected: 全绿。

- [ ] **Step 6: 变异验证**

| 变异 | 应变红 |
|---|---|
| 把 `"warning"` 改成 `"error"` | 第 1 条（severity 断言 + 不阻断断言） |
| 去掉 `peerSeeding &&` 前置条件 | 第 3 条（brand_voice 不该报） |

- [ ] **Step 7: 红线核对并提交**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
git add packages/agent-harness-core/src/validation.ts packages/agent-harness-core/src/validation.test.ts
git diff --cached --name-only
git commit -m "feat(harness): 素人模式提示标签堆品牌词"
git show --stat HEAD
```

---

### Task 6: 两个调用点都传参 + 提示词分叉

**Files:**
- Modify: `packages/agent-harness-core/src/runner.ts`
- Modify: `apps/api/src/agent-harness.service.ts`
- Test: `packages/agent-harness-core/src/validation.test.ts`

**Interfaces:**
- Consumes: `seedingMode` (Task 1), `projectName` (Task 5), the relaxed thread rules (Task 2), the removed `disclaimer` field (Task 3).
- Produces: nothing new. This task wires everything together and is the last code task.

**背景（实施者必读，这一条最容易出错）：**

`validateHarnessCandidates` 有**两个**调用点：

```
packages/agent-harness-core/src/runner.ts:864      生成时（行号已随 Task 2 变动）
apps/api/src/agent-harness.service.ts:885          断点恢复读结果时

另外 service 里 `runAgentHarness`（约 :998）与 `reviewHarnessCandidates`（约 :978）两个入口也不传 mode，
于是全部默认 peer_seeding。Task 2 已给这两个入口加了可选 seedingMode（WithSeedingMode 交集类型），
本任务要显式传 DEFAULT_HARNESS_SEEDING_MODE，让「默认值」在生产路径上是写出来的而不是省略出来的。
```

**这两处现在已经分叉了**：`runner.ts` 传了 `bodyLength`，`agent-harness.service.ts` **没传**，所以后者的正文长度检查静默按 `"medium"` 判。这是既有缺陷，正是本任务要防的那类 bug——同一份候选，界面上看到的校验结果和生成时的判定不一致。

本任务要两处都传 `seedingMode` 和 `projectName`，并**顺手补上** service 漏掉的 `bodyLength`（它在允许改的文件内，且不补的话本任务的一致性断言会显得虚伪）。

- [ ] **Step 1: 写失败测试（源码断言）**

追加到 `packages/agent-harness-core/src/validation.test.ts`：

```ts
test('两个调用点都传了 seedingMode,不能只改一处', () => {
  /*
   * validateHarnessCandidates 有两个调用点:runner(生成时)和 service(断点恢复读结果时)。
   * 只改一处的后果是同一份候选在两条路上判定不同——界面说合格、导出却被拦,极难查。
   * 实测既有缺陷:service 此前漏传 bodyLength,导致长度检查静默按 medium 判。
   */
  const runnerSource = readFileSync(new URL('./runner.ts', import.meta.url), 'utf8');
  const serviceSource = readFileSync(
    new URL('../../../apps/api/src/agent-harness.service.ts', import.meta.url), 'utf8');
  for (const [label, source] of [['runner', runnerSource], ['service', serviceSource]] as const) {
    assert.match(source, /seedingMode:/u, `${label} 没有传 seedingMode`);
    assert.match(source, /projectName:/u, `${label} 没有传 projectName`);
    assert.match(source, /bodyLength:/u, `${label} 没有传 bodyLength`);
  }
});

test('提示词区分博主本人与模拟读者:博主可讲自己时间线,读者不许编经历', () => {
  /*
   * 原提示词有一句禁止模拟读者提到「治疗/到访/朋友案例/恢复天数/结果/购买」。
   * 素人代发下博主本人恰恰要说「两个月了」「我刚做完」,所以这句必须区分主体:
   * 博主(author)可以讲自己的时间线,模拟读者仍不许编造自己的经历。
   * 不区分的话,这个模式会变成「谁都能编」——那是真的造假,不是代笔。
   */
  const source = readFileSync(new URL('./runner.ts', import.meta.url), 'utf8');
  const peerBlock = source.slice(source.indexOf('PEER_SEEDING_GUIDANCE'));
  assert.match(peerBlock, /author/u, '素人指引没有点明博主身份');
  assert.match(peerBlock, /simulated reader|模拟读者/u, '素人指引没有区分模拟读者');
});
```

测试文件顶部需要 `readFileSync`：

```ts
import { readFileSync } from 'node:fs';
```

- [ ] **Step 2: 跑测试确认失败**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
```
Expected: 两条都 FAIL。第一条报 `service 没有传 seedingMode`，第二条报找不到 `PEER_SEEDING_GUIDANCE`。

- [ ] **Step 3: runner.ts 传参**

`packages/agent-harness-core/src/runner.ts:848` 那个调用，在 `bodyLength` 后面加两个字段：

```ts
  const issues = validateHarnessCandidates(reconciledCandidates, allEvidence, disclosed, {
    mustInclude: input.task.mustInclude, forbidden: input.task.forbidden, claimAudit: audit, expectedCandidateCount: expectedCount,
    runMode, sourceCandidateIndex: input.sourceCandidate?.candidateIndex, revisionInstruction: input.revisionInstruction,
    selectedImages: input.images ?? [], bodyLength: input.task.bodyLength ?? input.task.methodProfile?.bodyLength,
    seedingMode: DEFAULT_HARNESS_SEEDING_MODE,
    projectName: input.project.name,
  });
```

`DEFAULT_HARNESS_SEEDING_MODE` 从 `./methods.js` 导入（`runner.ts:6` 已经从那里导入 `HARNESS_BODY_LENGTH_TARGETS`，加到同一行即可）。

- [ ] **Step 4: service 传参（含补上漏掉的 bodyLength）**

`apps/api/src/agent-harness.service.ts:885` 那个调用：

```ts
    const issues = validateHarnessCandidates(checkpoint.candidates, context.evidence, disclosed, {
      mustInclude: context.task.mustInclude,
      forbidden: context.task.forbidden,
      claimAudit: audit,
      expectedCandidateCount: job.run_kind === 'revision' ? 1 : 3,
      runMode: job.run_kind,
      sourceCandidateIndex: context.sourceCandidate?.candidateIndex,
      revisionInstruction: job.run_kind === 'revision' ? job.instruction : undefined,
      selectedImages: context.images,
      // 这三个此前漏传:bodyLength 缺失会让长度检查静默按 medium 判,与生成时的判定分叉。
      bodyLength: context.task.bodyLength ?? context.task.methodProfile?.bodyLength,
      seedingMode: DEFAULT_HARNESS_SEEDING_MODE,
      projectName: context.project.name,
    });
```

`DEFAULT_HARNESS_SEEDING_MODE` 加进 `apps/api/src/agent-harness.service.ts:11` 那个从 `@content-agent/agent-harness-core` 的 import 里（`validateHarnessCandidates` 已在其中）。

- [ ] **Step 5: 提示词分叉**

在 `packages/agent-harness-core/src/runner.ts` 里，紧挨现有那组评论区指引字符串的定义处，新增一个常量：

```ts
/*
 * 素人代发种草的写作指引。
 *
 * 与 brand_voice 的关键差别:博主是真人素人账号,可以第一人称讲自己的时间线;
 * 具体参数(价格、恢复期、麻醉方式)优先留给评论区由博主回答,正文只负责处境和一个窄问题。
 * 这套形态来自 67 篇真实对标语料的实测:博主回复 187 行里带机构口吻的只有 1 行。
 */
const PEER_SEEDING_GUIDANCE = [
  "This run is peer seeding: the publishing account is a real individual, not the brand. Write N.body in that person's own first person. The publisher may state their own timeline (just finished, day three, two months in) because a real person is posting it.",
  "Keep specific parameters out of N.body when they can be asked instead. Price, recovery time, anesthesia type, whether it can be done same-day: leave these for the comment section, answered by the publisher when a simulated reader asks. N.body establishes one situation and one narrow question.",
  "At least 2 Cref threads must be org_answer with postingIdentity 'author' — the publisher replying as themselves. Write these as friend-to-friend replies, short and plain. Do not write them as customer-service answers: no clarification clause, no verifiable-next-step clause, no boundary clause unless it genuinely belongs in that sentence.",
  "Threads with postingIdentity brand/staff/expert/publisher still require clarification, nextStep, boundary and stopReason. An institutional voice must carry its boundary; only the individual publisher is exempt.",
  "Let a person's name surface through being asked rather than announced. A simulated reader may ask which doctor or which clinic, and the publisher answers. N.body does not need the full name.",
  "Hashtags: category words and city words, not brand names.",
  "The publisher may describe their own experience. A simulated reader still may not invent their own treatment, visit, friend case, recovery day, outcome, purchase or endorsement — that would be fabricating other people's testimony, which is different from the publisher writing their own.",
] as const;
```

**先改原文那两行（审查发现的计划缺陷，必须做）。** `runner.ts:626-627` 现在写的是：

```
"...Mix all three threadKind values: 2-3 org_answer threads, at least 1 reader_exchange, and at least 1 organic_reaction..."
"org_answer is a residual reader question answered by an accountable publishing identity. It follows: direct answer -> ... -> clarification -> verifiable next step -> explicit stopReason."
```

只追加素人指引不够：这两行仍在要求「机构身份答疑」并把四段式写成硬性顺序，会和素人指引正面矛盾，而且解析器把缺省身份当 `publisher`（`runner.ts:363`），于是生产环境实跑会因「0 条 author 回复」直接撞 `comment_topology`——比改动前更严。所以这两行本身要按模式分叉：`peer_seeding` 下把「2-3 org_answer」改成「至少 2 条由 `author` 身份回复的 org_answer」，并把四段式从「必须」降为「机构身份才必须」。`brand_voice` 下两行原文逐字保留。

然后在组装提示词的地方，按模式选择插入。找到现有那段以 `"Create 4-6 Cref threads per candidate..."` 开头的指引数组，在其后按模式追加：

```ts
    ...(seedingMode === "peer_seeding" ? PEER_SEEDING_GUIDANCE : []),
```

其中 `seedingMode` 在该函数内取 `DEFAULT_HARNESS_SEEDING_MODE`。

**同时**要处理那句冲突：现有指引里 `"...Never invent treatment, visit, friend/colleague case, recovery day, outcome, purchase, endorsement or observed demand in any simulated reader voice."` —— 这句本身限定了 "in any simulated reader voice"，与素人指引最后一条并不矛盾（博主不是 simulated reader）。**保留原句不改**，靠新增那条把区别说明白。若实施时发现别处有无限定主体的禁止句，一并按上述原则限定主体，并在报告里说明改了哪一句。

- [ ] **Step 6: 跑测试与 typecheck**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core -- src/validation.test.ts
npm test -w @content-agent/agent-harness-core
npm run typecheck -w @content-agent/agent-harness-core
npm run typecheck -w @content-agent/api
```
Expected: 全绿。

- [ ] **Step 7: 变异验证**

| 变异 | 应变红 |
|---|---|
| 从 service 调用里删掉 `seedingMode` | 「两个调用点都传了」 |
| 从 `PEER_SEEDING_GUIDANCE` 删掉最后那条（区分模拟读者的） | 「提示词区分博主本人与模拟读者」 |

- [ ] **Step 8: 红线核对并提交**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
git add packages/agent-harness-core/src/runner.ts apps/api/src/agent-harness.service.ts packages/agent-harness-core/src/validation.test.ts
git diff --cached --name-only
git commit -m "feat(harness): 两个校验调用点对齐,提示词按模式分叉"
git show --stat HEAD
```

---

### Task 7: 语料回归与全量验收

**Files:**
- Create: `packages/agent-harness-core/src/corpus-check.mts`（临时脚本，验收后删除）
- 无生产代码改动

**Interfaces:**
- Consumes: 全部前置任务。
- Produces: 验收结论，无代码产物。

**背景：** 这是唯一能证明「改对了」的客观检验——用用户实际在发的 67 篇内容当判据，而不是我编的测试数据。语料在仓库外（`../70篇对标内容_AI提炼版.jsonl`，相对仓库根），CI 跑不到，所以这个脚本不进测试套件。

- [ ] **Step 1: 写语料回归脚本**

创建 `packages/agent-harness-core/src/corpus-check.mts`：

```ts
/*
 * 语料回归:拿 67 篇真实对标语料跑改后的正则,确认素人时间线不再被判死。
 *
 * 语料在仓库外,CI 跑不到,所以这个脚本不进测试套件。验收后删除。
 * 扫描范围必须是正文 + 评论区——校验的 visibleCandidateText 覆盖两者,
 * 只扫正文会低估命中率(实测只扫正文 5 篇、加评论区 13 篇)。
 */
import { readFileSync } from 'node:fs';

const TESTIMONIAL = /(亲测|我做过|我用过|我体验过|朋友做过|闺蜜做过|真实顾客|真实客户)/u;
const TIMELINE = /(?:我|朋友|闺蜜|同事|姐妹|家人).{0,12}(?:刚做|做完|做了|做的|术后|恢复)/u;

const path = new URL('../../../../70篇对标内容_AI提炼版.jsonl', import.meta.url);
const rows = readFileSync(path, 'utf8').trim().split('\n').map((line) => JSON.parse(line));
const seen = new Set<string>();
let testimonial = 0;
let timeline = 0;
for (const row of rows) {
  const key = `${row['标题'] ?? ''}||${(row['正文'] ?? '').slice(0, 60)}`;
  if (seen.has(key)) continue;
  seen.add(key);
  const visible = [row['标题'] ?? '', row['正文'] ?? '', row['评论区内容'] ?? ''].join('\n');
  if (TESTIMONIAL.test(visible)) testimonial += 1;
  if (TIMELINE.test(visible)) timeline += 1;
}
console.log(`去重样本: ${seen.size}`);
console.log(`口碑话术(两种模式都禁): ${testimonial} 篇`);
console.log(`时间线叙述(peer_seeding 放开): ${timeline} 篇`);
```

- [ ] **Step 2: 跑语料回归**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
node --import tsx packages/agent-harness-core/src/corpus-check.mts
```
Expected:
```
去重样本: 67
口碑话术(两种模式都禁): 0 篇
时间线叙述(peer_seeding 放开): 13 篇
```

判据：口碑话术必须是 **0 篇**（说明保留禁止那几支不会误伤真实语料），时间线必须是 **13 篇**（说明放开的正是卡住用户的那一支）。任一数字不符就说明正则拆错了，停下来报告。

- [ ] **Step 3: 删除临时脚本**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
rm packages/agent-harness-core/src/corpus-check.mts
git status --porcelain packages/agent-harness-core/src/
```
Expected: 不列出 `corpus-check.mts`（已删且从未提交）。

- [ ] **Step 4: 全量验收**

Run:
```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
npm test -w @content-agent/agent-harness-core
npm test -w @content-agent/web
npm run typecheck -w @content-agent/agent-harness-core
npm run typecheck -w @content-agent/api
npm run typecheck -w @content-agent/web
npm run build -w @content-agent/web
```

Expected：harness-core 与 web 测试全绿，三处 typecheck 干净，build 成功。

`apps/api` 全量测试有 14 条既有红（另一会话未提交的三个 `generation-*` 测试文件 + 需活模型网关的 `generation-batch` 冒烟），**不是本次引入**。若要跑，逐条比对基线，不得把这 14 条算作回归。

- [ ] **Step 5: 红线终检**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
git diff --stat "$(git merge-base main HEAD)"..HEAD
```

改动文件必须**全部**落在 Global Constraints 的允许清单内：

```
packages/agent-harness-core/src/types.ts
packages/agent-harness-core/src/methods.ts
packages/agent-harness-core/src/validation.ts
packages/agent-harness-core/src/validation.test.ts
packages/agent-harness-core/src/runner.ts
packages/agent-harness-core/src/runner.test.ts
apps/api/src/agent-harness.service.ts
apps/web/src/pages/AgentHarnessPage.tsx
apps/web/src/types.ts
docs/superpowers/specs/2026-08-04-素人代发种草模式-design.md
docs/superpowers/plans/2026-08-04-素人代发种草模式.md
```

出现任何 `packages/agent-core/`、`apps/api/src/generation.service.ts`、`apps/api/src/export.service.ts`、`apps/api/src/intelligence.service.ts`、`apps/web/src/pages/IntelligentSimpleFlow.tsx`、`apps/web/src/pages/GenerationResultPage.tsx`、`apps/web/src/components/quick/` 下的文件 —— **立即停止并报告违规**，不要自行回退（要让人看到发生了什么）。

- [ ] **Step 6: 确认主线未被触碰**

```bash
cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent"
git diff "$(git merge-base main HEAD)"..HEAD -- packages/agent-core apps/api/src/generation.service.ts apps/api/src/export.service.ts apps/api/src/intelligence.service.ts | wc -l
```
Expected: `0`

这一条是红线的机器判据。非 0 即违规。
