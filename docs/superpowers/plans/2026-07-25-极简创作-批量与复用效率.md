# 极简创作 · 批量与复用效率 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给极简创作频道加"二维批量生成 + 跨会话持久批次看板 + 历史配方回填 + 预设卡片化",让面向内容运营的独立产品支持"一次配好、批量出稿、挑选发布、一键同款"。

**Architecture:** 后端新增 `generation_batches` 表(迁移 v12)与 `POST /api/generation-batches` 展开接口——前端把"N 选题 × M 预设"编译成 N×M 个 `GenerateInput` 整包提交,后端在一个事务里逐个复用现有 job 创建核心并打上 `batch_id` 入队,现有 `drainQueue` 并发调度不变。前端在"产出"区上半部加批次看板(并发轮询现有 `GET /api/generations/:id`),创作区加选题多选 + 预设卡片多选,产出卡加"再来一篇"配方回填。底层生成/合规/分析逻辑零改动。

**Tech Stack:** NestJS + better-sqlite3(API)、React + TypeScript + Vite(web)、node:test(前后端单测)、monorepo workspaces(@content-agent/api、@content-agent/web)。

## Global Constraints

- 底层生成/合规/分析逻辑零改动:只加"批次账本 + 展开接口"与前端编排,不改 `agent.generate` / 校验 / 分析。
- 迁移版本:当前 `user_version = 11`,本轮批次迁移必须是 **v12**,遵循 `if (version < 12) this.transaction(() => { this.db.exec(\`… PRAGMA user_version = 12;\`) }); if (version < 12) version = 12;` 模式。
- job 终态 token 是 `completed`(不是 `succeeded`)、失败是 `failed`、进行中是 `queued`/`running`。批次状态用同名规则聚合。
- 批次 `ON DELETE SET NULL`:删批次不删 job,产出留在历史区。单篇生成 job 的 `batch_id` 恒为 NULL。
- SaaS 用户是本产品核心用户,新接口 `/api/generation-batches` 必须加进 `apps/api/src/saas-access.ts` 白名单,否则 saas 用户 403。
- 前端提交批量:复用 `buildSimpleGenerateInput`(apps/web/src/lib/simple-generation.ts)为每个"选题×预设"构造 `GenerateInput`,不新建输入构造逻辑。
- 权限:批量接口沿用 `generation.run` 权限(与单篇 `POST /api/generations` 一致)。
- 测试用 `node:test`:后端 `npm run test -w @content-agent/api`,前端 `npm run test -w @content-agent/web`,类型检查 `npm run typecheck -w @content-agent/api` / `-w @content-agent/web`。新测试文件必须加进对应 package.json 的 `test` 脚本文件列表,否则不会被跑到。
- 提交粒度:每个 Task 末尾 commit,message 用 `feat(api/web,quick): …` / `test(...)` 风格,中文描述。

---

## Task 1: 批次表迁移(v12)

**Files:**
- Modify: `apps/api/src/database.service.ts`(在 `if (version < 11) version = 11;` 之后、`onModuleDestroy` 之前追加 v12 块)
- Modify: `apps/api/test/api.test.ts`(或新建 `apps/api/test/generation-batch.test.ts` 并加进 package.json)

**Interfaces:**
- Produces: 表 `generation_batches(id, project_id, name, status, total_jobs, config_json, created_by, created_at, updated_at, completed_at)`;列 `generation_jobs.batch_id`(nullable, FK SET NULL);索引 `generation_batches_project_idx`、`generation_jobs_batch_idx`。迁移后 `PRAGMA user_version = 12`。

- [ ] **Step 1: Write the failing test**

新建 `apps/api/test/generation-batch.test.ts`:

```ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import { mkdtempSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { DatabaseService } from '../src/database.service.js';

function freshDb() {
  const dir = mkdtempSync(join(tmpdir(), 'batch-mig-'));
  const db = new DatabaseService({ dataDir: dir });
  return { db, cleanup: () => { db.onModuleDestroy(); rmSync(dir, { recursive: true, force: true }); } };
}

test('migration v12 creates generation_batches and jobs.batch_id', () => {
  const { db, cleanup } = freshDb();
  try {
    const version = db.prepare('PRAGMA user_version').get() as { user_version: number };
    assert.equal(version.user_version >= 12, true);
    const batchCols = db.prepare("PRAGMA table_info(generation_batches)").all() as Array<{ name: string }>;
    const names = batchCols.map((c) => c.name);
    for (const col of ['id', 'project_id', 'name', 'status', 'total_jobs', 'config_json', 'created_by', 'created_at', 'updated_at', 'completed_at']) {
      assert.equal(names.includes(col), true, `generation_batches 缺列 ${col}`);
    }
    const jobCols = db.prepare("PRAGMA table_info(generation_jobs)").all() as Array<{ name: string }>;
    assert.equal(jobCols.map((c) => c.name).includes('batch_id'), true, 'generation_jobs 缺 batch_id');
  } finally {
    cleanup();
  }
});
```

- [ ] **Step 2: 把新测试文件加进 api package.json 的 test 脚本**

`apps/api/package.json` 的 `"test"` 命令末尾文件列表追加 ` test/generation-batch.test.ts`。

- [ ] **Step 3: Run test to verify it fails**

Run: `npm run test -w @content-agent/api -- 2>&1 | grep -A3 generation-batch`
Expected: FAIL(v12 未建,`user_version >= 12` 为 false 或表不存在)

- [ ] **Step 4: Write the migration**

在 `apps/api/src/database.service.ts` 的 `if (version < 11) version = 11;` 之后追加:

```ts
    if (version < 12) this.transaction(() => {
      this.db.exec(`
        CREATE TABLE generation_batches (
          id           TEXT PRIMARY KEY,
          project_id   TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
          name         TEXT NOT NULL DEFAULT '',
          status       TEXT NOT NULL,
          total_jobs   INTEGER NOT NULL DEFAULT 0,
          config_json  TEXT NOT NULL DEFAULT '{}',
          created_by   TEXT NOT NULL REFERENCES users(id),
          created_at   TEXT NOT NULL,
          updated_at   TEXT NOT NULL,
          completed_at TEXT
        );
        CREATE INDEX generation_batches_project_idx ON generation_batches(project_id, created_at);

        ALTER TABLE generation_jobs ADD COLUMN batch_id TEXT REFERENCES generation_batches(id) ON DELETE SET NULL;
        CREATE INDEX generation_jobs_batch_idx ON generation_jobs(batch_id);

        PRAGMA user_version = 12;
      `);
    });
    if (version < 12) version = 12;
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npm run test -w @content-agent/api -- 2>&1 | grep -A3 generation-batch`
Expected: PASS

- [ ] **Step 6: 确认 user_version 断言测试不回归**

Run: `npm run test -w @content-agent/api`
Expected: 全绿(若有断言当前 user_version 的测试,把期望值 11 → 12)

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/database.service.ts apps/api/test/generation-batch.test.ts apps/api/package.json
git commit -m "feat(api,quick): 批次表迁移 v12 — generation_batches + jobs.batch_id"
```

---

## Task 2: 批次服务与展开接口

**Files:**
- Modify: `apps/api/src/generation.service.ts`(抽出 `insertJob`,新增 `createBatch` / `getBatch` / `listBatches` / `batchStatus` 聚合)
- Create: `apps/api/src/generation-batch.controller.ts`
- Modify: `apps/api/src/app.module.ts`(注册新 controller)
- Modify: `apps/api/test/generation-batch.test.ts`(追加聚合逻辑单测)

**Interfaces:**
- Consumes: Task 1 的 `generation_batches` 表、`generation_jobs.batch_id` 列;现有 `create(raw, principal)` 的核心插入逻辑。
- Produces:
  - `GenerationService.createBatch(raw: { projectId: string; name?: string; jobs: Record<string, unknown>[] }, principal): Record<string, unknown>` — 返回 `{ id, projectId, name, status, totalJobs, jobs: [...mapJob] }`。
  - `GenerationService.getBatch(id): Record<string, unknown>` — 返回批次 + 其 job 列表(每个 mapJob(row, false),含 status/progress/topic/presetId)。
  - `GenerationService.listBatches(projectId): Record<string, unknown>[]`。
  - `computeBatchStatus(jobStatuses: string[]): 'queued'|'running'|'completed'|'failed'|'partial'` — 纯函数,导出供单测。
  - 路由 `POST /api/generation-batches`、`GET /api/generation-batches?projectId=`、`GET /api/generation-batches/:id`。

- [ ] **Step 1: Write the failing test(状态聚合纯函数)**

在 `apps/api/test/generation-batch.test.ts` 追加:

```ts
import { computeBatchStatus } from '../src/generation.service.js';

test('computeBatchStatus aggregates job statuses', () => {
  assert.equal(computeBatchStatus(['queued', 'queued']), 'queued');
  assert.equal(computeBatchStatus(['running', 'queued']), 'running');
  assert.equal(computeBatchStatus(['completed', 'running']), 'running');
  assert.equal(computeBatchStatus(['completed', 'completed']), 'completed');
  assert.equal(computeBatchStatus(['failed', 'failed']), 'failed');
  assert.equal(computeBatchStatus(['completed', 'failed']), 'partial');
  assert.equal(computeBatchStatus([]), 'completed');
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -w @content-agent/api -- 2>&1 | grep -A3 computeBatchStatus`
Expected: FAIL(`computeBatchStatus` 未导出)

- [ ] **Step 3: 实现 computeBatchStatus 纯函数**

在 `apps/api/src/generation.service.ts` 文件底部(与 `isRecord` 等同级)追加并导出:

```ts
export function computeBatchStatus(jobStatuses: string[]): 'queued' | 'running' | 'completed' | 'failed' | 'partial' {
  if (!jobStatuses.length) return 'completed';
  const anyActive = jobStatuses.some((s) => s === 'queued' || s === 'running');
  if (anyActive) {
    // 全部还没开始 → queued;否则 running
    return jobStatuses.every((s) => s === 'queued') ? 'queued' : 'running';
  }
  const anyOk = jobStatuses.some((s) => s === 'completed');
  const anyFail = jobStatuses.some((s) => s === 'failed');
  if (anyOk && anyFail) return 'partial';
  if (anyFail) return 'failed';
  return 'completed';
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test -w @content-agent/api -- 2>&1 | grep -A3 computeBatchStatus`
Expected: PASS

- [ ] **Step 5: 抽出 insertJob(重构 create,行为不变)**

在 `generation.service.ts` 中,把 `create()` 里"从 `const id = randomUUID();` 到 `this.enqueue(id); return this.get(id);` 之前"的**入库 + event + audit** 段抽成私有方法 `insertJob`,让 `create` 调用它。关键:`insertJob` 多接一个 `batchId: string | null` 参数,写进 INSERT 的 `batch_id` 列。

`create()` 结尾改为:

```ts
    const id = this.insertJob(raw, principal, null);
    this.enqueue(id);
    return this.get(id);
```

抽出的方法(保留原有全部计算逻辑,只把 `raw`/`principal`/`batchId` 作为入参;INSERT 语句加 `batch_id` 列):

```ts
  private insertJob(raw: Record<string, unknown>, principal: SessionPrincipal, batchId: string | null): string {
    const projectId = this.requiredString(raw.projectId, 'projectId');
    const project = this.resources.projectRow(projectId);
    this.research.bootstrapProject(projectId, principal.userId);
    const releaseSnapshot = this.research.activeRuntimeSnapshot(projectId);
    const releaseFormulaId = typeof releaseSnapshot.id === 'string' && typeof releaseSnapshot.formulaVersionId === 'string'
      ? releaseSnapshot.formulaVersionId
      : undefined;
    const legacyConfig = isRecord(raw.config) ? raw.config : {};
    const requestedFormulaId = typeof raw.formulaVersion === 'string'
      ? raw.formulaVersion
      : typeof legacyConfig.formulaVersion === 'string'
        ? legacyConfig.formulaVersion
        : undefined;
    if (releaseFormulaId
      && requestedFormulaId
      && !['active', '项目默认'].includes(requestedFormulaId)
      && requestedFormulaId !== releaseFormulaId) {
      throw new BadRequestException({
        message: '正式生成只能使用 active release 绑定的公式版本',
        code: 'RELEASE_FORMULA_CONFLICT',
        releaseFormulaVersionId: releaseFormulaId,
        requestedFormulaVersionId: requestedFormulaId,
      });
    }
    const releaseParameterOverrides = isRecord(releaseSnapshot.parameterOverrides)
      ? releaseSnapshot.parameterOverrides
      : {};
    const planning = this.intelligence.prepareGeneration(projectId, raw);
    const topic = planning.topic;
    const normalizedRaw = {
      ...raw,
      ...(releaseFormulaId ? { formulaVersion: releaseFormulaId } : {}),
      topic,
      parameterValues: {
        ...releaseParameterOverrides,
        ...(isRecord(raw.parameterValues) ? raw.parameterValues : {}),
      },
    };
    const workspaceId = String(project.workspace_id);
    const resolution = this.presets.resolve(projectId, normalizedRaw, principal);
    const formulaVersion = resolution.formulaVersion;
    if (releaseFormulaId && formulaVersion.id !== releaseFormulaId) {
      throw new BadRequestException({
        message: '生成公式与 active release 不一致',
        code: 'RELEASE_FORMULA_CONFLICT',
        releaseFormulaVersionId: releaseFormulaId,
        resolvedFormulaVersionId: formulaVersion.id,
      });
    }
    const config = resolution.resolvedConfig;
    this.assertLatestSelectedKnowledge(projectId, config.knowledge.selectedFileIds);
    const provider = this.settings.provider(workspaceId, principal.userId);
    if (provider.mode === 'platform' && provider.apiKey) this.settings.consumePlatformQuota(workspaceId);

    const id = randomUUID();
    const now = nowIso();
    const mode = raw.mode === 'advanced' ? 'advanced' : 'simple';
    this.database
      .prepare(
        `INSERT INTO generation_jobs
          (id, project_id, status, config_json, seed, formula_version_id, created_by,
           created_at, updated_at, topic, goal, mode, progress, knowledge_context_json,
           preset_id, style_profile_version, resolution_snapshot_json, config_impact_json,
           opportunity_id, opportunity_snapshot_json, planning_context_json, image_context_json,
           release_manifest_id, research_snapshot_json, batch_id)
         VALUES (?, ?, 'queued', ?, ?, ?, ?, ?, ?, ?, ?, ?, 2, '{}', ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
      )
      .run(
        id,
        projectId,
        JSON.stringify(config),
        String(config.generation.baseSeed),
        formulaVersion.id,
        principal.userId,
        now,
        now,
        topic,
        config.task.goal,
        mode,
        resolution.preset.id,
        resolution.styleProfileVersion,
        JSON.stringify({
          ...resolution.resolutionSnapshot,
          parameterSelection: resolution.parameterSelection,
          preset: resolution.preset,
          sourceMap: resolution.sourceMap,
          directives: resolution.directives,
          conflicts: resolution.conflicts,
          warnings: resolution.warnings,
          requestOverrides: resolution.requestOverrides,
        }),
        JSON.stringify(resolution.impactReport),
        planning.opportunityId ?? null,
        JSON.stringify(planning.opportunitySnapshot),
        JSON.stringify(planning.planningContext),
        JSON.stringify(planning.imageContext),
        typeof releaseSnapshot.id === 'string' ? releaseSnapshot.id : null,
        JSON.stringify(releaseSnapshot),
        batchId,
      );
    this.event(id, 'queued', {
      providerMode: provider.mode,
      hasProviderKey: Boolean(provider.apiKey),
      presetId: resolution.preset.id,
      styleProfileVersion: resolution.styleProfileVersion,
      opportunityId: planning.opportunityId,
      imageAssetCount: planning.imageContext.length,
      changedParameters: resolution.impactPreview.filter((item) => item.source !== 'default').length,
      releaseManifestId: typeof releaseSnapshot.id === 'string' ? releaseSnapshot.id : null,
      batchId,
    });
    this.audit.record({ workspaceId, userId: principal.userId, action: 'generation.create', entityType: 'generation_job', entityId: id, details: { projectId, mode, presetId: resolution.preset.id, styleProfileVersion: resolution.styleProfileVersion, releaseManifestId: typeof releaseSnapshot.id === 'string' ? releaseSnapshot.id : null, batchId } });
    return id;
  }
```

- [ ] **Step 6: 实现 createBatch / getBatch / listBatches**

在 `generation.service.ts` 的 `GenerationService` 类内追加(紧邻 `create`):

```ts
  createBatch(raw: { projectId?: unknown; name?: unknown; jobs?: unknown }, principal: SessionPrincipal): Record<string, unknown> {
    const projectId = this.requiredString(raw.projectId, 'projectId');
    const jobs = Array.isArray(raw.jobs) ? raw.jobs.filter(isRecord) : [];
    if (!jobs.length) throw new BadRequestException('批量任务不能为空');
    if (jobs.length > 60) throw new BadRequestException('单批任务不能超过 60 篇');
    const name = typeof raw.name === 'string' ? raw.name.trim().slice(0, 120) : '';
    const batchId = randomUUID();
    const now = nowIso();
    // 先建批次账本(queued),再逐个插 job;任一 job 校验失败则整批回滚。
    const jobIds = this.database.transaction(() => {
      this.database
        .prepare(
          `INSERT INTO generation_batches
            (id, project_id, name, status, total_jobs, config_json, created_by, created_at, updated_at)
           VALUES (?, ?, ?, 'queued', ?, ?, ?, ?, ?)`,
        )
        .run(batchId, projectId, name, jobs.length, JSON.stringify({ count: jobs.length }), principal.userId, now, now);
      return jobs.map((job) => this.insertJob({ ...job, projectId }, principal, batchId));
    })();
    // 事务提交后再入队(enqueue 触发异步 process,不能在事务里跑)
    for (const id of jobIds) this.enqueue(id);
    return this.getBatch(batchId);
  }

  batchRow(id: string): Record<string, unknown> {
    const row = this.database.prepare('SELECT * FROM generation_batches WHERE id = ?').get(id) as Record<string, unknown> | undefined;
    if (!row) throw new NotFoundException('批次不存在');
    return row;
  }

  getBatch(id: string): Record<string, unknown> {
    const batch = this.batchRow(id);
    const jobRows = this.database.prepare('SELECT * FROM generation_jobs WHERE batch_id = ? ORDER BY created_at').all(id) as unknown as JobRow[];
    const jobs = jobRows.map((row) => this.mapJob(row, false));
    const status = computeBatchStatus(jobRows.map((row) => row.status));
    // 状态漂移时惰性回写(含 completed_at)
    if (status !== batch.status) {
      const completedAt = (status === 'completed' || status === 'failed' || status === 'partial') ? nowIso() : null;
      this.database.prepare('UPDATE generation_batches SET status=?, completed_at=?, updated_at=? WHERE id=?')
        .run(status, completedAt, nowIso(), id);
    }
    return {
      id: batch.id,
      projectId: batch.project_id,
      name: batch.name,
      status,
      totalJobs: batch.total_jobs,
      createdAt: batch.created_at,
      completedAt: batch.completed_at ?? undefined,
      jobs,
    };
  }

  listBatches(projectId: string): Record<string, unknown>[] {
    const rows = this.database.prepare('SELECT id FROM generation_batches WHERE project_id = ? ORDER BY created_at DESC').all(projectId) as Array<{ id: string }>;
    return rows.map((row) => this.getBatch(row.id));
  }
```

- [ ] **Step 7: 新建 controller**

Create `apps/api/src/generation-batch.controller.ts`:

```ts
import { BadRequestException, Body, Controller, ForbiddenException, Get, Inject, Param, Post, Query, Req, UseGuards } from '@nestjs/common';
import type { Request } from 'express';
import { GenerationService } from './generation.service.js';
import { CsrfGuard, PermissionGuard, SessionAuthGuard } from './guards.js';
import type { AuthenticatedRequest, Permission, SessionPrincipal } from './models.js';
import { ResourceService } from './resource.service.js';
import { requireObject } from './utils.js';

@Controller('api/generation-batches')
@UseGuards(SessionAuthGuard, CsrfGuard)
export class GenerationBatchController {
  constructor(
    @Inject(GenerationService) private readonly generations: GenerationService,
    @Inject(ResourceService) private readonly resources: ResourceService,
    @Inject(PermissionGuard) private readonly permissions: PermissionGuard,
  ) {}

  @Get()
  list(@Req() request: Request, @Query('projectId') projectId?: string) {
    if (typeof projectId !== 'string' || !projectId) throw new BadRequestException('projectId 不能为空');
    this.assert(request, projectId, 'project.read');
    return this.generations.listBatches(projectId);
  }

  @Get(':id')
  get(@Req() request: Request, @Param('id') id: string) {
    const batch = this.generations.batchRow(id);
    this.assert(request, String(batch.project_id), 'project.read');
    return this.generations.getBatch(id);
  }

  @Post()
  create(@Req() request: Request, @Body() rawBody: unknown) {
    const body = requireObject(rawBody);
    if (typeof body.projectId !== 'string') throw new BadRequestException('projectId 不能为空');
    this.assert(request, body.projectId, 'generation.run');
    return this.generations.createBatch(body, this.principal(request));
  }

  private assert(rawRequest: Request, projectId: string, permission: Permission): void {
    const principal = this.principal(rawRequest);
    const project = this.resources.projectRow(projectId);
    if (principal.systemRole !== 'admin' && !this.permissions.hasPermission(principal.userId, String(project.workspace_id), permission, projectId)) {
      throw new ForbiddenException(`缺少权限：${permission}`);
    }
  }

  private principal(request: Request): SessionPrincipal {
    return (request as unknown as AuthenticatedRequest).principal as SessionPrincipal;
  }
}
```

- [ ] **Step 8: 注册 controller**

`apps/api/src/app.module.ts`:import 追加 `import { GenerationBatchController } from './generation-batch.controller.js';`;`controllers` 数组在 `GenerationController,` 后加 `GenerationBatchController,`。

- [ ] **Step 9: typecheck + 全量测试**

Run: `npm run typecheck -w @content-agent/api && npm run test -w @content-agent/api`
Expected: 0 error;全绿(含 computeBatchStatus 与迁移测试)

- [ ] **Step 10: Commit**

```bash
git add apps/api/src/generation.service.ts apps/api/src/generation-batch.controller.ts apps/api/src/app.module.ts apps/api/test/generation-batch.test.ts
git commit -m "feat(api,quick): 批次展开接口 — createBatch 复用 insertJob + 状态聚合"
```

---

## Task 3: SaaS 白名单放行批次接口

**Files:**
- Modify: `apps/api/src/saas-access.ts`
- Modify: `apps/api/test/saas-access.test.ts`

**Interfaces:**
- Consumes: Task 2 的 `/api/generation-batches` 路由。
- Produces: `isSaasApiAllowed` 对 `/api/generation-batches` 及子路径放行。

- [ ] **Step 1: Write the failing test**

在 `apps/api/test/saas-access.test.ts` 的 `cases` 数组内(靠近 `/api/generations` 的用例)追加:

```ts
  ['POST', '/api/generation-batches', true],
  ['GET', '/api/generation-batches', true],
  ['GET', '/api/generation-batches/b1', true],
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -w @content-agent/api -- 2>&1 | grep -A3 saas`
Expected: FAIL(generation-batches 未放行,返回 false)

- [ ] **Step 3: 放行批次接口**

`apps/api/src/saas-access.ts`,在 `if (inScope('/api/generations')) return true;` 之后追加:

```ts
  if (inScope('/api/generation-batches')) return true;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test -w @content-agent/api -- 2>&1 | grep -A3 saas`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/saas-access.ts apps/api/test/saas-access.test.ts
git commit -m "feat(api,quick): SaaS 白名单放行 /api/generation-batches"
```

---

## Task 4: 前端批次类型 + API 客户端

**Files:**
- Modify: `apps/web/src/types.ts`(新增 `GenerationBatch` 接口)
- Modify: `apps/web/src/lib/api.ts`(新增 `generationBatches` 客户端)

**Interfaces:**
- Consumes: Task 2 的 `/api/generation-batches` 三个路由。
- Produces:
  - 类型 `GenerationBatch { id; projectId; name; status: 'queued'|'running'|'completed'|'failed'|'partial'; totalJobs; createdAt?; completedAt?; jobs: GenerationJob[] }`。
  - `api.generationBatches.create(input: { projectId: string; name?: string; jobs: GenerateInput[] }): Promise<GenerationBatch>`
  - `api.generationBatches.get(id: string): Promise<GenerationBatch>`
  - `api.generationBatches.list(projectId: string): Promise<GenerationBatch[]>`

- [ ] **Step 1: 新增 GenerationBatch 类型**

在 `apps/web/src/types.ts` 的 `GenerationJob` 接口之后追加:

```ts
export type GenerationBatchStatus = "queued" | "running" | "completed" | "failed" | "partial";

export interface GenerationBatch {
  id: string;
  projectId: string;
  name: string;
  status: GenerationBatchStatus;
  totalJobs: number;
  createdAt?: string;
  completedAt?: string;
  jobs: GenerationJob[];
}
```

- [ ] **Step 2: 新增 api.generationBatches 客户端**

在 `apps/web/src/lib/api.ts` 的 `generations: { ... }` 对象**之后**(同级,紧跟其闭合 `},`)追加。先确认文件顶部已 `import type { ..., GenerationBatch, ... }`(在现有 GenerationJob import 处补上 `GenerationBatch`):

```ts
  generationBatches: {
    create: (input: { projectId: string; name?: string; jobs: GenerateInput[] }) =>
      request<GenerationBatch>("/api/generation-batches", {
        method: "POST",
        body: JSON.stringify(input),
      }),
    get: (id: string) => request<GenerationBatch>(`/api/generation-batches/${encodeURIComponent(id)}`),
    list: (projectId: string) =>
      request<GenerationBatch[]>(`/api/generation-batches?projectId=${encodeURIComponent(projectId)}`),
  },
```

- [ ] **Step 3: typecheck**

Run: `npm run typecheck -w @content-agent/web`
Expected: 0 error

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/types.ts apps/web/src/lib/api.ts
git commit -m "feat(web,quick): 批次前端类型 GenerationBatch + api.generationBatches 客户端"
```

---

## Task 5: 批量展开纯函数 + 状态工具

**Files:**
- Create: `apps/web/src/lib/quick-batch.ts`
- Create: `apps/web/test/quick-batch.test.ts`
- Modify: `apps/web/package.json`(test 脚本加新文件)

**Interfaces:**
- Consumes: `buildSimpleGenerateInput` / `resolveSimpleGenerationSettings`(simple-generation.ts)、`GenerateInput`、`TopicOpportunity`、`ContentPreset`、`Project`、`SimpleSettingOverrides`、`GenerationBatchStatus`。
- Produces:
  - `buildBatchJobs(args: { project: Project; opportunities: TopicOpportunity[]; presets: ContentPreset[]; overrides: SimpleSettingOverrides; imageAssetIds: string[] }): GenerateInput[]` — 笛卡尔积:每个 opportunity × 每个 preset 生成一个 GenerateInput。
  - `batchStatusLabel(status: GenerationBatchStatus): string`(中文标签)。
  - `batchProgressText(jobs: GenerationJob[]): string` — 形如 `6/10 完成`;有失败时追加 ` · N 失败`(成功数与失败数分列,不把失败混入「完成」)。

- [ ] **Step 1: Write the failing test**

Create `apps/web/test/quick-batch.test.ts`:

```ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import { buildBatchJobs, batchStatusLabel, batchProgressText } from '../src/lib/quick-batch.js';
import type { ContentPreset, GenerationJob, Project, TopicOpportunity } from '../src/types.js';

const project = { id: 'p1', name: 'P', cities: [], doctors: [] } as unknown as Project;
const opp = (id: string, title: string) => ({ id, title, whyValuable: 'w', gapIds: [], strategyId: 's1' } as unknown as TopicOpportunity);
const preset = (id: string) => ({ id, projectId: 'p1', source: 'project', name: id, description: '', values: {}, isDefault: false } as unknown as ContentPreset);

test('buildBatchJobs makes cartesian product of opportunities x presets', () => {
  const jobs = buildBatchJobs({
    project,
    opportunities: [opp('o1', 'T1'), opp('o2', 'T2')],
    presets: [preset('pr1'), preset('pr2')],
    overrides: {},
    imageAssetIds: [],
  });
  assert.equal(jobs.length, 4);
  assert.deepEqual(jobs.map((j) => [j.opportunityId, j.presetId]), [
    ['o1', 'pr1'], ['o1', 'pr2'], ['o2', 'pr1'], ['o2', 'pr2'],
  ]);
  assert.equal(jobs.every((j) => j.projectId === 'p1' && j.mode === 'simple'), true);
});

test('buildBatchJobs with single preset keeps one job per opportunity', () => {
  const jobs = buildBatchJobs({ project, opportunities: [opp('o1', 'T1'), opp('o2', 'T2')], presets: [preset('pr1')], overrides: {}, imageAssetIds: [] });
  assert.equal(jobs.length, 2);
});

test('batchStatusLabel maps to Chinese', () => {
  assert.equal(batchStatusLabel('running'), '生成中');
  assert.equal(batchStatusLabel('completed'), '已完成');
  assert.equal(batchStatusLabel('partial'), '部分完成');
  assert.equal(batchStatusLabel('failed'), '全部失败');
  assert.equal(batchStatusLabel('queued'), '排队中');
});

test('batchProgressText separates success from failure', () => {
  const jobs = [
    { status: 'completed' }, { status: 'failed' }, { status: 'running' }, { status: 'queued' },
  ] as GenerationJob[];
  assert.equal(batchProgressText(jobs), '1/4 完成 · 1 失败');
});

test('batchProgressText omits failure suffix when nothing failed', () => {
  const jobs = [
    { status: 'completed' }, { status: 'completed' }, { status: 'running' },
  ] as GenerationJob[];
  assert.equal(batchProgressText(jobs), '2/3 完成');
});
```

- [ ] **Step 2: 把测试文件加进 web package.json test 脚本**

`apps/web/package.json` 的 `"test"` 末尾追加 ` test/quick-batch.test.ts`。

- [ ] **Step 3: Run test to verify it fails**

Run: `npm run test -w @content-agent/web -- 2>&1 | grep -A3 quick-batch`
Expected: FAIL(模块不存在)

- [ ] **Step 4: 实现 quick-batch.ts**

Create `apps/web/src/lib/quick-batch.ts`:

```ts
import { buildSimpleGenerateInput, resolveSimpleGenerationSettings, type SimpleSettingOverrides } from './simple-generation';
import type { ContentPreset, GenerateInput, GenerationBatchStatus, GenerationJob, Project, TopicOpportunity } from '../types';

interface BuildBatchArgs {
  project: Project;
  opportunities: TopicOpportunity[];
  presets: ContentPreset[];
  overrides: SimpleSettingOverrides;
  imageAssetIds: string[];
}

/** 二维批量展开:每个选题 × 每个预设 → 一个 GenerateInput(复用单篇构造逻辑,零重写)。 */
export function buildBatchJobs({ project, opportunities, presets, overrides, imageAssetIds }: BuildBatchArgs): GenerateInput[] {
  const jobs: GenerateInput[] = [];
  for (const opportunity of opportunities) {
    for (const preset of presets) {
      const settings = resolveSimpleGenerationSettings({ project, preset, opportunity, overrides });
      jobs.push(buildSimpleGenerateInput({
        projectId: project.id,
        opportunity,
        settings,
        imageAssetIds,
        lockedGapIds: [],
        presetId: preset.id,
        localFieldsEnabled: false,
        overrides: overrides as Record<string, unknown>,
        randomizationDimensions: [],
      }));
    }
  }
  return jobs;
}

const STATUS_LABELS: Record<GenerationBatchStatus, string> = {
  queued: '排队中',
  running: '生成中',
  completed: '已完成',
  partial: '部分完成',
  failed: '全部失败',
};

export function batchStatusLabel(status: GenerationBatchStatus): string {
  return STATUS_LABELS[status] ?? status;
}

export function batchProgressText(jobs: GenerationJob[]): string {
  const ok = jobs.filter((j) => j.status === 'completed').length;
  const failed = jobs.filter((j) => j.status === 'failed').length;
  return failed ? `${ok}/${jobs.length} 完成 · ${failed} 失败` : `${ok}/${jobs.length} 完成`;
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npm run test -w @content-agent/web -- 2>&1 | grep -A3 quick-batch`
Expected: PASS

- [ ] **Step 6: typecheck**

Run: `npm run typecheck -w @content-agent/web`
Expected: 0 error

- [ ] **Step 7: Commit**

```bash
git add apps/web/src/lib/quick-batch.ts apps/web/test/quick-batch.test.ts apps/web/package.json
git commit -m "feat(web,quick): 批量展开纯函数 buildBatchJobs + 批次状态工具"
```

---

## Task 6: 配方回填纯函数

**Files:**
- Create: `apps/web/src/lib/quick-recipe.ts`
- Create: `apps/web/test/quick-recipe.test.ts`
- Modify: `apps/web/package.json`(test 脚本加新文件)

**Interfaces:**
- Consumes: `GenerationJob`(含 `opportunityId` / `presetId` / `resolvedConfig` / `opportunitySnapshot`)、`TopicOpportunity`、`ContentPreset`、`SimpleSettingOverrides`。
- Produces:
  - `extractRecipe(job: GenerationJob): { opportunityId?: string; presetId?: string; overrides: SimpleSettingOverrides; imageAssetIds: string[] }` — 从 job 快照抽出可回灌的配方。
  - `resolveRecipeTargets(recipe, opportunities: TopicOpportunity[], presets: ContentPreset[]): { opportunityId: string; presetId: string | undefined; warnings: string[] }` — 校验选题/预设是否仍存在,失效时回落并给出中文 warnings。

- [ ] **Step 1: Write the failing test**

Create `apps/web/test/quick-recipe.test.ts`:

```ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import { extractRecipe, resolveRecipeTargets } from '../src/lib/quick-recipe.js';
import type { ContentPreset, GenerationJob, TopicOpportunity } from '../src/types.js';

const job = {
  id: 'j1', projectId: 'p1', topic: 'T', mode: 'simple', status: 'completed',
  opportunityId: 'o1', presetId: 'pr1',
  resolvedConfig: { task: { audienceStage: 'collecting', entryPoint: 'search', city: '上海', doctor: '张三', mustMention: '玻尿酸', forbidden: '最好' } },
} as unknown as GenerationJob;

test('extractRecipe pulls opportunity, preset, overrides from job', () => {
  const recipe = extractRecipe(job);
  assert.equal(recipe.opportunityId, 'o1');
  assert.equal(recipe.presetId, 'pr1');
  assert.equal(recipe.overrides.city, '上海');
  assert.equal(recipe.overrides.doctor, '张三');
});

test('resolveRecipeTargets keeps targets when both exist', () => {
  const opps = [{ id: 'o1', title: 'T' }] as unknown as TopicOpportunity[];
  const presets = [{ id: 'pr1', name: 'P' }] as unknown as ContentPreset[];
  const r = resolveRecipeTargets({ opportunityId: 'o1', presetId: 'pr1', overrides: {}, imageAssetIds: [] }, opps, presets);
  assert.equal(r.opportunityId, 'o1');
  assert.equal(r.presetId, 'pr1');
  assert.deepEqual(r.warnings, []);
});

test('resolveRecipeTargets warns and clears opportunity when it is gone', () => {
  const presets = [{ id: 'pr1', name: 'P' }] as unknown as ContentPreset[];
  const r = resolveRecipeTargets({ opportunityId: 'oX', presetId: 'pr1', overrides: {}, imageAssetIds: [] }, [], presets);
  assert.equal(r.opportunityId, '');
  assert.equal(r.warnings.length, 1);
  assert.match(r.warnings[0]!, /选题/);
});

test('resolveRecipeTargets falls back to default preset when preset deleted', () => {
  const opps = [{ id: 'o1', title: 'T' }] as unknown as TopicOpportunity[];
  const presets = [{ id: 'prDefault', name: 'D', isDefault: true }] as unknown as ContentPreset[];
  const r = resolveRecipeTargets({ opportunityId: 'o1', presetId: 'prX', overrides: {}, imageAssetIds: [] }, opps, presets);
  assert.equal(r.presetId, 'prDefault');
  assert.equal(r.warnings.some((w) => /预设/.test(w)), true);
});
```

- [ ] **Step 2: 把测试文件加进 web package.json test 脚本**

`apps/web/package.json` 的 `"test"` 末尾追加 ` test/quick-recipe.test.ts`。

- [ ] **Step 3: Run test to verify it fails**

Run: `npm run test -w @content-agent/web -- 2>&1 | grep -A3 quick-recipe`
Expected: FAIL(模块不存在)

- [ ] **Step 4: 实现 quick-recipe.ts**

Create `apps/web/src/lib/quick-recipe.ts`:

```ts
import type { ContentPreset, GenerationJob, TopicOpportunity } from '../types';
import type { CommentRichnessLevel, SimpleSettingOverrides } from './simple-generation';

export interface QuickRecipe {
  opportunityId?: string;
  presetId?: string;
  overrides: SimpleSettingOverrides;
  imageAssetIds: string[];
}

const str = (v: unknown): string | undefined => (typeof v === 'string' && v.trim() ? v : undefined);

/** 从历史 job 快照抽出可回灌的配方:选题 + 预设 + 高级参数覆盖 + 源图。 */
export function extractRecipe(job: GenerationJob): QuickRecipe {
  const config = (job.resolvedConfig ?? {}) as Record<string, unknown>;
  const task = (config.task ?? {}) as Record<string, unknown>;
  const overrides: SimpleSettingOverrides = {};
  const audienceStage = str(task.audienceStage);
  const entryPoint = str(task.entryPoint ?? task.entry);
  const city = str(task.city);
  const doctor = str(task.doctor ?? task.person);
  const mustInclude = str(task.mustMention ?? task.mustInclude);
  const forbidden = str(task.forbidden);
  if (audienceStage) overrides.audienceStage = audienceStage;
  if (entryPoint) overrides.entryPoint = entryPoint;
  if (city) overrides.city = city;
  if (doctor) overrides.doctor = doctor;
  if (mustInclude) overrides.mustInclude = mustInclude;
  if (forbidden) overrides.forbidden = forbidden;
  const richness = str(task.commentRichness) as CommentRichnessLevel | undefined;
  if (richness) overrides.commentRichness = richness;
  return {
    opportunityId: job.opportunityId,
    presetId: job.presetId,
    overrides,
    imageAssetIds: [],
  };
}

export interface ResolvedRecipeTargets {
  opportunityId: string;
  presetId: string | undefined;
  overrides: SimpleSettingOverrides;
  imageAssetIds: string[];
  warnings: string[];
}

/** 回灌前校验选题/预设是否仍存在;失效则回落并给出中文提示。 */
export function resolveRecipeTargets(
  recipe: QuickRecipe,
  opportunities: TopicOpportunity[],
  presets: ContentPreset[],
): ResolvedRecipeTargets {
  const warnings: string[] = [];
  const oppExists = recipe.opportunityId && opportunities.some((o) => o.id === recipe.opportunityId);
  const opportunityId = oppExists ? recipe.opportunityId! : '';
  if (recipe.opportunityId && !oppExists) warnings.push('原选题已不在选题池,请挑一个新选题套用这套配置。');

  let presetId = recipe.presetId;
  const presetExists = presetId && presets.some((p) => p.id === presetId);
  if (presetId && !presetExists) {
    presetId = presets.find((p) => p.isDefault)?.id ?? presets[0]?.id;
    warnings.push('原预设已删除,已回落到默认预设。');
  }
  return { opportunityId, presetId, overrides: recipe.overrides, imageAssetIds: recipe.imageAssetIds, warnings };
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npm run test -w @content-agent/web -- 2>&1 | grep -A3 quick-recipe`
Expected: PASS

- [ ] **Step 6: typecheck**

Run: `npm run typecheck -w @content-agent/web`
Expected: 0 error

- [ ] **Step 7: Commit**

```bash
git add apps/web/src/lib/quick-recipe.ts apps/web/test/quick-recipe.test.ts apps/web/package.json
git commit -m "feat(web,quick): 配方回填纯函数 extractRecipe + resolveRecipeTargets"
```

---

## Task 7: 预设卡片组件(单选/多选共用)

**Files:**
- Create: `apps/web/src/components/quick/PresetCards.tsx`
- Modify: `apps/web/src/styles.css`(追加 `.qc-preset-cards` 等样式)

**Interfaces:**
- Consumes: `ContentPreset`。
- Produces:
  - `PresetCards` 组件,props:`{ presets: ContentPreset[]; mode: 'single' | 'multi'; selectedId?: string; selectedIds?: string[]; onSelect?: (id: string) => void; onToggle?: (id: string) => void; onSave?: () => void; disabled?: boolean }`。
  - 单选(mode='single'):点卡选中一张,高亮 `selectedId`;多选(mode='multi'):勾选 `selectedIds`,右上打勾。
  - 末尾一张虚线 "+ 存为预设" 卡触发 `onSave`。

- [ ] **Step 1: 实现 PresetCards 组件**

Create `apps/web/src/components/quick/PresetCards.tsx`:

```tsx
import { Check, Plus, Star } from 'lucide-react';
import type { ContentPreset } from '../../types';

interface Props {
  presets: ContentPreset[];
  mode: 'single' | 'multi';
  selectedId?: string;
  selectedIds?: string[];
  onSelect?: (id: string) => void;
  onToggle?: (id: string) => void;
  onSave?: () => void;
  disabled?: boolean;
}

export function PresetCards({ presets, mode, selectedId, selectedIds = [], onSelect, onToggle, onSave, disabled }: Props) {
  const isSelected = (id: string) => (mode === 'single' ? id === selectedId : selectedIds.includes(id));
  return (
    <div className="qc-preset-cards" role={mode === 'single' ? 'radiogroup' : 'group'} aria-label="内容预设">
      {presets.map((preset) => {
        const selected = isSelected(preset.id);
        return (
          <button
            key={preset.id}
            type="button"
            className={`qc-preset-card${selected ? ' selected' : ''}`}
            role={mode === 'single' ? 'radio' : 'checkbox'}
            aria-checked={selected}
            disabled={disabled}
            onClick={() => (mode === 'single' ? onSelect?.(preset.id) : onToggle?.(preset.id))}
          >
            <span className="qc-preset-card__head">
              <strong>{preset.name}</strong>
              {preset.isDefault && <Star size={12} className="qc-preset-card__star" aria-label="默认" />}
            </span>
            {preset.description && <small className="qc-preset-card__desc">{preset.description}</small>}
            {selected && <span className="qc-preset-card__tick"><Check size={13} /></span>}
          </button>
        );
      })}
      {onSave && (
        <button type="button" className="qc-preset-card qc-preset-card--add" disabled={disabled} onClick={onSave}>
          <Plus size={16} />
          <span>存为预设</span>
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 追加样式**

在 `apps/web/src/styles.css` 末尾追加(沿用 v2 token:`--v2-line` / `--v2-brand` / `--v2-radius-sm` / `--v2-surface` / `--v2-fs-sm`):

```css
.qc-preset-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); gap: 8px; }
.qc-preset-card { position: relative; text-align: left; padding: 10px 12px; border: 1px solid var(--v2-line); border-radius: var(--v2-radius-sm); background: var(--v2-surface); cursor: pointer; display: flex; flex-direction: column; gap: 4px; transition: border-color .15s, box-shadow .15s; }
.qc-preset-card:hover:not(:disabled) { border-color: var(--v2-brand); }
.qc-preset-card.selected { border-color: var(--v2-brand); box-shadow: 0 0 0 1px var(--v2-brand) inset; }
.qc-preset-card__head { display: flex; align-items: center; gap: 4px; }
.qc-preset-card__head strong { font-size: var(--v2-fs-sm); }
.qc-preset-card__star { color: var(--v2-brand); }
.qc-preset-card__desc { color: var(--v2-muted, #888); font-size: 12px; line-height: 1.4; }
.qc-preset-card__tick { position: absolute; top: 6px; right: 6px; color: var(--v2-brand); }
.qc-preset-card--add { align-items: center; justify-content: center; flex-direction: row; gap: 4px; border-style: dashed; color: var(--v2-brand); }
.qc-preset-card:disabled { opacity: .5; cursor: not-allowed; }
```

- [ ] **Step 3: typecheck**

Run: `npm run typecheck -w @content-agent/web`
Expected: 0 error

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/components/quick/PresetCards.tsx apps/web/src/styles.css
git commit -m "feat(web,quick): 预设卡片组件 PresetCards(单选/多选共用)"
```

---

## Task 8: 选题池多选 + 批量操作条(TopicTab)

**Files:**
- Modify: `apps/web/src/components/quick/TopicTab.tsx`
- Modify: `apps/web/src/components/quick/CreateTab.tsx`(向下传多选状态)
- Modify: `apps/web/src/styles.css`(批量操作条样式)

**Interfaces:**
- Consumes: 现有 `TopicTab` props、`filterOpportunities`。
- Produces:TopicTab 新增 props `{ checkedIds: string[]; onToggleCheck: (id: string) => void; onConfigBatch: () => void }`。选题卡左侧勾选框;勾 ≥1 个时底部浮出 `.qc-batch-bar`(`已选 N 个选题 [配置批量生成]`)。单选(点选题名)行为不变。

- [ ] **Step 1: 在 TopicTab 加多选 props 与勾选框**

`apps/web/src/components/quick/TopicTab.tsx` 的 Props interface 追加:

```tsx
  /** 批量:已勾选的选题 id */
  checkedIds: string[];
  onToggleCheck: (id: string) => void;
  onConfigBatch: () => void;
```

函数签名解构追加 `checkedIds, onToggleCheck, onConfigBatch`。在每个 `.qc-topic` 的 `<button className="qc-topic__main">` 之前插入勾选框:

```tsx
            <input
              type="checkbox"
              className="qc-topic__check"
              checked={checkedIds.includes(o.id)}
              disabled={busy}
              onChange={() => onToggleCheck(o.id)}
              aria-label={`勾选「${o.title}」批量生成`}
            />
```

在 `.qc-topic-list` 结束标签之后、`<details>` 之前插入批量操作条:

```tsx
      {checkedIds.length > 0 && (
        <div className="qc-batch-bar">
          <span>已选 {checkedIds.length} 个选题</span>
          <Button disabled={busy} onClick={onConfigBatch}>配置批量生成</Button>
        </div>
      )}
```

- [ ] **Step 2: CreateTab 承接并下传多选状态**

`apps/web/src/components/quick/CreateTab.tsx`:Props 追加 `checkedIds: string[]; onToggleCheck: (id: string) => void; onConfigBatch: () => void; batchMode: boolean;`,解构后传入 `<TopicTab>`。(batchMode 供 Task 9 右栏切换用,先接线。)

- [ ] **Step 3: 样式**

`apps/web/src/styles.css` 末尾追加:

```css
.qc-topic__check { margin-right: 8px; flex: 0 0 auto; }
.qc-batch-bar { position: sticky; bottom: 0; display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 10px 12px; margin-top: 8px; background: var(--v2-surface); border: 1px solid var(--v2-brand); border-radius: var(--v2-radius-sm); }
```

- [ ] **Step 4: typecheck**

Run: `npm run typecheck -w @content-agent/web`
Expected: 0 error(QuickChannelPage 尚未传新 props 会报错 → 在 Task 9 一并补;若此步单独 typecheck 失败,允许在 Task 9 完成后统一验证)

说明:本 Task 与 Task 9 是同一块交互的两半(选题多选 + 右栏批量配置),QuickChannelPage 的状态接线在 Task 9。二者应连续实现、合并验证。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/quick/TopicTab.tsx apps/web/src/components/quick/CreateTab.tsx apps/web/src/styles.css
git commit -m "feat(web,quick): 选题池多选勾选框 + 批量操作条"
```

---

## Task 9: 右栏批量配置态 + 提交(ConfigTab + QuickChannelPage)

**Files:**
- Modify: `apps/web/src/components/quick/ConfigTab.tsx`(预设下拉换 PresetCards;新增批量态)
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(批量状态:checkedIds、batchMode、批量预设多选;提交跳看板)
- Modify: `apps/web/src/components/quick/CreateTab.tsx`(下传批量态给 ConfigTab)

**Interfaces:**
- Consumes: Task 4 `api.generationBatches.create`、`buildBatchJobs`(Task 5)、Task 7 `PresetCards`、`autoApproveAndGenerate` 的输入构造依赖。
- Produces:QuickChannelPage 持有 `checkedIds / batchMode / batchPresetIds`;ConfigTab 单篇态用 `PresetCards mode="single"`,批量态用 `mode="multi"` + 公共 overrides + 提交按钮(显示 `选题数 × 预设数` 总量)。提交后调批量接口 → `onBatchSubmitted(batchId)` → 跳产出区看板。

- [ ] **Step 1: ConfigTab 预设下拉换成卡片(单篇态)**

`apps/web/src/components/quick/ConfigTab.tsx`:import `PresetCards`。把现有 `<Field label="内容预设" required><select>…</select></Field>` 与 `.qc-preset-actions` 一段替换为:

```tsx
      <Field label="内容预设" required>
        <PresetCards
          presets={presets}
          mode="single"
          selectedId={presetId}
          onSelect={(id) => setPresetId(id)}
          onSave={() => setSaveOpen(true)}
          disabled={busy || presetWorking}
        />
      </Field>
      <div className="qc-preset-actions">
        <Button variant="ghost" disabled={!presetId || currentPreset?.isDefault || busy || presetWorking} onClick={() => void makeDefault()}>设为默认</Button>
        {currentPreset?.source === 'project' && (
          <Button variant="ghost" disabled={busy || presetWorking} onClick={() => setDeleteOpen(true)}>删除</Button>
        )}
      </div>
```

(「存为预设」按钮从下方一排移入卡片区的 `+` 卡,故 `.qc-preset-actions` 里删掉原「存为预设」Button。)

- [ ] **Step 2: ConfigTab 增加批量态 props 与渲染**

Props interface 追加:

```tsx
  batchMode: boolean;
  batchPresetIds: string[];
  onToggleBatchPreset: (id: string) => void;
  batchTopicCount: number;
  onSubmitBatch: () => void;
```

解构后,在 `return` 顶部按 `batchMode` 分叉。批量态渲染(替换单篇的预设卡 + 生成按钮部分,高级设置/源图区块两态共用):

```tsx
      {batchMode ? (
        <>
          <div className="qc-batch-head">批量生成 · {batchTopicCount} 个选题</div>
          <Field label="选版本(预设可多选)" required>
            <PresetCards presets={presets} mode="multi" selectedIds={batchPresetIds} onToggle={onToggleBatchPreset} disabled={busy} />
          </Field>
          <p className="qc-hint">{batchTopicCount} 选题 × {batchPresetIds.length} 预设 = {batchTopicCount * batchPresetIds.length} 篇</p>
        </>
      ) : (
        /* 原单篇预设卡 + qc-preset-actions(Step 1 的内容) */
      )}
```

生成按钮区按 `batchMode` 切换:

```tsx
      {batchMode ? (
        <Button loading={busy} disabled={busy || batchTopicCount === 0 || batchPresetIds.length === 0} onClick={onSubmitBatch}>
          提交批量生成 · 共 {batchTopicCount * batchPresetIds.length} 篇
        </Button>
      ) : (
        <Button loading={busy} disabled={!presetId || busy} onClick={() => void generate()}>生成文案</Button>
      )}
```

- [ ] **Step 3: QuickChannelPage 批量状态与提交编排**

`apps/web/src/pages/QuickChannelPage.tsx`:新增状态:

```tsx
  const [checkedIds, setCheckedIds] = useState<string[]>([]);
  const [batchMode, setBatchMode] = useState(false);
  const [batchPresetIds, setBatchPresetIds] = useState<string[]>([]);
```

新增 handlers:

```tsx
  const onToggleCheck = (id: string) =>
    setCheckedIds((cur) => (cur.includes(id) ? cur.filter((x) => x !== id) : [...cur, id]));
  const onConfigBatch = () => {
    setBatchMode(true);
    setBatchPresetIds(presets.filter((p) => p.isDefault).map((p) => p.id));
  };
  const onToggleBatchPreset = (id: string) =>
    setBatchPresetIds((cur) => (cur.includes(id) ? cur.filter((x) => x !== id) : [...cur, id]));
```

批量提交(用 Task 5 的 `buildBatchJobs` + Task 4 的 api;需要 opportunities、presets、project、overrides、imageAssetIds):

```tsx
  const onSubmitBatch = async () => {
    if (!project || checkedIds.length === 0 || batchPresetIds.length === 0) return;
    setBusy(true);
    try {
      const jobs = buildBatchJobs({
        project,
        opportunities: opportunities.filter((o) => checkedIds.includes(o.id)),
        presets: presets.filter((p) => batchPresetIds.includes(p.id)),
        overrides,
        imageAssetIds,
      });
      const batch = await api.generationBatches.create({ projectId: project.id, jobs });
      setBatchMode(false);
      setCheckedIds([]);
      setActiveTab('history');
      setActiveBatchId(batch.id);
    } catch (e) { fail(e, '批量生成提交失败'); }
  };
```

新增 `const [activeBatchId, setActiveBatchId] = useState<string | undefined>(undefined);`,传给 HistoryTab(Task 10 用)。把 `checkedIds / onToggleCheck / onConfigBatch / batchMode / batchPresetIds / onToggleBatchPreset / batchTopicCount={checkedIds.length} / onSubmitBatch` 一路传入 `<CreateTab>` → TopicTab/ConfigTab。import `buildBatchJobs`(from `../lib/quick-batch`,Task 5 创建)。

- [ ] **Step 4: CreateTab 下传批量态给 ConfigTab**

`apps/web/src/components/quick/CreateTab.tsx`:Props 追加 `batchPresetIds / onToggleBatchPreset / onSubmitBatch`,连同 Task 8 的 `batchMode / checkedIds` 一起,`batchMode` + 批量相关传入 `<ConfigTab>`,`batchTopicCount={checkedIds.length}`。

- [ ] **Step 5: typecheck + 单测**

Run: `npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: 0 error;现有 + 新增单测全绿。

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/components/quick/ConfigTab.tsx apps/web/src/pages/QuickChannelPage.tsx apps/web/src/components/quick/CreateTab.tsx
git commit -m "feat(web,quick): 右栏批量配置态 + 预设卡片化 + 批量提交编排"
```

---

## Task 10: 批次看板 + 并发轮询(HistoryTab 升级为产出中心)

**Files:**
- Create: `apps/web/src/components/quick/BatchBoard.tsx`(批次卡片墙 + 子卡三态)
- Create: `apps/web/src/lib/batch-polling.ts`(并发轮询纯逻辑 + 单测目标)
- Create: `apps/web/test/batch-polling.test.ts`
- Modify: `apps/web/src/components/quick/HistoryTab.tsx`(顶部挂 BatchBoard)
- Modify: `apps/web/src/styles.css`
- Modify: `apps/web/package.json`(加测试文件)

**Interfaces:**
- Consumes: Task 4 `api.generationBatches.list/get`、`api.generations.get`;`GenerationBatch` / `GenerationJob` 类型。
- Produces:
  - `pendingJobIds(jobs: {id: string; status: string}[]): string[]` — 返回 `queued|running` 的 id,纯函数,供单测。
  - `<BatchBoard project activeBatchId busy fail />` — 拉 `listBatches`,进行中置顶;对 pending job 并发轮询 `api.generations.get`,完成翻牌;子卡三态(排队/生成中 %、完成+质量标、失败+重试)。

- [ ] **Step 1: Write the failing test(pendingJobIds 纯函数)**

新建 `apps/web/test/batch-polling.test.ts`:

```ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import { pendingJobIds } from '../src/lib/batch-polling.js';

test('pendingJobIds returns only queued/running ids', () => {
  const jobs = [
    { id: 'a', status: 'queued' },
    { id: 'b', status: 'running' },
    { id: 'c', status: 'completed' },
    { id: 'd', status: 'failed' },
  ];
  assert.deepEqual(pendingJobIds(jobs), ['a', 'b']);
  assert.deepEqual(pendingJobIds([]), []);
});
```

- [ ] **Step 2: 加进 web package.json 的 test 列表**

`apps/web/package.json` 的 `"test"` 命令末尾追加 ` test/batch-polling.test.ts`。

- [ ] **Step 3: Run test to verify it fails**

Run: `npm run test -w @content-agent/web -- 2>&1 | grep -A3 pendingJobIds`
Expected: FAIL(模块/函数不存在)

- [ ] **Step 4: 实现 batch-polling.ts**

新建 `apps/web/src/lib/batch-polling.ts`:

```ts
export interface JobLike { id: string; status: string; }

export function pendingJobIds(jobs: JobLike[]): string[] {
  return jobs.filter((j) => j.status === 'queued' || j.status === 'running').map((j) => j.id);
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npm run test -w @content-agent/web -- 2>&1 | grep -A3 pendingJobIds`
Expected: PASS

- [ ] **Step 6: 实现 BatchBoard 组件**

新建 `apps/web/src/components/quick/BatchBoard.tsx`:

```tsx
import { useEffect, useState, useCallback } from 'react';
import { RefreshCw, CheckCircle2, XCircle, Clock } from 'lucide-react';
import { Button } from '../Ui';
import { api } from '../../lib/api';
import { pendingJobIds } from '../../lib/batch-polling';
import type { GenerationBatch, GenerationJob, Project } from '../../types';

interface Props {
  project: Project;
  activeBatchId?: string;
  busy: boolean;
  fail: (e: unknown, fallback: string) => void;
}

export function BatchBoard({ project, activeBatchId, fail }: Props) {
  const [batches, setBatches] = useState<GenerationBatch[]>([]);
  const [jobsByBatch, setJobsByBatch] = useState<Record<string, GenerationJob[]>>({});

  const loadBatches = useCallback(async () => {
    try {
      // api.generationBatches.list 返回 GenerationBatch[](已含 jobs),直接用,无 .items
      const list = await api.generationBatches.list(project.id);
      setBatches(list);
      setJobsByBatch(Object.fromEntries(list.map((b) => [b.id, b.jobs])));
    } catch (e) { fail(e, '加载批次失败'); }
  }, [project.id, fail]);

  useEffect(() => { void loadBatches(); }, [loadBatches, activeBatchId]);

  // 并发轮询:所有批次里 pending 的 job,每 2s 拉一次,全部终态停止
  useEffect(() => {
    const allJobs = Object.values(jobsByBatch).flat();
    const pending = pendingJobIds(allJobs);
    if (pending.length === 0) return;
    const timer = setTimeout(async () => {
      const updated = await Promise.all(pending.map((id) => api.generations.get(id).catch(() => null)));
      setJobsByBatch((cur) => {
        const next = { ...cur };
        for (const [bid, jobs] of Object.entries(next)) {
          next[bid] = jobs.map((j) => updated.find((u) => u?.id === j.id) ?? j);
        }
        return next;
      });
    }, 2000);
    return () => clearTimeout(timer);
  }, [jobsByBatch]);

  const retry = async (job: GenerationJob) => {
    try {
      await api.generations.create({ projectId: project.id, opportunityId: job.opportunityId, presetId: job.presetId, mode: 'simple' } as never);
      await loadBatches();
    } catch (e) { fail(e, '重试失败'); }
  };

  if (batches.length === 0) return null;

  return (
    <div className="qc-batch-board">
      {batches.map((batch) => {
        const jobs = jobsByBatch[batch.id] ?? [];
        const done = jobs.filter((j) => j.status === 'completed').length;
        return (
          <div key={batch.id} className="qc-batch-card">
            <div className="qc-batch-card__head">
              <strong>{batch.name || `批次 ${batch.createdAt?.slice(0, 10)}`}</strong>
              <span className="qc-batch-card__count">{done}/{jobs.length}</span>
            </div>
            <div className="qc-batch-card__grid">
              {jobs.map((job) => (
                <div key={job.id} className={`qc-jobcell qc-jobcell--${job.status}`}>
                  <span className="qc-jobcell__topic">{job.topic || '选题'}</span>
                  {job.status === 'completed' && (
                    <span className="qc-jobcell__badge">
                      <CheckCircle2 size={13} /> {job.qualityStatus === 'passed' ? '可发布' : '待核查'}
                    </span>
                  )}
                  {(job.status === 'queued' || job.status === 'running') && (
                    <span className="qc-jobcell__badge"><RefreshCw size={13} className="spin" /> {job.progress ?? 0}%</span>
                  )}
                  {job.status === 'failed' && (
                    <span className="qc-jobcell__badge qc-jobcell__badge--fail">
                      <XCircle size={13} /> 失败
                      <Button variant="ghost" onClick={() => void retry(job)}>重试</Button>
                    </span>
                  )}
                  {job.status === 'queued' && <Clock size={12} />}
                </div>
              ))}
            </div>
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 7: HistoryTab 顶部挂 BatchBoard**

`apps/web/src/components/quick/HistoryTab.tsx`:import `BatchBoard`,Props 追加 `activeBatchId?: string`,在返回的列表容器最前插入 `{project && <BatchBoard project={project} activeBatchId={activeBatchId} busy={busy} fail={fail} />}`。QuickChannelPage 的 `<HistoryTab>` 传 `activeBatchId={activeBatchId}`。

- [ ] **Step 8: 样式**

`apps/web/src/styles.css` 末尾追加:

```css
.qc-batch-board { display: flex; flex-direction: column; gap: 14px; margin-bottom: 20px; }
.qc-batch-card { border: 1px solid var(--v2-line); border-radius: var(--v2-radius-sm); padding: 14px; }
.qc-batch-card__head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px; }
.qc-batch-card__count { font-size: var(--v2-fs-sm); color: var(--v2-ai); }
.qc-batch-card__grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); gap: 8px; }
.qc-jobcell { display: flex; flex-direction: column; gap: 6px; padding: 10px; border: 1px solid var(--v2-line); border-radius: var(--v2-radius-sm); font-size: var(--v2-fs-sm); }
.qc-jobcell--completed { border-color: var(--v2-ok); }
.qc-jobcell--failed { border-color: var(--v2-error, #d33); }
.qc-jobcell__badge { display: inline-flex; align-items: center; gap: 4px; }
.qc-jobcell__badge--fail { color: var(--v2-error, #d33); }
```

- [ ] **Step 9: typecheck + 全测**

Run: `npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: 0 error;全绿。

- [ ] **Step 10: Commit**

```bash
git add apps/web/src/components/quick/BatchBoard.tsx apps/web/src/lib/batch-polling.ts apps/web/test/batch-polling.test.ts apps/web/src/components/quick/HistoryTab.tsx apps/web/src/pages/QuickChannelPage.tsx apps/web/src/styles.css apps/web/package.json
git commit -m "feat(web,quick): 批次看板 + 并发轮询实时翻牌"
```

---

## Task 11: 配方回填「再来一篇」

**Files:**
- Modify: `apps/web/src/components/quick/HistoryTab.tsx` 与 `BatchBoard.tsx`(加「再来一篇」按钮)
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(`onReuseRecipe` 回灌创作区)

**复用说明:** 配方抽取与失效回落的纯函数已在 **Task 6** 实现(`apps/web/src/lib/quick-recipe.ts` 的 `extractRecipe` + `resolveRecipeTargets`),本 Task 不再重写,直接调用。

**Interfaces:**
- Consumes: Task 6 的 `extractRecipe(job)` / `resolveRecipeTargets(recipe, opportunities, presets)`;`GenerationJob`;现有创作区状态 setter。
- Produces:
  - QuickChannelPage `onReuseRecipe(job)`:用 `extractRecipe` 抽配方 → 拉最新预设 → `resolveRecipeTargets` 校验选题/预设是否仍在(失效回落 + 中文 warnings)→ 回灌 `opportunityId/presetId/overrides/imageAssetIds` → `setActiveTab('create')`。

- [ ] **Step 1: QuickChannelPage 回灌 handler(复用 Task 6 纯函数)**

`apps/web/src/pages/QuickChannelPage.tsx` import `{ extractRecipe, resolveRecipeTargets } from '../lib/quick-recipe'`,新增:

```tsx
  const onReuseRecipe = async (job: GenerationJob) => {
    if (!project) return;
    setBusy(true);
    try {
      const list = await api.presets.list(project.id);
      setPresets(list.items);
      const targets = resolveRecipeTargets(extractRecipe(job), opportunities, list.items);
      setOpportunityId(targets.opportunityId);
      setPresetId(targets.presetId);
      setOverrides(targets.overrides);
      setImageAssetIds(targets.imageAssetIds);
      setResults(clearResults().results);
      setJobId(undefined);
      setBatchMode(false);
      setActiveTab('create');
      for (const warning of targets.warnings) toast.push(warning, 'info');
      setBusy(false);
    } catch (e) { fail(e, '回填配方失败'); }
  };
```

把 `onReuseRecipe` 传给 `<HistoryTab>`(→ BatchBoard 子卡 + 单篇历史卡的「再来一篇」按钮)。

- [ ] **Step 2: 确认 Task 6 单测已覆盖抽取/回落逻辑**

Run: `npm run test -w @content-agent/web -- 2>&1 | grep -A3 quick-recipe`
Expected: PASS(Task 6 已建 `extractRecipe`/`resolveRecipeTargets` 单测;本 Task 只做 UI 接线,无新纯函数)

- [ ] **Step 3: HistoryTab / BatchBoard 加「再来一篇」按钮**

两处产出项各加一个 `<Button variant="ghost" onClick={() => void onReuseRecipe(job)}>再来一篇</Button>`(props 逐级下传 `onReuseRecipe`:QuickChannelPage → HistoryTab → BatchBoard 子卡 / 单篇历史卡)。

- [ ] **Step 4: typecheck + 全测**

Run: `npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: 0 error;全绿。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/quick/HistoryTab.tsx apps/web/src/components/quick/BatchBoard.tsx apps/web/src/pages/QuickChannelPage.tsx
git commit -m "feat(web,quick): 历史配方回填「再来一篇」"
```

---

## Task 12: 引导词模板打通(TopicTab「换一批」内联命名)

设计第 5 节② —— 引导词模板已基本达标(chips 一键填入),本轮只做体验微调:把「存为模板」从弹窗改为内联命名,减一层交互。改动集中在 TopicTab,纯 UI,不动数据/接口。

**Files:**
- Modify: `apps/web/src/components/quick/TopicTab.tsx`

**Interfaces:**
- Consumes: 现有 `saveTemplate` / `templates` / `guidance` 状态与 `api.promptTemplates`。
- Produces:「换一批」details 内的「存为模板」由 `Modal` 改为内联输入行(模板名 input + 保存/取消),`showSaveTpl` 语义保留为「内联行是否展开」。chips 一键填入、删除逻辑不变。

- [ ] **Step 1: 将「存为模板」弹窗改为内联命名行**

`apps/web/src/components/quick/TopicTab.tsx`:把「换一批」`<details>` 内那个触发 `setShowSaveTpl(true)` 的 `<Button>存为模板</Button>` 替换为按 `showSaveTpl` 展开的内联行(其余 `guidance` textarea、chips 不动):

```tsx
          {!showSaveTpl ? (
            <Button variant="ghost" disabled={busy || !guidance.trim()} onClick={() => setShowSaveTpl(true)}>存为模板</Button>
          ) : (
            <div className="qc-tpl-inline">
              <input
                autoFocus
                value={tplLabel}
                onChange={(e) => setTplLabel(e.target.value)}
                placeholder="模板名,如:术后恢复方向"
                aria-label="模板名"
                onKeyDown={(e) => { if (e.key === 'Escape') { setShowSaveTpl(false); setTplLabel(''); } }}
              />
              <Button loading={busy} disabled={!tplLabel.trim()} onClick={() => void saveTemplate()}>保存</Button>
              <Button variant="ghost" onClick={() => { setShowSaveTpl(false); setTplLabel(''); }}>取消</Button>
            </div>
          )}
```

删除文件底部原来的「存为模板」`<Modal open={showSaveTpl} …>…</Modal>` 整块(其内容已迁入上面的内联行)。`saveTemplate` 成功回调里已 `setShowSaveTpl(false)`,保持不变。

- [ ] **Step 2: 追加内联行样式**

`apps/web/src/styles.css` 末尾追加:

```css
.qc-tpl-inline { display: flex; align-items: center; gap: 8px; }
.qc-tpl-inline input { flex: 1 1 auto; }
```

- [ ] **Step 3: typecheck + 全测**

Run: `npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: 0 error;全绿(纯 UI,不涉及单测)。

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/components/quick/TopicTab.tsx apps/web/src/styles.css
git commit -m "feat(web,quick): 换一批「存为模板」改内联命名,减一层弹窗"
```

---

## 最终验证(全部任务完成后)

- [ ] **后端全测 + typecheck**

```bash
npm run test -w @content-agent/api
npm run typecheck -w @content-agent/api
```
Expected:批次迁移/展开/聚合/白名单测试全绿;既有测试不回归。

- [ ] **前端全测 + typecheck + build**

```bash
npm run test -w @content-agent/web
npm run typecheck -w @content-agent/web
npm run build -w @content-agent/web
```
Expected:全绿,0 error。

- [ ] **浏览器端到端实测(闭环)**

1. 选项目 → 知识库分析 → 创作区。
2. 左栏勾选 ≥2 个选题 → 底部批量条「配置批量生成」。
3. 右栏批量态选 ≥2 个预设 → 看到「N 选题 × M 预设 = X 篇」→ 提交。
4. 自动跳「产出」区看板 → 子卡从排队/生成中 % 实时翻牌到完成(可发布/待核查)。
5. 若有失败 → 单独「重试」。
6. 对任一产出点「再来一篇」→ 回到创作区,选题/预设/参数已回填 → 可直接生成或微调。
7. 刷新页面 → 回「产出」区,批次与进度仍在(跨会话持久验证)。
8. 单篇流程回归:不勾任何选题,点单个选题 → 右栏单篇预设卡(单选) → 生成文案,行为与改动前一致。

## 备注

- Task 8 与 Task 9 是同一交互的两半,建议连续实现、合并验证(QuickChannelPage 状态在 Task 9 接线)。
- 批次 job 并发受后端 `maxConcurrentJobs = 2` 限制,大批量会排队,属预期。
- 「重试」用现有 `POST /api/generations`(带 opportunityId + presetId),重试产生的新 job 不自动并入原批次(batch_id 为 NULL),看板通过重新 `loadBatches` 反映;如需并入批次可作后续增强(非本轮范围)。
