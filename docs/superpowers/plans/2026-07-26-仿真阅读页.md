# 仿真阅读页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把极简创作独立阅读页 `/quick/read/:jobId` 的预览区从「字段卡片列表」改成尽可能沉浸的小红书笔记仿真，工作信息（校验／判断依据／导出／改稿）一项不丢地移到预览下方。

**Architecture:** 新增 `NoteCard`（仿真笔记外壳，只接 candidate + projectName，不知道校验与导出的存在）与 `NoteComments`（会话流评论区，复用现有 `lib/comment-view.ts` 的身份判定）。`ReaderDetail` 删掉第一段成品卡片，收缩为纯工作区，并接收 `CommentPlanCard` 承接从评论区挪出的内部编排字段。候选下标从 `ReaderDetail` 内部状态提升到 `QuickReaderPage`。

**Tech Stack:** React 18 + TypeScript + Vite；纯函数测试用 `node:test` + `tsx`；样式写在单文件 `apps/web/src/styles.css`，用 `--v2-*` 设计令牌。

## Global Constraints

- 设计规格：`docs/superpowers/specs/2026-07-26-仿真阅读页-design.md`，本计划的所有取舍以它为准。
- 工作目录：`/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent`，分支 `feat/xhs-reader`。
- 不加后端字段。全部内容取自现有 `GET /api/generations/:id/reader` 投影。
- 注释、提交信息、界面文案一律中文。
- **新测试文件必须手写进 `apps/web/package.json` 的 `test` 脚本文件列表末尾**，不得用字符串替换改到别的文件名。
- 每个任务结束前跑 `npx tsc --noEmit` 与 `npm test`（在 `apps/web` 下）。
- 只使用真实存在的设计令牌：`--v2-muted`、`--v2-muted-2`、`--v2-canvas`、`--v2-surface`、`--v2-line`、`--v2-line-dark`、`--v2-ink`、`--v2-ink-2`、`--v2-ok(-soft)`、`--v2-warn(-soft)`、`--v2-error(-soft)`、`--v2-brand(-soft/-ink/-strong)`、`--v2-radius(-sm/-lg)`、`--v2-shadow-sm/-md`、`--v2-fs-micro/xs/sm/md/lg/xl/h1`。**不存在** `--v2-accent`、`--v2-ink-soft`、`--v2-ink-faint`、`--v2-surface-soft`。
- 真实性三条红线（违反即为实现错误）：点赞／收藏**不得出现任何数字**；「情景演练参考，非真实用户发言」必须以 ⓘ 形式在预览区可达；`org_answer` 线程答复必须保留可追责身份标记。
- `styles.css` 里 `.qs-main .page { max-width: none }` 特异性为 (0,2,0)，覆盖它需要同等或更强选择器。
- 全局 `input, textarea, select { width: 100% }`（styles.css:57）会压扁 flex 行内的裸 input。

---

## File Structure

| 文件 | 责任 |
|---|---|
| `apps/web/src/lib/note-view.ts`（新） | 纯函数：头像底色派生、笔记日期格式化、账号名回落 |
| `apps/web/src/components/quick/NoteCard.tsx`（新） | 仿真笔记外壳：头部／配图位／标题正文话题／互动栏，内嵌 NoteComments |
| `apps/web/src/components/quick/NoteComments.tsx`（新） | 会话流评论区：昵称、楼层缩进、追问、ⓘ 声明 |
| `apps/web/src/components/quick/NoteAlertBar.tsx`（新） | 预览上方校验细条，复用 `issueVerdict` |
| `apps/web/src/components/quick/CommentPlanCard.tsx`（新） | 工作区评论核对卡：编排信息 + 声明全文 |
| `apps/web/src/components/quick/ReaderDetail.tsx`（改） | 删第一段成品卡与 CommentSection；候选下标改由外部传入 |
| `apps/web/src/pages/QuickReaderPage.tsx`（改） | 持有 activeIndex，组装 NoteAlertBar + NoteCard + ReaderDetail |
| `apps/web/src/components/quick/CommentSection.tsx`（删） | 能力由 NoteComments + CommentPlanCard 接管 |
| `apps/web/src/styles.css`（改） | 新增 `.xhs-*` 段，删除 `.qc-comments` 段 |
| `apps/web/test/note-view.test.ts`（新） | note-view 纯函数测试 |

任务顺序：先纯函数（Task 1），再自底向上建组件（Task 2 评论区 → Task 3 笔记卡 → Task 4 告警条 → Task 5 工作区核对卡），最后组装与清理（Task 6 页面接线 → Task 7 样式 → Task 8 浏览器实测）。

---

## Task 1: note-view 纯函数

**Files:**
- Create: `apps/web/src/lib/note-view.ts`
- Test: `apps/web/test/note-view.test.ts`
- Modify: `apps/web/package.json`（`test` 脚本末尾追加 ` test/note-view.test.ts`）

**Interfaces:**
- Consumes: 无
- Produces:
  - `avatarTone(name: string): { bg: string; fg: string; initial: string }`
  - `noteDate(job: { completedAt?: string; createdAt?: string }): string | undefined`
  - `accountName(projectName?: string | null): string`

**注意：** 规格里提过的 `noteAlertSummary` 不在这里实现。校验分级已有现成实现 `lib/issue-verdict.ts` 的 `issueVerdict()`（error/warning 分组 + headline + publishable），Task 4 直接复用它，不重写。

- [ ] **Step 1: 写失败测试**

创建 `apps/web/test/note-view.test.ts`：

```ts
import test from 'node:test';
import assert from 'node:assert/strict';
import { accountName, avatarTone, noteDate } from '../src/lib/note-view';

test('avatarTone 对同一账号名稳定', () => {
  const a = avatarTone('稳行驾校');
  const b = avatarTone('稳行驾校');
  assert.deepEqual(a, b);
});

test('avatarTone 不同名字落到不同底色', () => {
  const names = ['稳行驾校', '毛毛驿站宠物美容', '云图健身', 'A', 'B'];
  const tones = new Set(names.map((n) => avatarTone(n).bg));
  assert.ok(tones.size >= 3, `期望至少 3 种底色，实际 ${tones.size}`);
});

test('avatarTone 取首字符作为头像字', () => {
  assert.equal(avatarTone('稳行驾校').initial, '稳');
  assert.equal(avatarTone('acme').initial, 'A');
});

test('avatarTone 空名字不崩，给兜底字', () => {
  assert.equal(avatarTone('').initial, '号');
});

// fixture 挑正午而不是 UTC 午夜:noteDate 按本地时区格式化(对产品是对的),
// 用 00:00Z 会让测试在西半球跨到前一天,变成跟机器时区绑定的假绿。
test('noteDate 优先用 completedAt', () => {
  assert.equal(
    noteDate({ completedAt: '2026-07-25T12:00:00.000Z', createdAt: '2026-07-01T12:00:00.000Z' }),
    '2026-07-25',
  );
});

test('noteDate 只有 createdAt 时退到它', () => {
  assert.equal(noteDate({ createdAt: '2026-07-01T12:00:00.000Z' }), '2026-07-01');
});

test('noteDate 两者皆缺返回 undefined', () => {
  assert.equal(noteDate({}), undefined);
});

test('noteDate 遇到无法解析的时间返回 undefined 而不是 Invalid Date', () => {
  assert.equal(noteDate({ completedAt: '不是时间' }), undefined);
});

test('accountName 缺项目名时回落发布账号', () => {
  assert.equal(accountName(undefined), '发布账号');
  assert.equal(accountName(''), '发布账号');
  assert.equal(accountName('稳行驾校'), '稳行驾校');
});
```

- [ ] **Step 2: 注册测试文件并确认失败**

编辑 `apps/web/package.json`，在 `test` 脚本字符串**末尾**追加 ` test/note-view.test.ts`（保留其余 35 个文件名原样）。

Run: `cd apps/web && npx tsx --test test/note-view.test.ts`
Expected: FAIL — `Cannot find module '../src/lib/note-view'`

- [ ] **Step 3: 实现**

创建 `apps/web/src/lib/note-view.ts`：

```ts
/**
 * 仿真笔记的展示派生值。
 *
 * 这里只放「从已有数据算出来的显示值」,不含任何真实性判断——校验分级复用
 * lib/issue-verdict.ts 的 issueVerdict(),不在这里重写一套。
 */

/**
 * 头像底色。
 *
 * 系统没有真实头像,也不该有:上传头像是平台账号的事,这里只是产出预览。用账号名
 * 派生一个稳定的柔和底色,同一项目每次进页面颜色一致(否则用户会以为换了账号)。
 */
const TONES = [
  { bg: '#ffe0e5', fg: '#c8395a' },
  { bg: '#e2ecff', fg: '#3a5fa8' },
  { bg: '#e4f6e8', fg: '#2f7a45' },
  { bg: '#fff2dc', fg: '#a8792e' },
  { bg: '#efe6ff', fg: '#6b4aa8' },
  { bg: '#dff4f4', fg: '#2b7a7a' },
] as const;

export function avatarTone(name: string): { bg: string; fg: string; initial: string } {
  const text = name.trim();
  // 逐字符累加而不是取首字符 charCode:「稳行驾校」和「稳定健身」首字相同,
  // 只看首字会让同一批项目撞成一个色。
  let sum = 0;
  for (const ch of text) sum += ch.codePointAt(0) ?? 0;
  const tone = TONES[sum % TONES.length]!;
  const first = [...text][0];
  return {
    bg: tone.bg,
    fg: tone.fg,
    // 拉丁字母大写,中文原样;空名字给「号」而不是空白圆圈。
    initial: first ? first.toUpperCase() : '号',
  };
}

/**
 * 笔记日期。真实小红书笔记页脚显示发布日期,这里用完成时间——它是这篇稿子
 * 「成形」的时间。两者皆缺就不渲染日期行,不编一个今天。
 */
export function noteDate(job: { completedAt?: string; createdAt?: string }): string | undefined {
  const raw = job.completedAt ?? job.createdAt;
  if (!raw) return undefined;
  const date = new Date(raw);
  // 历史数据里出现过非 ISO 的脏值;Invalid Date 直接不显示,别把
  // 「Invalid Date」印在仿真笔记上。
  if (Number.isNaN(date.getTime())) return undefined;
  const pad = (n: number) => String(n).padStart(2, '0');
  return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}`;
}

/** 账号名。项目名缺失时用中性称呼,不留空。 */
export function accountName(projectName?: string | null): string {
  const text = (projectName ?? '').trim();
  return text || '发布账号';
}
```

- [ ] **Step 4: 跑测试**

Run: `cd apps/web && npx tsx --test test/note-view.test.ts`
Expected: PASS，9 项全绿

再跑一次换时区，确认日期格式化不与机器时区绑定：
Run: `cd apps/web && TZ=America/New_York npx tsx --test test/note-view.test.ts`
Expected: 同样 9 项全绿

- [ ] **Step 5: 类型检查**

Run: `cd apps/web && npx tsc --noEmit`
Expected: 无输出

- [ ] **Step 6: 提交**

```bash
git add apps/web/src/lib/note-view.ts apps/web/test/note-view.test.ts apps/web/package.json
git commit -m "feat(web): 仿真笔记的展示派生值

头像底色按账号名逐字符派生(取首字会让同一批项目撞色),日期用完成时间且
脏值一律不渲染。校验分级复用 issue-verdict,不在这里重写。"
```

---

## Task 2: NoteComments 会话流评论区

**Files:**
- Create: `apps/web/src/components/quick/NoteComments.tsx`

**Interfaces:**
- Consumes: `lib/comment-view.ts` 的 `commentSectionView(comments, gapNames)`、`gapNameMap(gapCards, ledgerEntries)`（均已存在，签名见该文件）；`lib/note-view.ts` 的 `avatarTone`
- Produces: `<NoteComments candidate={...} accountLabel={string} />`，props 类型 `NoteCommentsProps`

现有 `commentSectionView` 返回的 `CommentRow` 已含 `displayName`、`askerLabel`、`answererLabel`、`threadKind`、`followUps`——本任务只做渲染，不新增判定逻辑。

- [ ] **Step 1: 创建组件**

创建 `apps/web/src/components/quick/NoteComments.tsx`：

```tsx
import { Copy, Info } from 'lucide-react';
import { Button, useToast } from '../Ui';
import { commentSectionView, gapNameMap } from '../../lib/comment-view';
import { avatarTone } from '../../lib/note-view';
import type { ReaderCandidate } from '../../types';

type Source = Pick<
  ReaderCandidate,
  'comments' | 'commentDisclaimer' | 'commentOwnedFirstComment' | 'gapCards' | 'gapLedger'
>;

export interface NoteCommentsProps {
  candidate: Source;
  /** 发布账号名,用于自备首评与机构答复的署名 */
  accountLabel: string;
}

/**
 * 仿真评论区。
 *
 * 与旧 CommentSection 的区别不只是样式:那版把每条都打上「模拟读者」「员工身份」
 * 徽标,并把 boundary/nextStep/承担缺口平铺在问答旁边——内部编排指令直接展示给
 * 用户,而且徽标密度高到看不出这是一条评论。
 *
 * 这里改成会话流:昵称用模型产出的真实 displayName(实测「打呼的小海豹」「酸梅汤
 * 加冰」这类),楼层缩进,追问跟在下面。口径收到一处:标题行一个 ⓘ 承载
 * 「情景演练参考,非真实用户发言」。这个 ⓘ 不能删——界面不声明就等于在断言这些是
 * 真实留言,而方法论明确禁止假冒消费者。编排信息移交工作区 CommentPlanCard。
 */
export function NoteComments({ candidate, accountLabel }: NoteCommentsProps) {
  const toast = useToast();
  const view = commentSectionView(
    candidate.comments,
    gapNameMap(candidate.gapCards, candidate.gapLedger?.entries),
  );
  if (!view) return null;

  const copyOne = async (text: string) => {
    try { await navigator.clipboard.writeText(text); toast.push('已复制'); }
    catch { toast.push('复制失败，请手动选择文本', 'error'); }
  };

  const total = view.rows.length + (candidate.commentOwnedFirstComment ? 1 : 0);

  return (
    <section className="xhs-comments">
      <header className="xhs-comments__head">
        <span>共 {total} 条评论</span>
        {/* 声明挂在 ⓘ 的 title 上而不是常驻一行:预览区要沉浸,但这句必须可达。 */}
        <span
          className="xhs-comments__note"
          title={candidate.commentDisclaimer || '情景演练与答复参考，不代表真实用户发言。'}
          aria-label="关于这些评论"
        >
          <Info size={12} />
        </span>
      </header>

      {candidate.commentOwnedFirstComment && (
        <Row
          name={accountLabel}
          badge="作者"
          text={candidate.commentOwnedFirstComment}
          onCopy={() => void copyOne(candidate.commentOwnedFirstComment!)}
        />
      )}

      {view.rows.map((row, i) => (
        <Row
          key={row.id ?? `${row.question}-${i}`}
          name={row.displayName || '读者'}
          text={row.question}
          onCopy={() => void copyOne(row.question)}
          reply={
            row.answer?.trim()
              ? {
                  // answererLabel 已由 comment-view 按 threadKind 判定:只有 org_answer
                  // 才是可追责身份,reader_exchange 是模拟读者接话。这里据此决定署名和
                  // 「作者」标,不自己重判。
                  name: row.answererLabel === '模拟读者接话' ? (row.displayName ? '另一位读者' : '读者') : accountLabel,
                  badge: row.answererLabel && row.answererLabel !== '模拟读者接话' ? '作者' : undefined,
                  text: row.answer,
                  onCopy: () => void copyOne(row.answer),
                }
              : undefined
          }
          followUps={row.followUps.filter((f) => f.question?.trim())}
          onCopyFollow={(text) => void copyOne(text)}
        />
      ))}
    </section>
  );
}

interface RowProps {
  name: string;
  badge?: string;
  text: string;
  onCopy: () => void;
  reply?: { name: string; badge?: string; text: string; onCopy: () => void };
  followUps?: Array<{ question: string; answer: string }>;
  onCopyFollow?: (text: string) => void;
}

function Row({ name, badge, text, onCopy, reply, followUps = [], onCopyFollow }: RowProps) {
  const tone = avatarTone(name);
  return (
    <div className="xhs-comment">
      <span className="xhs-comment__avatar" style={{ background: tone.bg, color: tone.fg }}>{tone.initial}</span>
      <div className="xhs-comment__main">
        <div className="xhs-comment__name">
          {name}
          {badge && <em>{badge}</em>}
          <Button variant="ghost" icon={<Copy size={11} />} onClick={onCopy} aria-label="复制这条" />
        </div>
        <p className="xhs-comment__text">{text}</p>

        {reply && (
          <div className="xhs-comment__reply">
            <div className="xhs-comment__name">
              {reply.name}
              {reply.badge && <em>{reply.badge}</em>}
              <Button variant="ghost" icon={<Copy size={11} />} onClick={reply.onCopy} aria-label="复制答复" />
            </div>
            <p className="xhs-comment__text">{reply.text}</p>
          </div>
        )}

        {followUps.map((f, i) => (
          <div key={`${f.question}-${i}`} className="xhs-comment__reply">
            <div className="xhs-comment__name">
              读者
              {onCopyFollow && (
                <Button variant="ghost" icon={<Copy size={11} />} onClick={() => onCopyFollow(f.question)} aria-label="复制追问" />
              )}
            </div>
            <p className="xhs-comment__text">{f.question}</p>
            {/* organic_reaction 按设计没有机构答复,空答复不渲染出一行空白。 */}
            {f.answer?.trim() && <p className="xhs-comment__text xhs-comment__text--reply">{f.answer}</p>}
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 类型检查**

Run: `cd apps/web && npx tsc --noEmit`
Expected: 无输出。`Button` 的 `children` 可选（继承 `ButtonHTMLAttributes`），无 children 时它渲染一个空 `<span>`，样式任务里已按纯图标尺寸压过。

- [ ] **Step 3: 构建**

Run: `cd apps/web && npm run build`
Expected: `✓ built in …`

- [ ] **Step 4: 提交**

```bash
git add apps/web/src/components/quick/NoteComments.tsx
git commit -m "feat(web): 会话流样式的仿真评论区

昵称用模型产出的 displayName,楼层缩进+追问,徽标从每条收到标题行一个 ⓘ。
署名与「作者」标沿用 comment-view 按 threadKind 的判定,不自己重判——
reader_exchange 的 answer 是模拟读者接话,标成机构发言是实测过的错标。
编排信息(boundary/nextStep/缺口)不进预览区。"
```

---

## Task 3: NoteCard 仿真笔记外壳

**Files:**
- Create: `apps/web/src/components/quick/NoteCard.tsx`

**Interfaces:**
- Consumes: Task 1 的 `avatarTone`、`noteDate`、`accountName`；Task 2 的 `<NoteComments candidate accountLabel />`
- Produces: `<NoteCard candidate={ReaderCandidate} job={ReaderJob} projectName={string | undefined} />`

- [ ] **Step 1: 创建组件**

创建 `apps/web/src/components/quick/NoteCard.tsx`：

```tsx
import { Bookmark, Copy, Heart, MessageCircle } from 'lucide-react';
import { Button, useToast } from '../Ui';
import { NoteComments } from './NoteComments';
import { accountName, avatarTone, noteDate } from '../../lib/note-view';
import type { ReaderCandidate, ReaderJob } from '../../types';

interface Props {
  candidate: ReaderCandidate;
  job: Pick<ReaderJob, 'completedAt' | 'createdAt'>;
  projectName?: string;
}

/**
 * 仿真笔记预览。
 *
 * 只做一件事:把这一版成品渲染成「发出去会长什么样」。它不知道校验、导出、改稿的
 * 存在——那些在下方工作区。
 *
 * 两处刻意的不仿真:
 * - 点赞/收藏只有图标,不带数字。系统没有平台数据,编一个「129 赞」一旦出现在界面
 *   上,用户截图给老板时它就成了业绩证据,与「研究数据不是平台表现数据」这条红线
 *   直接冲突。图标不构成断言,数字构成。
 * - 配图位是虚线框 + 配图说明文字。系统只产出拍摄要求,没有真图;放一张占位图会让
 *   人以为配图已经有了。
 */
export function NoteCard({ candidate, job, projectName }: Props) {
  const toast = useToast();
  const account = accountName(projectName);
  const tone = avatarTone(account);
  const date = noteDate(job);

  const copy = async (text: string, label = '已复制') => {
    try { await navigator.clipboard.writeText(text); toast.push(label); }
    catch { toast.push('复制失败，请手动选择文本', 'error'); }
  };

  // 「复制全文」要能直接粘进小红书发布框,所以只含标题/正文/话题。
  // imageBrief 故意不进来——它是拍摄指令,不是发布内容,混进去会被误发。
  // 它有自己的复制按钮(见配图位)。
  const fullText = [candidate.title, candidate.body, candidate.tags.join(' ')]
    .filter((part) => part && part.trim())
    .join('\n\n');

  return (
    <article className="xhs-note">
      <header className="xhs-note__head">
        <span className="xhs-note__avatar" style={{ background: tone.bg, color: tone.fg }}>{tone.initial}</span>
        <span className="xhs-note__account">{account}</span>
        {/* 装饰,不可点:这是预览,没有真实的关注关系可建立 */}
        <span className="xhs-note__follow" aria-hidden="true">关注</span>
        <Button variant="ghost" icon={<Copy size={13} />} onClick={() => void copy(fullText, '已复制全文')}>
          复制全文
        </Button>
      </header>

      {candidate.imageBrief && (
        <div className="xhs-note__cover">
          {/* 配图说明是要被复制走交给摄影/选图的,所以它需要自己的复制入口:
              「复制全文」按设计不含它。按钮与说明标签同一行,不挤压虚线框里的正文。 */}
          <div className="xhs-note__unit">
            <p>{candidate.imageBrief}</p>
            <Button variant="ghost" icon={<Copy size={12} />} onClick={() => void copy(candidate.imageBrief!, '已复制配图说明')} aria-label="复制配图说明" />
          </div>
          <small>配图说明 · 按此拍摄或选图</small>
        </div>
      )}

      <div className="xhs-note__body">
        {candidate.title && (
          <div className="xhs-note__unit">
            <h2 className="xhs-note__title">{candidate.title}</h2>
            <Button variant="ghost" icon={<Copy size={12} />} onClick={() => void copy(candidate.title, '已复制标题')} aria-label="复制标题" />
          </div>
        )}
        {candidate.body?.trim()
          ? (
            <div className="xhs-note__unit">
              <p className="xhs-note__text">{candidate.body}</p>
              <Button variant="ghost" icon={<Copy size={12} />} onClick={() => void copy(candidate.body, '已复制正文')} aria-label="复制正文" />
            </div>
          )
          : <p className="xhs-note__text xhs-note__text--empty">本次未产出正文</p>}

        {candidate.tags.length > 0 && (
          <div className="xhs-note__unit">
            <div className="xhs-note__tags">
              {candidate.tags.map((tag) => <span key={tag}>{tag}</span>)}
            </div>
            <Button variant="ghost" icon={<Copy size={12} />} onClick={() => void copy(candidate.tags.join(' '), '已复制话题')} aria-label="复制话题" />
          </div>
        )}

        {date && <div className="xhs-note__date">{date}</div>}
      </div>

      {/* 无数字:见组件头注释 */}
      <div className="xhs-note__bar">
        <span><Heart size={15} />点赞</span>
        <span><Bookmark size={15} />收藏</span>
        <span><MessageCircle size={15} />评论{candidate.comments.length > 0 ? ` ${candidate.comments.length}` : ''}</span>
      </div>

      <NoteComments candidate={candidate} accountLabel={account} />
    </article>
  );
}
```

- [ ] **Step 2: 类型检查与构建**

Run: `cd apps/web && npx tsc --noEmit && npm run build`
Expected: 无类型错误，构建通过

- [ ] **Step 3: 提交**

```bash
git add apps/web/src/components/quick/NoteCard.tsx
git commit -m "feat(web): 仿真笔记预览卡

头部/配图位/标题正文话题/互动栏/评论区,只管「发出去长什么样」,不知道校验与
导出的存在。点赞收藏不带数字(编造的赞数会被当成平台业绩证据),配图位是虚线框
加拍摄说明而非占位图。"
```

---

## Task 4: NoteAlertBar 校验细条

**Files:**
- Create: `apps/web/src/components/quick/NoteAlertBar.tsx`

**Interfaces:**
- Consumes: `lib/issue-verdict.ts` 的 `issueVerdict(validation)` → `{ publishable, blocking: VerdictItem[], advisory: VerdictItem[], headline: string }`（已存在）
- Produces: `<NoteAlertBar validation={candidate.validation} onSeeDetail={() => void} />`

- [ ] **Step 1: 创建组件**

创建 `apps/web/src/components/quick/NoteAlertBar.tsx`：

```tsx
import { AlertTriangle, CheckCircle2, Info } from 'lucide-react';
import { issueVerdict } from '../../lib/issue-verdict';
import type { VerdictInput } from '../../lib/issue-verdict';

interface Props {
  validation?: VerdictInput;
  /** 点「看详情」滚到下方工作区的校验全文 */
  onSeeDetail: () => void;
}

/**
 * 预览上方一条细横条。
 *
 * 为什么不是卡片:这页第一屏要给成品。但也不能只靠禁用导出按钮来表达——实测该项目
 * 21 篇产出里 11 篇是「需人工核对」,不是罕见路径,用户会先复制正文再发现导不出。
 * 细条是两者的折中:一行,不抢版面,但跑不掉。
 *
 * 分级直接用 issueVerdict:它已经处理了「按数组顺序取首条会把 warning 当结论」
 * 这个实测过的坑(129 个未通过候选里 110 个首条是 warning)。
 *
 * 三态而不是两态:能不能导出看 publishable,不看 warning 数。实测 229 个候选里 46 个
 * 是 valid=true 且带 warning——这些可以直接发,拿黄条写「N 项建议核对」会被读成没过校验。
 */
export function NoteAlertBar({ validation, onSeeDetail }: Props) {
  const verdict = issueVerdict(validation);
  const blocking = verdict.blocking.length;
  const advisory = verdict.advisory.length;

  // 一态:干净通过。
  if (verdict.publishable && advisory === 0) {
    return (
      <div className="xhs-alert xhs-alert--ok">
        <CheckCircle2 size={13} />
        <span>可直接发布</span>
      </div>
    );
  }

  // 二态:通过了但有建议项。先说结论(可发布),再说还有什么可看,配中性信息色。
  if (verdict.publishable) {
    return (
      <div className="xhs-alert xhs-alert--info">
        <Info size={13} />
        <span>{`可直接发布 · ${advisory} 项建议核对`}</span>
        <button type="button" className="xhs-alert__link" onClick={onSeeDetail}>看详情</button>
      </div>
    );
  }

  // 三态:未通过。有 error 时点明是硬门槛;没 error 也要说清导不出,别只说「建议」。
  return (
    <div className={`xhs-alert xhs-alert--${blocking > 0 ? 'error' : 'warn'}`}>
      {blocking > 0 ? <AlertTriangle size={13} /> : <Info size={13} />}
      <span>
        {blocking > 0
          ? `${blocking} 项需核对才能导出`
          : `未通过校验 · ${advisory} 项待核对`}
        {blocking > 0 && advisory > 0 && `，另有 ${advisory} 项建议核对`}
      </span>
      <button type="button" className="xhs-alert__link" onClick={onSeeDetail}>看详情</button>
    </div>
  );
}
```

- [ ] **Step 2: 确认 VerdictInput 已导出**

Run: `cd apps/web && grep -n "export interface VerdictInput" src/lib/issue-verdict.ts`
Expected: 命中一行。若未导出，给该 interface 加 `export`。

- [ ] **Step 3: 类型检查与构建**

Run: `cd apps/web && npx tsc --noEmit && npm run build`
Expected: 无类型错误，构建通过

- [ ] **Step 4: 提交**

```bash
git add apps/web/src/components/quick/NoteAlertBar.tsx
git commit -m "feat(web): 预览上方校验细条

一行,不抢第一屏,但跑不掉——实测 21 篇里 11 篇需人工核对,只靠禁用导出按钮
表达会让用户先复制再发现导不出。分级复用 issueVerdict。"
```

---

## Task 5: CommentPlanCard 工作区评论核对

**Files:**
- Create: `apps/web/src/components/quick/CommentPlanCard.tsx`

**Interfaces:**
- Consumes: `commentSectionView`、`gapNameMap`（同 Task 2）
- Produces: `<CommentPlanCard candidate={ReaderCandidate} />`

承接从预览区移出的内部编排字段（`stageLabel`、`functionLabel`、`gapLabel`、`boundary`、`nextStep`）与声明全文。这些是旧 `CommentSection` 里「编排信息」折叠区的内容，运营核对时仍要看，所以移交而不是删除。

- [ ] **Step 1: 创建组件**

创建 `apps/web/src/components/quick/CommentPlanCard.tsx`：

```tsx
import { Fragment } from 'react';
import { ClipboardCheck } from 'lucide-react';
import { uncoveredGapLabels } from '../../lib/comment-cref';
import { commentSectionView, gapNameMap } from '../../lib/comment-view';
import type { ReaderCandidate } from '../../types';

type Source = Pick<
  ReaderCandidate,
  'comments' | 'commentDisclaimer' | 'commentUncoveredGaps' | 'gapCards' | 'gapLedger'
>;

/**
 * 评论核对(工作区)。
 *
 * 预览区要沉浸,所以 boundary/nextStep/承担缺口/线程功能/读者阶段这些编排字段从
 * 评论区移到这里。它们是写给运营核对的计划字段,不是发布文案——旧版平铺在问答旁边,
 * 于是「不得夸大监督效果」这类内部指令直接展示给了用户。删掉也不行:核对时要用。
 */
export function CommentPlanCard({ candidate }: { candidate: Source }) {
  const view = commentSectionView(
    candidate.comments,
    gapNameMap(candidate.gapCards, candidate.gapLedger?.entries),
  );
  if (!view) return null;

  // 追问 boundary 也算「有计划字段」,否则一条只有追问边界的线程会被过滤掉,
  // 下面新加的追问分支永远进不去。
  const rows = view.rows.filter(
    (row) =>
      row.stageLabel || row.functionLabel || row.gapLabel || row.boundary || row.nextStep ||
      row.followUps.some((f) => f.boundary?.trim()),
  );

  // 未展开缺口存的是 gap id(gap_1、gap_aftercare 这类)。同一张卡上 gapLabel 走了名称映射,
  // 这里再摆原始 id 就自相矛盾;查不到的 id 由 uncoveredGapLabels 原样透传,不编名称。
  //
  // 注意 uncoveredGapLabels 的第二参数原本要求完整的 InformationGapPlanningCard[],
  // 而阅读投影的 gapCards 是窄化版本(没有 category/importance 等)。该函数体只读
  // gapId/label,所以把参数放宽成 Array<Pick<…,'gapId'|'label'>>——两个既有调用点
  // 传完整卡片,数组协变,不受影响。
  const uncovered = uncoveredGapLabels(candidate.commentUncoveredGaps, candidate.gapCards);

  return (
    <section className="quick-card">
      <header>
        <h3><ClipboardCheck size={13} />评论核对</h3>
        {view.identitySummary.length > 0 && (
          <small className="qc-hint">答复身份：{view.identitySummary.join(' / ')}</small>
        )}
      </header>
      <div className="quick-card__body">
        {candidate.commentDisclaimer && <p className="qc-disclaimer">{candidate.commentDisclaimer}</p>}

        {rows.length > 0 && (
          <ol className="qc-planlist">
            {rows.map((row, i) => (
              <li key={row.id ?? `${row.question}-${i}`}>
                <p className="qc-planlist__q">{row.question}</p>
                <dl>
                  {row.stageLabel && <><dt>读者阶段</dt><dd>{row.stageLabel}</dd></>}
                  {row.functionLabel && <><dt>线程功能</dt><dd>{row.functionLabel}</dd></>}
                  {row.gapLabel && <><dt>承担缺口</dt><dd>{row.gapLabel}</dd></>}
                  {row.boundary && <><dt>边界要求</dt><dd>{row.boundary}</dd></>}
                  {row.nextStep && <><dt>下一步核验</dt><dd>{row.nextStep}</dd></>}
                  {/* 追问自己的 boundary 也要列:字段从 reader 投影一路通到这里,而旧
                      CommentSection 是渲染它的——本任务之后那个组件会被删,不接就永久看不到了。
                      实测当前 168 条追问 0 条带值,但通着的字段迟早会被填。 */}
                  {row.followUps.map((f, j) =>
                    f.boundary?.trim()
                      ? <Fragment key={`fu-${j}`}><dt>追问 {j + 1} 边界要求</dt><dd>{f.boundary}</dd></Fragment>
                      : null,
                  )}
                </dl>
              </li>
            ))}
          </ol>
        )}

        {/* 未展开缺口存的是 id(gap_1 / gap_aftercare 这类),不是人能读的名称。
            同一张卡上 gapLabel 走名称映射就是为了不摆 id,这里也必须解析。 */}
        {uncovered.length > 0 && (
          <small className="qc-uncovered">评论区未展开：{uncovered.join('、')}</small>
        )}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: 类型检查与构建**

Run: `cd apps/web && npx tsc --noEmit && npm run build`
Expected: 通过

- [ ] **Step 3: 提交**

```bash
git add apps/web/src/components/quick/CommentPlanCard.tsx
git commit -m "feat(web): 工作区评论核对卡

承接从仿真评论区移出的编排字段(读者阶段/线程功能/承担缺口/边界/下一步)与
声明全文。这些是写给运营核对的计划字段,不该摆在仿真笔记里,但也不能删。"
```

---

## Task 6: ReaderDetail 收缩为工作区

**Files:**
- Modify: `apps/web/src/components/quick/ReaderDetail.tsx`
- Delete: `apps/web/src/components/quick/CommentSection.tsx`

**Interfaces:**
- Consumes: Task 5 的 `<CommentPlanCard candidate />`
- Produces: `ReaderDetail` 的 props 新增两项，`index` 内部状态移除：
  - `activeIndex: number`
  - `onPickIndex: (index: number) => void`

- [ ] **Step 1: 改 props 与状态归属**

在 `apps/web/src/components/quick/ReaderDetail.tsx`：

1. `interface Props` 追加：

```ts
  /** 当前候选下标。原为内部 useState,现在预览区与工作区共用同一个下标,提升到页面层 */
  activeIndex: number;
  onPickIndex: (index: number) => void;
```

2. 函数签名改为 `export function ReaderDetail({ job, onExport, onRevise, revisingId, onRetry, retrying, activeIndex, onPickIndex }: Props)`
3. 删除 `const [index, setIndex] = useState(0);`
4. `const current = candidates[Math.min(index, candidates.length - 1)]!;` 里的 `index` 换成 `activeIndex`
5. `<CandidateDiffBar … activeIndex={index} onPick={setIndex} />` 换成 `activeIndex={activeIndex} onPick={onPickIndex}`
6. `import { useState } from 'react';` 保持不动 —— `instruction`（按意见修改的输入框）仍是内部状态，只有候选下标提升出去

- [ ] **Step 2: 删除第一段成品卡与评论区**

删掉整段（含 `{/* 第一段:成品 */}` 注释与其中的 `<div className="qc-reader__section">` 块）：标题 CopyCard、正文 CopyCard、标签 CopyCard、配图说明 CopyCard、`<CommentSection candidate={current} />`。

随之删除已无用的 `CopyCard` 函数定义、`import { CommentSection } from './CommentSection';`、以及不再被引用的 `Copy` 图标导入（`按发布顺序复制` 仍用 `Copy`，保留）。检查 `copyText` 是否还有调用点：`按发布顺序复制` 用它，保留。

- [ ] **Step 3: 在判断依据段插入评论核对卡**

在 `{/* 第二段:判断依据 */}` 的 `<FactLedgerCard candidate={current} />` 之后插入：

```tsx
        <CommentPlanCard candidate={current} />
```

并在文件顶部加 `import { CommentPlanCard } from './CommentPlanCard';`

- [ ] **Step 4: 删除 CommentSection**

```bash
git rm apps/web/src/components/quick/CommentSection.tsx
```

- [ ] **Step 5: 确认没有遗留引用**

Run: `cd apps/web && grep -rn "CommentSection" src/ test/`
Expected: 无输出

- [ ] **Step 6: 类型检查**

Run: `cd apps/web && npx tsc --noEmit`
Expected: 报 `QuickReaderPage.tsx` 缺 `activeIndex` / `onPickIndex` 两个 prop —— 这是预期的，下一任务修

---

## Task 7: QuickReaderPage 组装

**Files:**
- Modify: `apps/web/src/pages/QuickReaderPage.tsx:229-242`

**Interfaces:**
- Consumes: Task 3 `<NoteCard candidate job projectName />`、Task 4 `<NoteAlertBar validation onSeeDetail />`、Task 6 的 `ReaderDetail` 新 props
- Produces: 无（页面为叶子）

- [ ] **Step 1: 加状态与引用**

在 `QuickReaderPage` 的 `const [now, setNow] = useState(() => Date.now());` 之后加：

```ts
  /** 候选下标:预览区(NoteCard)与工作区(ReaderDetail)共用,所以归页面持有 */
  const [activeIndex, setActiveIndex] = useState(0);
  /** 「看详情」滚到工作区校验全文 */
  const workbenchRef = useRef<HTMLDivElement | null>(null);
```

顶部导入改为 `import { useCallback, useEffect, useRef, useState } from 'react';`

- [ ] **Step 2: 切篇时重置下标**

在 `useEffect(() => { void load(); }, [load]);` 之后加：

```ts
  // 换一篇要回到第一个候选:上一篇选了第 3 版,下一篇可能只有 1 版,
  // 沿用旧下标会让 NoteCard 与工作区读到不同候选(Math.min 各自兜底)。
  useEffect(() => { setActiveIndex(0); }, [jobId]);
```

- [ ] **Step 3: 替换渲染块**

把 `apps/web/src/pages/QuickReaderPage.tsx:233-242` 的 `{job && job.candidates.length > 0 && (<ReaderDetail … />)}` 整块换成：

```tsx
      {/* 排队中/失败的任务不进 NoteCard:半成品套上笔记外壳会像已经发布过了,
          这两种状态各自有 WaitCard 与失败块。 */}
      {job && job.status === 'completed' && job.candidates.length > 0 && (() => {
        // 两头都夹,与 ReaderDetail 里同一套:预览与工作区读同一个下标,
        // 任一侧只夹上界都会在负数/NaN 时崩在非空断言上。
        const current = job.candidates[Math.min(Math.max(0, activeIndex || 0), job.candidates.length - 1)]!;
        return (
          <>
            {job.candidates.length > 1 && (
              <div className="xhs-switch">
                {job.candidates.map((candidate, i) => (
                  <button
                    key={candidate.id}
                    type="button"
                    className={i === activeIndex ? 'active' : ''}
                    onClick={() => setActiveIndex(i)}
                  >
                    第 {i + 1} 版
                  </button>
                ))}
              </div>
            )}

            <NoteAlertBar
              validation={current.validation}
              onSeeDetail={() => workbenchRef.current?.scrollIntoView({ behavior: 'smooth', block: 'start' })}
            />

            <NoteCard candidate={current} job={job} projectName={project?.name} />

            <div className="xhs-workbench" ref={workbenchRef}>
              <ReaderDetail
                job={job}
                activeIndex={activeIndex}
                onPickIndex={setActiveIndex}
                onExport={exportAs}
                onRevise={revise}
                revisingId={revisingId}
                onRetry={() => void retry()}
                retrying={retrying}
              />
            </div>
          </>
        );
      })()}
```

- [ ] **Step 4: 加导入**

```tsx
import { NoteAlertBar } from '../components/quick/NoteAlertBar';
import { NoteCard } from '../components/quick/NoteCard';
```

- [ ] **Step 5: 类型检查与构建**

Run: `cd apps/web && npx tsc --noEmit && npm run build`
Expected: 无类型错误，构建通过

- [ ] **Step 6: 跑全套测试**

Run: `cd apps/web && npm test 2>&1 | grep -E "^# (tests|pass|fail)"`
Expected: `fail 1` —— 唯一那条红是 `presets.test.ts` 的 `comment_multi_turn_growth`（另一会话在改评论参数分类），与本次改动无关。若出现第二条红，是真回归，必须修。

- [ ] **Step 7: 提交**

```bash
git add apps/web/src/components/quick/ReaderDetail.tsx apps/web/src/pages/QuickReaderPage.tsx
git rm --cached apps/web/src/components/quick/CommentSection.tsx 2>/dev/null || true
git commit -m "feat(web): 阅读页装上仿真笔记预览,ReaderDetail 收缩为工作区

预览区(切换条+校验细条+NoteCard)在上,工作区(校验全文/判断依据/评论核对/
发布方案/导出/改稿)在下。候选下标提升到页面层供两侧共用,切篇时重置——
沿用旧下标会让两侧在候选数不同的下一篇里读到不同版本。

排队中与失败的任务不进 NoteCard:半成品套笔记外壳会像已经发布过。
CommentSection 删除,能力由 NoteComments 与 CommentPlanCard 接管。"
```

---

## Task 8: 样式

**Files:**
- Modify: `apps/web/src/styles.css`

**Interfaces:**
- Consumes: Task 2–7 用到的类名：`.xhs-note`、`.xhs-note__head/__avatar/__account/__follow/__cover/__body/__title/__text/__tags/__date/__bar`、`.xhs-comments`、`.xhs-comments__head/__note`、`.xhs-comment`、`.xhs-comment__avatar/__main/__name/__text/__reply`、`.xhs-alert`、`.xhs-alert--ok/--info/--warn/--error`、`.xhs-alert__link`、`.xhs-switch`、`.xhs-workbench`、`.qc-planlist`
- Produces: 无

- [ ] **Step 1: 追加仿真笔记样式段**

在 `apps/web/src/styles.css` 末尾追加。注意选择器都加 `.quick-shell` 前缀以避免影响完整版工作台：

```css
/* ============ 仿真阅读页 ============
   预览区照小红书笔记的排版节奏来:窄栏、大标题、蓝色话题、会话流评论。
   刻意不做的两件事在 NoteCard 组件注释里(点赞数、真配图)。 */
.quick-shell .xhs-switch { display: flex; gap: 4px; }
.quick-shell .xhs-switch button {
  border: 1px solid var(--v2-line); background: var(--v2-surface); color: var(--v2-muted);
  font-size: var(--v2-fs-xs); padding: 3px 11px; border-radius: 999px; cursor: pointer;
}
.quick-shell .xhs-switch button.active {
  border-color: var(--v2-brand); color: var(--v2-brand-ink); background: var(--v2-brand-soft); font-weight: 500;
}

.quick-shell .xhs-alert {
  display: flex; align-items: center; gap: 7px;
  font-size: var(--v2-fs-xs); padding: 7px 12px; border-radius: var(--v2-radius);
  border: 1px solid var(--v2-line);
}
.quick-shell .xhs-alert--ok { color: var(--v2-ok); background: var(--v2-ok-soft); border-color: transparent; }
/* info = 可发布但有建议项(实测 46/229 的主路径)。中性色:它不是警告,别让用户以为导不出 */
.quick-shell .xhs-alert--info { color: var(--v2-ink-2); background: var(--v2-canvas); }
.quick-shell .xhs-alert--warn { color: var(--v2-warn); background: var(--v2-warn-soft); border-color: transparent; }
.quick-shell .xhs-alert--error { color: var(--v2-error); background: var(--v2-error-soft); border-color: transparent; }
.quick-shell .xhs-alert__link {
  margin-left: auto; background: none; border: none; cursor: pointer;
  color: inherit; text-decoration: underline; font-size: var(--v2-fs-xs); padding: 0;
}

/* 笔记卡:宽度按手机笔记的观感收窄,但不钉死 390px——桌面上留白过多反而不像在读 */
.quick-shell .xhs-note {
  background: #fff; border: 1px solid var(--v2-line); border-radius: 14px;
  overflow: hidden; max-width: 560px; width: 100%; margin: 0 auto;
}
.quick-shell .xhs-note__head {
  display: flex; align-items: center; gap: 9px; padding: 11px 14px; border-bottom: 1px solid var(--v2-line);
}
.quick-shell .xhs-note__avatar,
.quick-shell .xhs-comment__avatar {
  display: inline-flex; align-items: center; justify-content: center;
  border-radius: 50%; font-weight: 600; flex: none;
}
.quick-shell .xhs-note__avatar { width: 30px; height: 30px; font-size: var(--v2-fs-sm); }
.quick-shell .xhs-note__account { font-size: var(--v2-fs-sm); font-weight: 600; color: var(--v2-ink); }
.quick-shell .xhs-note__follow {
  font-size: var(--v2-fs-micro); color: #ff2e4d; border: 1px solid #ff2e4d;
  border-radius: 11px; padding: 2px 11px; user-select: none;
}
.quick-shell .xhs-note__head .button { margin-left: auto; }

/* 配图位:虚线框 + 拍摄说明。放占位图会让人以为配图已经有了 */
.quick-shell .xhs-note__cover {
  aspect-ratio: 3 / 4; max-height: 380px; display: flex; flex-direction: column;
  align-items: center; justify-content: center; gap: 8px; text-align: center;
  padding: 22px; border-bottom: 1px solid var(--v2-line);
  background: var(--v2-canvas); border-radius: 0;
  outline: 1px dashed var(--v2-line-dark); outline-offset: -12px;
}
.quick-shell .xhs-note__cover p { font-size: var(--v2-fs-xs); color: var(--v2-muted); line-height: 1.65; margin: 0; }
.quick-shell .xhs-note__cover small { font-size: var(--v2-fs-micro); color: var(--v2-muted-2); }
/* 配图位里的 unit 不做悬停显形:虚线框里只有这一个按钮,藏起来等于没有。
   选择器比下面的 .xhs-note__unit .button 多一个类(0,3,0 vs 0,2,0),所以能赢,
   与书写顺序无关——但别把它改成两类选择器,那样就要靠顺序了。 */
.quick-shell .xhs-note__cover .xhs-note__unit { width: 100%; justify-content: center; }
.quick-shell .xhs-note__cover .xhs-note__unit > p { flex: 0 1 auto; }
.quick-shell .xhs-note__cover .xhs-note__unit .button { opacity: 1; flex: none; padding: 2px 8px; }

.quick-shell .xhs-note__body { padding: 14px 16px 8px; }
/* 可复制单元:复制按钮悬停才显现,常亮会让笔记看起来像个表单 */
.quick-shell .xhs-note__unit { display: flex; align-items: flex-start; gap: 6px; }
.quick-shell .xhs-note__unit > :first-child { flex: 1; min-width: 0; }
.quick-shell .xhs-note__unit .button { opacity: 0; padding: 2px 6px; flex: none; transition: opacity .12s; }
.quick-shell .xhs-note__unit:hover .button,
.quick-shell .xhs-note__unit .button:focus-visible { opacity: 1; }
.quick-shell .xhs-note__title {
  font-size: var(--v2-fs-lg); font-weight: 700; line-height: 1.42;
  letter-spacing: -.2px; color: var(--v2-ink); margin: 0 0 8px;
}
.quick-shell .xhs-note__text {
  font-size: var(--v2-fs-md); line-height: 1.78; color: var(--v2-ink);
  white-space: pre-wrap; margin: 0 0 10px;
}
.quick-shell .xhs-note__text--empty { color: var(--v2-muted-2); }
.quick-shell .xhs-note__tags { display: flex; flex-wrap: wrap; gap: 7px; margin-bottom: 10px; }
.quick-shell .xhs-note__tags span { font-size: var(--v2-fs-sm); color: #3a6ea8; }
.quick-shell .xhs-note__date { font-size: var(--v2-fs-micro); color: var(--v2-muted-2); }

.quick-shell .xhs-note__bar {
  display: flex; gap: 20px; padding: 10px 16px;
  border-top: 1px solid var(--v2-line); border-bottom: 1px solid var(--v2-line);
  font-size: var(--v2-fs-xs); color: var(--v2-muted);
}
.quick-shell .xhs-note__bar span { display: inline-flex; align-items: center; gap: 5px; }

/* 评论区:会话流 */
.quick-shell .xhs-comments { padding: 12px 16px 16px; }
.quick-shell .xhs-comments__head {
  display: flex; align-items: center; gap: 6px;
  font-size: var(--v2-fs-xs); color: var(--v2-muted); margin-bottom: 12px;
}
.quick-shell .xhs-comments__note { display: inline-flex; color: var(--v2-muted-2); cursor: help; }
.quick-shell .xhs-comment { display: flex; gap: 9px; margin-bottom: 13px; }
.quick-shell .xhs-comment__avatar { width: 26px; height: 26px; font-size: var(--v2-fs-micro); }
.quick-shell .xhs-comment__main { min-width: 0; flex: 1; }
.quick-shell .xhs-comment__name {
  display: flex; align-items: center; gap: 6px;
  font-size: var(--v2-fs-xs); color: var(--v2-muted); margin-bottom: 3px;
}
.quick-shell .xhs-comment__name em {
  font-style: normal; font-size: var(--v2-fs-micro); padding: 0 5px; border-radius: 3px;
  background: var(--v2-brand-soft); color: var(--v2-brand-ink);
}
/* 复制按钮悬停才显现:每条评论挂一个常亮按钮会毁掉会话流的观感 */
.quick-shell .xhs-comment__name .button { opacity: 0; padding: 1px 5px; transition: opacity .12s; }
.quick-shell .xhs-comment:hover .xhs-comment__name .button,
.quick-shell .xhs-comment__name .button:focus-visible { opacity: 1; }
.quick-shell .xhs-comment__text {
  font-size: var(--v2-fs-sm); line-height: 1.68; color: var(--v2-ink); margin: 0; white-space: pre-wrap;
}
.quick-shell .xhs-comment__reply {
  margin-top: 9px; padding-left: 10px; border-left: 2px solid var(--v2-line);
}
.quick-shell .xhs-comment__text--reply { color: var(--v2-ink-2); margin-top: 4px; }

/* 工作区与预览之间拉开距离并换底色:两种阅读姿态,视觉上要能分开 */
.quick-shell .xhs-workbench { margin-top: 10px; padding-top: 18px; border-top: 1px solid var(--v2-line); }

.quick-shell .qc-planlist { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 12px; }
.quick-shell .qc-planlist__q { font-size: var(--v2-fs-sm); color: var(--v2-ink); margin: 0 0 4px; }
.quick-shell .qc-planlist dl {
  display: grid; grid-template-columns: auto 1fr; gap: 3px 10px; margin: 0;
  font-size: var(--v2-fs-xs);
}
.quick-shell .qc-planlist dt { color: var(--v2-muted-2); }
.quick-shell .qc-planlist dd { color: var(--v2-muted); margin: 0; }

@media (max-width: 560px) {
  .quick-shell .xhs-note { border-radius: 0; border-left: none; border-right: none; }
}
```

- [ ] **Step 2: 删除旧评论区样式**

Run: `cd apps/web && grep -n "qc-comments\|qc-comment__" src/styles.css`

删掉命中的规则行（`.qc-comments`、`.qc-comments__list`、`.qc-comment`、`.qc-comment__meta`、`.qc-comment__q`、`.qc-comment__a`、`.qc-comment__plan`、`.qc-comment__follow`、`.qc-comment--owned`）。**保留** `.qc-disclaimer` 与 `.qc-uncovered`——`CommentPlanCard` 仍在用。

改完再跑一次 `grep -rn "qc-comment" src/` 确认 `src` 里没有组件仍引用被删的类名。

- [ ] **Step 3: 确认 --v2-radius 存在**

Run: `cd apps/web && grep -n "\-\-v2-radius:" src/styles.css`
Expected: 命中定义行。若只有 `--v2-radius-sm` / `-lg`，把上面 `.xhs-alert` 的 `var(--v2-radius)` 改成 `var(--v2-radius-sm)`。

- [ ] **Step 4: 构建**

Run: `cd apps/web && npm run build`
Expected: 构建通过

- [ ] **Step 5: 提交**

```bash
git add apps/web/src/styles.css
git commit -m "style(web): 仿真笔记预览样式

窄栏、大标题、蓝色话题、会话流评论。复制按钮悬停才显现——每条常亮会毁掉
会话流观感。工作区换底色与预览分开。删掉旧 qc-comments 段,qc-disclaimer 与
qc-uncovered 保留给评论核对卡。"
```

---

## Task 9: 浏览器实测

**Files:** 无代码改动（发现问题则回到对应任务修）

**Interfaces:**
- Consumes: Task 1–8 的全部产出
- Produces: 无

项目现无 DOM 测试设施，组件渲染由浏览器实测覆盖。API 在 `127.0.0.1:8780`，web 在 `127.0.0.1:5173`。登录口令取自 `.env` 的 `BOOTSTRAP_ADMIN_PASSWORD`（**不要把值回显到输出里**）。

- [ ] **Step 1: 确认两端在跑**

Run: `curl -s -o /dev/null -w "web %{http_code}\n" http://127.0.0.1:5173/`
Expected: `web 200`。不通则在 `apps/web` 跑 `npm run dev`，在 `apps/api` 跑 `npm start`（**不要用 `--watch`**：另一会话在重建 `agent-core/dist`，watch 会持续重启）。

- [ ] **Step 2: 逐项目视觉核对**

用 puppeteer-core（装在 `/tmp/ca-probe/node_modules/`，系统 Chrome 在 `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`）登录后依次截图：

1. 一篇 `completed` 且有 3 候选的产出 → 切换「第 1/2/3 版」，确认预览与工作区同步换版
2. 一篇校验未通过的 → 细条显示「N 项需核对才能导出」，点「看详情」滚到工作区
3. 一篇校验通过的 → 细条「可直接发布」
4. 一篇 `failed` 的 → **不出现** NoteCard，只有失败块
5. 无评论的候选 → 评论区整块不渲染，互动栏「评论」无数字
6. 视口宽 390px → 笔记卡满宽、配图位仍 3:4

逐张看截图，不只看 DOM 断言。

- [ ] **Step 3: 三条红线核对**

在截图与 DOM 上确认：

- 互动栏没有任何数字（除真实评论数）
- 评论区标题行有 ⓘ 且 `title` 含「非真实用户发言」或 `commentDisclaimer` 全文
- `org_answer` 线程的答复带「作者」标

Run: `cd /tmp/ca-probe && node --input-type=module -e "…"` 取页面 innerText 与 `[title]` 属性核对。

- [ ] **Step 4: 控制台无错**

确认四种状态页面均无 `pageerror` 与 4xx（登录页 `/api/auth/me` 的 401 是未登录探测，属正常）。

- [ ] **Step 5: 全套测试与最终提交**

```bash
cd apps/web && npx tsc --noEmit && npm test 2>&1 | grep -E "^# (tests|pass|fail)"
cd ../api && npm test 2>&1 | grep -E "^# (tests|pass|fail)"
```
Expected: web `fail 1`（`comment_multi_turn_growth`，与本次无关）；api 全绿。

若 Step 2–4 发现问题，回到对应任务修，修完重跑本任务。全部通过后推分支开 PR：

```bash
git push -u origin feat/xhs-reader
gh pr create --base main --title "feat(saas): 阅读页改为仿真小红书笔记" --body "$(cat <<'EOF'
## 改了什么

阅读页预览区从「字段卡片列表」改成仿真小红书笔记:头部、配图位、标题正文话题、
互动栏、会话流评论区。工作信息(校验/判断依据/评论核对/发布方案/导出/改稿)
一项不丢地移到预览下方。

## 真实性口径

- 点赞收藏只有图标不带数字:系统没有平台数据,编造的赞数会被当成业绩证据
- 评论区标题行 ⓘ 承载「情景演练参考,非真实用户发言」,不可删
- org_answer 答复保留「作者」标,落地「答复只能由可追责身份发布」
- 内部编排字段(边界/下一步/承担缺口)移到工作区评论核对卡,不进仿真笔记

## 测过什么

- note-view 纯函数 10 项
- 浏览器实测:3 候选切换、校验未通过/通过、失败任务不进笔记卡、无评论、390px 窄屏
- web 348/349(唯一红灯 comment_multi_turn_growth 属另一会话在改的评论参数分类)、api 全绿

设计规格:`docs/superpowers/specs/2026-07-26-仿真阅读页-design.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

