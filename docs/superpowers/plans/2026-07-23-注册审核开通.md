# 注册申请 + 管理员审核开通 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 开放前端自助注册申请,由管理员在后台审核开通(通过即自动建账号 + 专属工作区),并关闭登录页的 demo 兜底。

**Architecture:** 新增公开接口 `POST /api/register` 把申请写入独立表 `registration_requests`(密码提交即 argon2 哈希);`AdminController` 新增列出/通过/拒绝三个仅管理员接口,approve 复用抽出的建号建工作区逻辑;前端新增公开 `/register` 页 + 登录页入口 + TeamPage 审核区块;移除 AuthContext 的 demo 降级。

**Tech Stack:** NestJS + node:sqlite(better-sqlite 风格 prepare/transaction)+ @node-rs/argon2 + React + react-router-dom + vitest(web)/ node:test(api)。

## Global Constraints

- 迁移机制:`PRAGMA user_version` 递增迁移,本功能挂 **v10**(当前最新 v9)。verbatim schema 见 Task 1。
- 密码规则:≥12 且 ≤256 字符(沿用 `AuthService.validatePassword`)。
- 用户名规则:3–64 字符,`/^[^\s/\\]+$/`(沿用 `AuthService.createUser`)。
- 手机号规则:中国大陆 `/^1[3-9]\d{9}$/`。
- 机构名称:必填,≤120 字符。
- 密码全程不存明文:提交时即 `argon2` 哈希存入 `registration_requests.password_hash`;approve 时直接搬用该哈希。
- approve 后新用户 `must_change_password=0`(密码是用户自设)。
- 客服微信常量:`wjyy5035`,前端不明文渲染,仅"复制"交互。
- 审计动作名:`registration.approve` / `registration.reject`。
- 唯一性用应用层查询(非 DB UNIQUE),以支持"拒绝后可用同名重申"。
- 后端测试命令:`cd content-agent && npm run test -w @content-agent/api`。
- 前端类型检查:`cd content-agent && npm run typecheck -w @content-agent/web`。

---

### Task 1: 数据库迁移 v10 — registration_requests 表

**Files:**
- Modify: `apps/api/src/database.service.ts`(在 `if (version < 9)` 块之后、`migrate()` 结尾之前追加 v10 块)
- Test: `apps/api/src/registration.service.spec.ts`(本任务先建一个最小 schema 断言测试)

**Interfaces:**
- Produces: 表 `registration_requests(id, username, password_hash, organization_name, phone, status, review_note, reviewed_by, reviewed_at, created_user_id, created_at, updated_at)`;索引 `registration_requests_status_idx`。

- [ ] **Step 1: 先确认现有迁移尾部结构**

Run: `grep -n "if (version < 9)" apps/api/src/database.service.ts`
Expected: 命中两行(一处建表块 `if (version < 9) this.transaction`,一处 `if (version < 9) version = 9;`)。记下 `version = 9;` 的行号,新块加在其后。

- [ ] **Step 2: 追加 v10 迁移块**

在 `if (version < 9) version = 9;` 之后插入:

```ts
    if (version < 10) this.transaction(() => {
      this.db.exec(`
        CREATE TABLE registration_requests (
          id TEXT PRIMARY KEY,
          username TEXT NOT NULL,
          password_hash TEXT NOT NULL,
          organization_name TEXT NOT NULL,
          phone TEXT NOT NULL,
          status TEXT NOT NULL DEFAULT 'pending'
            CHECK(status IN ('pending','approved','rejected')),
          review_note TEXT,
          reviewed_by TEXT REFERENCES users(id),
          reviewed_at TEXT,
          created_user_id TEXT REFERENCES users(id),
          created_at TEXT NOT NULL,
          updated_at TEXT NOT NULL
        );
        CREATE INDEX registration_requests_status_idx ON registration_requests(status, created_at);
        PRAGMA user_version = 10;
      `);
    });
    if (version < 10) version = 10;
```

- [ ] **Step 3: 写迁移断言测试**

创建 `apps/api/src/registration.service.spec.ts`(先只测迁移落表,后续任务往同文件追加):

```ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import { mkdtempSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { DatabaseService } from './database.service.js';
import type { ApiOptions } from './config.js';

function makeDb(): DatabaseService {
  const dir = mkdtempSync(join(tmpdir(), 'reg-test-'));
  const options = {
    dataDir: dir,
    databasePath: join(dir, 'test.db'),
    adminUsername: 'admin',
    adminPassword: 'Admin-change-me-2026!',
    sessionTtlMs: 3_600_000,
  } as unknown as ApiOptions;
  return new DatabaseService(options);
}

test('migration v10 creates registration_requests table', () => {
  const db = makeDb();
  const row = db
    .prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='registration_requests'")
    .get() as { name: string } | undefined;
  assert.equal(row?.name, 'registration_requests');
  const version = Number(db.prepare('PRAGMA user_version').get()?.user_version);
  assert.equal(version, 10);
});
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: 新测试 PASS,已有测试不回归。

> 注:若 `ApiOptions` 字段与上面不符(字段名/必填项),先 `grep -n "interface ApiOptions" apps/api/src/config.ts` 读取真实结构,用真实字段构造 options。

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/database.service.ts apps/api/src/registration.service.spec.ts
git commit -m "feat(api): registration_requests table migration (v10)"
```

---

### Task 2: 抽出可复用的「建账号 + 建工作区」方法

**Files:**
- Modify: `apps/api/src/auth.service.ts`(新增 `provisionUserWithWorkspace`,并让 `ensureBootstrapAdmin` 复用它以避免重复)
- Test: `apps/api/src/registration.service.spec.ts`(追加)

**Interfaces:**
- Consumes: 现有 `hashPassword`、`this.database`、`nowIso`、`slugify`、`randomUUID`。
- Produces:
  ```ts
  // 在已有事务内调用(调用方负责包 transaction);返回新建 user/workspace id
  provisionUserWithWorkspace(input: {
    username: string;
    passwordHash: string;          // 已哈希,不接收明文
    systemRole: 'admin' | 'user';
    mustChangePassword: boolean;
    workspaceName: string;
  }): { userId: string; workspaceId: string }
  ```

- [ ] **Step 1: 读现有 ensureBootstrapAdmin / createUser,确认插入语句字段**

Run: `grep -n "INSERT INTO users\|INSERT INTO workspaces\|INSERT INTO workspace_members\|uniqueSlug\|slugify" apps/api/src/auth.service.ts apps/api/src/resource.service.ts`
Expected: 看到 users/workspaces/workspace_members 三张表的列清单,以及 slug 生成方式(`ResourceService.uniqueSlug` 或 `slugify`)。以真实列名为准。

- [ ] **Step 2: 写失败测试**

追加到 `registration.service.spec.ts`:

```ts
import { AuthService } from './auth.service.js';

function makeAuth(db: DatabaseService): AuthService {
  const options = { adminUsername: 'admin', adminPassword: 'Admin-change-me-2026!', sessionTtlMs: 3_600_000 } as unknown as ApiOptions;
  return new AuthService(db, options);
}

test('provisionUserWithWorkspace creates user + workspace + Owner member', async () => {
  const db = makeDb();
  const auth = makeAuth(db);
  const passwordHash = await auth.hashPassword('a-strong-password-123');
  const { userId, workspaceId } = db.transaction(() =>
    auth.provisionUserWithWorkspace({
      username: 'clinic01',
      passwordHash,
      systemRole: 'user',
      mustChangePassword: false,
      workspaceName: '示范口腔诊所',
    }),
  );
  const user = db.prepare('SELECT username, system_role, must_change_password FROM users WHERE id = ?').get(userId) as any;
  assert.equal(user.username, 'clinic01');
  assert.equal(user.system_role, 'user');
  assert.equal(Number(user.must_change_password), 0);
  const member = db.prepare('SELECT role FROM workspace_members WHERE workspace_id = ? AND user_id = ?').get(workspaceId, userId) as any;
  assert.equal(member.role, 'Owner');
});
```

- [ ] **Step 3: 运行确认失败**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: FAIL — `provisionUserWithWorkspace is not a function`。

- [ ] **Step 4: 实现方法**

在 `AuthService` 内新增(字段以 Step 1 读到的真实列为准;下面按 spec 已知 schema 写):

```ts
  provisionUserWithWorkspace(input: {
    username: string;
    passwordHash: string;
    systemRole: 'admin' | 'user';
    mustChangePassword: boolean;
    workspaceName: string;
  }): { userId: string; workspaceId: string } {
    const userId = randomUUID();
    const workspaceId = randomUUID();
    const now = nowIso();
    const slug = this.uniqueWorkspaceSlug(input.workspaceName);
    this.database
      .prepare(
        `INSERT INTO users
           (id, username, password_hash, system_role, must_change_password, created_at, updated_at)
         VALUES (?, ?, ?, ?, ?, ?, ?)`,
      )
      .run(userId, input.username, input.passwordHash, input.systemRole, input.mustChangePassword ? 1 : 0, now, now);
    this.database
      .prepare(
        `INSERT INTO workspaces (id, slug, name, owner_id, created_at, updated_at)
         VALUES (?, ?, ?, ?, ?, ?)`,
      )
      .run(workspaceId, slug, input.workspaceName, userId, now, now);
    this.database
      .prepare(
        `INSERT INTO workspace_members (workspace_id, user_id, role, created_at, updated_at)
         VALUES (?, ?, 'Owner', ?, ?)`,
      )
      .run(workspaceId, userId, now, now);
    return { userId, workspaceId };
  }

  private uniqueWorkspaceSlug(base: string): string {
    let candidate = slugify(base) || 'workspace';
    let suffix = 2;
    while (this.database.prepare('SELECT 1 FROM workspaces WHERE slug = ?').get(candidate)) {
      candidate = `${(slugify(base) || 'workspace').slice(0, 56)}-${suffix++}`;
    }
    return candidate;
  }
```

确保 `slugify` 已从 `./utils.js` 导入(文件顶部已有 `requireString, slugify` 则无需改)。

- [ ] **Step 5: 重构 ensureBootstrapAdmin 复用它(DRY)**

将 `ensureBootstrapAdmin` 事务体内手写的三段 INSERT 替换为调用 `provisionUserWithWorkspace`,保持 admin 语义(systemRole='admin'、mustChangePassword=true、workspaceName='默认工作区'、slug 仍为 'default')。若默认 slug 必须固定为 'default',在该调用后不走 `uniqueWorkspaceSlug`——为最小改动,可保留 `ensureBootstrapAdmin` 原样、仅新增方法供 approve 用;二选一,以不破坏现有 bootstrap 测试为准。

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: bootstrap 相关测试仍 PASS。

- [ ] **Step 6: 运行全部测试确认通过**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: 新增与已有测试全部 PASS。

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/auth.service.ts apps/api/src/registration.service.spec.ts
git commit -m "feat(api): extract provisionUserWithWorkspace for reuse"
```

---

### Task 3: RegistrationService — 提交/查重/审核核心逻辑

**Files:**
- Create: `apps/api/src/registration.service.ts`
- Modify: `apps/api/src/app.module.ts`(注册 provider)
- Test: `apps/api/src/registration.service.spec.ts`(追加)

**Interfaces:**
- Consumes: `DatabaseService`、`AuthService`(`hashPassword`、`provisionUserWithWorkspace`)、`AuditService`(可选,记审计)、`nowIso`、`randomUUID`。
- Produces:
  ```ts
  submit(input: { username: unknown; password: unknown; organizationName: unknown; phone: unknown }): Promise<{ ok: true }>
  list(status?: string): Array<{ id; username; organizationName; phone; status; createdAt }>
  approve(id: string, reviewerId: string): { userId: string; workspaceId: string }
  reject(id: string, reviewerId: string, reason: string): void
  loginHintFor(username: string): { status: 'pending' } | { status: 'rejected'; note: string } | null
  ```

- [ ] **Step 1: 写失败测试(提交 + 查重 + approve + reject + 重申 + loginHint)**

追加到 `registration.service.spec.ts`:

```ts
import { RegistrationService } from './registration.service.js';

function makeReg(db: DatabaseService, auth: AuthService): RegistrationService {
  const audit = { record: () => undefined } as any;
  return new RegistrationService(db, auth, audit);
}

const validInput = () => ({
  username: 'clinicA',
  password: 'a-strong-password-123',
  organizationName: 'A 口腔',
  phone: '13800138000',
});

test('submit stores a pending request with hashed password', async () => {
  const db = makeDb();
  const reg = makeReg(db, makeAuth(db));
  await reg.submit(validInput());
  const row = db.prepare('SELECT status, password_hash, phone FROM registration_requests WHERE username = ?').get('clinicA') as any;
  assert.equal(row.status, 'pending');
  assert.notEqual(row.password_hash, 'a-strong-password-123');
  assert.match(row.password_hash, /^\$argon2/);
});

test('submit rejects duplicate username taken by an existing user', async () => {
  const db = makeDb();
  const auth = makeAuth(db);
  db.transaction(() => auth.provisionUserWithWorkspace({ username: 'taken', passwordHash: 'x', systemRole: 'user', mustChangePassword: false, workspaceName: 'w' }));
  const reg = makeReg(db, auth);
  await assert.rejects(() => reg.submit({ ...validInput(), username: 'taken' }));
});

test('submit rejects duplicate pending phone', async () => {
  const db = makeDb();
  const reg = makeReg(db, makeAuth(db));
  await reg.submit(validInput());
  await assert.rejects(() => reg.submit({ ...validInput(), username: 'clinicB' }));
});

test('approve provisions user and marks request approved', async () => {
  const db = makeDb();
  const auth = makeAuth(db);
  const reg = makeReg(db, auth);
  await reg.submit(validInput());
  const req = db.prepare('SELECT id FROM registration_requests WHERE username = ?').get('clinicA') as any;
  const { userId } = reg.approve(req.id, 'admin-id');
  const user = db.prepare('SELECT username, must_change_password FROM users WHERE id = ?').get(userId) as any;
  assert.equal(user.username, 'clinicA');
  assert.equal(Number(user.must_change_password), 0);
  const after = db.prepare('SELECT status, created_user_id FROM registration_requests WHERE id = ?').get(req.id) as any;
  assert.equal(after.status, 'approved');
  assert.equal(after.created_user_id, userId);
});

test('reject stores note and allows re-apply with same username', async () => {
  const db = makeDb();
  const reg = makeReg(db, makeAuth(db));
  await reg.submit(validInput());
  const req = db.prepare('SELECT id FROM registration_requests WHERE username = ?').get('clinicA') as any;
  reg.reject(req.id, 'admin-id', '资料不全');
  assert.equal(reg.loginHintFor('clinicA')?.status, 'rejected');
  await reg.submit(validInput()); // 拒绝后可重申,不抛错
  assert.equal(reg.loginHintFor('clinicA')?.status, 'pending');
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: FAIL — 找不到 `./registration.service.js`。

- [ ] **Step 3: 实现 RegistrationService**

创建 `apps/api/src/registration.service.ts`:

```ts
import { randomUUID } from 'node:crypto';
import { BadRequestException, ConflictException, Inject, Injectable, NotFoundException } from '@nestjs/common';
import { AuditService } from './audit.service.js';
import { AuthService } from './auth.service.js';
import { DatabaseService } from './database.service.js';
import { nowIso, requireString } from './utils.js';

const PHONE_RE = /^1[3-9]\d{9}$/;

interface RequestRow {
  id: string;
  username: string;
  password_hash: string;
  organization_name: string;
  phone: string;
  status: 'pending' | 'approved' | 'rejected';
  review_note: string | null;
  created_at: string;
}

@Injectable()
export class RegistrationService {
  constructor(
    @Inject(DatabaseService) private readonly database: DatabaseService,
    @Inject(AuthService) private readonly auth: AuthService,
    @Inject(AuditService) private readonly audit: AuditService,
  ) {}

  async submit(input: { username: unknown; password: unknown; organizationName: unknown; phone: unknown }): Promise<{ ok: true }> {
    const username = requireString(input.username, '用户名', { min: 3, max: 64, pattern: /^[^\s/\\]+$/u });
    if (typeof input.password !== 'string' || input.password.length < 12 || input.password.length > 256) {
      throw new BadRequestException('密码长度必须在 12-256 个字符之间');
    }
    const organizationName = requireString(input.organizationName, '机构名称', { max: 120 });
    const phone = requireString(input.phone, '手机号', { min: 11, max: 11 });
    if (!PHONE_RE.test(phone)) throw new BadRequestException('请输入有效的手机号');

    const userTaken = this.database.prepare('SELECT 1 FROM users WHERE username = ? COLLATE NOCASE').get(username);
    if (userTaken) throw new ConflictException('该用户名已被使用或已提交申请');
    const pendingSameName = this.database
      .prepare("SELECT 1 FROM registration_requests WHERE username = ? COLLATE NOCASE AND status = 'pending'")
      .get(username);
    if (pendingSameName) throw new ConflictException('该用户名已被使用或已提交申请');
    const pendingSamePhone = this.database
      .prepare("SELECT 1 FROM registration_requests WHERE phone = ? AND status = 'pending'")
      .get(phone);
    if (pendingSamePhone) throw new ConflictException('该手机号已提交申请,请勿重复提交');

    const passwordHash = await this.auth.hashPassword(input.password);
    const now = nowIso();
    this.database
      .prepare(
        `INSERT INTO registration_requests
           (id, username, password_hash, organization_name, phone, status, created_at, updated_at)
         VALUES (?, ?, ?, ?, ?, 'pending', ?, ?)`,
      )
      .run(randomUUID(), username, passwordHash, organizationName, phone, now, now);
    return { ok: true };
  }

  list(status = 'pending'): Array<Record<string, unknown>> {
    return this.database
      .prepare(
        `SELECT id, username, organization_name AS organizationName, phone, status, created_at AS createdAt
         FROM registration_requests WHERE status = ? ORDER BY created_at DESC`,
      )
      .all(status);
  }

  approve(id: string, reviewerId: string): { userId: string; workspaceId: string } {
    return this.database.transaction(() => {
      const req = this.database.prepare('SELECT * FROM registration_requests WHERE id = ?').get(id) as RequestRow | undefined;
      if (!req) throw new NotFoundException('申请不存在');
      if (req.status !== 'pending') throw new ConflictException('该申请已处理');
      if (this.database.prepare('SELECT 1 FROM users WHERE username = ? COLLATE NOCASE').get(req.username)) {
        throw new ConflictException('用户名已被占用,无法通过');
      }
      const { userId, workspaceId } = this.auth.provisionUserWithWorkspace({
        username: req.username,
        passwordHash: req.password_hash,
        systemRole: 'user',
        mustChangePassword: false,
        workspaceName: req.organization_name,
      });
      const now = nowIso();
      this.database
        .prepare("UPDATE registration_requests SET status = 'approved', created_user_id = ?, reviewed_by = ?, reviewed_at = ?, updated_at = ? WHERE id = ?")
        .run(userId, reviewerId, now, now, id);
      this.audit.record({ workspaceId, userId: reviewerId, action: 'registration.approve', entityType: 'registration', entityId: id, details: { username: req.username } });
      return { userId, workspaceId };
    });
  }

  reject(id: string, reviewerId: string, reason: string): void {
    const note = requireString(reason, '拒绝原因', { max: 500 });
    const req = this.database.prepare('SELECT status FROM registration_requests WHERE id = ?').get(id) as { status: string } | undefined;
    if (!req) throw new NotFoundException('申请不存在');
    if (req.status !== 'pending') throw new ConflictException('该申请已处理');
    const now = nowIso();
    this.database
      .prepare("UPDATE registration_requests SET status = 'rejected', review_note = ?, reviewed_by = ?, reviewed_at = ?, updated_at = ? WHERE id = ?")
      .run(note, reviewerId, now, now, id);
    this.audit.record({ userId: reviewerId, action: 'registration.reject', entityType: 'registration', entityId: id, details: { reason: note } });
  }

  loginHintFor(username: string): { status: 'pending' } | { status: 'rejected'; note: string } | null {
    const row = this.database
      .prepare("SELECT status, review_note FROM registration_requests WHERE username = ? COLLATE NOCASE ORDER BY created_at DESC LIMIT 1")
      .get(username) as { status: string; review_note: string | null } | undefined;
    if (!row) return null;
    if (row.status === 'pending') return { status: 'pending' };
    if (row.status === 'rejected') return { status: 'rejected', note: row.review_note ?? '' };
    return null;
  }
}
```

> 注:`requireString` 的 options 形态(`min/max/pattern`)以 `apps/api/src/utils.ts` 真实签名为准,若不同则调整调用。`audit.record` 的参数形态以 `AuditService.record` 真实签名为准(参见 project.controller 已有调用)。

- [ ] **Step 4: 在 app.module 注册 provider**

Run: `grep -n "AuthService\|providers" apps/api/src/app.module.ts`
在 `providers` 数组里加入 `RegistrationService`(并 import)。

- [ ] **Step 5: 运行测试确认通过**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: Task 3 全部测试 PASS。

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/registration.service.ts apps/api/src/app.module.ts apps/api/src/registration.service.spec.ts
git commit -m "feat(api): RegistrationService submit/list/approve/reject"
```

---

### Task 4: 公开注册接口 + 限流

**Files:**
- Create: `apps/api/src/registration.controller.ts`
- Modify: `apps/api/src/app.module.ts`(注册 controller)
- Modify: `apps/api/src/registration.service.ts`(加入 IP 限流计数,复用 AuthService 失败计数思路)
- Test: `apps/api/src/registration.service.spec.ts`(追加限流测试)

**Interfaces:**
- Consumes: `RegistrationService.submit`。
- Produces: `POST /api/register`(公开,无 guard)。限流方法 `assertSubmitAllowed(key: string)` / `recordSubmit(key: string)`。

- [ ] **Step 1: 确认现有公开接口模式(health / 无 guard 的 controller)**

Run: `grep -rn "@Controller\|UseGuards" apps/api/src/health.controller.ts`
Expected: 确认存在不挂 SessionAuthGuard 的 controller 写法,照此写 register controller(不加任何 Auth guard)。

- [ ] **Step 2: 写限流失败测试**

追加到 `registration.service.spec.ts`:

```ts
test('rate limit blocks after threshold per key', async () => {
  const db = makeDb();
  const reg = makeReg(db, makeAuth(db));
  for (let i = 0; i < 5; i++) reg.recordSubmit('1.2.3.4');
  assert.throws(() => reg.assertSubmitAllowed('1.2.3.4'), /频繁|too many/i);
});
```

- [ ] **Step 3: 运行确认失败**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: FAIL — `recordSubmit is not a function`。

- [ ] **Step 4: 在 RegistrationService 加入限流**

```ts
import { HttpException, HttpStatus } from '@nestjs/common';

// 类内:
  private readonly submitFailures = new Map<string, { count: number; resetAt: number }>();

  assertSubmitAllowed(key: string): void {
    const current = this.submitFailures.get(key);
    if (!current) return;
    if (current.resetAt <= Date.now()) { this.submitFailures.delete(key); return; }
    if (current.count >= 5) throw new HttpException('申请过于频繁,请稍后再试', HttpStatus.TOO_MANY_REQUESTS);
  }

  recordSubmit(key: string): void {
    const now = Date.now();
    const current = this.submitFailures.get(key);
    this.submitFailures.set(key, current && current.resetAt > now
      ? { count: current.count + 1, resetAt: current.resetAt }
      : { count: 1, resetAt: now + 15 * 60_000 });
  }
```

- [ ] **Step 5: 实现 controller**

创建 `apps/api/src/registration.controller.ts`:

```ts
import { Body, Controller, Ip, Post, Req } from '@nestjs/common';
import type { Request } from 'express';
import { RegistrationService } from './registration.service.js';
import { requireObject } from './utils.js';

@Controller('api/register')
export class RegistrationController {
  constructor(private readonly registration: RegistrationService) {}

  @Post()
  async submit(@Ip() ip: string, @Body() rawBody: unknown) {
    const key = ip || 'unknown';
    this.registration.assertSubmitAllowed(key);
    this.registration.recordSubmit(key);
    const body = requireObject(rawBody);
    return this.registration.submit({
      username: body.username,
      password: body.password,
      organizationName: body.organizationName,
      phone: body.phone,
    });
  }
}
```

> 注:controller 构造函数注入若项目统一用 `@Inject(...)`,照现有 controller 风格补全。确认 `RegistrationController` 不被任何全局 guard 拦截(检查 app.module 是否有 APP_GUARD;若有,需在此 controller 或 handler 上用 `@Public()` 等既有豁免机制——先 `grep -rn "APP_GUARD\|@Public" apps/api/src`)。

- [ ] **Step 6: app.module 注册 controller**

在 `controllers` 数组加入 `RegistrationController`(并 import)。

- [ ] **Step 7: 运行测试确认通过**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: 全部 PASS。

- [ ] **Step 8: Commit**

```bash
git add apps/api/src/registration.controller.ts apps/api/src/registration.service.ts apps/api/src/app.module.ts apps/api/src/registration.service.spec.ts
git commit -m "feat(api): public POST /api/register with rate limit"
```

---

### Task 5: 管理员审核接口(list/approve/reject)

**Files:**
- Modify: `apps/api/src/admin.controller.ts`(新增三个 handler)
- Test: `apps/api/src/registration.service.spec.ts`(已覆盖 service 层;controller 层豁免非管理员由现有 guard 保证,补一条断言注释即可)

**Interfaces:**
- Consumes: `RegistrationService.list/approve/reject`,`requireSystemAdmin`(现有私有方法),`principal.userId`。
- Produces: `GET /api/admin/registrations?status=`、`POST /api/admin/registrations/:id/approve`、`POST /api/admin/registrations/:id/reject`。

- [ ] **Step 1: 读 admin.controller 现有结构**

Run: `grep -n "requireSystemAdmin\|constructor\|@Inject" apps/api/src/admin.controller.ts`
Expected: 确认 `requireSystemAdmin(request)` 私有方法与构造注入方式,照此加新 handler 并注入 `RegistrationService`。

- [ ] **Step 2: 新增三个 handler**

在 `AdminController` 构造函数注入 `@Inject(RegistrationService) private readonly registration: RegistrationService`(import 之),新增:

```ts
  @Get('registrations')
  registrations(@Req() request: Request, @Query('status') status?: string) {
    this.requireSystemAdmin(request);
    return this.registration.list(status || 'pending');
  }

  @Post('registrations/:id/approve')
  approveRegistration(@Req() request: Request, @Param('id') id: string) {
    this.requireSystemAdmin(request);
    const principal = (request as unknown as AuthenticatedRequest).principal as SessionPrincipal;
    return this.registration.approve(id, principal.userId);
  }

  @Post('registrations/:id/reject')
  rejectRegistration(@Req() request: Request, @Param('id') id: string, @Body() rawBody: unknown) {
    this.requireSystemAdmin(request);
    const principal = (request as unknown as AuthenticatedRequest).principal as SessionPrincipal;
    const body = requireObject(rawBody);
    const reason = typeof body.reason === 'string' ? body.reason : '';
    this.registration.reject(id, principal.userId, reason);
    return { ok: true };
  }
```

补齐 import:`Get, Post, Param, Query, Body` from `@nestjs/common`(部分已存在)。

- [ ] **Step 3: 类型检查 + 测试**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: 全部 PASS(service 层测试已覆盖 approve/reject 行为)。

- [ ] **Step 4: Commit**

```bash
git add apps/api/src/admin.controller.ts
git commit -m "feat(api): admin registration list/approve/reject endpoints"
```

---

### Task 6: 登录接口回查申请状态给出提示

**Files:**
- Modify: `apps/api/src/auth.controller.ts`(登录失败/用户名不存在时,回查 registration 提示)或 `auth.service.ts` 的 login。以最小侵入为准:在 `auth.controller` 的 login handler catch `UnauthorizedException` 时,调用 `registration.loginHintFor(username)`,若命中 pending/rejected 则替换错误消息。
- Test: `apps/api/src/registration.service.spec.ts`(loginHintFor 已测)

**Interfaces:**
- Consumes: `RegistrationService.loginHintFor`。
- Produces: 登录失败响应在有 pending/rejected 申请时返回定制中文提示;其余不变。

- [ ] **Step 1: 读 auth.controller 的 login handler**

Run: `grep -n "login\|@Post\|constructor" apps/api/src/auth.controller.ts`
Expected: 找到 login handler 与注入方式。

- [ ] **Step 2: 注入 RegistrationService 并包裹提示逻辑**

在 login handler 中,当 `auth.login` 抛 `UnauthorizedException('用户名或密码错误')` 时:

```ts
    try {
      // ...existing login success path...
    } catch (error) {
      if (error instanceof UnauthorizedException) {
        const hint = this.registration.loginHintFor(usernameString);
        if (hint?.status === 'pending') throw new UnauthorizedException('你的申请正在审核中,请耐心等待');
        if (hint?.status === 'rejected') throw new UnauthorizedException(`申请未通过:${hint.note || '请联系客服'}`);
      }
      throw error;
    }
```

`usernameString` 为已从 body 取出的用户名字符串(login 之前解析)。注意仅当用户名**无正式账号**时 loginHint 才可能命中(有账号时是真的密码错),此边界由 loginHintFor 只查 registration 表天然满足;但为不给"用户名是否存在"泄露信号,仅在 hint 命中时替换文案,未命中保持原通用错误。

- [ ] **Step 3: 类型检查 + 测试**

Run: `cd content-agent && npm run test -w @content-agent/api`
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
git add apps/api/src/auth.controller.ts
git commit -m "feat(api): surface pending/rejected registration status on login"
```

---

### Task 7: 前端 API client + 类型

**Files:**
- Modify: `apps/web/src/lib/api.ts`(新增 `register` 与 `admin.registrations*`)
- Modify: `apps/web/src/types.ts`(新增 `RegistrationRequest` 类型)

**Interfaces:**
- Produces:
  ```ts
  api.register(input: { username; password; organizationName; phone }): Promise<{ ok: true }>
  api.admin.registrations(status?: string): Promise<RegistrationRequest[]>
  api.admin.approveRegistration(id: string): Promise<{ userId; workspaceId }>
  api.admin.rejectRegistration(id: string, reason: string): Promise<{ ok: true }>
  ```

- [ ] **Step 1: 读现有 api client 结构(auth/admin 段)**

Run: `grep -n "admin:\|createUser:\|login:\|request<" apps/web/src/lib/api.ts`
Expected: 找到 `admin` 对象与 `request` 用法,照此扩展。

- [ ] **Step 2: 新增类型**

在 `apps/web/src/types.ts` 加:

```ts
export interface RegistrationRequest {
  id: string;
  username: string;
  organizationName: string;
  phone: string;
  status: 'pending' | 'approved' | 'rejected';
  createdAt: string;
}
```

- [ ] **Step 3: 扩展 api client**

在 `api` 对象内新增顶层 `register`(注意:注册是公开接口,`request` 若默认带 CSRF header 需确认公开接口不因缺 CSRF 被拒——公开接口无 SessionAuthGuard/CsrfGuard,故 CSRF header 可有可无):

```ts
  register: (input: { username: string; password: string; organizationName: string; phone: string }) =>
    request<{ ok: true }>('/api/register', { method: 'POST', body: JSON.stringify(input) }),
```

在 `admin` 对象内新增:

```ts
    registrations: (status = 'pending') =>
      request<RegistrationRequest[]>(`/api/admin/registrations?status=${encodeURIComponent(status)}`),
    approveRegistration: (id: string) =>
      request<{ userId: string; workspaceId: string }>(`/api/admin/registrations/${id}/approve`, { method: 'POST' }),
    rejectRegistration: (id: string, reason: string) =>
      request<{ ok: true }>(`/api/admin/registrations/${id}/reject`, { method: 'POST', body: JSON.stringify({ reason }) }),
```

import `RegistrationRequest` 类型。

- [ ] **Step 4: 类型检查**

Run: `cd content-agent && npm run typecheck -w @content-agent/web`
Expected: 0 error。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/lib/api.ts apps/web/src/types.ts
git commit -m "feat(web): api client for registration submit + admin review"
```

---

### Task 8: 注册页 `/register` + 登录页入口 + 路由

**Files:**
- Create: `apps/web/src/pages/RegisterPage.tsx`
- Modify: `apps/web/src/App.tsx`(加公开路由 `/register`)
- Modify: `apps/web/src/pages/LoginPage.tsx`(加"申请开通"入口)
- Modify: `apps/web/src/styles.css`(注册页样式,复用 `.login-*` 体系)

**Interfaces:**
- Consumes: `api.register`,`useToast`。
- Produces: 路由 `/register`;客服微信常量 `wjyy5035` 复制交互。

- [ ] **Step 1: 加路由**

在 `App.tsx` 的 `<Route path="/login" ... />` 旁加:

```tsx
      <Route path="/register" element={<RegisterPage />} />
```

import `RegisterPage`。

- [ ] **Step 2: 写 RegisterPage**

创建 `apps/web/src/pages/RegisterPage.tsx`,四字段 + 确认密码 + 前端校验 + 提交成功态 + 客服复制:

```tsx
import { ArrowRight, Check, Copy } from 'lucide-react';
import { type FormEvent, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { ApiError, api } from '../lib/api';
import { useToast } from '../components/Ui';

const SUPPORT_WECHAT = 'wjyy5035';
const PHONE_RE = /^1[3-9]\d{9}$/;

export function RegisterPage() {
  const [form, setForm] = useState({ username: '', password: '', confirm: '', organizationName: '', phone: '' });
  const [submitting, setSubmitting] = useState(false);
  const [done, setDone] = useState(false);
  const [error, setError] = useState('');
  const [revealWechat, setRevealWechat] = useState(false);
  const toast = useToast();
  const navigate = useNavigate();

  const update = (key: keyof typeof form) => (e: { target: { value: string } }) => setForm({ ...form, [key]: e.target.value });

  const copyWechat = async () => {
    try {
      await navigator.clipboard.writeText(SUPPORT_WECHAT);
      toast.push('客服微信已复制,去微信添加即可');
    } catch {
      setRevealWechat(true);
    }
  };

  const submit = async (event: FormEvent) => {
    event.preventDefault();
    setError('');
    if (form.username.trim().length < 3) return setError('用户名至少 3 个字符');
    if (form.password.length < 12) return setError('密码至少 12 个字符');
    if (form.password !== form.confirm) return setError('两次输入的密码不一致');
    if (!form.organizationName.trim()) return setError('请填写机构名称');
    if (!PHONE_RE.test(form.phone.trim())) return setError('请输入有效的手机号');
    setSubmitting(true);
    try {
      await api.register({
        username: form.username.trim(),
        password: form.password,
        organizationName: form.organizationName.trim(),
        phone: form.phone.trim(),
      });
      setDone(true);
    } catch (err) {
      setError(err instanceof ApiError ? err.message : '提交失败,请稍后再试');
    } finally {
      setSubmitting(false);
    }
  };

  if (done) {
    return (
      <div className="login-page">
        <section className="login-panel">
          <div className="login-card">
            <div className="login-card__heading"><span><Check size={16} /> 已提交</span><h2>申请已提交,等待审核</h2><p>我们会尽快审核你的开通申请。如需加急,可添加客服微信。</p></div>
            <button type="button" className="login-submit" onClick={() => void copyWechat()}><Copy size={16} /><span>复制客服微信</span></button>
            {revealWechat && <p className="login-help">客服微信:{SUPPORT_WECHAT}</p>}
            <p className="login-help"><Link to="/login">返回登录</Link></p>
          </div>
        </section>
      </div>
    );
  }

  return (
    <div className="login-page">
      <section className="login-panel">
        <div className="login-card">
          <div className="login-card__heading"><span>申请开通</span><h2>注册使用申请</h2><p>提交后由管理员审核开通</p></div>
          <form onSubmit={submit}>
            <label className="field"><span className="field__label">用户名</span><input value={form.username} onChange={update('username')} minLength={3} maxLength={64} required /></label>
            <label className="field"><span className="field__label">密码(至少 12 位)</span><input type="password" value={form.password} onChange={update('password')} minLength={12} required /></label>
            <label className="field"><span className="field__label">确认密码</span><input type="password" value={form.confirm} onChange={update('confirm')} required /></label>
            <label className="field"><span className="field__label">机构名称</span><input value={form.organizationName} onChange={update('organizationName')} maxLength={120} required /></label>
            <label className="field"><span className="field__label">联系手机号</span><input value={form.phone} onChange={update('phone')} inputMode="numeric" required /></label>
            {error && <div className="form-error">{error}</div>}
            <button type="submit" className="login-submit" disabled={submitting}>{submitting ? <><span className="spinner spinner--small" />提交中…</> : <><span>提交申请</span><ArrowRight size={18} /></>}</button>
          </form>
          <p className="login-help">已有账号?<Link to="/login">返回登录</Link></p>
        </div>
      </section>
    </div>
  );
}
```

> 注:`useToast` 的实际导出位置以 `apps/web/src/components/Ui.tsx` 为准(TeamPage 从 `../components/Ui` 引入)。

- [ ] **Step 3: 登录页加入口**

在 `LoginPage.tsx` 的 `.login-help` 那行附近(第 66 行)追加:

```tsx
          <p className="login-help">还没有账号?<Link to="/register">申请开通 →</Link></p>
```

在文件顶部 import：`import { Link } from 'react-router-dom';`(若已有 `useNavigate` 从 react-router-dom,合并 import)。

- [ ] **Step 4: 类型检查**

Run: `cd content-agent && npm run typecheck -w @content-agent/web`
Expected: 0 error。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/pages/RegisterPage.tsx apps/web/src/App.tsx apps/web/src/pages/LoginPage.tsx apps/web/src/styles.css
git commit -m "feat(web): public /register page + login entry"
```

---

### Task 9: TeamPage 注册申请审核区块

**Files:**
- Modify: `apps/web/src/pages/TeamPage.tsx`(新增"注册申请"区块 + 拒绝原因弹窗)

**Interfaces:**
- Consumes: `api.admin.registrations/approveRegistration/rejectRegistration`,`RegistrationRequest` 类型。

- [ ] **Step 1: 在 load() 里拉取待审核申请**

在 `TeamPage` 的 `load` 的 `Promise.all` 中,当 `isSystemAdmin` 时加入 `api.admin.registrations('pending')`,存入新 state `registrations`。新增:

```tsx
  const [registrations, setRegistrations] = useState<RegistrationRequest[]>([]);
  const [rejecting, setRejecting] = useState<RegistrationRequest | null>(null);
  const [rejectReason, setRejectReason] = useState('');
```

- [ ] **Step 2: 审核动作**

```tsx
  const approve = async (item: RegistrationRequest) => {
    try {
      await api.admin.approveRegistration(item.id);
      setRegistrations((cur) => cur.filter((r) => r.id !== item.id));
      toast.push(`已通过 ${item.organizationName} 的申请,账号与工作区已创建`);
      await load();
    } catch (error) {
      toast.push(error instanceof Error ? error.message : '操作失败', 'error');
    }
  };

  const confirmReject = async () => {
    if (!rejecting || !rejectReason.trim()) return;
    try {
      await api.admin.rejectRegistration(rejecting.id, rejectReason.trim());
      setRegistrations((cur) => cur.filter((r) => r.id !== rejecting.id));
      setRejecting(null);
      setRejectReason('');
      toast.push('已拒绝该申请');
    } catch (error) {
      toast.push(error instanceof Error ? error.message : '操作失败', 'error');
    }
  };
```

- [ ] **Step 3: 区块 UI(仅 isSystemAdmin 且有 pending 时显示)**

在成员表 `section` 之后插入:

```tsx
      {!loading && isSystemAdmin && registrations.length > 0 && (
        <section className="panel team-panel">
          <header className="panel__header">
            <div><h2>注册申请</h2><p>{registrations.length} 条待审核 · 通过后自动创建账号与专属工作区</p></div>
          </header>
          <div className="team-table">
            <div className="team-row team-row--head"><span>机构</span><span>用户名</span><span>手机号</span><span>申请时间</span><span>操作</span></div>
            {registrations.map((item) => (
              <div className="team-row" key={item.id}>
                <span><strong>{item.organizationName}</strong></span>
                <span>{item.username}</span>
                <span>{item.phone}</span>
                <span>{new Date(item.createdAt).toLocaleString('zh-CN')}</span>
                <span className="team-actions">
                  <button type="button" onClick={() => void approve(item)}>通过</button>
                  <button type="button" className="danger" onClick={() => { setRejecting(item); setRejectReason(''); }}>拒绝</button>
                </span>
              </div>
            ))}
          </div>
        </section>
      )}
```

- [ ] **Step 4: 拒绝原因弹窗(复用 Modal)**

在文件末尾其它 Modal 旁加:

```tsx
      <Modal
        open={Boolean(rejecting)}
        onClose={() => setRejecting(null)}
        title={`拒绝申请 · ${rejecting?.organizationName || ''}`}
        description="填写拒绝原因,用户下次登录时可见。"
        footer={<><Button variant="ghost" onClick={() => setRejecting(null)}>取消</Button><Button loading={saving} disabled={!rejectReason.trim()} onClick={() => void confirmReject()}>确认拒绝</Button></>}
      >
        <Field label="拒绝原因"><textarea value={rejectReason} onChange={(e) => setRejectReason(e.target.value)} rows={3} maxLength={500} /></Field>
      </Modal>
```

import `RegistrationRequest` 类型(从 `../types`)。

- [ ] **Step 5: 类型检查**

Run: `cd content-agent && npm run typecheck -w @content-agent/web`
Expected: 0 error。

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/pages/TeamPage.tsx
git commit -m "feat(web): registration review block in TeamPage"
```

---

### Task 10: 关闭登录 demo 兜底

**Files:**
- Modify: `apps/web/src/components/AuthContext.tsx`(移除 demo 降级分支)
- Modify: `apps/web/src/lib/fixtures.ts`(若 `demoUser` 清理后无引用则删除该导出)

**Interfaces:**
- Produces: 登录失败即如实抛错,无 demo 态。

- [ ] **Step 1: 找出所有 demo 引用**

Run: `grep -rn "demoUser\|isDemo\|content-agent-demo-session\|canUseDemoFallback" apps/web/src`
Expected: 列出全部引用点(AuthContext + 可能的消费方如 AppShell 显示 demo 标记)。

- [ ] **Step 2: 简化 AuthContext.login / me / logout**

`login` 改为不再 catch 后降级:

```tsx
  const login = async (username: string, password: string) => {
    setUser(await api.auth.login(username, password));
  };
```

`me()` 初始化的 catch 里移除 `storedDemo` 分支,失败即保持未登录。`logout` 移除 demo 分支与 sessionStorage 操作。移除 `isDemo` state 与 `canUseDemoFallback`、`demoUser` import。若 `AuthContextValue` 暴露 `isDemo`,一并从接口与 provider value 移除。

- [ ] **Step 3: 清理消费方**

Run: `grep -rn "isDemo" apps/web/src`
若有组件(如 AppShell)读取 `isDemo` 显示 demo 徽标,移除该分支。**注意:AppShell 有并行 v2 改动,仅移除 demo 相关行,不动 v2 结构。**

- [ ] **Step 4: 删除或保留 demoUser**

Run: `grep -rn "demoUser" apps/web/src`
若已无引用,从 `fixtures.ts` 删除 `demoUser` 导出;若 fixtures 还有其它用途保留文件。

- [ ] **Step 5: 类型检查**

Run: `cd content-agent && npm run typecheck -w @content-agent/web`
Expected: 0 error(无 unused / 无残留引用)。

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/components/AuthContext.tsx apps/web/src/lib/fixtures.ts
git commit -m "refactor(web): remove login demo fallback"
```

---

### Task 11: 端到端浏览器验证

**Files:** 无(验证任务)

- [ ] **Step 1: 确保 API/web dev 起来**

Run: `cd content-agent && npm run dev -w @content-agent/api`(后台)+ web dev。确认 8780 /health 200、5173 可访问。

- [ ] **Step 2: 走注册链路**

浏览器(或 CDP harness)访问 `/register`:填 username/密码/确认/机构/手机号 → 提交 → 出现"申请已提交"态 → 点"复制客服微信" → toast 出现。

- [ ] **Step 3: 后台审核**

用 admin 登录 → TeamPage 出现"注册申请"区块 → 点"通过" → toast 提示账号已创建。

- [ ] **Step 4: 新账号登录**

登出 admin → 用刚注册的 username + 密码登录 → 成功进入,且落在以机构名命名的专属工作区。

- [ ] **Step 5: 拒绝 + 提示链路**

再注册一个 → 后台"拒绝"填原因 → 用该 username 登录 → 看到"申请未通过:{原因}"。

- [ ] **Step 6: demo 兜底已关闭验证**

停掉 API → 尝试登录 → 如实报错(不再进入 demo 态)。重启 API。

- [ ] **Step 7: 最终类型检查 + 全量测试**

Run: `cd content-agent && npm run typecheck -w @content-agent/web && npm run test -w @content-agent/api`
Expected: 0 error;后端测试全绿。

---

## Self-Review 记录

- **Spec 覆盖**:注册页(T8)、客服复制(T8)、登录入口(T8)、待审核登录提示(T6)、审核区块(T9)、registration_requests 表(T1)、公开接口+限流(T4)、管理员接口(T5)、approve 建号建工作区(T2+T3)、reject+重申(T3)、demo 关闭(T10)、测试(各任务 TDD + T11 e2e)—— 逐条有对应任务。
- **Placeholder**:无 TBD/TODO;每个代码步骤含完整代码。
- **类型一致**:`provisionUserWithWorkspace` 签名在 T2 定义、T3 消费一致;`RegistrationRequest` 在 T7 定义、T9 消费一致;`loginHintFor` 返回型在 T3 定义、T6 消费一致。
- **已知需现场核对项**(已在对应步骤标注):`ApiOptions` 字段、`requireString` options 形态、`AuditService.record` 签名、是否存在 `APP_GUARD` 需公开豁免、`useToast` 导出位置 —— 均给出 grep 命令先行确认。
