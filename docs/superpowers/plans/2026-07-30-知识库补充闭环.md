# 知识库补充闭环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让信息缺口有真正能关闭的路径——人工确认与资料支撑同等有效,且关闭后答案在生成端真的被采用。

**Architecture:** `sourceStatus` 从 `data_json` 一路透传到 `agent-core`(此前完全没传),`bindGapEvidence` 为 `sourceStatus === 'user_supplied'` 的答案给出独立证据 id `evidence_human_approved`。缺口编辑器让人能设这个状态。资料变动后禁止确认失效分析,知识库页给常驻待办条。

**Tech Stack:** TypeScript monorepo。`packages/agent-core` 用 vitest;`apps/api`(NestJS)与 `apps/web`(React)用 `node:test`。`apps/api` 的测试脚本是 glob(`test/*.test.ts`),`apps/web` 是**显式文件列表**,新增测试文件必须手工加进 `apps/web/package.json`。

## Global Constraints

- 注释、文案、提交信息用中文,与仓库既有风格一致。
- `apps/web` 不引入 vitest / `@testing-library/react` / react-markdown;纯逻辑用 `node:test`。
- `apps/api` 无 `class-validator`,校验手写在 `utils.ts`(`requireObject` / `requireString` / `optionalString`)。
- 每个任务的测试都要做**变异验证**:改坏实现必须让测试变红。绿了说明测试是装饰。
- `supplied_fact` **只能由分析器写**,任何人工路径都不得设置它。
- 不修 `planningEvidenceSupports` 的格式敏感问题(spec「明确不做」)。
- 不建新表,不写迁移。`sourceStatus` 存在 `data_json` 内,无 CHECK 约束。

## 基线(开工前必须复现)

固定环境跑,记录数字:

```bash
# agent-core:328 passed / 1 failed。
# 那 1 条红是 content-prompt-revision.test.ts 的 "rejects excessively nested model JSON",
# 属于另一会话未提交的 model.ts(+92 行嵌套深度校验),与本计划无关。
npm test -w @content-agent/agent-core

# apps/web:509 passed / 0 failed(含已提交的预检批 2429242)
npm test -w @content-agent/web

# apps/api:必须去掉 ANTHROPIC_BASE_URL,否则 cross-tenant-isolation 的 BYOK 用例
# 会因 config.ts:211 把它当 platformBaseUrl 而失败(报「必须使用 HTTPS」)。
cd apps/api && env -u ANTHROPIC_BASE_URL -u OPENAI_BASE_URL node --import tsx --test test/*.test.ts
```

任何一条与上面不符,先查清原因再开工——工作树有约 115 个未提交文件,其中约 101 个属于另一会话。

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `packages/agent-core/src/types.ts` | `InformationGap` 增加 `sourceStatus` 字段 | 修改 |
| `packages/agent-core/src/engine.ts` | 新增 `HUMAN_APPROVED_EVIDENCE_ID`,`bindGapEvidence` 加第四条证据路径 | 修改 |
| `packages/agent-core/test/gap-human-evidence.test.ts` | 人工确认答案在生成端被采用 | 新建 |
| `apps/api/src/intelligence.service.ts` | `normalizeGap` 透传 `sourceStatus`;`approveResource` 拒绝确认 stale 分析 | 修改 |
| `apps/api/test/gap-source-status.test.ts` | 透传与 stale 确认门禁 | 新建 |
| `apps/web/src/types.ts` | `sourceStatus` 增加 `user_supplied` | 修改 |
| `apps/web/src/lib/gap-source.ts` | 「资料来源」下拉的取值、标签、默认值——纯逻辑 | 新建 |
| `apps/web/test/gap-source.test.ts` | 上述纯逻辑 | 新建 |
| `apps/web/src/pages/IntelligentSimpleFlow.tsx` | 缺口编辑器接入下拉 | 修改 |
| `apps/web/src/components/knowledge/KnowledgeEnrichmentModal.tsx` | 目标文件选择器 + 保存后指向下一步 | 修改 |
| `apps/web/src/pages/KnowledgePage.tsx` | `stale` 待办条文案 | 修改 |
| `apps/web/package.json` | 注册两个新测试文件 | 修改 |

任务 1→2 是数据透传链,必须按序。任务 3 起互相独立。

---

### Task 1: `sourceStatus` 透传到 agent-core

**Files:**
- Modify: `packages/agent-core/src/types.ts:1228-1243`(`InformationGap`)
- Modify: `apps/api/src/intelligence.service.ts`(`normalizeGap`,搜 `export function normalizeGap`)
- Test: `apps/api/test/gap-source-status.test.ts`(新建)

**Interfaces:**
- Consumes: 无
- Produces:
  - `InformationGap.sourceStatus?: "supplied_fact" | "user_supplied" | "inference" | "hypothesis" | "unknown"`
    ——内联联合类型,不单独起名(`agent-core` 的既有字段都是内联写法)
  - `normalizeGap(row: Record<string, unknown>): Record<string, unknown>` 返回对象新增
    `sourceStatus` 键,取值同上,认不出时为 `undefined`

  任务 2 依赖这两者。

- [ ] **Step 1: 写失败测试**

新建 `apps/api/test/gap-source-status.test.ts`:

```ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import { normalizeGap } from '../src/intelligence.service.js';

/**
 * sourceStatus 的透传。
 *
 * 此前 normalizeGap 回传 14 个字段但不含 sourceStatus,而前端 gapStats /
 * isGapPending 的判据都读它,拿到的恒为 undefined——两处注释声称「与后端
 * pendingGaps 同一判据」,实际不一致。这条测试守住透传。
 */
function row(data: Record<string, unknown>): Record<string, unknown> {
  return { id: 'g1', title: '缺口', status: 'approved', data_json: JSON.stringify(data) };
}

test('normalizeGap 透传 sourceStatus', () => {
  assert.equal(normalizeGap(row({ sourceStatus: 'user_supplied' })).sourceStatus, 'user_supplied');
  assert.equal(normalizeGap(row({ sourceStatus: 'supplied_fact' })).sourceStatus, 'supplied_fact');
});

test('没有 sourceStatus 时回 undefined,不编造默认值', () => {
  // 编造 'unknown' 会让「分析没标」和「分析标了未知」混同
  assert.equal(normalizeGap(row({})).sourceStatus, undefined);
});

test('认不出的取值不透传,避免污染下游联合类型', () => {
  assert.equal(normalizeGap(row({ sourceStatus: 'made_up' })).sourceStatus, undefined);
  assert.equal(normalizeGap(row({ sourceStatus: 123 })).sourceStatus, undefined);
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/api && node --import tsx --test test/gap-source-status.test.ts
```

预期 FAIL:`undefined !== 'user_supplied'`。

- [ ] **Step 3: 实现透传**

在 `apps/api/src/intelligence.service.ts` 顶部常量区(靠 `APPROVAL_STATUSES` 附近)加:

```ts
/**
 * 缺口答案的来源。supplied_fact 只能由分析器基于 evidenceSections 判定;
 * user_supplied 表示人工填写并确认过,只能由人设置——分析提示词不列出它。
 */
const GAP_SOURCE_STATUSES = new Set([
  'supplied_fact', 'user_supplied', 'inference', 'hypothesis', 'unknown',
]);
```

在 `normalizeGap` 的返回对象里,紧跟 `evidenceIds` 之后加:

```ts
    // 认不出的取值不透传:下游是联合类型,污染它会让 agent-core 的分支判断失准
    sourceStatus: typeof data.sourceStatus === 'string' && GAP_SOURCE_STATUSES.has(data.sourceStatus)
      ? data.sourceStatus
      : undefined,
```

在 `packages/agent-core/src/types.ts` 的 `InformationGap`(第 1228 行起),`evidenceIds: string[];` 之后加:

```ts
  /**
   * 答案来源。决定 bindGapEvidence 是否给出人工背书证据。
   *
   * - supplied_fact 资料里有出处,分析器基于 evidenceSections 判定
   * - user_supplied 人工填写并确认过,与资料支撑同等有效
   * - inference / hypothesis / unknown 仍属待补充
   */
  sourceStatus?: "supplied_fact" | "user_supplied" | "inference" | "hypothesis" | "unknown";
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd apps/api && node --import tsx --test test/gap-source-status.test.ts
npm run typecheck -w @content-agent/api && npm run typecheck -w @content-agent/agent-core
```

预期:3 passed,两个 typecheck 干净。

- [ ] **Step 5: 变异验证**

把实现改成 `sourceStatus: data.sourceStatus as any`(去掉白名单),重跑——第 3 条必须红。改回。
再把实现整行删掉,重跑——第 1 条必须红。改回。

- [ ] **Step 6: 提交**

```bash
git add packages/agent-core/src/types.ts apps/api/src/intelligence.service.ts apps/api/test/gap-source-status.test.ts
git commit -m "feat: 缺口 sourceStatus 透传到 agent-core

normalizeGap 此前不回传该字段,前端判据读到的恒为 undefined。
认不出的取值不透传,避免污染下游联合类型。"
```

---

### Task 2: 人工确认的答案在生成端被采用

**Files:**
- Modify: `packages/agent-core/src/engine.ts:101-102`(常量)、`:554-576`(`bindGapEvidence`)
- Test: `packages/agent-core/test/gap-human-evidence.test.ts`(新建)

**Interfaces:**
- Consumes: 任务 1 的 `InformationGap.sourceStatus`
- Produces: `HUMAN_APPROVED_EVIDENCE_ID = "evidence_human_approved"`(模块私有常量,不导出;通过 `bindGapEvidence` 的输出可见)

**只改第 554 行那个 `evidenceFor`。** 第 609 行 `defaultInformationGaps` 里同名闭包的缺口来自 config,没有 `sourceStatus`,不要动。

- [ ] **Step 1: 写失败测试**

新建 `packages/agent-core/test/gap-human-evidence.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { ContentGenerationAgent, createDefaultGenerationConfig, DEFAULT_FORMULA_VERSION } from "../src/index.js";
import type { InformationGap, KnowledgeDocument } from "../src/index.js";
import { indexKnowledgeSource } from "../src/index.js";

/**
 * 人工确认的答案必须在生成端真的被采用。
 *
 * bindGapEvidence 的规则是「答案拿不到证据就置 undefined、proofability 归 0」。
 * 人工填的答案不在资料里,分节匹配不中;此前它靠 planningEvidenceSupports 的自证
 * 侥幸成立——而那条路格式敏感:JSON 转义会在换行、半角双引号、制表符、反斜杠处
 * 插入反斜杠,破坏子串匹配。缺口编辑器的答案框是 textarea,多行是常态,
 * 所以效力取决于用户有没有敲回车。这里改成显式路径。
 */
const knowledge: KnowledgeDocument[] = [indexKnowledgeSource({
  projectId: "p1",
  path: "facts.md",
  content: "# 项目事实\n\n资料里确认过的一句话。",
})];

function config() {
  return createDefaultGenerationConfig({
    project: { id: "p1", name: "项目", domain: "健康信息", productPoints: ["资料确认的项目事实"], organizationPoints: [], cities: [], doctors: [] },
    task: { theme: "信息核实", audienceStage: "comparing", forbidden: [] },
  });
}

function gap(overrides: Partial<InformationGap> = {}): InformationGap {
  return {
    id: "g1",
    label: "资质编号",
    question: "资质编号是多少？",
    category: "decision",
    audienceStages: ["comparing"],
    importance: 0.9,
    decisionLeverage: 0.9,
    proofability: 0.8,
    evidenceIds: [],
    required: true,
    ...overrides,
  };
}

async function boundGap(input: InformationGap) {
  const result = await new ContentGenerationAgent().generate({
    jobId: "human-evidence-job",
    config: config(),
    formulaVersion: DEFAULT_FORMULA_VERSION,
    knowledge,
    planningContext: { informationGaps: [input] },
  });
  const cards = result.packages[0]?.orchestrationSnapshot?.gapPlanningCards ?? [];
  return cards.find((card) => card.gapId === "g1");
}

describe("人工确认的缺口答案", () => {
  it("多行答案标了 user_supplied 也拿到证据并被采用", async () => {
    // 多行是这条路径的关键形态:旧的自证路线在这里必然失败
    const card = await boundGap(gap({ answer: "编号待确认\n证件在门店", sourceStatus: "user_supplied" }));
    expect(card?.evidenceIds).toContain("evidence_human_approved");
    expect(card?.answer).toBe("编号待确认\n证件在门店");
  });

  it("含半角双引号的答案同样成立", async () => {
    const card = await boundGap(gap({ answer: '按"套内面积"计算', sourceStatus: "user_supplied" }));
    expect(card?.evidenceIds).toContain("evidence_human_approved");
  });

  it("没标 user_supplied 的答案不获得人工证据", async () => {
    const card = await boundGap(gap({ answer: "编号待确认\n证件在门店", sourceStatus: "unknown" }));
    expect(card?.evidenceIds ?? []).not.toContain("evidence_human_approved");
  });

  it("人工证据与资料分节证据不混同,来源可追溯", async () => {
    const card = await boundGap(gap({ answer: "资料里确认过的一句话", sourceStatus: "user_supplied" }));
    const ids = card?.evidenceIds ?? [];
    expect(ids).toContain("evidence_human_approved");
    expect(ids.some((id) => id.startsWith("evidence_section_"))).toBe(true);
  });

  it("没有答案时不凭空给人工证据", async () => {
    const card = await boundGap(gap({ sourceStatus: "user_supplied" }));
    expect(card?.evidenceIds ?? []).not.toContain("evidence_human_approved");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd packages/agent-core && npx vitest run test/gap-human-evidence.test.ts
```

预期 FAIL:第 1、2 条断言 `evidenceIds` 不含 `evidence_human_approved`。

- [ ] **Step 3: 实现第四条证据路径**

`packages/agent-core/src/engine.ts` 第 102 行之后加常量:

```ts
/**
 * 人工背书。人在缺口编辑器填了答案并确认(sourceStatus=user_supplied)时给出。
 *
 * 与资料支撑同等有效,但保持独立 id——证据台账里要能区分「资料背书」与「人工背书」,
 * 否则以后审「哪些结论是人说的」就查不出来。
 */
const HUMAN_APPROVED_EVIDENCE_ID = "evidence_human_approved";
```

`bindGapEvidence`(第 554 行起)的 `evidenceFor` 内,在 `planningEvidenceSupports` 那行之后加一行:

```ts
    if (gap.sourceStatus === "user_supplied") mapped.push(HUMAN_APPROVED_EVIDENCE_ID);
```

注意:`evidenceFor` 开头已有 `if (!statement?.trim()) return [];`,所以「没有答案」的情形天然不会拿到人工证据,不需要额外判断。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd packages/agent-core && npx vitest run test/gap-human-evidence.test.ts
npm run build -w @content-agent/agent-core
```

预期:5 passed,build 干净。

- [ ] **Step 5: 全量回归**

```bash
npm test -w @content-agent/agent-core
```

预期:333 passed / 1 failed。那 1 条红仍是基线里 `content-prompt-revision.test.ts` 的嵌套深度用例(另一会话的 `model.ts`)。**若出现任何其他红,是本任务的回归,必须查清。**

- [ ] **Step 6: 变异验证**

删掉 Step 3 加的那一行,重跑——第 1、2、4 条必须红。改回。
把条件改成 `gap.sourceStatus !== undefined`,重跑——第 3 条必须红。改回。

- [ ] **Step 7: 提交**

```bash
git add packages/agent-core/src/engine.ts packages/agent-core/test/gap-human-evidence.test.ts
git commit -m "feat: 人工确认的缺口答案获得独立证据 id

sourceStatus=user_supplied 时给出 evidence_human_approved,与资料支撑
同等有效但可区分来源。此前人工答案靠 planningEvidenceSupports 自证,
而那条路格式敏感——多行/半角引号会因 JSON 转义落空,答案被静默丢弃。"
```

---

### Task 3: 缺口编辑器的「资料来源」下拉

**Files:**
- Create: `apps/web/src/lib/gap-source.ts`
- Create: `apps/web/test/gap-source.test.ts`
- Modify: `apps/web/src/types.ts:1231`、`:1327`(两处 `sourceStatus` 联合类型)
- Modify: `apps/web/src/pages/IntelligentSimpleFlow.tsx`(缺口编辑弹窗,搜 `已有批准答案`)
- Modify: `apps/web/package.json`(测试列表)

**Interfaces:**
- Consumes: 任务 1 的取值集合
- Produces:
  - `type HumanGapSource = 'user_supplied' | 'inference' | 'hypothesis' | 'unknown'`
  - `GAP_SOURCE_OPTIONS: ReadonlyArray<{ value: HumanGapSource; label: string; hint: string }>`
  - `sourceForAnswer(answer: string, current?: string): HumanGapSource | 'supplied_fact'`
    ——返回类型含 `supplied_fact`:分析器判定的资料支撑要能原样保留,不因用户碰了
    答案框就降级成人工背书

- [ ] **Step 1: 写失败测试**

新建 `apps/web/test/gap-source.test.ts`:

```ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import { GAP_SOURCE_OPTIONS, sourceForAnswer } from '../src/lib/gap-source';

/**
 * 缺口答案的「资料来源」。
 *
 * supplied_fact 不可手选:它表示「资料里有出处」,只能由分析器基于 evidenceSections
 * 判定。若允许手选,用户会声称有资料支撑却拿不到任何 evidenceId(分节匹配不中,
 * 又因 sourceStatus !== 'user_supplied' 拿不到人工证据),答案在生成端照样被丢弃。
 */
test('不提供 supplied_fact 选项', () => {
  assert.ok(!GAP_SOURCE_OPTIONS.some((option) => option.value === 'supplied_fact'));
});

test('提供四种人能合法声明的来源', () => {
  assert.deepEqual(
    GAP_SOURCE_OPTIONS.map((option) => option.value),
    ['user_supplied', 'inference', 'hypothesis', 'unknown'],
  );
});

test('每个选项都有中文标签与说明', () => {
  for (const option of GAP_SOURCE_OPTIONS) {
    assert.ok(option.label.trim().length > 0, `${option.value} 缺标签`);
    assert.ok(option.hint.trim().length > 0, `${option.value} 缺说明`);
  }
});

test('「我确认过」的说明要点明它会被生成采用', () => {
  const confirmed = GAP_SOURCE_OPTIONS.find((option) => option.value === 'user_supplied');
  assert.match(confirmed!.hint, /采用/u);
});

test('填了答案默认选中「我确认过」', () => {
  assert.equal(sourceForAnswer('每天九点到六点'), 'user_supplied');
});

test('清空答案回落 unknown:没有答案的「我确认过」是矛盾状态', () => {
  assert.equal(sourceForAnswer('', 'user_supplied'), 'unknown');
  assert.equal(sourceForAnswer('   ', 'user_supplied'), 'unknown');
});

test('已经手动选过别的来源就不覆盖', () => {
  assert.equal(sourceForAnswer('答案', 'inference'), 'inference');
  assert.equal(sourceForAnswer('答案', 'hypothesis'), 'hypothesis');
});

test('分析写的 supplied_fact 不被人工路径改掉', () => {
  // 分析器判定的资料支撑要保留,不能因为用户碰了一下答案框就降级
  assert.equal(sourceForAnswer('答案', 'supplied_fact'), 'supplied_fact');
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/web && node --import tsx --test test/gap-source.test.ts
```

预期 FAIL:找不到模块 `../src/lib/gap-source`。

- [ ] **Step 3: 实现纯逻辑**

新建 `apps/web/src/lib/gap-source.ts`:

```ts
/** 人能合法声明的答案来源。supplied_fact 不在内——只有分析器能判定资料支撑。 */
export type HumanGapSource = 'user_supplied' | 'inference' | 'hypothesis' | 'unknown';

export const GAP_SOURCE_OPTIONS: ReadonlyArray<{ value: HumanGapSource; label: string; hint: string }> = [
  { value: 'user_supplied', label: '我确认过', hint: '你为这条答案背书,生成会采用它' },
  { value: 'inference', label: '我的推断', hint: '还算待补充,生成不会当事实用' },
  { value: 'hypothesis', label: '假设', hint: '还算待补充,生成不会当事实用' },
  { value: 'unknown', label: '还不确定', hint: '还算待补充' },
];

/**
 * 答案变化时该落哪个来源。
 *
 * 填了答案默认「我确认过」——这是人工填写最常见的意图,也是唯一能让答案在生成端
 * 被采用的人工路径。但已经明确选过别的来源就不覆盖,分析器判定的 supplied_fact
 * 同样保留:不能因为用户碰了一下答案框就把资料支撑降级成人工背书。
 *
 * 清空答案回落 unknown:没有答案的「我确认过」是矛盾状态。
 */
export function sourceForAnswer(answer: string, current?: string): HumanGapSource | 'supplied_fact' {
  if (!answer.trim()) return 'unknown';
  if (current === 'supplied_fact') return 'supplied_fact';
  if (current === 'inference' || current === 'hypothesis') return current;
  return 'user_supplied';
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd apps/web && node --import tsx --test test/gap-source.test.ts
```

预期:8 passed。

- [ ] **Step 5: 扩 web 类型的联合取值**

`apps/web/src/types.ts` 第 1231 与 1327 行,两处都从

```ts
  sourceStatus?: "supplied_fact" | "inference" | "hypothesis" | "unknown";
```

改成

```ts
  sourceStatus?: "supplied_fact" | "user_supplied" | "inference" | "hypothesis" | "unknown";
```

- [ ] **Step 6: 缺口编辑器接入**

`apps/web/src/pages/IntelligentSimpleFlow.tsx`,在缺口编辑弹窗里找到「已有批准答案」那个 `Field`。它现在的 `onChange` 是:

```tsx
onChange={(event) => setEditingGap((current) => ({ ...current, answer: event.target.value, answerability: event.target.value ? "approved" : "verifiable" }))}
```

改成同时维护 `sourceStatus`:

```tsx
onChange={(event) => setEditingGap((current) => ({
  ...current,
  answer: event.target.value,
  answerability: event.target.value ? "approved" : "verifiable",
  sourceStatus: sourceForAnswer(event.target.value, current?.sourceStatus),
}))}
```

在这个 `Field` 之后加来源选择:

```tsx
<Field label="资料来源" hint="决定这条答案能否被生成采用">
  <select
    value={editingGap?.sourceStatus === 'supplied_fact' ? 'supplied_fact' : (editingGap?.sourceStatus || 'unknown')}
    onChange={(event) => setEditingGap((current) => ({ ...current, sourceStatus: event.target.value as InformationGap['sourceStatus'] }))}
  >
    {/* 分析器判定的资料支撑要能显示,但不作为可选项提供 */}
    {editingGap?.sourceStatus === 'supplied_fact' && <option value="supplied_fact">资料里有出处(分析器判定)</option>}
    {GAP_SOURCE_OPTIONS.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}
  </select>
</Field>
```

顶部加导入:

```tsx
import { GAP_SOURCE_OPTIONS, sourceForAnswer } from "../lib/gap-source";
```

- [ ] **Step 7: 注册测试并全量回归**

`apps/web/package.json` 的 `test` 脚本末尾(在 `test/knowledge-instrument.test.ts` 之后)追加 ` test/gap-source.test.ts`。

```bash
npm run typecheck -w @content-agent/web
npm test -w @content-agent/web
```

预期:typecheck 干净,517 passed / 0 failed。

- [ ] **Step 8: 变异验证**

把 `GAP_SOURCE_OPTIONS` 里加一项 `{ value: 'supplied_fact' as any, ... }`,重跑——第 1 条必须红。改回。
把 `sourceForAnswer` 的空答案分支改成 `return 'user_supplied'`,重跑——第 6 条必须红。改回。
把 `current === 'supplied_fact'` 那行删掉,重跑——第 8 条必须红。改回。

- [ ] **Step 9: 提交**

```bash
git add apps/web/src/lib/gap-source.ts apps/web/test/gap-source.test.ts apps/web/src/types.ts apps/web/src/pages/IntelligentSimpleFlow.tsx apps/web/package.json
git commit -m "feat: 缺口编辑器能设答案来源

填了答案默认「我确认过」(user_supplied),生成端据此给人工证据。
supplied_fact 不可手选——它只能由分析器基于 evidenceSections 判定,
手选会让用户声称有资料支撑却拿不到证据,答案照样被丢弃。"
```

---

### Task 4: 失效的分析不能确认

**Files:**
- Modify: `apps/api/src/intelligence.service.ts:2417-2447`(`approveResource`)
- Test: `apps/api/test/gap-source-status.test.ts`(任务 1 建的文件,追加)

**Interfaces:**
- Consumes: 无
- Produces: 无(行为收紧)

只对 `project_intelligence` 生效。缺口、策略、选题的 stale 是分析级联下来的,重新分析会重建,拦它们只会把用户堵死。

- [ ] **Step 1: 写失败测试**

追加到 `apps/api/test/gap-source-status.test.ts`。这条需要真实应用,所以用 HTTP:

```ts
import { mkdtemp, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { after, before } from 'node:test';
import type { NestExpressApplication } from '@nestjs/platform-express';
import { createApplication } from '../src/app.js';
import { DatabaseService } from '../src/database.service.js';

let app: NestExpressApplication;
let baseUrl = '';
let dataDir = '';
let cookie = '';
let csrf = '';
let projectId = '';

const BOOTSTRAP = 'Admin-bootstrap-123!';

async function call(path: string, options: RequestInit = {}) {
  const headers = new Headers(options.headers);
  if (cookie) headers.set('cookie', cookie);
  if (csrf && !['GET', 'HEAD'].includes(options.method ?? 'GET')) headers.set('x-csrf-token', csrf);
  if (typeof options.body === 'string') headers.set('content-type', 'application/json');
  const response = await fetch(`${baseUrl}${path}`, { ...options, headers });
  const type = response.headers.get('content-type') ?? '';
  return { status: response.status, body: type.includes('json') ? await response.json() : await response.text() };
}

before(async () => {
  dataDir = await mkdtemp(join(tmpdir(), 'content-agent-stale-approve-'));
  app = await createApplication({ dataDir, adminUsername: 'admin', adminPassword: BOOTSTRAP, masterEncryptionKey: 'integration-test-master-encryption-key', logger: false });
  await app.listen(0, '127.0.0.1');
  baseUrl = await app.getUrl();
  const login = await fetch(`${baseUrl}/api/auth/login`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ username: 'admin', password: BOOTSTRAP }) });
  cookie = login.headers.get('set-cookie')!.split(';', 1)[0]!;
  csrf = (await login.json()).csrfToken;
  await call('/api/auth/change-password', { method: 'POST', body: JSON.stringify({ currentPassword: BOOTSTRAP, newPassword: 'Admin-updated-456!' }) });
  const project = await call('/api/projects', { method: 'POST', body: JSON.stringify({ name: '失效确认门禁', domain: '装修' }) });
  projectId = project.body.id;
});

after(async () => {
  await app.close();
  await rm(dataDir, { recursive: true, force: true });
});

/** 直接造一行 stale 的分析:跑真分析要调模型,这里只验门禁。 */
function seedIntelligence(status: string): string {
  const db = app.get(DatabaseService);
  const id = `intel-${status}-${Date.now()}`;
  const now = new Date().toISOString();
  db.prepare(
    `INSERT INTO project_intelligence (id, project_id, version, status, map_json, created_by, created_at, updated_at)
     VALUES (?, ?, 1, ?, '{}', (SELECT id FROM users WHERE username='admin'), ?, ?)`,
  ).run(id, projectId, status, now, now);
  return id;
}

test('stale 的分析不能确认,错误信息说清该做什么', async () => {
  /*
   * approveResource 此前只翻 status,不校验 stale。实测线上时间线:
   *   18:29 分析完成 → 18:33 补充存新版本 markProjectStale → 18:34 点「确认」变回 approved
   * 结果缺口与 evidenceId 全部来自资料变动之前,界面挂满「引用已失效」。
   * 不拦这一步,任何待办提示都能被一次点击消掉。
   */
  const id = seedIntelligence('stale');
  const result = await call(`/api/projects/${projectId}/intelligence/${id}/approve`, { method: 'POST', body: JSON.stringify({}) });
  assert.equal(result.status, 400, JSON.stringify(result.body));
  assert.match(String(result.body.message), /重新分析/u);
});

test('draft 的分析仍然可以确认', async () => {
  // 门禁只拦 stale。把正常流程也堵死就是过度收紧。
  const id = seedIntelligence('draft');
  const result = await call(`/api/projects/${projectId}/intelligence/${id}/approve`, { method: 'POST', body: JSON.stringify({}) });
  assert.equal(result.status, 201, JSON.stringify(result.body));
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/api && node --import tsx --test test/gap-source-status.test.ts
```

预期 FAIL:第一条拿到 201 而不是 400。

- [ ] **Step 3: 加门禁**

`apps/api/src/intelligence.service.ts` 的 `approveResource`,在 `if (requested === 'approved') assertResourceMetricsReady(table, current);` 之前插入:

```ts
      /*
       * 失效的项目分析不能直接确认。
       *
       * 此前只翻 status,于是资料变动后点一下「确认」就能把 stale 变回 approved,
       * 而缺口与 evidenceId 全是变动之前算出来的——用户看到的「引用已失效」正源于此。
       *
       * 只拦 project_intelligence:缺口/策略/选题的 stale 是分析级联下来的,
       * 重新分析会重建它们,拦住只会把用户堵死。
       */
      if (
        table === 'project_intelligence'
        && requested === 'approved'
        && String(current.status) === 'stale'
      ) {
        throw new BadRequestException('资料已变动，这份分析已失效。请重新分析后再确认。');
      }
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd apps/api && node --import tsx --test test/gap-source-status.test.ts
npm run typecheck -w @content-agent/api
```

预期:5 passed(任务 1 的 3 条 + 本任务 2 条),typecheck 干净。

- [ ] **Step 5: 全量回归**

```bash
cd apps/api && env -u ANTHROPIC_BASE_URL -u OPENAI_BASE_URL node --import tsx --test test/*.test.ts
```

与基线对比。**已有测试若因这次收紧变红,要逐条判断是真回归还是测试本身依赖了旧的宽松行为**——后者要连同测试一起改,并在提交信息里说明。

- [ ] **Step 6: 变异验证**

把 `String(current.status) === 'stale'` 改成 `false`,重跑——第 1 条必须红。改回。
把 `table === 'project_intelligence'` 那行删掉,重跑——`information_gaps` 的既有确认测试应出现红(证明限定生效)。改回。

- [ ] **Step 7: 提交**

```bash
git add apps/api/src/intelligence.service.ts apps/api/test/gap-source-status.test.ts
git commit -m "fix: 失效的项目分析不能直接确认

资料变动后点「确认」能把 stale 变回 approved,而缺口与 evidenceId
全是变动之前的。只拦 project_intelligence,缺口/策略/选题不受影响。"
```

---

### Task 5: 补充弹窗的目标文件选择与下一步指引

**Files:**
- Modify: `apps/web/src/components/knowledge/KnowledgeEnrichmentModal.tsx`
- Modify: `apps/web/src/lib/enrich-types.ts`(加一个纯函数)
- Test: `apps/web/test/enrich-shortfall.test.ts`(既有文件,追加)

**Interfaces:**
- Consumes: 无
- Produces: `enrichSavedHint(): string`——保存成功后的提示文案

API 早已支持 `targetFile`(`parseSaveRequest` / `parseMergeRequest` 都解析它),前端只回显不给选。实测后果:33.6 KB 原始资料没被触碰,补充内容独立成 2.9 KB 的 `INDEX.md`(猜想)。

- [ ] **Step 1: 写失败测试**

追加到 `apps/web/test/enrich-shortfall.test.ts`:

```ts
import { enrichSavedHint } from '../src/lib/enrich-types';

/**
 * 保存后的提示要指向下一步。
 *
 * 旧文案只说「缺口要等你补上真实资料才会关闭」——诚实但是死路:用户不知道
 * 认可的草稿内容该往哪去。现在缺口编辑器能设「我确认过」,那才是关闭缺口的路径。
 */
test('保存提示指向缺口池的人工确认,不承诺缺口会自动关闭', () => {
  const hint = enrichSavedHint();
  assert.match(hint, /新版本/u);
  assert.match(hint, /缺口/u);
  // 不能暗示保存本身就关掉了缺口
  assert.doesNotMatch(hint, /已(关闭|解决|完成)/u);
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/web && node --import tsx --test test/enrich-shortfall.test.ts
```

预期 FAIL:`enrichSavedHint` 不存在。

- [ ] **Step 3: 实现文案函数**

`apps/web/src/lib/enrich-types.ts` 末尾加:

```ts
/**
 * 补充保存成功后的提示。
 *
 * 保存本身不关闭缺口——存进去的是模型推测(证据类型「猜想」),没有人背书过。
 * 但它不再是死路:认可的内容填进缺口答案并选「我确认过」,那一刻缺口才关闭,
 * 且生成端会给它人工背书证据。这句话要把这条路说出来。
 */
export function enrichSavedHint(): string {
  return '已保存为新版本。缺口不会因此关闭——把你认可的内容填进缺口答案并选「我确认过」,才算补上了。';
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd apps/web && node --import tsx --test test/enrich-shortfall.test.ts
```

- [ ] **Step 5: 弹窗接入文案与目标文件选择**

`KnowledgeEnrichmentModal.tsx` 的 `save()` 里,把

```ts
      toast.push('已保存为新版本。补充内容整理了「还缺什么」,缺口要等你补上真实资料才会关闭');
```

换成

```ts
      toast.push(enrichSavedHint());
```

顶部导入 `enrichSavedHint`(与既有 `enrich-types` 导入合并)。

在预览步骤(`step === 'preview'`)显示 `targetFile` 的那段附近,把纯文本 `<code>{targetFile}</code>` 换成可选择:

```tsx
<Field label="保存到" hint="默认合并进知识地图；也可以并入你已有的某份资料">
  <select value={targetFile} onChange={(event) => setTargetFile(event.target.value)}>
    {/* 后端算出的默认目标始终可选,即使它是将要新建的文件 */}
    {!files.some((file) => file.name === targetFile) && (
      <option value={targetFile}>{targetFile}（新建）</option>
    )}
    {files.map((file) => <option key={file.name} value={file.name}>{file.name}</option>)}
  </select>
</Field>
```

弹窗需要文件列表,但三个入口手头的数据不一样(已核实):

| 入口 | 位置 | 有文件列表吗 |
|---|---|---|
| 专业版知识库 | `KnowledgePage.tsx:571` | 有,传 `currentFiles`(已折叠到最新版) |
| 快捷版工作台 | `ProjectKnowledgeTab.tsx:299` | 有,传它自己的 `files` |
| 缺口池 | `IntelligentSimpleFlow.tsx:1404` | **没有**,该页不拉 `knowledge.list` |

所以 `files` 必须是**可选** prop:

```tsx
/** 可选:缺口池入口没有文件列表,那里不显示选择器,沿用后端算出的默认目标。 */
files?: Array<{ name: string }>;
```

`files` 缺省时不渲染「保存到」选择器,只显示后端回的 `targetFile` 文本——保持现有行为。不要为了统一而在缺口池里新增一次 `knowledge.list` 请求:那是为了 UI 一致去加一条网络往返,而该入口的用户本来就在处理缺口,不在管理文件。

- [ ] **Step 6: 全量回归**

```bash
npm run typecheck -w @content-agent/web
npm test -w @content-agent/web
```

预期:typecheck 干净,518 passed。既有的 `enrich-entry-points.test.ts` 会检查三个入口都挂载弹窗——`files` 是可选 prop,所以那些测试不受影响,但仍要确认。

- [ ] **Step 7: 变异验证**

把 `enrichSavedHint` 改成 `'已保存,缺口已关闭'`,重跑——新测试必须红。改回。

- [ ] **Step 8: 提交**

```bash
git add apps/web/src/lib/enrich-types.ts apps/web/test/enrich-shortfall.test.ts apps/web/src/components/knowledge/KnowledgeEnrichmentModal.tsx apps/web/src/pages/KnowledgePage.tsx apps/web/src/components/quick/ProjectKnowledgeTab.tsx
git commit -m "feat: 补充可选目标文件,保存提示指向缺口确认

targetFile 后端一直支持,前端只回显不给选,导致补充内容独立成
INDEX.md(猜想)而没并入用户的原始资料。保存提示改为指向
「填进缺口答案并选我确认过」——那才是关闭缺口的路径。"
```

---

### Task 6: 知识库页的重新分析待办条

**Files:**
- Modify: `apps/web/src/lib/knowledge-instrument.ts`(`preflightHeadline` 的 `stale` 分支)
- Test: `apps/web/test/knowledge-instrument.test.ts`(既有文件,追加)

**Interfaces:**
- Consumes: 既有 `KnowledgePreflight.analysis`、`PreflightHeadline.needsAnalysis`
- Produces: 无新接口,只改文案与语气

引导面板已经存在(`needsAnalysis` 为真时渲染)。这一步只把 `stale` 的文案从"提示"改成"待办",并说明为什么必须重跑。

- [ ] **Step 1: 写失败测试**

追加到 `apps/web/test/knowledge-instrument.test.ts`:

```ts
test('资料变动后的待办条要说明缺口和证据都会被更新', () => {
  /*
   * 用户实际卡住的地方:补充保存后以为完事了,结果缺口还在、证据全失效。
   * 待办条要把「重新分析会一并更新缺口与证据引用」说出来,否则用户会去
   * 逐条手工重选证据——那是错的动作,evidenceId 含 documentId,
   * 一次保存会让该文件所有引用同时失效。
   */
  const view = preflightHeadline(preflight({ analysis: 'stale', canGenerate: false }));
  assert.equal(view?.needsAnalysis, true);
  assert.match(view!.nextStep, /缺口/u);
  assert.match(view!.nextStep, /证据|引用/u);
  assert.doesNotMatch(view!.nextStep, /逐条|重新选择/u);
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/web && node --import tsx --test test/knowledge-instrument.test.ts
```

预期 FAIL:`nextStep` 不含「证据」。

- [ ] **Step 3: 改文案**

`apps/web/src/lib/knowledge-instrument.ts` 的 `analysis === 'stale'` 分支,`nextStep` 改成:

```ts
      nextStep: '资料改动过,之前的分析已失效。到「内容生成」重新分析——缺口和证据引用会一并更新,不需要你逐条重选。',
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd apps/web && node --import tsx --test test/knowledge-instrument.test.ts
npm test -w @content-agent/web
```

预期:519 passed。

- [ ] **Step 5: 变异验证**

把 `nextStep` 改回旧文案,重跑——新测试必须红。改回。

- [ ] **Step 6: 提交**

```bash
git add apps/web/src/lib/knowledge-instrument.ts apps/web/test/knowledge-instrument.test.ts
git commit -m "fix: 失效待办条说明重新分析会一并更新缺口与证据

避免用户去逐条手工重选证据——evidenceId 含 documentId,
一次保存会让该文件所有引用同时失效,重新分析才是一次解决的路径。"
```

---

### Task 7: 端到端验收

**Files:**
- 无代码改动。只跑验证并记录结果。

**Interfaces:**
- Consumes: 任务 1-6 全部
- Produces: 验收记录(写进 PR 描述)

- [ ] **Step 1: 三端全量**

```bash
npm test -w @content-agent/agent-core
npm test -w @content-agent/web
cd apps/api && env -u ANTHROPIC_BASE_URL -u OPENAI_BASE_URL node --import tsx --test test/*.test.ts
```

与基线逐一对比。除基线那 1 条 `content-prompt-revision` 外不得有红。

- [ ] **Step 2: 三端 typecheck 与构建**

```bash
npm run typecheck -w @content-agent/agent-core && npm run typecheck -w @content-agent/api && npm run typecheck -w @content-agent/web
npm run build -w @content-agent/agent-core && npm run build -w @content-agent/api && npm run build -w @content-agent/web
```

- [ ] **Step 3: 真实项目验一次生成**

spec 记的风险:任务 2 会让此前被静默丢弃的人工答案开始被采用,这是意图,但会改变既有项目的生成结果。

对数据副本(**不要指向 `data/`**,启动会开 WAL 与线上进程抢同一个库文件)跑一次真实生成:

```bash
cd /tmp && rm -rf ca-verify && mkdir ca-verify
sqlite3 "<repo>/data/app.db" ".backup '/tmp/ca-verify/app.db'"
cp -R "<repo>/data/knowledge" /tmp/ca-verify/knowledge
```

挑一个有 `user_supplied` 缺口的项目生成一篇,人工读正文:确认新被采用的答案没有引入失真表述,也没有把人工填的内容说成"资料显示"。

- [ ] **Step 4: 闭环走一遍**

在数据副本上按顺序做完整闭环,每步记录结果:

1. 传一份资料 → 分析 → 确认
2. 用 AI 补充 → 保存(选一个已有文件作为目标,不是 INDEX.md)
3. 确认分析变 `stale`,知识库页出现待办条
4. 尝试点「确认」→ 应被拒,错误信息提示重新分析
5. 在缺口池填一条答案,选「我确认过」
6. 重新分析 → 确认 → 生成
7. 确认第 5 步那条缺口的答案出现在生成结果里,且证据 id 含 `evidence_human_approved`

- [ ] **Step 5: 记录并清理**

把三端测试数字、生成验收结论、闭环七步结果写成 PR 描述草稿。删掉 `/tmp/ca-verify`。

---

## 提交与合并

工作树有约 115 个未提交文件,其中约 101 个属于另一会话,`knowledge.service.ts` / `api.ts` / `types.ts` 为双方共同修改。**每次 `git add` 只加本任务明确列出的文件,不要用 `git add -A`。**

若某个文件同时含对方改动,整文件提交会把对方那部分带进来。仓库先例(PR #61)的处理是保留并在 PR 描述里披露。开 PR 前确认一次归属,并在描述中说明。

分支从 `feat/topic-diversity` 切出,PR 目标同分支。仓库合并惯例是 merge commit(PR #53–#65 全部如此,零 squash)。
