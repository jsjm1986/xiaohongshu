# 预检分档谎报修复 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 知识库「还要补什么」面板不再把分析器的判定谎报成用户的手工确认——引用随资料新版本失效的缺口独立成一档「引用已失效」。

**Architecture:** `PreflightGapInput` 加 `sourceStatus`,`PreflightTier` 加第五档 `evidence_stale`,`classifyGap` 在 `selfProofSurvives` 之前插一个三条件分支。前端有 6 处消费 tier 联合类型,其中 `api.ts` 的运行时白名单不触发 typecheck、必须由测试兜住。

**Tech Stack:** TypeScript;`apps/api` 与 `apps/web` 用 `node:test`;`packages/agent-core` 用 vitest(本计划不动它)。

设计规格:`docs/superpowers/specs/2026-07-30-预检分档谎报修复-design.md`

## Global Constraints

- 注释、文案、提交信息用**中文**,与仓库既有风格一致。
- `apps/api` 与 `apps/web` 用 **`node:test`**,不是 vitest。仓库无 vitest / `@testing-library/react`,**不写渲染 React 组件的测试**。
- 不引入任何新依赖。不建新表,不写迁移。
- 新档只认 `sourceStatus === 'supplied_fact'`,不认其他取值。
- 每个任务都要做**变异验证**,变异清单在任务里逐条列出。
- 工作树有约 106 个**另一会话**的未提交文件。`git add` 只加任务列出的文件,**绝不用 `git add -A` 或 `git add .`**,提交前用 `git diff --cached --name-only` 核对。

## 基线(已实测,可信)

- `web`:523 passed / 0 failed;typecheck 干净。**无既有红,任何红都是回归。**
- `apps/api`:唯一稳定红是 `generation-batch` 冒烟(需活的模型网关,与 `cross-tenant-isolation` 的 BYOK 要求互斥,单一环境无法两者同绿)。跑法:
  ```bash
  cd apps/api && env -u ANTHROPIC_BASE_URL -u OPENAI_BASE_URL node --import tsx --test test/*.test.ts
  ```
  很慢(数分钟)。迭代时只跑单文件,提交前跑一次全量。
- `agent-core` 本计划不动,不必跑。

## 文件结构

| 文件 | 责任 | 任务 |
|---|---|---|
| `apps/api/src/knowledge-preflight.ts` | 分档纯逻辑、档位类型、口径文案 | 1 |
| `apps/api/src/knowledge.service.ts` | 取数并把 `sourceStatus` 喂给 `classifyGap` | 2 |
| `apps/web/src/types.ts` | 前端档位联合类型 | 3 |
| `apps/web/src/lib/api.ts` | 运行时档位白名单与 `tiers` 归一化 | 3 |
| `apps/web/src/lib/knowledge-instrument.ts` | 三张文案表、排序权重、主结论分支 | 4 |
| `apps/web/src/pages/KnowledgePage.tsx` | 仪表盘第五张卡 | 5 |

分四个可独立评审的交付:后端判据(1-2)、前端类型与白名单(3)、前端文案与主结论(4)、仪表盘(5)。

---

### Task 1: 分档加 `evidence_stale` 档

**Files:**
- Modify: `apps/api/src/knowledge-preflight.ts`
- Test: `apps/api/test/knowledge-preflight.test.ts`(既有文件,追加)

**Interfaces:**
- Consumes: 无
- Produces:
  - `PreflightTier` 新成员 `'evidence_stale'`
  - `PreflightGapInput` 新可选字段 `sourceStatus?: string`
  - `settled()` 行为不变(白名单,新档不加进去 → 计入 `requiredOpen`)

**背景(实施者必须理解,否则会写错分支顺序):**

`classifyGap` 现在只看「答案能否在当前资料里匹配到分节」。匹配不中就落 `approved_only`,文案说「依据是你填写并确认过」。但实测某项目 18 条缺口 15 条如此,而它们的 `sourceStatus` 是 `supplied_fact`——分析器判定资料里有出处,用户从没手填过。这句话是假的。

它们匹配不中的真实原因:`evidenceId` 含 `documentId`(每版新 uuid),一份资料存成新版本,指向它的引用全部失效。

- [ ] **Step 1: 写失败测试**

追加到 `apps/api/test/knowledge-preflight.test.ts`。

该文件 `:20` 已有 `gap(overrides)` helper(默认 `availableEvidenceIds` 是空 Set,即「项目里没有任何可用证据」),`classifyGap` / `summarize` / `PreflightGapInput` 都已导入。**复用 helper,不要重复定义**。

```ts
/**
 * 引用失效不能说成「你填写并确认过」。
 *
 * 分析器判定 supplied_fact 的缺口,一旦资料存了新版本,evidenceId 全变,
 * 分节匹配就不中。旧逻辑于是落 approved_only 并声称「依据是你填写并确认过」——
 * 而用户从没填过这条。实测 18 条缺口 15 条被这么谎报。
 */
test('分析器给过出处、引用已失效 → 独立成 evidence_stale,不谎报成人工确认', () => {
  const result = classifyGap(gap({
    label: '易用性',
    answer: '不需要额外培训,前台四个核心价值即可上手',
    declaredEvidenceIds: ['evidence_section_deadbeef'],
    sourceStatus: 'supplied_fact',
  }));
  assert.equal(result.tier, 'evidence_stale');
  assert.ok(result.reasons.some((reason) => /引用/u.test(reason)), result.reasons.join('|'));
  // 关键:不能再声称是用户填的
  assert.ok(!result.reasons.some((reason) => /你填写并确认过/u.test(reason)), result.reasons.join('|'));
});

test('没有失效引用作证时不进新档:supplied_fact 是上一轮的判定,不能凭它作保', () => {
  // 声明的引用都还在可用集合里 → 出处没断 → 落不进新档
  const result = classifyGap(gap({
    answer: '一句能自证的单行答案',
    declaredEvidenceIds: ['evidence_section_alive'],
    availableEvidenceIds: new Set(['evidence_section_alive']),
    sourceStatus: 'supplied_fact',
  }));
  assert.equal(result.tier, 'approved_only');
});

test('只认 supplied_fact:user_supplied 本来就是人工确认,不该改口', () => {
  const result = classifyGap(gap({
    answer: '我到院面诊后才确定',
    declaredEvidenceIds: ['evidence_section_deadbeef'],
    sourceStatus: 'user_supplied',
  }));
  assert.equal(result.tier, 'approved_only');
});

test('内容仍能匹配上就仍算有资料支撑:引用失效不改变这个事实', () => {
  const result = classifyGap(gap({
    answer: '能匹配到分节的答案',
    declaredEvidenceIds: ['evidence_section_deadbeef'],
    sectionEvidenceIds: ['evidence_section_hit'],
    availableEvidenceIds: new Set(['evidence_section_hit']),
    sourceStatus: 'supplied_fact',
  }));
  assert.equal(result.tier, 'evidence_backed');
});

test('格式坏的仍归会被丢弃:重新分析修不了换行,逐字符提示不能丢', () => {
  const result = classifyGap(gap({
    answer: '第一行\n第二行',
    declaredEvidenceIds: ['evidence_section_deadbeef'],
    sourceStatus: 'supplied_fact',
  }));
  assert.equal(result.tier, 'will_be_dropped');
  assert.ok(result.reasons.some((reason) => /换行/u.test(reason)), result.reasons.join('|'));
});

test('认不出的 sourceStatus 不进新档:库里真有 unacknowledged 这种值', () => {
  const result = classifyGap(gap({
    answer: '一句能自证的单行答案',
    declaredEvidenceIds: ['evidence_section_deadbeef'],
    sourceStatus: 'unacknowledged',
  }));
  assert.equal(result.tier, 'approved_only');
});

test('必答缺口落在 evidence_stale → 不算已落实,挣住生成', () => {
  const stale = classifyGap(gap({
    required: true,
    answer: '一句能自证的单行答案',
    declaredEvidenceIds: ['evidence_section_deadbeef'],
    sourceStatus: 'supplied_fact',
  }));
  const summary = summarize([stale], 'approved');
  assert.equal(stale.tier, 'evidence_stale');
  assert.equal(summary.canGenerate, false);
  assert.equal(summary.requiredOpen.length, 1);
  assert.equal(summary.tiers.evidence_stale, 1);
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/api && node --import tsx --test test/knowledge-preflight.test.ts
```

预期 FAIL:`sourceStatus` 不在 `PreflightGapInput` 上(TS 报未知属性),且 `evidence_stale` 不是合法档位。

- [ ] **Step 3: 档位类型加第五档**

`apps/api/src/knowledge-preflight.ts` 的 `PreflightTier`(第 22 行起),在 `approved_only` 与 `will_be_dropped` 之间插入:

```ts
  /**
   * 分析器判定资料里有出处,但那些引用已经失效(资料存过新版本或被删除)。
   *
   * 生成时答案仍会被采用(靠规划上下文自证),但**没有任何当前资料为它作证**——
   * 结论不可复核。所以它既不是 evidence_backed,也不该谎报成「你填写并确认过」。
   */
  | 'evidence_stale'
```

- [ ] **Step 4: 输入结构加 `sourceStatus`**

同文件 `PreflightGapInput`,在 `category` 之后加:

```ts
  /**
   * 缺口答案的来源标记,由分析器或缺口编辑器写。
   *
   * 只用来区分「分析器说有出处」与「用户自己填的」:前者引用失效时要说实话
   * (evidence_stale),后者本来就是人工确认,不该改口。调用方须先过白名单——
   * 库里实测有 'unacknowledged' 这种不在联合类型里的值。
   */
  sourceStatus?: string;
```

- [ ] **Step 5: 插入分档分支**

同文件 `classifyGap`,在 `else if (selfProofSurvives(proposed))` **之前**插入新分支。改完这一段应该是:

```ts
  } else if (sectionEvidenceIds.length) {
    tier = 'evidence_backed';
    reasons.push(`答案能在已上传资料里找到支撑(${sectionEvidenceIds.length} 处分节)。`);
  } else if (
    /*
     * 三个条件都必需:
     * - supplied_fact:分析器当初给过出处。
     * - 有失效引用:那个出处**确实断了**的实证。只看 sourceStatus 会替一个可能已
     *   过时的判定作保——资料被整份删掉时,说「重新分析会重建引用」是空头承诺。
     * - selfProofSurvives:格式坏的答案是真会被丢弃,得留给 will_be_dropped 去说
     *   清是哪个字符;重新分析修不了换行。
     */
    input.sourceStatus === 'supplied_fact'
    && staleDeclaredEvidenceIds.length > 0
    && selfProofSurvives(proposed)
  ) {
    tier = 'evidence_stale';
    reasons.push('原本有资料出处,但引用随资料的新版本失效了。重新分析会重建引用。');
  } else if (selfProofSurvives(proposed)) {
```

- [ ] **Step 6: 新档不再重复播报失效**

同文件末尾那段 `if (staleDeclaredEvidenceIds.length) { … }`,条件加一个排除——新档的首句已经说了引用失效,再追加就是同一件事说两遍:

```ts
  if (staleDeclaredEvidenceIds.length && tier !== 'evidence_stale') {
```

- [ ] **Step 7: `summarize` 的 `tiers` 补格子**

同文件 `summarize` 里那个 `Record<PreflightTier, number>` 字面量,加一行(位置与类型声明顺序一致):

```ts
    evidence_stale: 0,
```

`settled()` **不要动**——它是白名单(`evidence_backed || approved_only`),新档不加进去正是我们要的「算未落实」。

- [ ] **Step 8: 订正三处失准文案**

这一步是设计规格明确要求的,漏了会留下自相矛盾的注释。

**8a.** 文件头注释里「结论因此是**保守下界**:预检说站得住的一定站得住,预检说会被丢的,在极少数情况下可能被路径 2 救回。」改成:

```
 * 结论是**保守下界**:预检说有资料支撑的一定有;说会被丢的,极少数情况下可能被
 * 路径 2 救回。另有一档 `evidence_stale` 是「生成时会被采用,但没有当前资料为它
 * 作证」——站得住不等于可复核,这一档说的是后者。
```

**8b.** `PREFLIGHT_NOTE`(第 251 行)最后那句「结论是保守下界:显示站得住的一定站得住。」改成:

```ts
  + '因为部分证据路径依赖生成时的上下文,结论是保守下界:显示有资料支撑的一定有。'
  + '标为「引用已失效」的仍会被生成采用,但没有当前资料为它作证,需要重新分析重建引用。';
```

(把原来那一句整体替换,保留前两句不动。)

**8c.** `PreflightSummary.requiredOpen` 的注释「站不住的必答缺口。生成时正文会出现「关于X我还没问明白」。」改成:

```ts
  /**
   * 站不住的必答缺口。
   *
   * `blank` / `will_be_dropped` 会让生成时正文出现「关于X我还没问明白」;
   * `evidence_stale` 不会(答案仍被采用),但它的结论无法用当前资料复核,
   * 同样不该就这么生成。
   */
```

- [ ] **Step 9: 跑测试确认通过**

```bash
cd apps/api && node --import tsx --test test/knowledge-preflight.test.ts
npm run typecheck -w @content-agent/api
```

预期:全绿,typecheck 干净。

- [ ] **Step 10: 变异验证**

逐个改、跑、确认变红、改回。**只跑 `test/knowledge-preflight.test.ts` 就够。**

| 变异 | 应变红的测试 |
|---|---|
| 新分支挪到 `selfProofSurvives(proposed)` 那个分支**之后** | 「分析器给过出处、引用已失效 → 独立成 evidence_stale」 |
| 去掉 `&& staleDeclaredEvidenceIds.length > 0` | 「没有失效引用作证时不进新档」 |
| 条件放宽成 `input.sourceStatus === 'supplied_fact' \|\| input.sourceStatus === 'user_supplied'` | 「只认 supplied_fact」 |
| 去掉 `&& selfProofSurvives(proposed)` | 「格式坏的仍归会被丢弃」 |
| `settled()` 里加上 `\|\| tier === 'evidence_stale'` | 「必答缺口落在 evidence_stale → 挣住生成」 |

每个变异都必须**只**让对应那条红。若有别的测试跟着红,说明分支顺序或条件写错了,查清再继续。

- [ ] **Step 11: 全量回归并提交**

```bash
cd apps/api && env -u ANTHROPIC_BASE_URL -u OPENAI_BASE_URL node --import tsx --test test/*.test.ts
```

对比基线:除 `generation-batch` 冒烟外不得有红。

```bash
git add apps/api/src/knowledge-preflight.ts apps/api/test/knowledge-preflight.test.ts
git diff --cached --name-only   # 必须只有这两个
git commit -m "$(cat <<'EOF'
feat(api): 引用失效独立成一档,不再谎报成人工确认

分析器判定 supplied_fact 的缺口,资料存新版本后 evidenceId 全变、分节匹配
不中,旧逻辑落 approved_only 并声称「依据是你填写并确认过」——用户从没填过。
实测某项目 18 条缺口 15 条被这么谎报。

新档三个条件都必需:supplied_fact 是分析器给过出处,有失效引用是出处确实断了
的实证(只看前者会替过时判定作保),selfProofSurvives 把格式坏的留给
will_be_dropped(重新分析修不了换行)。

顺带订正三处随之失准的文案:模块头「保守下界」、PREFLIGHT_NOTE、
requiredOpen 注释。
EOF
)"
```

---

### Task 2: 取数时把 `sourceStatus` 喂进分档

**Files:**
- Modify: `apps/api/src/knowledge.service.ts`(`preflight()` 方法,约 `:436-482`)
- Test: `apps/api/test/knowledge-preflight-route.test.ts`(既有文件,追加)

**Interfaces:**
- Consumes: Task 1 的 `PreflightGapInput.sourceStatus?: string`
- Produces: 无新接口

**背景:** Task 1 让 `classifyGap` 会读 `sourceStatus`,但 `preflight()` 目前没传它——不做这一步,新档永远不会触发。

`preflight()` 直接读 `data_json`,**不经 `normalizeGap`**,所以要自己过白名单:库里实测有 5 行 `sourceStatus: 'unacknowledged'`,不在 `GAP_SOURCE_STATUSES`(`intelligence.service.ts:70`)之内。

- [ ] **Step 1: 写失败测试**

追加到 `apps/api/test/knowledge-preflight-route.test.ts`。先读该文件,照它既有的建项目/建缺口/调接口写法对齐——下面是断言要点,**载荷形状以既有写法为准,不要照抄我的字段名**:

```ts
/**
 * sourceStatus 必须真的到达分档。
 *
 * preflight() 直接读 data_json 而不经 normalizeGap,漏传这个字段的话
 * evidence_stale 永远不触发,Task 1 的判据从接口上看不出效果。
 */
test('引用失效的 supplied_fact 缺口,接口返回 evidence_stale', async () => {
  // 建一条缺口:有答案(单行,能自证)、声明一个不存在的证据 id、标 supplied_fact。
  // supplied_fact 只能由分析器写(写入侧有守卫),所以直接改库落这个值。
  // 项目里不要有能匹配上这句答案的资料,否则会落 evidence_backed。
  // 断言:该缺口的 tier === 'evidence_stale',且 tiers.evidence_stale >= 1
});

test('认不出的 sourceStatus 不会污染分档', async () => {
  // 同上但值写 'unacknowledged'（库里真实存在的脏值）
  // 断言:tier 为 'approved_only',不是 'evidence_stale'
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/api && node --import tsx --test test/knowledge-preflight-route.test.ts
```

预期 FAIL:第一条拿到 `approved_only` 而不是 `evidence_stale`(字段没传进去)。

- [ ] **Step 3: 模块级白名单**

`apps/api/src/knowledge.service.ts`,在文件的模块级常量区(靠近其他 `const`)加:

```ts
/**
 * 缺口 sourceStatus 的合法取值。
 *
 * preflight 直接读 data_json,不经 normalizeGap,所以白名单要在这里再过一遍——
 * 库里实测有 'unacknowledged' 这种不在联合类型里的历史值。与
 * intelligence.service.ts 的 GAP_SOURCE_STATUSES 保持一致。
 */
const GAP_SOURCE_STATUSES = new Set([
  'supplied_fact', 'user_supplied', 'inference', 'hypothesis', 'unknown',
]);
```

- [ ] **Step 4: 传字段**

同文件 `preflight()` 里那个 `classifyGap({ … })` 调用,在 `availableEvidenceIds,` 之后加:

```ts
        sourceStatus: typeof data.sourceStatus === 'string' && GAP_SOURCE_STATUSES.has(data.sourceStatus)
          ? data.sourceStatus
          : undefined,
```

- [ ] **Step 5: 跑测试确认通过**

```bash
cd apps/api && node --import tsx --test test/knowledge-preflight-route.test.ts test/knowledge-preflight.test.ts
npm run typecheck -w @content-agent/api
```

- [ ] **Step 6: 变异验证**

| 变异 | 应变红 |
|---|---|
| 删掉 Step 4 那三行 | 「接口返回 evidence_stale」 |
| 白名单判断改成直接透传 `data.sourceStatus as string` | 「认不出的 sourceStatus 不会污染分档」——注意:只有当 `classifyGap` 只认 `supplied_fact` 时这条才会红,若没红,说明测试的脏值选得不对,换一个能触发新档的脏值再验 |

- [ ] **Step 7: 全量回归并提交**

```bash
cd apps/api && env -u ANTHROPIC_BASE_URL -u OPENAI_BASE_URL node --import tsx --test test/*.test.ts
git add apps/api/src/knowledge.service.ts apps/api/test/knowledge-preflight-route.test.ts
git diff --cached --name-only
git commit -m "$(cat <<'EOF'
feat(api): 预检取数补传 sourceStatus,新档才会真的触发

preflight() 直接读 data_json 不经 normalizeGap,所以白名单要在这里再过
一遍——库里实测有 5 行 'unacknowledged',不在 GAP_SOURCE_STATUSES 之内。
EOF
)"
```

---

### Task 3: 前端类型与运行时白名单

**Files:**
- Modify: `apps/web/src/types.ts:91-95`(`KnowledgePreflightTier`)
- Modify: `apps/web/src/lib/api.ts:234`(`PREFLIGHT_TIERS`)、`:274-279`(`tiers` 字面量)
- Test: `apps/web/test/knowledge-instrument.test.ts`(既有文件,追加一条源码断言)

**Interfaces:**
- Consumes: Task 1 的档位名 `'evidence_stale'`
- Produces: `KnowledgePreflightTier` 新成员 `'evidence_stale'`

**⚠️ 这个任务是整个修复的成败点。**

`apps/web/src/lib/api.ts:234` 的 `PREFLIGHT_TIERS` 是**运行时白名单数组,不是 `Record`**,所以漏了它**不会有任何编译错误**。而 `preflightTier()`(`:236`)会把认不出的档走 `:241` 的兜底、**静默改判成 `will_be_dropped`**——新档一条都到不了界面,`tiers.will_be_dropped` 还被虚增。那句兜底注释本身是对的设计,问题只在白名单没跟上。

所以本任务必须有一条测试守住它。

- [ ] **Step 1: 写失败测试**

追加到 `apps/web/test/knowledge-instrument.test.ts`。顶部若还没有,加 `import { readFileSync } from 'node:fs';`(照 `apps/web/test/gap-source.test.ts:2` 的写法)。

```ts
/**
 * api.ts 的 PREFLIGHT_TIERS 是硬编码的第二份档位清单,而且是运行时数组、
 * 不触发 typecheck。漏掉新档时 preflightTier() 会把它静默改判成
 * will_be_dropped——新档一条都到不了界面,而 tiers.will_be_dropped 被虚增。
 * 唯一能兜住它的就是这条源码断言。
 */
test('api.ts 的运行时档位白名单认得 evidence_stale', () => {
  const source = readFileSync(new URL('../src/lib/api.ts', import.meta.url), 'utf8');
  const match = /const PREFLIGHT_TIERS = \[([^\]]+)\]/u.exec(source);
  assert.ok(match, 'api.ts 里找不到 PREFLIGHT_TIERS');
  assert.match(match[1], /evidence_stale/u);
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/web && node --import tsx --test test/knowledge-instrument.test.ts
```

预期 FAIL:白名单里没有 `evidence_stale`。

- [ ] **Step 3: 前端档位联合类型加成员**

`apps/web/src/types.ts:91` 那个联合类型,在 `'approved_only'` 与 `'will_be_dropped'` 之间加 `| 'evidence_stale'`。同时它上方的文档注释(`:88` 附近的档位列表)补一行:

```
 * - `evidence_stale` 分析器给过出处但引用已失效,生成仍会采用、结论无法复核
```

- [ ] **Step 4: 运行时白名单加成员**

`apps/web/src/lib/api.ts:234`:

```ts
const PREFLIGHT_TIERS = ["evidence_backed", "approved_only", "evidence_stale", "will_be_dropped", "blank"] as const;
```

- [ ] **Step 5: `tiers` 归一化补格子**

同文件 `:274-279` 那个字面量加一行(这一处漏了会编译报错,但顺手一起改):

```ts
      evidence_stale: Number(tiers.evidence_stale ?? 0),
```

- [ ] **Step 6: 跑测试与 typecheck**

```bash
cd apps/web && node --import tsx --test test/knowledge-instrument.test.ts
cd ../.. && npm run typecheck -w @content-agent/web
```

**typecheck 现在预期会报错**,因为下面这些 `Record<KnowledgePreflightTier, …>` 还缺 `evidence_stale` 格子:

- `knowledge-instrument.ts` 的 `TIER_LABEL` / `TIER_NOTE` / `TIER_TONE` / `actionableGaps` 的 `weight`(归 Task 4)
- `apps/web/test/knowledge-instrument.test.ts:118` 那个 `preflight()` helper 的 `tiers` 字面量(Task 4 Step 1 会补)

这是预期的——记下报错文件与行号,它们正是 Task 4 的清单。

> 若你是逐任务提交的实施者:本任务允许在 typecheck 未全绿的状态下提交,因为缺的格子归 Task 4。但**测试必须绿**。

- [ ] **Step 7: 变异验证**

删掉 Step 4 里的 `"evidence_stale", ` → 源码断言那条测试必须红。改回。

- [ ] **Step 8: 提交**

```bash
git add apps/web/src/types.ts apps/web/src/lib/api.ts apps/web/test/knowledge-instrument.test.ts
git diff --cached --name-only
git commit -m "$(cat <<'EOF'
feat(web): 档位联合类型与运行时白名单认得 evidence_stale

api.ts 的 PREFLIGHT_TIERS 是运行时数组、不触发 typecheck,漏了它会让新档
被静默改判成 will_be_dropped——新档到不了界面,will_be_dropped 还被虚增。
加一条源码断言守住它。
EOF
)"
```

---

### Task 4: 前端文案、排序与主结论

**Files:**
- Modify: `apps/web/src/lib/knowledge-instrument.ts`(`TIER_LABEL` `:62`、`TIER_NOTE` `:74`、`TIER_TONE` `:83`、`actionableGaps` 的 `weight` `:198`、`preflightHeadline` 的 `!canGenerate` 分支 `:157`)
- Test: `apps/web/test/knowledge-instrument.test.ts`(既有文件,追加)

**Interfaces:**
- Consumes: Task 3 的 `KnowledgePreflightTier` 新成员
- Produces: 无新接口(三张表与既有形状一致)

**背景:一处真实矛盾。** 我起初以为「引用失效几乎总伴随 `analysis === 'stale'`」,用真实数据核过之后不成立:项目「眼袋王」`analysis` 是 `approved`、**零个知识文件**、49 条 `supplied_fact` 缺口——所有引用必然失效,而分析已确认。这时 `preflightHeadline` 走 `:157` 的 `!canGenerate` 分支,文案是「补上这几条的真实资料,或改掉会被丢弃的答案格式」、`needsAnalysis: false`、没有跳转按钮。对引用失效这种情形是**错的指引**:改格式没用,要做的是重新分析。

- [ ] **Step 1: 写失败测试**

追加到 `apps/web/test/knowledge-instrument.test.ts`。该文件已有 `preflight()` helper(约 `:113`)构造 `KnowledgePreflight`,复用它。

**先做两处必要修补,否则新测试编译不过:**

1. 该文件顶部的导入(`:3-11`)只有 `TIER_NOTE`,新测试还要用 `TIER_LABEL` 与 `TIER_TONE`——加进去。
2. `preflight()` helper 里的 `tiers` 字面量(`:118`)是完整的 `Record<KnowledgePreflightTier, number>`,Task 3 加了联合成员后它缺格子会报错。补成:

```ts
    tiers: { evidence_backed: 0, approved_only: 0, evidence_stale: 0, will_be_dropped: 0, blank: 0 },
```

```ts
test('三张档位表都有 evidence_stale 的格子,说明要指向重新分析', () => {
  assert.ok(TIER_LABEL.evidence_stale.trim().length > 0);
  assert.ok(TIER_NOTE.evidence_stale.trim().length > 0);
  // tone 是字符串联合,断言非空没意义,直接钉具体值:它可修复且重新分析能自愈,
  // 与 blank 同级,不该用 error 吓人
  assert.equal(TIER_TONE.evidence_stale, 'warn');
  // 这一档唯一的出路是重新分析,说明里必须说出来
  assert.match(TIER_NOTE.evidence_stale, /重新分析/u);
  // 不能沿用「你填写并确认过」那套说法——本次修的就是这个谎
  assert.ok(!/你填写|确认过/u.test(TIER_NOTE.evidence_stale), TIER_NOTE.evidence_stale);
});

test('待处理排序:引用失效比「会被丢弃」轻,比「仅人工确认」重', () => {
  const gaps = [
    { id: 'a', label: '仅人工确认的', status: 'approved', required: false, category: '', tier: 'approved_only' as const, sectionEvidenceIds: [], staleDeclaredEvidenceIds: ['x'], reasons: [] },
    { id: 'b', label: '引用失效的', status: 'approved', required: false, category: '', tier: 'evidence_stale' as const, sectionEvidenceIds: [], staleDeclaredEvidenceIds: ['x'], reasons: [] },
    { id: 'c', label: '会被丢弃的', status: 'approved', required: false, category: '', tier: 'will_be_dropped' as const, sectionEvidenceIds: [], staleDeclaredEvidenceIds: [], reasons: [] },
  ];
  const ordered = actionableGaps(preflight({ gaps })).map((gap) => gap.id);
  assert.deepEqual(ordered, ['c', 'b', 'a']);
});

/**
 * 分析已确认 + 引用失效,是真实存在的组合(项目「眼袋王」:analysis 为 approved、
 * 零个知识文件、49 条 supplied_fact 缺口)。旧文案在这里说「改掉会被丢弃的答案
 * 格式」——改格式没用,要做的是重新分析。
 */
test('分析已确认但必答缺口引用失效 → 指向重新分析,并给出跳转', () => {
  const view = preflightHeadline(preflight({
    analysis: 'approved',
    canGenerate: false,
    requiredOpen: [{ id: 'g1', label: '易用性', tier: 'evidence_stale' }],
  }));
  assert.match(view!.nextStep, /重新分析/u);
  assert.equal(view?.needsAnalysis, true);
  // 不该让用户去改格式
  assert.ok(!/格式/u.test(view!.nextStep), view!.nextStep);
});

test('必答缺口不是引用失效时,仍走原来的补资料/改格式文案', () => {
  const view = preflightHeadline(preflight({
    analysis: 'approved',
    canGenerate: false,
    requiredOpen: [{ id: 'g1', label: '价格信息', tier: 'blank' }],
  }));
  assert.match(view!.nextStep, /资料|格式/u);
  assert.equal(view?.needsAnalysis, false);
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd apps/web && node --import tsx --test test/knowledge-instrument.test.ts
```

预期 FAIL:三张表没有 `evidence_stale` 格子(TS 报缺属性)。

- [ ] **Step 3: 三张表加格子**

`apps/web/src/lib/knowledge-instrument.ts`:

`TIER_LABEL`(`:62`)加:
```ts
  evidence_stale: '引用已失效',
```

`TIER_NOTE`(`:74`)加:
```ts
  evidence_stale: '出处随资料新版本失效,重新分析会重建',
```

`TIER_TONE`(`:83`)加:
```ts
  evidence_stale: 'warn',
```

用 `warn` 而非 `danger`:它可修复,且重新分析就能自愈,与 `blank` 同级。

- [ ] **Step 4: 排序权重加档位**

同文件 `actionableGaps` 的 `weight`(`:198`),在 `will_be_dropped: 0` 与 `blank: 1` 之间重排。改完是:

```ts
  // 会丢弃的最急(隐性故障);引用失效次之(答案会被采用,但结论无法复核);
  // 再是无答案;仅人工确认与有资料支撑排最后。
  const weight: Record<KnowledgePreflightTier, number> = {
    will_be_dropped: 0,
    evidence_stale: 1,
    blank: 2,
    approved_only: 3,
    evidence_backed: 4,
  };
```

`actionableGaps` 的 `filter` **不用改**——它已经用 `staleDeclaredEvidenceIds.length > 0` admit 了这类缺口,而新档必然有失效引用。

- [ ] **Step 5: `actionable` 计入新档**

`preflightHeadline` 开头(约 `:130`)的 `actionable` 现在是 `tiers.will_be_dropped + tiers.blank`,漏掉新档会让「需要你动手的条数」少算这一批(而 `actionableGaps` 的清单里它们是在的,两个数字会打架)。改成:

```ts
  // 引用失效同样需要用户动手(去重新分析),不计入的话这个数字会和
  // actionableGaps 的清单长度打架——那个 filter 已经 admit 了这类缺口。
  const actionable = tiers.will_be_dropped + tiers.evidence_stale + tiers.blank;
```

- [ ] **Step 6: 主结论分支按档位分情况**

同文件 `preflightHeadline` 的 `if (!canGenerate) { … }`(`:157`)改成:

```ts
  // 分析已确认,往下才是缺口层面的结论
  if (!canGenerate) {
    /*
     * 引用失效要单独说。实测存在 analysis='approved' 而引用全失效的项目
     * (零个知识文件 + 49 条 supplied_fact 缺口),此时旧文案让用户「改掉会被
     * 丢弃的答案格式」——改格式没用,要做的是重新分析重建引用。
     */
    const staleOpen = requiredOpen.filter((gap) => gap.tier === 'evidence_stale').length;
    if (staleOpen === requiredOpen.length) {
      return {
        ...base,
        text: `还不能生成:${requiredOpen.length} 条必答缺口的引用已失效`,
        tone: 'error',
        nextStep: '这些答案的资料出处已随新版本失效。到「内容生成」重新分析,引用会一并重建。',
        needsAnalysis: true,
      };
    }
    return {
      ...base,
      text: `还不能生成:${requiredOpen.length} 条必答缺口没落实`,
      tone: 'error',
      nextStep: staleOpen > 0
        ? `补上缺资料的那几条,或改掉会被丢弃的答案格式;其中 ${staleOpen} 条是引用失效,重新分析会一并重建。`
        : '补上这几条的真实资料,或改掉会被丢弃的答案格式。',
      // needsAnalysis 是必填 boolean,不能条件展开。混合情形也给 true:
      // 重新分析是其中一部分缺口的唯一出路,不给跳转等于藏起出路。
      needsAnalysis: staleOpen > 0,
    };
  }
```

混合情形也给 `needsAnalysis: true`:重新分析是其中一部分缺口的唯一出路,不给跳转等于藏起出路。

- [ ] **Step 7: 跑测试与 typecheck**

```bash
cd apps/web && node --import tsx --test test/knowledge-instrument.test.ts
cd ../.. && npm run typecheck -w @content-agent/web
```

预期:测试全绿。typecheck **可能仍报** `KnowledgePage.tsx` 相关错误(若 Task 5 未做),那是预期的;但 `knowledge-instrument.ts` 自身不得有错。

- [ ] **Step 8: 变异验证**

| 变异 | 应变红 |
|---|---|
| `TIER_NOTE.evidence_stale` 改成 `'你填写并确认过,生成会用'` | 「三张档位表…说明要指向重新分析」 |
| `weight` 里把 `evidence_stale` 改成 `4` | 「待处理排序」 |
| 删掉 Step 6 的 `if (staleOpen === requiredOpen.length)` 整块 | 「分析已确认但必答缺口引用失效」 |
| Step 6 里 `staleOpen === requiredOpen.length` 改成 `staleOpen > 0` | 「必答缺口不是引用失效时…」——不一定红,若不红说明该测试的 fixture 需要含一条 `evidence_stale` 才能区分,补上再验 |
| Step 5 里 `actionable` 去掉 `+ tiers.evidence_stale` | 若无测试变红,补一条:`tiers.evidence_stale` 为 N 时 `preflightHeadline(...).actionable` 应含这 N 条 |

- [ ] **Step 9: 提交**

```bash
git add apps/web/src/lib/knowledge-instrument.ts apps/web/test/knowledge-instrument.test.ts
git diff --cached --name-only
git commit -m "$(cat <<'EOF'
feat(web): 引用失效的文案、排序与主结论

主结论此前有一处真实矛盾:实测存在 analysis='approved' 而引用全失效的项目
(零个知识文件 + 49 条 supplied_fact 缺口),旧文案让用户「改掉会被丢弃的答案
格式」——改格式没用,要做的是重新分析。现在按 requiredOpen 的档位分情况,
并给出跳转。

排序上引用失效比「会被丢弃」轻(答案仍会被采用),比「仅人工确认」重
(结论无法复核)。
EOF
)"
```

---

### Task 5: 仪表盘第五张卡

**Files:**
- Modify: `apps/web/src/pages/KnowledgePage.tsx:381-420`(四段手写档位卡之后)
- Test: 无(仪表盘是 JSX,仓库无渲染测试;由 typecheck + 人工确认覆盖)

**Interfaces:**
- Consumes: Task 3 的档位类型、Task 4 的三张表
- Produces: 无

**背景:** `KnowledgePage.tsx:381-420` 的档位卡是**四段手写 JSX,不是遍历**。不补第五张,那 15 条缺口会从仪表盘**消失**——而让计数停止骗人正是本次的首要目标。

- [ ] **Step 1: 确认图标可用**

新卡需要一个 lucide 图标。先看该文件顶部的 `lucide-react` 导入,确认 `Link2Off` 是否可用:

```bash
cd apps/web && grep -n "from 'lucide-react'" src/pages/KnowledgePage.tsx
node -e "const m=require('lucide-react');console.log(typeof m.Link2Off)"
```

若输出 `function`,用 `Link2Off` 并加进导入;若不可用,复用该文件已导入的 `TriangleAlert`(`will_be_dropped` 那张在用),不要为图标另装依赖。

- [ ] **Step 2: 加第五张卡**

在 `preflight.tiers.approved_only > 0` 那张卡之后、`will_be_dropped` 那张之前插入(顺序与 `TIER_*` 的语义强弱一致):

```tsx
        {preflight && preflight.tiers.evidence_stale > 0 && (
          <V2InstrumentCell
            tone={TIER_TONE.evidence_stale}
            icon={<Link2Off size={15} />}
            label={TIER_LABEL.evidence_stale}
            value={preflight.tiers.evidence_stale}
            unit="条"
            note={TIER_NOTE.evidence_stale}
          />
        )}
```

(Step 1 若判定 `Link2Off` 不可用,把图标换成 `<TriangleAlert size={15} />`。)

- [ ] **Step 3: typecheck 与全量测试**

```bash
npm run typecheck -w @content-agent/web
npm test -w @content-agent/web
```

预期:typecheck **现在必须全绿**(这是最后一个补格子的地方);测试 523 + 本次新增条数,0 failed。

- [ ] **Step 4: 构建**

```bash
npm run build -w @content-agent/web
```

预期干净。

- [ ] **Step 5: 提交**

```bash
git add apps/web/src/pages/KnowledgePage.tsx
git diff --cached --name-only
git commit -m "$(cat <<'EOF'
feat(web): 仪表盘补「引用已失效」那一格

档位卡是四段手写 JSX 而非遍历,不补第五张那些缺口会直接从仪表盘消失——
而让计数停止骗人正是本次的首要目标。
EOF
)"
```

---

### Task 6: 端到端验收

**Files:** 无代码改动。

**Interfaces:**
- Consumes: Task 1-5 全部

- [ ] **Step 1: 两端全量与构建**

```bash
npm test -w @content-agent/web
npm run typecheck -w @content-agent/web && npm run build -w @content-agent/web
npm run typecheck -w @content-agent/api && npm run build -w @content-agent/api
cd apps/api && env -u ANTHROPIC_BASE_URL -u OPENAI_BASE_URL node --import tsx --test test/*.test.ts
```

`web` 须 0 failed。`apps/api` 除 `generation-batch` 冒烟外不得有红。`agent-core` 本次未动,不必跑。

- [ ] **Step 2: 对真实数据只读复核**

**只读,不要启动任何服务。** 直接查库:

```bash
cd "<repo>" && sqlite3 -readonly data/app.db "
SELECT p.name,
       (SELECT status FROM project_intelligence WHERE project_id=p.id AND deleted_at IS NULL
        ORDER BY CASE status WHEN 'approved' THEN 0 ELSE 1 END, version DESC LIMIT 1) AS 分析,
       (SELECT COUNT(*) FROM knowledge_files WHERE project_id=p.id AND deleted_at IS NULL) AS 资料数,
       (SELECT COUNT(*) FROM information_gaps WHERE project_id=p.id AND deleted_at IS NULL
        AND json_extract(data_json,'\$.sourceStatus')='supplied_fact') AS sf缺口
FROM projects p WHERE p.deleted_at IS NULL;"
```

记下「测试」(应有 12 条 `supplied_fact`)与「眼袋王」(`approved` + 0 资料 + 49 条)两个项目——它们分别是本次修复的两个典型场景。

- [ ] **Step 3: 人工确认界面**

`apps/web/dist` 由 `apps/api` 静态托管,所以 Step 1 的 build 之后**不需要重启服务**。让用户硬刷新知识库页,确认:

1. 「测试」项目:原先 15 条「仅人工确认」现在应大部分转为「引用已失效」,仪表盘多一格
2. 缺口卡的说明不再出现「依据是你填写并确认过」
3. 「眼袋王」项目:主结论应说「引用已失效…重新分析」并有跳转按钮,不再说「改掉答案格式」

- [ ] **Step 4: 记录**

把两端测试数字、Step 2 的查询结果、Step 3 的三条确认写成 PR 描述草稿。

---

## 提交与合并

分支从 `origin/main` 切出,PR 目标 `main`。仓库合并惯例是 **merge commit**(PR #53–#68 全部如此,零 squash)。

工作树有约 106 个另一会话的未提交文件。每次 `git add` 只加任务列出的文件;若某文件同时含对方改动,先 `git diff <file>` 看清归属,必要时用 `git update-index --cacheinfo` 只暂存自己那几处(PR #66 的先例),并验证提交版文件完整可编译。
