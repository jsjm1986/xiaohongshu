# 极简创作频道交互体验优化 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `/quick`(QuickChannelPage + 5 子标签)的视觉与交互对齐已在作用域内的 `.v2` 设计系统,让它像成品:清晰、方便、简易、不冗余。

**Architecture:** 以纯前端为主。`.qc-*` 样式的硬编码值全部替换为 `.v2` CSS 变量;复用 `V2.tsx` 原语(V2Instrument/V2InstrumentCell/V2SecLabel);新增一个轻量进度组件复用后端已有的 `analysis_tasks` 只读轮询。不改标签导航整体架构,不改任何后端契约。

**Tech Stack:** React + TypeScript(apps/web),既有 `.v2` 设计令牌与 `V2.tsx` 原语,既有 `api.intelligence.tasks.list` 只读接口。

## Global Constraints

- 所有 `.qc-*` 规则中的颜色/圆角/阴影/字号,一律使用 `.v2` 令牌,不得新增十六进制字面量(v2 未覆盖的语义需在评审时列出)。可用令牌(精确名,verbatim):
  - 颜色:`--v2-canvas: #f5f6f2`、`--v2-surface: #ffffff`、`--v2-ink: #1e2924`、`--v2-ink-2: #3d4741`、`--v2-muted: #6d7973`、`--v2-muted-2: #8a938d`、`--v2-line: #e3e7e2`、`--v2-line-dark: #d5dbd5`、`--v2-brand: #f05d52`、`--v2-brand-ink: #d94940`、`--v2-brand-soft: #fff0ed`、`--v2-ai: #7257c8`、`--v2-ai-ink: #6148b8`、`--v2-ai-soft: #f0ecff`、`--v2-ok: #258362`、`--v2-ok-soft: #eaf7f1`、`--v2-warn: #9b6b16`、`--v2-error: #b74545`
  - 圆角:`--v2-radius: 10px`、`--v2-radius-sm: 8px`、`--v2-radius-lg: 14px`
  - 阴影:`--v2-shadow-sm: 0 1px 2px rgb(30 41 36 / 5%)`、`--v2-shadow-md: 0 8px 28px rgb(30 41 36 / 9%)`
  - 字号:`--v2-fs-micro: 10px`、`--v2-fs-sm: 12.5px`、`--v2-fs-md: 14px`、`--v2-fs-lg: 16px`、`--v2-fs-h1: 28px`
- 复用现有 `.spin` 动画类(styles.css:97)与 `RefreshCw`(lucide-react)做加载指示,不自造关键帧。
- `.v2` 作用域已由 `AppShell.tsx:64`(`<div className="app-shell v2">`)提供,QuickChannelPage 及全部子标签都在其内,无需改动任何 DOM 祖先。
- 不改后端;不改标签导航的路由/组件架构;不改已修好的 HistoryTab 客户端导出(`quickCandidateToMarkdown`)。
- 每个任务结束跑:`npm run typecheck -w @content-agent/web`(0 error)与 `npm run test -w @content-agent/web`(保持既有通过数,当前基线由实现者首个任务确认)。**注意 cwd 陷阱**:命令必须在 worktree 目录 `/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval` 下执行,`cd content-agent` 会落到主 checkout。
- 浏览器端到端实测因 dev API 原生依赖(`@node-rs/argon2-darwin-arm64`)缺失暂无法登录;各任务以 typecheck + 单测为验证,浏览器核验如实标注留给人工。

---

## 文件结构(改动映射)

| 文件 | 责任 | 改动类型 |
|------|------|---------|
| `apps/web/src/styles.css` | 所有 `.qc-*` **与 `.quick-*`**(结果页/历史展开卡共用)视觉规则:legacy 变量(`--border`/`--muted`/`--coral`)→ v2 令牌;新增 tab pill、进度条、仪表卡、发丝线、语义徽章样式 | 重写多处 + 新增 |
| `apps/web/src/components/QuickResult.tsx` | 结果卡渲染(样式经 `.quick-*` 迁移覆盖,无需改 TSX) | 无 TSX 改动 |
| `apps/web/src/pages/QuickChannelPage.tsx` | 标签导航:序号 pill + 状态点 + 下一步引导条 | 改 `<nav>` 段 + 新增引导条 |
| `apps/web/src/components/quick/ProjectKnowledgeTab.tsx` | 分析状态卡 → V2Instrument;两步上传 → 选中即传;分析进度轮询 | 状态卡 / 上传 / 进度 |
| `apps/web/src/components/quick/QuickTaskProgress.tsx` | 新:复用 `.task-progress` 范式的只读进度组件 | 新建 |
| `apps/web/src/components/quick/TopicTab.tsx` | 选题卡片、换一批引导 v2 化 | className 调整 |
| `apps/web/src/components/quick/ConfigTab.tsx` | 生成期间确定性阶段文案轮播 | 新增进度态 |
| `apps/web/src/components/quick/ResultTab.tsx` | 结果卡 v2 化(经 QuickResult 透传) | 样式为主 |
| `apps/web/src/components/quick/HistoryTab.tsx` | 历史行 v2 发丝线 + 状态语义徽章 | className + 徽章 |
| `apps/web/src/lib/quick-channel-state.ts` | 「步骤完成度」纯函数(供导航状态点) | 新增纯函数 + 单测 |

---

## Task 1:视觉基座 — `.qc-*` legacy 变量迁移到 v2 令牌

**Files:**
- Modify: `apps/web/src/styles.css:2786-2804`
- Test: 无(纯样式;由 typecheck + 单测回归保证不破坏构建)

**Interfaces:**
- Consumes: 既有 `.v2` 令牌(styles.css:1731-1766,见 Global Constraints 清单)
- Produces: 升级后的 `.qc-*` 基础样式,供 Task 2–7 的 className 复用

- [ ] **Step 1:替换 `.qc-tabs` / `.qc-panel` / `.qc-step` / `.qc-hint`**

把 styles.css:2786-2792 整段替换为(仅换变量与字号令牌,布局值不变):

```css
.qc-page .qc-tabs { display: flex; gap: 8px; margin: 20px 0 16px; border-bottom: 1px solid var(--v2-line); }
.qc-tabs button { padding: 10px 16px; border: none; background: none; cursor: pointer; color: var(--v2-muted); border-bottom: 2px solid transparent; font-size: var(--v2-fs-md); }
.qc-tabs button.active { color: var(--v2-brand); border-bottom-color: var(--v2-brand); font-weight: 600; }
.qc-tabs button:disabled { opacity: .4; cursor: not-allowed; }
.qc-panel { max-width: 720px; }
.qc-step { display: flex; flex-direction: column; gap: 16px; }
.qc-hint { color: var(--v2-muted); font-size: var(--v2-fs-sm); }
```

- [ ] **Step 2:替换 `.qc-advanced` / `.qc-project-row` / `.qc-file-list`**

把 styles.css:2793-2798 整段替换为:

```css
.qc-advanced { border-top: 1px solid var(--v2-line); padding-top: 12px; margin-top: 4px; }
.qc-advanced summary { cursor: pointer; color: var(--v2-brand); font-size: var(--v2-fs-md); }
.qc-advanced-grid { display: flex; flex-direction: column; gap: 12px; margin-top: 12px; }
.qc-project-row { display: flex; align-items: center; gap: 8px; }
.qc-file-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: 8px; }
.qc-file-list li { display: flex; align-items: center; justify-content: space-between; border: 1px solid var(--v2-line); border-radius: var(--v2-radius-sm); padding: 8px 12px; background: var(--v2-surface); transition: border-color .15s; }
.qc-file-list li:hover { border-color: var(--v2-line-dark); }
```

- [ ] **Step 3:替换 `.qc-topic` / `.qc-history-*`**

把 styles.css:2799-2804 整段替换为:

```css
.qc-topic-list { display: flex; flex-direction: column; gap: 10px; }
.qc-topic { text-align: left; padding: 14px 16px; border: 1px solid var(--v2-line); border-radius: var(--v2-radius); background: var(--v2-surface); cursor: pointer; font-size: var(--v2-fs-md); color: var(--v2-ink); transition: border-color .15s, box-shadow .15s; }
.qc-topic:hover { border-color: var(--v2-line-dark); box-shadow: var(--v2-shadow-sm); }
.qc-topic.selected { border-color: var(--v2-brand); box-shadow: var(--v2-shadow-sm); }
.qc-history-list { display: flex; flex-direction: column; gap: 10px; }
.qc-history-item { border: 1px solid var(--v2-line); border-radius: var(--v2-radius); padding: 12px 14px; background: var(--v2-surface); }
.qc-history-candidate { border-top: 1px dashed var(--v2-line); margin-top: 10px; padding-top: 10px; }
```

- [ ] **Step 4:替换 `.quick-*`(结果页与历史展开卡共用)**

结果页 `QuickResult.tsx` 与历史展开卡用的是 `.quick-*` 类(**不是 `.qc-*`**),同样是 legacy 变量 + 硬编码。把 styles.css:2769-2783 整段替换为:

```css
.quick-result { display: flex; flex-direction: column; gap: 14px; margin-top: 20px; }
.quick-tabs { display: flex; gap: 8px; }
.quick-tabs button { padding: 8px 14px; border: 1px solid var(--v2-line); border-radius: var(--v2-radius-sm); background: var(--v2-surface); cursor: pointer; display: inline-flex; align-items: center; gap: 6px; color: var(--v2-ink); }
.quick-tabs button.active { border-color: var(--v2-brand); color: var(--v2-brand); font-weight: 600; }
.quick-tab-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--v2-warn); display: inline-block; }
.quick-card { border: 1px solid var(--v2-line); border-radius: var(--v2-radius-lg); padding: 14px 16px; background: var(--v2-surface); }
.quick-card header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 8px; }
.quick-card header h3 { font-size: var(--v2-fs-sm); color: var(--v2-muted); margin: 0; }
.quick-body { white-space: pre-wrap; }
.quick-tag-row { display: flex; flex-wrap: wrap; gap: 6px; }
.quick-tag { background: var(--v2-canvas); padding: 3px 8px; border-radius: var(--v2-radius-sm); font-size: var(--v2-fs-sm); color: var(--v2-ink-2); }
.quick-qa { list-style: none; padding: 0; display: flex; flex-direction: column; gap: 12px; }
.quick-qa small { display: block; color: var(--v2-muted); }
.quick-followup { display: flex; flex-direction: column; padding-left: 12px; border-left: 2px solid var(--v2-line); margin-top: 4px; font-size: var(--v2-fs-sm); }
.quick-result__actions { display: flex; gap: 10px; margin-top: 8px; }
```

- [ ] **Step 5:确认基线并回归**

先记录测试基线(实现者报告实际数字,供后续任务判定):

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: typecheck 0 error;单测全绿(记录通过数为基线,如 90/90)。纯 CSS 改动不应改变任何数字。

- [ ] **Step 6:提交**

```bash
git add apps/web/src/styles.css
git commit -m "style(quick): .qc-*/.quick-* 视觉基座对齐 v2 设计令牌"
```

---

## Task 2:标签导航 — 序号 pill + 状态点 + 下一步引导条

**Files:**
- Create: `apps/web/src/lib/__tests__/quick-channel-state.test.ts`
- Modify: `apps/web/src/lib/quick-channel-state.ts`(新增 `stepStatus` 纯函数)
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(`<nav>` 段 + 引导条)
- Modify: `apps/web/src/styles.css`(新增 `.qc-tabs` pill/状态点、`.qc-guide` 引导条样式)

**Interfaces:**
- Consumes: `QuickTab`、`tabReachable` 的返回结构(`quick-channel-state.ts`);`reachable`、`activeTab`、各 state(QuickChannelPage)
- Produces: `stepStatus(input): Record<QuickTab, 'done'|'current'|'active'|'locked'>` — 供导航渲染序号/状态点/禁用

- [ ] **Step 1:写失败测试 — `stepStatus` 纯函数**

新建 `apps/web/src/lib/__tests__/quick-channel-state.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { stepStatus } from '../quick-channel-state';

test('stepStatus:无项目时仅 project 为 current,其余 locked', () => {
  const s = stepStatus({
    activeTab: 'project', hasProject: false, opportunityCount: 0, hasOpportunity: false, resultCount: 0,
  });
  assert.equal(s.project, 'current');
  assert.equal(s.topic, 'locked');
  assert.equal(s.config, 'locked');
  assert.equal(s.result, 'locked');
  assert.equal(s.history, 'locked');
});

test('stepStatus:已选项目+已有选题,project 为 done,topic 为 current', () => {
  const s = stepStatus({
    activeTab: 'topic', hasProject: true, opportunityCount: 3, hasOpportunity: false, resultCount: 0,
  });
  assert.equal(s.project, 'done');
  assert.equal(s.topic, 'current');
  assert.equal(s.history, 'active');
});

test('stepStatus:当前所在标签恒为 current(即使其前置已完成)', () => {
  const s = stepStatus({
    activeTab: 'config', hasProject: true, opportunityCount: 3, hasOpportunity: true, resultCount: 0,
  });
  assert.equal(s.project, 'done');
  assert.equal(s.topic, 'done');
  assert.equal(s.config, 'current');
  assert.equal(s.result, 'locked');
});

test('stepStatus:有结果后 result 为 done 或 current', () => {
  const s = stepStatus({
    activeTab: 'result', hasProject: true, opportunityCount: 3, hasOpportunity: true, resultCount: 2,
  });
  assert.equal(s.result, 'current');
  assert.equal(s.config, 'done');
});
```

- [ ] **Step 2:运行测试,确认失败**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run test -w @content-agent/web`
Expected: FAIL — `stepStatus` 未导出/未定义。

- [ ] **Step 3:实现 `stepStatus`**

在 `apps/web/src/lib/quick-channel-state.ts` 末尾追加(复用现有 `tabReachable` 与 `TAB_ORDER` 概念;若文件无 `TAB_ORDER` 常量则内联数组):

```ts
export type QuickStepState = 'done' | 'current' | 'active' | 'locked';

export interface StepStatusInput {
  activeTab: QuickTab;
  hasProject: boolean;
  opportunityCount: number;
  hasOpportunity: boolean;
  resultCount: number;
}

export function stepStatus(input: StepStatusInput): Record<QuickTab, QuickStepState> {
  const reachable = tabReachable(input);
  // 每个标签「是否已完成其产出」:完成度沿链条判定
  const completed: Record<QuickTab, boolean> = {
    project: input.hasProject,
    topic: input.opportunityCount > 0,
    config: input.hasOpportunity,
    result: input.resultCount > 0,
    history: false,
  };
  const order: QuickTab[] = ['project', 'topic', 'config', 'result', 'history'];
  const out = {} as Record<QuickTab, QuickStepState>;
  for (const tab of order) {
    if (tab === input.activeTab) out[tab] = 'current';
    else if (!reachable[tab]) out[tab] = 'locked';
    else if (completed[tab]) out[tab] = 'done';
    else out[tab] = 'active';
  }
  return out;
}
```

- [ ] **Step 4:运行测试,确认通过**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run test -w @content-agent/web`
Expected: PASS(新增 4 个用例全过,原有测试不受影响)。

- [ ] **Step 5:导航渲染序号 + 状态点 + 引导条(QuickChannelPage.tsx)**

在 `import` 区加入 `stepStatus`(来自 `../lib/quick-channel-state`)。在 `reachable` 的 `useMemo` 之后新增:

```tsx
const steps = useMemo(
  () => stepStatus({
    activeTab,
    hasProject: Boolean(project),
    opportunityCount: opportunities.length,
    hasOpportunity: Boolean(opportunityId),
    resultCount: results.length,
  }),
  [activeTab, project, opportunities.length, opportunityId, results.length],
);
const SEQ: Record<QuickTab, string> = { project: '1', topic: '2', config: '3', result: '4', history: '5' };
```

把现有 `<nav className="qc-tabs">…</nav>` 整段替换为:

```tsx
<nav className="qc-tabs">
  {TAB_ORDER.map((tab) => (
    <button
      key={tab}
      type="button"
      className={`qc-tab qc-tab--${steps[tab]}${tab === activeTab ? ' active' : ''}`}
      disabled={!reachable[tab]}
      title={reachable[tab] ? '' : TAB_HINT[tab]}
      onClick={() => setActiveTab(tab)}
    >
      <span className="qc-tab__seq">{steps[tab] === 'done' ? '✓' : SEQ[tab]}</span>
      {TAB_LABELS[tab]}
    </button>
  ))}
</nav>
{TAB_HINT[activeTab] && !reachable[nextTab(activeTab)] ? null : null}
```

紧接 `<nav>` 之后、`<div className="qc-panel">` 之前,插入下一步引导条:

```tsx
{activeTab !== 'history' && (
  <div className="qc-guide" role="note">
    {activeTab === 'project' && '下一步:选择或新建项目,上传知识后点「分析知识库」生成选题。'}
    {activeTab === 'topic' && '下一步:挑一个选题进入配置;不满意可「换一批」。'}
    {activeTab === 'config' && '下一步:选内容预设(可留默认),点「生成文案」。'}
    {activeTab === 'result' && '已生成。可复制、重新生成,或「换个选题」再来一篇。'}
  </div>
)}
```

（移除上面示例里那行占位的 `{TAB_HINT[activeTab] …}`,它只是说明位置,不要写入。引导条直接用上面的 `.qc-guide` 块。)

- [ ] **Step 6:新增 pill / 状态点 / 引导条样式(styles.css)**

在 `.qc-tabs button:disabled` 规则之后追加:

```css
.qc-tab { display: inline-flex; align-items: center; gap: 6px; }
.qc-tab__seq {
  display: inline-flex; align-items: center; justify-content: center;
  width: 18px; height: 18px; border-radius: 50%;
  font-size: var(--v2-fs-micro); font-weight: 700; line-height: 1;
  background: var(--v2-line); color: var(--v2-muted);
}
.qc-tab--current .qc-tab__seq { background: var(--v2-brand); color: #fff; }
.qc-tab--done .qc-tab__seq { background: var(--v2-ok); color: #fff; }
.qc-tab--active .qc-tab__seq { background: var(--v2-brand-soft); color: var(--v2-brand-ink); }
.qc-guide {
  max-width: 720px; margin: 0 0 16px;
  padding: 10px 14px; border-radius: var(--v2-radius-sm);
  background: var(--v2-brand-soft); color: var(--v2-brand-ink);
  font-size: var(--v2-fs-sm); line-height: 1.5;
}
```

- [ ] **Step 7:typecheck + 单测回归**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: typecheck 0 error;单测在基线上 +4 通过。

- [ ] **Step 8:提交**

```bash
git add apps/web/src/lib/quick-channel-state.ts apps/web/src/lib/__tests__/quick-channel-state.test.ts apps/web/src/pages/QuickChannelPage.tsx apps/web/src/styles.css
git commit -m "feat(quick): 标签导航加序号/状态点/下一步引导"
```

---

## Task 3:进度组件 `QuickTaskProgress` + 分析阶段进度轮询

**Files:**
- Create: `apps/web/src/components/quick/QuickTaskProgress.tsx`
- Modify: `apps/web/src/components/quick/ProjectKnowledgeTab.tsx`(分析期间轮询 `api.intelligence.tasks.list`,渲染进度)
- Modify: `apps/web/src/styles.css`(新增 `.qc-progress` 样式;复用现有 `.spin`)

**Interfaces:**
- Consumes: `AnalysisTask` 类型(`apps/web/src/types.ts`:`{ id, projectId, kind, status: 'queued'|'running'|'completed'|'failed', attemptCount, error, createdAt, ... }`);`api.intelligence.tasks.list(projectId): Promise<AnalysisTask[]>`;`.spin` 动画类;`RefreshCw`(lucide-react)
- Produces: `QuickTaskProgress({ text, task })` — 供分析(本任务)与后续潜在复用

- [ ] **Step 1:新建 `QuickTaskProgress` 组件**

新建 `apps/web/src/components/quick/QuickTaskProgress.tsx`(移植 `IntelligentSimpleFlow` 的 `TaskProgressRow` 范式,增加 `failed` 态显示 error):

```tsx
import { RefreshCw } from 'lucide-react';
import type { AnalysisTask } from '../../types';

export function QuickTaskProgress({ text, task }: { text: string; task: AnalysisTask | null }) {
  const active = task !== null && (task.status === 'running' || task.status === 'queued');
  const failed = task?.status === 'failed';
  return (
    <div className={`qc-progress${failed ? ' qc-progress--failed' : ''}`} role="status">
      {!failed && <RefreshCw size={15} className="spin" />}
      <span>{failed ? (task?.error || '分析失败,请重试') : text}</span>
      {!failed && <small>{active ? `后台进行中 · 第 ${task.attemptCount} 次尝试` : '后台进行中'}</small>}
      {!failed && <i className="qc-progress__track"><b /></i>}
    </div>
  );
}
```

- [ ] **Step 2:在 ProjectKnowledgeTab 引入进度状态与轮询**

在 `ProjectKnowledgeTab.tsx` 顶部 import 追加:

```tsx
import { QuickTaskProgress } from './QuickTaskProgress';
import type { AnalysisTask } from '../../types';
```

在组件内的 `useState` 区新增:

```tsx
const [analysisTask, setAnalysisTask] = useState<AnalysisTask | null>(null);
const [analyzing, setAnalyzing] = useState(false);
```

新增一个只读的 latestTask 拉取函数(取 `kind==='project'` 且 `createdAt` 最新一条):

```tsx
const refreshAnalysisTask = () => {
  if (!project) return Promise.resolve();
  return api.intelligence.tasks.list(project.id).then((tasks) => {
    const sorted = [...tasks].sort((a, b) => b.createdAt.localeCompare(a.createdAt));
    setAnalysisTask(sorted.find((t) => t.kind === 'project') ?? null);
  }).catch(() => { /* 非致命 */ });
};
```

新增轮询 effect(仅在分析进行中轮询,1800ms 复用原频道节奏):

```tsx
useEffect(() => {
  if (!analyzing) return;
  const timer = window.setInterval(() => { void refreshAnalysisTask(); }, 1800);
  return () => window.clearInterval(timer);
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [analyzing, project]);
```

- [ ] **Step 3:让 `analyze()` 驱动 analyzing 态**

把现有 `analyze` 函数改为在开始/结束翻转 `analyzing`,并在成功后清空任务条(其余逻辑不变):

```tsx
const analyze = async () => {
  if (!project) return;
  setBusy(true);
  setAnalyzing(true);
  void refreshAnalysisTask();
  try {
    const result = await api.intelligence.analyze(project.id, true);
    setIntel(result.intelligence);
    const opps = await api.opportunities.list(project.id);
    setTopicCount(opps.items.length);
    setAnalyzing(false);
    setAnalysisTask(null);
    onAnalyzed(opps.items);
    setBusy(false);
  } catch (e) { setAnalyzing(false); fail(e, '分析知识库失败'); }
};
```

- [ ] **Step 4:在按钮区上方渲染进度条**

在 `.qc-analysis-status` 卡片之后、按钮行(`<div className="qc-project-row">…分析…`)之前插入:

```tsx
{analyzing && (
  <QuickTaskProgress text="三段模型串联运行:蓝图 → 缺口与策略 → 选题。完成前请勿离开或重复触发。" task={analysisTask} />
)}
```

并给两个分析按钮加 `disabled={busy || analyzing}`(避免重复触发)。

- [ ] **Step 5:进度条样式(styles.css)**

在 Task 2 追加的样式之后追加:

```css
.qc-progress { display: flex; align-items: center; gap: 8px; padding: 10px 12px; border-radius: var(--v2-radius-sm); background: var(--v2-ai-soft); color: var(--v2-ai-ink); font-size: var(--v2-fs-sm); }
.qc-progress small { margin-left: auto; color: var(--v2-ai); }
.qc-progress__track { flex: 0 0 64px; height: 3px; border-radius: 2px; background: rgb(114 87 200 / 20%); overflow: hidden; position: relative; }
.qc-progress__track b { position: absolute; inset: 0; width: 40%; background: var(--v2-ai); border-radius: 2px; animation: qc-progress-slide 1.2s ease-in-out infinite; }
@keyframes qc-progress-slide { 0% { left: -40%; } 100% { left: 100%; } }
.qc-progress--failed { background: #fbeaea; color: var(--v2-error); }
```

（`#fbeaea` 是 error-soft;v2 未定义该令牌,评审时如已有等价软色令牌应替换。)

- [ ] **Step 6:typecheck + 单测回归**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: typecheck 0 error;单测保持基线(无新增测试;纯 UI + 轮询)。

- [ ] **Step 7:提交**

```bash
git add apps/web/src/components/quick/QuickTaskProgress.tsx apps/web/src/components/quick/ProjectKnowledgeTab.tsx apps/web/src/styles.css
git commit -m "feat(quick): 分析知识库显示真实后台阶段进度"
```

---

## Task 4:项目页 — V2Instrument 状态卡 + 选中即传 + 发丝线文件行

**Files:**
- Modify: `apps/web/src/components/quick/ProjectKnowledgeTab.tsx`(状态卡 → V2Instrument;`onChange` 选中即传;文件行结构微调)
- Modify: `apps/web/src/styles.css`(`.qc-analysis-status` 卡片如仍保留则删除;新增 `.qc-file-name` 省略)

**Interfaces:**
- Consumes: `V2Instrument`、`V2InstrumentCell`(`apps/web/src/components/V2.tsx`);`Sparkles`/`Building2`/`Lightbulb`/`Upload`/`Trash2`(lucide-react);已有 `intel`、`topicCount`、`files`、`analyzed` 状态
- Produces: 成品级项目页(与 Task 3 的分析进度条协同)

- [ ] **Step 1:import 追加 V2 原语与图标**

在 `ProjectKnowledgeTab.tsx` 顶部 import 区(Task 3 已加入 `QuickTaskProgress`/`AnalysisTask` 的基础上)追加:

```tsx
import { Sparkles, Building2, Lightbulb } from 'lucide-react';
import { V2Instrument, V2InstrumentCell } from '../V2';
```

（`Trash2`、`Upload` 已在既有 import。）

- [ ] **Step 2:分析状态卡替换为 V2Instrument**

把现有 `<div className={`qc-analysis-status${analyzed ? ' analyzed' : ''}`}>…</div>`(:161-170)整段替换为:

```tsx
{analyzed ? (
  <V2Instrument columns={3}>
    <V2InstrumentCell tone="brand" icon={<Sparkles size={14} />} label="实体" value={intel?.entity || '已分析'} />
    <V2InstrumentCell tone="ai" icon={<Building2 size={14} />} label="行业" value={intel?.industry || '已建立内容地图'} />
    <V2InstrumentCell tone="ok" icon={<Lightbulb size={14} />} label="可用选题" value={topicCount} unit="个" />
  </V2Instrument>
) : (
  <V2Instrument columns={2}>
    <V2InstrumentCell tone="brand" icon={<Sparkles size={14} />} label="状态" value="尚未分析" />
    <V2InstrumentCell tone="ai" icon={<Lightbulb size={14} />} label="下一步" value="上传知识 → 分析" note="AI 会建立内容地图并生成选题" />
  </V2Instrument>
)}
```

- [ ] **Step 3:两步上传改为选中即传**

把上传区(:184-194,含 `<Field label="上传知识文件…">`、独立的 `.qc-advanced` 分类折叠、以及独立「上传」按钮)替换为「选中即传 + 分类折叠仍在其下」:

```tsx
<Field label="上传知识文件（.md / .txt，可多选，选中即上传）">
  <input
    type="file"
    accept=".md,.txt"
    multiple
    disabled={busy}
    onChange={(e) => {
      const picked = Array.from(e.target.files ?? []);
      e.target.value = '';
      if (picked.length > 0) void uploadFiles(picked);
    }}
  />
  {busy && pending.length > 0 && <small className="qc-hint">正在上传 {pending.length} 个文件…</small>}
</Field>
<details className="qc-advanced">
  <summary>上传分类（默认：未分类 / 已知事实；改分类需删除后重传）</summary>
  <div className="qc-advanced-grid">
    <Field label="分类"><select value={category} onChange={(e) => setCategory(e.target.value)}>{CATEGORIES.map((c) => <option key={c}>{c}</option>)}</select></Field>
    <Field label="证据类型"><select value={kind} onChange={(e) => setKind(e.target.value)}>{KINDS.map((k) => <option key={k}>{k}</option>)}</select></Field>
  </div>
</details>
```

- [ ] **Step 4:新增 `uploadFiles(picked)`,替换旧 `uploadAll`**

把现有 `uploadAll`(:80-90)替换为接收显式文件数组的 `uploadFiles`(选中即传,`pending` 仅用于「上传中」计数显示):

```tsx
const uploadFiles = async (picked: File[]) => {
  if (!project || picked.length === 0) return;
  setBusy(true);
  setPending(picked);
  try {
    for (const f of picked) await api.knowledge.upload(project.id, f, category, kind);
    const r = await api.knowledge.list(project.id);
    setFiles(r.items);
    setPending([]);
    setBusy(false);
  } catch (e) { setPending([]); fail(e, '上传失败'); }
};
```

（`pending` state 保留其声明,改为「本次上传中」计数;旧 `uploadAll` 全部引用已在 Step 3 移除。）

- [ ] **Step 5:文件行结构 v2 化**

把 `.qc-file-list li` 内部(:174-178)改为左侧名字块 + 右侧 ghost 删除,配合 Task 1 的发丝线样式:

```tsx
{files.map((f) => (
  <li key={f.id}>
    <span className="qc-file-name">{f.name}<small className="qc-hint"> · {f.category ?? '未分类'}</small></span>
    <Button variant="ghost" icon={<Trash2 size={14} />} onClick={() => void removeFile(f.id)}>删除</Button>
  </li>
))}
{files.length === 0 && <li className="qc-hint">还没有知识文件</li>}
```

- [ ] **Step 6:清理 `.qc-analysis-status` 旧样式 + 新增 `.qc-file-name`**

`.qc-analysis-status` 若在 styles.css 中仍有规则(#17 引入),因已被 V2Instrument 取代,删除其规则块(搜索 `.qc-analysis-status`);新增:

```css
.qc-file-name { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
```

- [ ] **Step 7:typecheck + 单测回归**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: typecheck 0 error（`topicCount` 为 number,V2InstrumentCell 的 `value: ReactNode` 接受）;单测保持基线。

- [ ] **Step 8:提交**

```bash
git add apps/web/src/components/quick/ProjectKnowledgeTab.tsx apps/web/src/styles.css
git commit -m "feat(quick): 项目页状态卡改用 V2Instrument,上传改为选中即传"
```

---

## Task 5:选题页 — 选题卡 v2 化 + 换一批引导收敛

**Files:**
- Modify: `apps/web/src/components/quick/TopicTab.tsx`(选中态描述、空态文案微调;结构基本不变,主要吃 Task 1 的 `.qc-topic` v2 样式)
- Modify: `apps/web/src/styles.css`(新增 `.qc-topic__hint` 选中提示样式,可选)

**Interfaces:**
- Consumes: 已有 `opportunities`、`opportunityId`、`pick`、`refresh`、`guidance`;Task 1 升级后的 `.qc-topic` 样式
- Produces: 与整体 v2 语言一致的选题页

- [ ] **Step 1:选题卡加选中态视觉反馈**

`TopicTab.tsx` 的选题按钮已有 `.qc-topic.selected`(Task 1 已加 brand 边框 + 阴影)。在按钮内标题后追加一个选中标记,让「已选」更明确。把选题 map 块替换为:

```tsx
{opportunities.map((o) => (
  <button key={o.id} type="button" className={`qc-topic${o.id === opportunityId ? ' selected' : ''}`} onClick={() => void pick(o.id)}>
    <span>{o.title}</span>
    {o.id === opportunityId && <small className="qc-topic__hint">已选 · 进入配置</small>}
  </button>
))}
{opportunities.length === 0 && <p className="qc-hint">没有可用选题，试试换一批。</p>}
```

- [ ] **Step 2:`.qc-topic` 内部布局 + 选中提示样式(styles.css)**

在 Task 3 追加的进度样式之后追加:

```css
.qc-topic { display: flex; align-items: center; justify-content: space-between; gap: 10px; }
.qc-topic__hint { color: var(--v2-brand-ink); font-size: var(--v2-fs-micro); white-space: nowrap; }
```

（注:Task 1 的 `.qc-topic` 已设 `text-align: left`;此处加 flex 不冲突,标题 `<span>` 自动左对齐,提示靠右。）

- [ ] **Step 3:typecheck + 单测回归**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: typecheck 0 error;单测保持基线。

- [ ] **Step 4:提交**

```bash
git add apps/web/src/components/quick/TopicTab.tsx apps/web/src/styles.css
git commit -m "style(quick): 选题卡 v2 化并明确选中态"
```

---

## Task 6:配置页 — 生成期间确定性阶段文案轮播

**Files:**
- Modify: `apps/web/src/components/quick/ConfigTab.tsx`(生成期间显示阶段文案;复用 `QuickTaskProgress` 的样式壳但传 `task={null}`,文案自轮播)
- Modify: `apps/web/src/styles.css`(无新增;复用 `.qc-progress`)

**Interfaces:**
- Consumes: 已有 `generate`、`busy`;Task 3 的 `.qc-progress` 样式与 `.spin`
- Produces: 生成长耗时期间的诚实进度反馈(非后台任务,故为确定性文案轮播,不伪造 attemptCount)

- [ ] **Step 1:新增本地生成进度状态与文案轮播**

`autoApproveAndGenerate` 是一次同步 await,不产生 `analysis_task`,因此这里用确定性阶段文案轮播(每 ~2.5s 前进一句),如实呈现「进行中」,不伪造后台任务数据。

在 `ConfigTab.tsx` 顶部 import 追加:

```tsx
import { useEffect, useState } from 'react';
import { RefreshCw } from 'lucide-react';
```

在组件内(`patch` 定义之前)新增:

```tsx
const GEN_STAGES = ['解析选题与已确认信息', '组织内容结构', '生成初稿', '质检与合规校验'];
const [stageIdx, setStageIdx] = useState(0);

useEffect(() => {
  if (!busy) { setStageIdx(0); return; }
  const timer = window.setInterval(() => {
    setStageIdx((i) => Math.min(i + 1, GEN_STAGES.length - 1));
  }, 2500);
  return () => window.clearInterval(timer);
}, [busy]);
```

- [ ] **Step 2:生成按钮上方渲染进度**

把末尾的 `<Button loading={busy} disabled={!presetId} onClick={() => void generate()}>生成文案</Button>` 替换为进度条 + 按钮:

```tsx
{busy && (
  <div className="qc-progress" role="status">
    <RefreshCw size={15} className="spin" />
    <span>正在生成:{GEN_STAGES[stageIdx]}…</span>
    <small>请勿离开或重复点击</small>
    <i className="qc-progress__track"><b /></i>
  </div>
)}
<Button loading={busy} disabled={!presetId || busy} onClick={() => void generate()}>生成文案</Button>
```

- [ ] **Step 3:typecheck + 单测回归**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: typecheck 0 error（注意 `useEffect`/`useState` 需从 react 正确导入,ConfigTab 原本无 hooks import）;单测保持基线。

- [ ] **Step 4:提交**

```bash
git add apps/web/src/components/quick/ConfigTab.tsx
git commit -m "feat(quick): 生成期间显示阶段进度提示"
```

---

## Task 7:历史页 — 发丝线行 + 状态语义徽章

**Files:**
- Modify: `apps/web/src/components/quick/HistoryTab.tsx`(状态改语义徽章;行结构吃 Task 1 发丝线样式)
- Modify: `apps/web/src/styles.css`(新增 `.qc-badge` 语义徽章样式)

**Interfaces:**
- Consumes: 已有 `history`、`expanded`、`copy`、`exportMarkdown`;`GenerationJob.status`;Task 1 的 `.qc-history-*` v2 样式
- Produces: 与 v2 语言一致的历史页(不动已修好的客户端导出逻辑)

- [ ] **Step 1:新增状态徽章映射 + 渲染**

在 `HistoryTab.tsx` 组件函数体上方(import 之后)新增语义映射:

```tsx
const STATUS_LABEL: Record<string, { text: string; tone: 'ok' | 'warn' | 'error' | 'muted' }> = {
  ready: { text: '已完成', tone: 'ok' },
  completed: { text: '已完成', tone: 'ok' },
  draft: { text: '草稿', tone: 'muted' },
  pending: { text: '进行中', tone: 'warn' },
  failed: { text: '失败', tone: 'error' },
};
```

把历史行头部(`<div className="qc-project-row">…{job.status}…</div>`,:55-59)替换为:

```tsx
<div className="qc-project-row">
  <strong>{job.topic || '未命名选题'}</strong>
  {(() => { const s = STATUS_LABEL[job.status] ?? { text: job.status, tone: 'muted' as const }; return <span className={`qc-badge qc-badge--${s.tone}`}>{s.text}</span>; })()}
  <small className="qc-hint">{job.createdAt ? new Date(job.createdAt).toLocaleString() : ''}</small>
  <Button variant="ghost" onClick={() => setExpanded(open ? null : job.id)}>{open ? '收起' : '查看'}</Button>
</div>
```

- [ ] **Step 2:徽章样式(styles.css)**

在 Task 5 追加的样式之后追加:

```css
.qc-badge { display: inline-flex; align-items: center; padding: 1px 8px; border-radius: 999px; font-size: var(--v2-fs-micro); font-weight: 600; }
.qc-badge--ok { background: var(--v2-ok-soft); color: var(--v2-ok); }
.qc-badge--warn { background: #fff3e0; color: var(--v2-warn); }
.qc-badge--error { background: #fbeaea; color: var(--v2-error); }
.qc-badge--muted { background: var(--v2-line); color: var(--v2-muted); }
```

（`#fff3e0` / `#fbeaea` 为 warn/error 软底;v2 未定义对应软色令牌,评审时若有等价令牌应替换。)

- [ ] **Step 3:typecheck + 单测回归**

Run: `cd "/Users/a1234/Desktop/开发项目/小红书创作/文案/content-agent/.claude/worktrees/register-approval" && npm run typecheck -w @content-agent/web && npm run test -w @content-agent/web`
Expected: typecheck 0 error;单测保持基线。

- [ ] **Step 4:提交**

```bash
git add apps/web/src/components/quick/HistoryTab.tsx apps/web/src/styles.css
git commit -m "style(quick): 历史页发丝线行 + 状态语义徽章"
```

---

## 完成后

全部 7 个任务完成后,按 `superpowers:finishing-a-development-branch` 收尾:核实 `npm run test -w @content-agent/web` 全绿 → 呈现 4 选项(合并/PR/保留/丢弃)。建议走「推送 + 新 PR」,与 PR #17 的模式一致。

**已知验证缺口(如实标注)**:浏览器端到端实测因 dev API 原生依赖(`@node-rs/argon2-darwin-arm64`)缺失暂无法登录,各任务以 typecheck + 单测为准;真实渲染(v2 仪表卡、进度条动画、中文文件名、选中即传)需人工在本地跑起 dev 后核验。
