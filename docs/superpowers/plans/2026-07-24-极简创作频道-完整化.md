# 极简创作频道 · 完整化(单页多标签) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `/quick` 从线性向导升级为单页 5 标签的完整频道,让简单用户在这一页完成项目全生命周期(建/管项目、维护知识、选题、配置、生成、看历史),不切换其他频道。

**Architecture:** 新建一个壳组件 `QuickChannelPage`(顶部 5 标签 + 集中状态 + 生成编排),5 个纯展示子标签组件接 props/回调,标签可达性与级联失效抽成 `quick-channel-state.ts` 纯函数。复用上一版已测的 `autoApproveAndGenerate` / `quickCandidateFields` / `QuickResult`,以及 `resolveSimpleGenerationSettings` / `buildSimpleGenerateInput`。零后端改动。

**Tech Stack:** React + TypeScript(apps/web),Vite,react-router,lucide-react 图标;测试用 `node --import tsx --test`(无 jsdom,纯逻辑测试)。

## Global Constraints

- **零后端改动。** 所有能力走现有 API 端点(`apps/api` 一行不改)。
- **不改其他频道的业务逻辑与前端。** IntelligentSimpleFlow / GeneratorPage / KnowledgePage / ProjectsPage / simple-generation 一律不动。
- **现有文件仅授权改动:** `apps/web/src/App.tsx`(`/quick` 路由 element 换成 `QuickChannelPage` + import);删除 `apps/web/src/pages/QuickCreatePage.tsx`;`apps/web/src/styles.css` 末尾追加 `.qc-*` 样式(不改现有规则)。`AppShell.tsx` 导航项不变(仍「极简创作」→`/quick`)。
- **只显示可用文案。** 结果沿用 `quickCandidateFields` 白名单投影,绝不渲染审计字段(score/diagnostics/sources/validation 明细等)。
- **复用已测资产,禁止重写。** `autoApproveAndGenerate`、`quickCandidateFields`、`quickCandidateToMarkdown`、`QuickResult`、`resolveSimpleGenerationSettings`、`buildSimpleGenerateInput`。
- **历史无删除、知识无就地改分类**(现有 API 无对应端点):历史标签只查看/复制/导出;知识改分类 = 删除后重传。
- **测试遵循现有运行器约束。** 只写纯逻辑测试;新测试文件追加进 `apps/web/package.json` 的 `test` 脚本文件列表。
- **web 测试运行命令:** `cd apps/web && npm test`;类型检查 `cd apps/web && npm run typecheck`;构建 `cd apps/web && npm run build`。

## 壳状态契约(所有标签共享,Task 3 建立)

`QuickChannelPage` 持有全部跨标签状态,子标签只接 props + 回调:

```ts
type QuickTab = 'project' | 'topic' | 'config' | 'result' | 'history';
project: Project | null;              // 当前项目
projects: Project[];                  // useProjects().projects
opportunities: TopicOpportunity[];    // 分析产出选题
opportunityId: string;                // 选中的选题 id
presets: ContentPreset[];             // 预设列表
presetId: string | undefined;         // 选中的预设
overrides: SimpleSettingOverrides;    // ③高级配置
results: QuickCandidateView[];        // 生成结果(白名单投影)
history: GenerationJob[];             // ⑤历史
activeTab: QuickTab;
busy: boolean;
```

真实类型参考(勿猜,均来自 `apps/web/src/types.ts` 与库文件):
- `Project`: `{ id: string; name: string; cities?: string[]; doctors?: Array<{name:string;points?:string[]}>; generationDefaults?: {...}; knowledgeCount?: number; ... }`
- `SimpleSettingOverrides`: `{ audienceStage?: string; entryPoint?: string; city?: string; doctor?: string; mustInclude?: string; forbidden?: string; commentRichness?: 'restrained'|'balanced'|'dense' }`(from `../lib/simple-generation`)
- `GenerationJob`: `{ id; projectId; topic; mode; status; candidates?: Candidate[]; createdAt?; completedAt?; error?; ... }`
- `QuickCandidateView`: from `../lib/quick-generation`(上一版)
- `TopicOpportunity`: `{ id; title; whyValuable; gapIds: string[]; strategyId?; compatibleStrategyIds: string[]; ... }`
- `ContentPreset`: `{ id; name; isDefault?; values: {...} }`

真实 API/context 签名(勿猜):
- `useProjects()` → `{ projects: Project[]; addProject({name}): Promise<Project>; updateProject(id, Partial<Project>): Promise<Project>; removeProject(id): Promise<void>; refresh(): Promise<void> }`(from `../components/ProjectContext`)
- `api.knowledge.list(projectId)` → `Promise<{items: KnowledgeFile[]; total}>`;`upload(projectId, file, category, kind)`;`remove(id)`
- `api.intelligence.analyze(projectId, force=false)`;`api.opportunities.list(projectId)` → `{items, total}`;`refresh(projectId, userGuidance?)`
- `api.presets.list(projectId)` → `{items, total}`
- `api.generations.list(projectId?)` → `Promise<{items: GenerationJob[]; total}>`;`get(id)`;`exportUrl(id, candidateId, 'markdown'|'json'|'docx'|'pdf')` → string
- `autoApproveAndGenerate({project, opportunityId, presetId?, overrides?, pollIntervalMs?, maxPolls?, deps?})` → `Promise<GenerationJob>`(Task 2 加 `overrides`)

UI 组件(from `../components/Ui`):`Button({variant,loading,icon,disabled,...})`、`Field({label,hint,required,children})`、`Modal({open,title,description,children,onClose,footer,size})`、`Skeleton({lines})`、`useToast()` → `{push(msg, kind?)}`。`V2Hero({index?,status,title,description?})` from `../components/V2`。

---

### Task 1: `quick-channel-state.ts` — 标签可达性 + 级联失效(纯函数)

**Files:**
- Create: `apps/web/src/lib/quick-channel-state.ts`
- Modify: `apps/web/package.json`(test 脚本追加新测试文件)
- Test: `apps/web/test/quick-channel-state.test.ts`

**Interfaces:**
- Produces:
  ```ts
  export type QuickTab = 'project' | 'topic' | 'config' | 'result' | 'history';
  export interface QuickReachabilityInput {
    hasProject: boolean;
    opportunityCount: number;
    hasOpportunity: boolean;
    resultCount: number;
  }
  export function tabReachable(input: QuickReachabilityInput): Record<QuickTab, boolean>;
  export interface QuickDownstream { opportunities: unknown[]; opportunityId: string; results: unknown[]; }
  export function clearDownstreamOfProject(): { opportunities: never[]; opportunityId: ''; results: never[] };
  export function clearResults(): { results: never[] };
  ```

- [ ] **Step 1: 写失败测试**

创建 `apps/web/test/quick-channel-state.test.ts`:

```ts
import assert from 'node:assert/strict';
import { test } from 'node:test';
import {
  tabReachable,
  clearDownstreamOfProject,
  clearResults,
} from '../src/lib/quick-channel-state.js';

test('project and history tabs reachable whenever a project exists', () => {
  const r = tabReachable({ hasProject: true, opportunityCount: 0, hasOpportunity: false, resultCount: 0 });
  assert.equal(r.project, true);
  assert.equal(r.history, true);
  assert.equal(r.topic, false);
  assert.equal(r.config, false);
  assert.equal(r.result, false);
});

test('project tab reachable even with no project; history needs a project', () => {
  const r = tabReachable({ hasProject: false, opportunityCount: 0, hasOpportunity: false, resultCount: 0 });
  assert.equal(r.project, true);
  assert.equal(r.history, false);
});

test('topic reachable once opportunities exist, config once one is picked', () => {
  const r = tabReachable({ hasProject: true, opportunityCount: 3, hasOpportunity: true, resultCount: 0 });
  assert.equal(r.topic, true);
  assert.equal(r.config, true);
  assert.equal(r.result, false);
});

test('result reachable once results exist', () => {
  const r = tabReachable({ hasProject: true, opportunityCount: 3, hasOpportunity: true, resultCount: 3 });
  assert.equal(r.result, true);
});

test('changing project clears opportunities, selection, and results', () => {
  const cleared = clearDownstreamOfProject();
  assert.deepEqual(cleared.opportunities, []);
  assert.equal(cleared.opportunityId, '');
  assert.deepEqual(cleared.results, []);
});

test('changing topic clears only results', () => {
  const cleared = clearResults();
  assert.deepEqual(cleared.results, []);
  assert.equal((cleared as Record<string, unknown>).opportunityId, undefined);
});
```

- [ ] **Step 2: 追加到 test 脚本并确认失败**

编辑 `apps/web/package.json` 的 `"test"` 脚本,在文件列表末尾追加 ` test/quick-channel-state.test.ts`。当前脚本以 `test/quick-generation.test.ts` 结尾;改成其后再多一个 `test/quick-channel-state.test.ts`。

Run: `cd apps/web && npm test`
Expected: FAIL — `Cannot find module '../src/lib/quick-channel-state.js'`。

- [ ] **Step 3: 实现纯函数**

创建 `apps/web/src/lib/quick-channel-state.ts`:

```ts
export type QuickTab = 'project' | 'topic' | 'config' | 'result' | 'history';

export interface QuickReachabilityInput {
  hasProject: boolean;
  opportunityCount: number;
  hasOpportunity: boolean;
  resultCount: number;
}

export function tabReachable(input: QuickReachabilityInput): Record<QuickTab, boolean> {
  return {
    project: true,
    topic: input.opportunityCount > 0,
    config: input.hasOpportunity,
    result: input.resultCount > 0,
    history: input.hasProject,
  };
}

export function clearDownstreamOfProject() {
  return { opportunities: [] as never[], opportunityId: '' as const, results: [] as never[] };
}

export function clearResults() {
  return { results: [] as never[] };
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd apps/web && npm test`
Expected: 6 个新测试 PASS,其余既有测试保持通过。

- [ ] **Step 5: typecheck**

Run: `cd apps/web && npm run typecheck`
Expected: 0 error。

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/lib/quick-channel-state.ts apps/web/test/quick-channel-state.test.ts apps/web/package.json
git commit -m "feat(web): quick-channel tab reachability + cascade pure functions"
```

---

### Task 2: `autoApproveAndGenerate` 增量加可选 `overrides` 参数

**Files:**
- Modify: `apps/web/src/lib/quick-generation.ts`(给 `autoApproveAndGenerate` 加 `overrides` 参数并透传)
- Test: `apps/web/test/quick-generation.test.ts`(追加一条透传断言)

**Interfaces:**
- Consumes: `SimpleSettingOverrides` from `./simple-generation`;现有 `buildSimpleGenerateInput`(已接受 `overrides`)。
- Produces(修改后签名):
  ```ts
  export async function autoApproveAndGenerate(args: {
    project: Project;
    opportunityId: string;
    presetId?: string;
    overrides?: SimpleSettingOverrides;
    pollIntervalMs?: number;
    maxPolls?: number;
    deps?: AutoDeps;
  }): Promise<GenerationJob>;
  ```

**背景(已核实):** `buildSimpleGenerateInput` 已接受 `overrides` 并在内部合并进 `overrides` 字段;`resolveSimpleGenerationSettings({overrides,...})` 也已用 `overrides` 决定 user 级设置。当前 `autoApproveAndGenerate` 调用两者时未传 `overrides`。本任务把新参数透传给这两处。

- [ ] **Step 1: 写失败测试**

在 `apps/web/test/quick-generation.test.ts` 末尾追加(复用文件顶部已有的 `makeDeps()` 辅助 —— 它已在上一版定义;新测试用它记录 `buildInput` 收到的参数)。新测试自带一个捕获 deps:

```ts
test('autoApproveAndGenerate forwards overrides into buildInput and resolveSettings', async () => {
  const seen: { resolveOverrides?: unknown; buildOverrides?: unknown } = {};
  const base = makeDeps().deps;
  const deps = {
    ...base,
    resolveSettings: (arg: { overrides?: unknown }) => { seen.resolveOverrides = arg.overrides; return {}; },
    buildInput: (arg: { overrides?: unknown }) => {
      seen.buildOverrides = arg.overrides;
      return { projectId: 'proj1', mode: 'simple', opportunityId: 'o1', topic: 't', goal: 'w', audienceStage: 'hesitation', entryPoint: 'x' };
    },
  };
  const project = { id: 'proj1', name: 'p' } as any;
  const overrides = { city: '上海', commentRichness: 'dense' as const };
  await autoApproveAndGenerate({ project, opportunityId: 'o1', overrides, pollIntervalMs: 1, maxPolls: 5, deps: deps as any });
  assert.deepEqual(seen.resolveOverrides, overrides);
  assert.deepEqual(seen.buildOverrides, overrides);
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd apps/web && npm test`
Expected: FAIL — 当前 `resolveSettings`/`buildInput` 收到的 `overrides` 为 `undefined`(断言不等)。

- [ ] **Step 3: 实现透传**

编辑 `apps/web/src/lib/quick-generation.ts`。在文件顶部 import 补上类型:

```ts
import type { SimpleSettingOverrides } from './simple-generation';
```

在 `autoApproveAndGenerate` 的参数解构里加 `overrides`,并把它传给 `resolveSettings` 和 `buildInput`。找到现有的这两处调用:

```ts
  const settings = d.resolveSettings({ project, preset, opportunity });
  const input: GenerateInput = d.buildInput({
    projectId,
    opportunity,
    settings,
    imageAssetIds: [],
    lockedGapIds: [],
    presetId,
    localFieldsEnabled: false,
    randomizationDimensions: [],
  });
```

改成:

```ts
  const settings = d.resolveSettings({ project, preset, opportunity, overrides });
  const input: GenerateInput = d.buildInput({
    projectId,
    opportunity,
    settings,
    imageAssetIds: [],
    lockedGapIds: [],
    presetId,
    localFieldsEnabled: false,
    overrides,
    randomizationDimensions: [],
  });
```

并把函数签名的参数解构行改为包含 `overrides`(在 `presetId` 之后):

```ts
  const { project, opportunityId, presetId, overrides } = args;
```

同时在 `args` 的类型注解里加 `overrides?: SimpleSettingOverrides;`。

- [ ] **Step 4: 运行确认通过**

Run: `cd apps/web && npm test`
Expected: 新测试 PASS;上一版所有测试(含审批顺序、轮询、失败、intelligence-before-opportunity)保持 PASS。

- [ ] **Step 5: typecheck**

Run: `cd apps/web && npm run typecheck`
Expected: 0 error。

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/lib/quick-generation.ts apps/web/test/quick-generation.test.ts
git commit -m "feat(web): thread optional overrides through autoApproveAndGenerate"
```

---

### Task 3: `QuickChannelPage` 壳 + 标签导航 + 路由替换

**Files:**
- Create: `apps/web/src/pages/QuickChannelPage.tsx`
- Modify: `apps/web/src/App.tsx`(import 换成 `QuickChannelPage`,`/quick` 路由 element 换掉)
- Delete: `apps/web/src/pages/QuickCreatePage.tsx`
- Modify: `apps/web/src/styles.css`(末尾追加 `.qc-*` 样式)

**Interfaces:**
- Consumes: `tabReachable`、`QuickTab`、`clearDownstreamOfProject`、`clearResults` from `../lib/quick-channel-state`;`useProjects` from `../components/ProjectContext`;`useToast`、`Button` from `../components/Ui`;`V2Hero` from `../components/V2`;子标签组件(Task 4-8,先用占位)。
- Produces:
  ```ts
  export function QuickChannelPage(): JSX.Element;
  ```
  壳持有并向子标签下发的回调(Task 4-8 消费,签名固定):
  ```ts
  interface QuickTabProps {
    project: Project | null;
    projects: Project[];
    opportunities: TopicOpportunity[];
    opportunityId: string;
    presets: ContentPreset[];
    presetId: string | undefined;
    overrides: SimpleSettingOverrides;
    results: QuickCandidateView[];
    history: GenerationJob[];
    busy: boolean;
    setBusy: (b: boolean) => void;
    fail: (e: unknown, fallback: string) => void;
    onProjectChosen: (p: Project) => void;         // 选/建项目;内部级联清下游
    onAnalyzed: (opps: TopicOpportunity[]) => void; // 分析完成;点亮并切到 topic
    onPickTopic: (id: string, presets: ContentPreset[]) => void; // 选题;切到 config
    setOverrides: (o: SimpleSettingOverrides) => void;
    setPresetId: (id: string | undefined) => void;
    onGenerated: (results: QuickCandidateView[]) => void; // 生成完;切到 result
    goTo: (tab: QuickTab) => void;                  // 标签切换
    setHistory: (h: GenerationJob[]) => void;
  }
  ```

**说明:** 本任务只搭壳 + 导航 + 状态 + 路由替换。子标签先用最小占位组件(每个渲染一个 `<div>标签名</div>`),在 Task 4-8 逐个实现真身。这样壳可独立编译、路由可点、可人工验证标签切换与可达性,不被子标签阻塞。

- [ ] **Step 1: 实现壳组件(子标签占位)**

创建 `apps/web/src/pages/QuickChannelPage.tsx`:

```tsx
import { useMemo, useState } from 'react';
import { useProjects } from '../components/ProjectContext';
import { Button, useToast } from '../components/Ui';
import { V2Hero } from '../components/V2';
import { api } from '../lib/api';
import { quickCandidateFields, type QuickCandidateView } from '../lib/quick-generation';
import { tabReachable, clearDownstreamOfProject, clearResults, type QuickTab } from '../lib/quick-channel-state';
import type { ContentPreset, GenerationJob, Project, TopicOpportunity } from '../types';
import type { SimpleSettingOverrides } from '../lib/simple-generation';

const TAB_LABELS: Record<QuickTab, string> = {
  project: '项目 & 知识',
  topic: '选题',
  config: '配置',
  result: '结果',
  history: '历史',
};

const TAB_ORDER: QuickTab[] = ['project', 'topic', 'config', 'result', 'history'];

const TAB_HINT: Record<QuickTab, string> = {
  project: '',
  topic: '先分析知识库生成选题',
  config: '先选择一个选题',
  result: '先生成文案',
  history: '先选择或新建项目',
};

export function QuickChannelPage() {
  const { projects } = useProjects();
  const toast = useToast();
  const [project, setProject] = useState<Project | null>(null);
  const [opportunities, setOpportunities] = useState<TopicOpportunity[]>([]);
  const [opportunityId, setOpportunityId] = useState('');
  const [presets, setPresets] = useState<ContentPreset[]>([]);
  const [presetId, setPresetId] = useState<string | undefined>(undefined);
  const [overrides, setOverrides] = useState<SimpleSettingOverrides>({});
  const [results, setResults] = useState<QuickCandidateView[]>([]);
  const [history, setHistory] = useState<GenerationJob[]>([]);
  const [activeTab, setActiveTab] = useState<QuickTab>('project');
  const [busy, setBusy] = useState(false);

  const reachable = useMemo(
    () => tabReachable({
      hasProject: Boolean(project),
      opportunityCount: opportunities.length,
      hasOpportunity: Boolean(opportunityId),
      resultCount: results.length,
    }),
    [project, opportunities.length, opportunityId, results.length],
  );

  const fail = (e: unknown, fallback: string) => {
    toast.push(e instanceof Error ? e.message : fallback, 'error');
    setBusy(false);
  };

  const goTo = (tab: QuickTab) => { if (reachable[tab]) setActiveTab(tab); };

  const onProjectChosen = (p: Project) => {
    setProject(p);
    const cleared = clearDownstreamOfProject();
    setOpportunities(cleared.opportunities);
    setOpportunityId(cleared.opportunityId);
    setResults(cleared.results);
    setPresetId(undefined);
    setOverrides({});
  };

  const onAnalyzed = (opps: TopicOpportunity[]) => {
    setOpportunities(opps);
    setActiveTab('topic');
  };

  const onPickTopic = (id: string, loadedPresets: ContentPreset[]) => {
    setOpportunityId(id);
    setResults(clearResults().results);
    setPresets(loadedPresets);
    setPresetId(loadedPresets.find((p) => p.isDefault)?.id ?? loadedPresets[0]?.id);
    setActiveTab('config');
  };

  const onGenerated = (r: QuickCandidateView[]) => {
    setResults(r);
    setActiveTab('result');
  };

  const tabProps = {
    project, projects, opportunities, opportunityId, presets, presetId,
    overrides, results, history, busy, setBusy, fail,
    onProjectChosen, onAnalyzed, onPickTopic, setOverrides, setPresetId,
    onGenerated, goTo, setHistory,
  };

  return (
    <div className="page qc-page">
      <V2Hero index="Q" status={<>极简创作 · 完整频道</>} title="极简创作" description="一个页面完成建项目、传资料、选题、配置、生成、看历史。" />

      <nav className="qc-tabs">
        {TAB_ORDER.map((tab) => (
          <button
            key={tab}
            type="button"
            className={tab === activeTab ? 'active' : ''}
            disabled={!reachable[tab]}
            title={reachable[tab] ? '' : TAB_HINT[tab]}
            onClick={() => setActiveTab(tab)}
          >
            {TAB_LABELS[tab]}
          </button>
        ))}
      </nav>

      <div className="qc-panel">
        {activeTab === 'project' && <div>[项目 &amp; 知识]</div>}
        {activeTab === 'topic' && <div>[选题]</div>}
        {activeTab === 'config' && <div>[配置]</div>}
        {activeTab === 'result' && <div>[结果]</div>}
        {activeTab === 'history' && <div>[历史]</div>}
      </div>
    </div>
  );
}
```

> 注:占位块引用了 `tabProps`/`Button`/`api`/`quickCandidateFields` 的 import 但暂未全用到 —— 为避免 typecheck 报未使用,本步先删掉 Step 1 里尚未用到的 import(`Button`、`api`、`quickCandidateFields`),在 Task 4-8 接入子标签时再按需加回。`tabProps` 变量同理:占位阶段先不声明它,等 Task 4 第一个子标签接入时再加。保持每一步都能通过 typecheck。

- [ ] **Step 2: 替换路由**

编辑 `apps/web/src/App.tsx`:把 `import { QuickCreatePage } from './pages/QuickCreatePage';` 改成 `import { QuickChannelPage } from './pages/QuickChannelPage';`;把 `<Route path="quick" element={<QuickCreatePage />} />` 改成 `<Route path="quick" element={<QuickChannelPage />} />`。

- [ ] **Step 3: 删除旧向导页**

```bash
git rm apps/web/src/pages/QuickCreatePage.tsx
```

- [ ] **Step 4: 追加 CSS(styles.css 末尾)**

```css
/* 极简创作 · 完整频道 */
.qc-page .qc-tabs { display: flex; gap: 8px; margin: 20px 0 16px; border-bottom: 1px solid var(--border); }
.qc-tabs button { padding: 10px 16px; border: none; background: none; cursor: pointer; color: var(--muted); border-bottom: 2px solid transparent; font-size: 15px; }
.qc-tabs button.active { color: var(--coral); border-bottom-color: var(--coral); font-weight: 600; }
.qc-tabs button:disabled { opacity: .4; cursor: not-allowed; }
.qc-panel { max-width: 720px; }
.qc-step { display: flex; flex-direction: column; gap: 16px; }
.qc-hint { color: var(--muted); font-size: 13px; }
.qc-advanced { border-top: 1px solid var(--border); padding-top: 12px; margin-top: 4px; }
.qc-advanced summary { cursor: pointer; color: var(--coral); font-size: 14px; }
.qc-advanced-grid { display: flex; flex-direction: column; gap: 12px; margin-top: 12px; }
.qc-project-row { display: flex; align-items: center; gap: 8px; }
.qc-file-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: 8px; }
.qc-file-list li { display: flex; align-items: center; justify-content: space-between; border: 1px solid var(--border); border-radius: 8px; padding: 8px 12px; }
.qc-topic-list { display: flex; flex-direction: column; gap: 10px; }
.qc-topic { text-align: left; padding: 14px 16px; border: 1px solid var(--border); border-radius: 10px; background: #fff; cursor: pointer; font-size: 15px; }
.qc-topic.selected { border-color: var(--coral); }
.qc-history-list { display: flex; flex-direction: column; gap: 10px; }
.qc-history-item { border: 1px solid var(--border); border-radius: 10px; padding: 12px 14px; }
```

- [ ] **Step 5: typecheck + build**

Run: `cd apps/web && npm run typecheck && npm run build`
Expected: 0 error;build 成功。

- [ ] **Step 6: 全量测试回归**

Run: `cd apps/web && npm test`
Expected: 全绿(Task 1-2 的新测试 + 上一版测试)。

- [ ] **Step 7: Commit**

```bash
git add apps/web/src/pages/QuickChannelPage.tsx apps/web/src/App.tsx apps/web/src/styles.css
git commit -m "feat(web): quick-channel shell with tab nav, replace old wizard route"
```

---

### Task 4: `ProjectKnowledgeTab` — 项目 CRUD + 知识维护 + 分析

**Files:**
- Create: `apps/web/src/components/quick/ProjectKnowledgeTab.tsx`
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(接入子标签,替换 project 占位)

**Interfaces:**
- Consumes: `QuickTabProps`(见 Task 3);`useProjects`;`api.knowledge.list/upload/remove`;`api.intelligence.analyze`;`api.opportunities.list`;`Button`、`Field`、`Modal` from `../components/Ui`。
- Produces:
  ```ts
  export function ProjectKnowledgeTab(props: QuickTabProps): JSX.Element;
  ```

**说明:** 无 DOM 测试环境,本任务不写单测(壳的可达性纯函数已在 Task 1 覆盖)。正确性靠 typecheck + 人工冒烟。知识分类/证据类型的默认值用 `'未分类'` / `'已知事实'`(与 KnowledgePage 一致,已核实)。分类可选项:`['未分类','知识地图','项目与服务','用户与场景','案例样本','方法论','约束']`;证据类型:`['已知事实','案例样本','用户观点','方法论推理','猜想','信息不足','禁止表达']`。

- [ ] **Step 1: 实现 ProjectKnowledgeTab**

创建 `apps/web/src/components/quick/ProjectKnowledgeTab.tsx`:

```tsx
import { useEffect, useState } from 'react';
import { Trash2, Upload } from 'lucide-react';
import { useProjects } from '../ProjectContext';
import { Button, Field, Modal } from '../Ui';
import { api } from '../../lib/api';
import type { KnowledgeFile, Project, TopicOpportunity } from '../../types';

const CATEGORIES = ['未分类', '知识地图', '项目与服务', '用户与场景', '案例样本', '方法论', '约束'];
const KINDS = ['已知事实', '案例样本', '用户观点', '方法论推理', '猜想', '信息不足', '禁止表达'];

interface Props {
  project: Project | null;
  projects: Project[];
  busy: boolean;
  setBusy: (b: boolean) => void;
  fail: (e: unknown, fallback: string) => void;
  onProjectChosen: (p: Project) => void;
  onAnalyzed: (opps: TopicOpportunity[]) => void;
}

export function ProjectKnowledgeTab({ project, projects, busy, setBusy, fail, onProjectChosen, onAnalyzed }: Props) {
  const { addProject, updateProject, removeProject, refresh } = useProjects();
  const [newName, setNewName] = useState('');
  const [renaming, setRenaming] = useState('');
  const [files, setFiles] = useState<KnowledgeFile[]>([]);
  const [pending, setPending] = useState<File[]>([]);
  const [category, setCategory] = useState('未分类');
  const [kind, setKind] = useState('已知事实');
  const [confirmDelete, setConfirmDelete] = useState(false);

  useEffect(() => {
    if (!project) { setFiles([]); return; }
    api.knowledge.list(project.id).then((r) => setFiles(r.items)).catch(() => setFiles([]));
    setRenaming(project.name);
  }, [project]);

  const createProject = async () => {
    if (!newName.trim()) return;
    setBusy(true);
    try {
      const created = await addProject({ name: newName.trim() });
      setNewName('');
      onProjectChosen(created);
      setBusy(false);
    } catch (e) { fail(e, '创建项目失败'); }
  };

  const rename = async () => {
    if (!project || !renaming.trim() || renaming.trim() === project.name) return;
    setBusy(true);
    try {
      const updated = await updateProject(project.id, { name: renaming.trim() });
      onProjectChosen(updated);
      setBusy(false);
    } catch (e) { fail(e, '重命名失败'); }
  };

  const doDelete = async () => {
    if (!project) return;
    setBusy(true);
    try {
      await removeProject(project.id);
      await refresh();
      setConfirmDelete(false);
      setBusy(false);
      window.location.reload();
    } catch (e) { fail(e, '删除项目失败'); }
  };

  const uploadAll = async () => {
    if (!project || pending.length === 0) return;
    setBusy(true);
    try {
      for (const f of pending) await api.knowledge.upload(project.id, f, category, kind);
      const r = await api.knowledge.list(project.id);
      setFiles(r.items);
      setPending([]);
      setBusy(false);
    } catch (e) { fail(e, '上传失败'); }
  };

  const removeFile = async (id: string) => {
    if (!project) return;
    setBusy(true);
    try {
      await api.knowledge.remove(id);
      setFiles((cur) => cur.filter((f) => f.id !== id));
      setBusy(false);
    } catch (e) { fail(e, '删除文件失败'); }
  };

  const analyze = async () => {
    if (!project) return;
    setBusy(true);
    try {
      await api.intelligence.analyze(project.id, true);
      const opps = await api.opportunities.list(project.id);
      onAnalyzed(opps.items);
      setBusy(false);
    } catch (e) { fail(e, '分析知识库失败'); }
  };

  return (
    <div className="qc-step">
      <Field label="项目">
        <div className="qc-project-row">
          <select value={project?.id ?? ''} onChange={(e) => {
            const p = projects.find((x) => x.id === e.target.value);
            if (p) onProjectChosen(p);
          }}>
            <option value="" disabled>选择已有项目…</option>
            {projects.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}
          </select>
        </div>
      </Field>

      <Field label="或新建项目">
        <div className="qc-project-row">
          <input value={newName} onChange={(e) => setNewName(e.target.value)} placeholder="项目名称" />
          <Button variant="secondary" loading={busy} disabled={!newName.trim()} onClick={() => void createProject()}>新建</Button>
        </div>
      </Field>

      {project && (
        <>
          <details className="qc-advanced">
            <summary>项目设置</summary>
            <div className="qc-advanced-grid">
              <Field label="重命名">
                <div className="qc-project-row">
                  <input value={renaming} onChange={(e) => setRenaming(e.target.value)} />
                  <Button variant="ghost" loading={busy} onClick={() => void rename()}>保存</Button>
                </div>
              </Field>
              <Button variant="ghost" icon={<Trash2 size={15} />} onClick={() => setConfirmDelete(true)}>删除项目</Button>
            </div>
          </details>

          <Field label="知识文件">
            <ul className="qc-file-list">
              {files.map((f) => (
                <li key={f.id}>
                  <span>{f.name}<small className="qc-hint"> · {f.category ?? '未分类'}</small></span>
                  <Button variant="ghost" icon={<Trash2 size={14} />} onClick={() => void removeFile(f.id)}>删除</Button>
                </li>
              ))}
              {files.length === 0 && <li className="qc-hint">还没有知识文件</li>}
            </ul>
          </Field>

          <Field label="上传知识文件（.md / .txt，可多选）">
            <input type="file" accept=".md,.txt" multiple onChange={(e) => setPending(Array.from(e.target.files ?? []))} />
          </Field>
          <details className="qc-advanced">
            <summary>上传分类（默认：未分类 / 已知事实；改分类需删除后重传）</summary>
            <div className="qc-advanced-grid">
              <Field label="分类"><select value={category} onChange={(e) => setCategory(e.target.value)}>{CATEGORIES.map((c) => <option key={c}>{c}</option>)}</select></Field>
              <Field label="证据类型"><select value={kind} onChange={(e) => setKind(e.target.value)}>{KINDS.map((k) => <option key={k}>{k}</option>)}</select></Field>
            </div>
          </details>
          <Button variant="secondary" loading={busy} disabled={pending.length === 0} icon={<Upload size={15} />} onClick={() => void uploadAll()}>上传 {pending.length > 0 ? `(${pending.length})` : ''}</Button>

          <Button loading={busy} onClick={() => void analyze()}>分析知识库</Button>
        </>
      )}

      <Modal open={confirmDelete} title="删除项目" description={`确定删除「${project?.name}」？此操作不可撤销。`} onClose={() => setConfirmDelete(false)}
        footer={<><Button variant="ghost" onClick={() => setConfirmDelete(false)}>取消</Button><Button loading={busy} onClick={() => void doDelete()}>确认删除</Button></>}>
        <p className="qc-hint">项目及其知识、选题、历史都将被移除。</p>
      </Modal>
    </div>
  );
}
```

> 注:删项目后调用 `window.location.reload()` 是最省事的状态重置(壳的 project 指向已失效)。若不希望整页刷新,可改为 `onProjectChosen` 传一个空态 —— 但壳的 `onProjectChosen` 要求 `Project`,故本方案用 reload 简单可靠。实现者若想更优雅,可在壳里补一个 `onProjectDeleted` 回调清空 `project`;非必须。

- [ ] **Step 2: 壳接入 ProjectKnowledgeTab**

编辑 `apps/web/src/pages/QuickChannelPage.tsx`:import `ProjectKnowledgeTab`,把 `{activeTab === 'project' && <div>[项目 &amp; 知识]</div>}` 换成:

```tsx
{activeTab === 'project' && (
  <ProjectKnowledgeTab
    project={project} projects={projects} busy={busy} setBusy={setBusy} fail={fail}
    onProjectChosen={onProjectChosen} onAnalyzed={onAnalyzed}
  />
)}
```

- [ ] **Step 3: typecheck + build**

Run: `cd apps/web && npm run typecheck && npm run build`
Expected: 0 error;build 成功。

- [ ] **Step 4: 全量测试回归**

Run: `cd apps/web && npm test`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/quick/ProjectKnowledgeTab.tsx apps/web/src/pages/QuickChannelPage.tsx
git commit -m "feat(web): project & knowledge tab — CRUD, upload, analyze"
```

---

### Task 5: `TopicTab` — 选题卡片 + 换一批

**Files:**
- Create: `apps/web/src/components/quick/TopicTab.tsx`
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(接入子标签,替换 topic 占位)

**Interfaces:**
- Consumes: `useProjects` 无关;`api.opportunities.refresh`、`api.presets.list`;`Button`、`Field` from `../Ui`;壳回调 `onPickTopic(id, presets)`、`onAnalyzed(opps)`(换一批复用它刷新选题)、`setBusy`、`fail`。
- Produces:
  ```ts
  export function TopicTab(props: Props): JSX.Element;
  ```

**说明:** 无 DOM 测试。选题卡片只显示标题(`opportunity.title`),点选即调 `onPickTopic`。选一个选题时需先加载该项目的预设列表(`api.presets.list`)交给壳,壳据此点亮 config 并预选默认预设。「换一批」调 `api.opportunities.refresh(projectId, guidance?)`,把新列表交给 `onAnalyzed`(它会 setOpportunities 并停在 topic —— 注意 `onAnalyzed` 会切到 topic,本身即当前标签,无副作用)。

- [ ] **Step 1: 实现 TopicTab**

创建 `apps/web/src/components/quick/TopicTab.tsx`:

```tsx
import { useState } from 'react';
import { Field } from '../Ui';
import { Button } from '../Ui';
import { api } from '../../lib/api';
import type { ContentPreset, Project, TopicOpportunity } from '../../types';

interface Props {
  project: Project | null;
  opportunities: TopicOpportunity[];
  opportunityId: string;
  busy: boolean;
  setBusy: (b: boolean) => void;
  fail: (e: unknown, fallback: string) => void;
  onPickTopic: (id: string, presets: ContentPreset[]) => void;
  onAnalyzed: (opps: TopicOpportunity[]) => void;
}

export function TopicTab({ project, opportunities, opportunityId, busy, setBusy, fail, onPickTopic, onAnalyzed }: Props) {
  const [guidance, setGuidance] = useState('');
  const [showGuidance, setShowGuidance] = useState(false);

  const pick = async (id: string) => {
    if (!project) return;
    setBusy(true);
    try {
      const list = await api.presets.list(project.id);
      onPickTopic(id, list.items);
      setBusy(false);
    } catch (e) { fail(e, '读取预设失败'); }
  };

  const refresh = async () => {
    if (!project) return;
    setBusy(true);
    try {
      const opps = await api.opportunities.refresh(project.id, guidance.trim() || undefined);
      onAnalyzed(opps.items);
      setBusy(false);
    } catch (e) { fail(e, '换一批失败'); }
  };

  return (
    <div className="qc-step">
      <div className="qc-topic-list">
        {opportunities.map((o) => (
          <button key={o.id} type="button" className={`qc-topic${o.id === opportunityId ? ' selected' : ''}`} onClick={() => void pick(o.id)}>
            {o.title}
          </button>
        ))}
        {opportunities.length === 0 && <p className="qc-hint">没有可用选题，试试换一批。</p>}
      </div>

      <details className="qc-advanced" open={showGuidance} onToggle={(e) => setShowGuidance((e.target as HTMLDetailsElement).open)}>
        <summary>换一批（可选填引导词）</summary>
        <div className="qc-advanced-grid">
          <Field label="引导词（可留空）">
            <textarea value={guidance} rows={2} maxLength={600} placeholder="例如：多一些针对术后恢复期的选题" onChange={(e) => setGuidance(e.target.value)} />
          </Field>
        </div>
      </details>
      <Button variant="secondary" loading={busy} onClick={() => void refresh()}>换一批选题</Button>
    </div>
  );
}
```

- [ ] **Step 2: 壳接入 TopicTab**

编辑 `apps/web/src/pages/QuickChannelPage.tsx`:import `TopicTab`,把 `{activeTab === 'topic' && <div>[选题]</div>}` 换成:

```tsx
{activeTab === 'topic' && (
  <TopicTab
    project={project} opportunities={opportunities} opportunityId={opportunityId}
    busy={busy} setBusy={setBusy} fail={fail} onPickTopic={onPickTopic} onAnalyzed={onAnalyzed}
  />
)}
```

- [ ] **Step 3: typecheck + build**

Run: `cd apps/web && npm run typecheck && npm run build`
Expected: 0 error;build 成功。

- [ ] **Step 4: 全量测试回归**

Run: `cd apps/web && npm test`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/quick/TopicTab.tsx apps/web/src/pages/QuickChannelPage.tsx
git commit -m "feat(web): topic tab — title-only cards + refresh batch"
```

---

### Task 6: `ConfigTab` — 预设下拉 + 高级折叠 + 生成

**Files:**
- Create: `apps/web/src/components/quick/ConfigTab.tsx`
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(接入子标签,替换 config 占位)

**Interfaces:**
- Consumes: 壳 props/回调 `project`、`opportunityId`、`presets`、`presetId`、`overrides`、`busy`、`setBusy`、`fail`、`setPresetId`、`setOverrides`、`onGenerated`;`autoApproveAndGenerate`、`quickCandidateFields` from `../../lib/quick-generation`;`Button`、`Field` from `../Ui`;`CommentRichnessLevel` from `../../lib/simple-generation`。
- Produces:
  ```ts
  export function ConfigTab(props: Props): JSX.Element;
  ```

**说明:** 无 DOM 测试。ConfigTab 只做三件事:①必露的预设下拉(壳已在 `onPickTopic` 里预选默认 `isDefault` 预设,这里只读 `presetId`/`setPresetId`);②默认收起的高级折叠(七项设置全给默认值,不展开也能生成),写进 `overrides`;③「生成文案」调 `autoApproveAndGenerate({project, opportunityId, presetId, overrides})`,把候选经 `quickCandidateFields` 投影后交给 `onGenerated`。七项设置的常量值必须与既有频道一致(已核实 `simple-generation.ts`),**逐字使用**下方值,勿另造。

七项高级设置(默认值 = 各自第一项/balanced):
- 读者阶段 `audienceStage`：`discovering`「刚开始了解」/ `collecting`「正在收集信息」/ `comparing`「正在比较选择」/ `hesitating`「已了解但犹豫」/ `ready`「准备采取下一步」。
- 内容入口 `entryPoint`：`search`「主动搜索」/ `recommendation`「推荐流情景（来源未核实）」/ `profile`「主页浏览」/ `return_visit`「再次访问」。
- 评论信息丰富度 `commentRichness`（类型 `CommentRichnessLevel`）：`restrained` / `balanced`（默认）/ `dense`。
- 城市 `city`（datalist 用 `project.cities`）、医生 `doctor`、必含词 `mustInclude`、禁用词 `forbidden`：纯文本输入,留空即不覆盖。

- [ ] **Step 1: 实现 ConfigTab**

创建 `apps/web/src/components/quick/ConfigTab.tsx`:

```tsx
import { Button, Field } from '../Ui';
import { autoApproveAndGenerate, quickCandidateFields, type QuickCandidateView } from '../../lib/quick-generation';
import type { CommentRichnessLevel, SimpleSettingOverrides } from '../../lib/simple-generation';
import type { ContentPreset, Project } from '../../types';

const STAGES: Array<{ id: string; title: string }> = [
  { id: 'discovering', title: '刚开始了解' },
  { id: 'collecting', title: '正在收集信息' },
  { id: 'comparing', title: '正在比较选择' },
  { id: 'hesitating', title: '已了解但犹豫' },
  { id: 'ready', title: '准备采取下一步' },
];

const ENTRIES: Array<{ id: string; title: string }> = [
  { id: 'search', title: '主动搜索' },
  { id: 'recommendation', title: '推荐流情景（来源未核实）' },
  { id: 'profile', title: '主页浏览' },
  { id: 'return_visit', title: '再次访问' },
];

const RICHNESS: Array<{ id: CommentRichnessLevel; title: string }> = [
  { id: 'restrained', title: '克制' },
  { id: 'balanced', title: '均衡（默认）' },
  { id: 'dense', title: '丰富' },
];

interface Props {
  project: Project | null;
  opportunityId: string;
  presets: ContentPreset[];
  presetId: string | undefined;
  overrides: SimpleSettingOverrides;
  busy: boolean;
  setBusy: (b: boolean) => void;
  fail: (e: unknown, fallback: string) => void;
  setPresetId: (id: string | undefined) => void;
  setOverrides: (o: SimpleSettingOverrides) => void;
  onGenerated: (results: QuickCandidateView[]) => void;
}

export function ConfigTab({ project, opportunityId, presets, presetId, overrides, busy, setBusy, fail, setPresetId, setOverrides, onGenerated }: Props) {
  const patch = (p: Partial<SimpleSettingOverrides>) => setOverrides({ ...overrides, ...p });

  const generate = async () => {
    if (!project || !opportunityId) return;
    setBusy(true);
    try {
      const job = await autoApproveAndGenerate({ project, opportunityId, presetId, overrides });
      const results = (job.candidates ?? []).map(quickCandidateFields);
      onGenerated(results);
      setBusy(false);
    } catch (e) { fail(e, '生成失败'); }
  };

  return (
    <div className="qc-step">
      <Field label="内容预设" required>
        <select value={presetId ?? ''} onChange={(e) => setPresetId(e.target.value || undefined)}>
          {presets.map((p) => <option key={p.id} value={p.id}>{p.name}{p.isDefault ? '（默认）' : ''}</option>)}
        </select>
      </Field>

      <details className="qc-advanced">
        <summary>高级设置（可留空，全部有默认值）</summary>
        <div className="qc-advanced-grid">
          <Field label="读者阶段">
            <select value={overrides.audienceStage ?? ''} onChange={(e) => patch({ audienceStage: e.target.value || undefined })}>
              <option value="">默认</option>
              {STAGES.map((s) => <option key={s.id} value={s.id}>{s.title}</option>)}
            </select>
          </Field>
          <Field label="内容入口">
            <select value={overrides.entryPoint ?? ''} onChange={(e) => patch({ entryPoint: e.target.value || undefined })}>
              <option value="">默认</option>
              {ENTRIES.map((s) => <option key={s.id} value={s.id}>{s.title}</option>)}
            </select>
          </Field>
          <Field label="评论信息丰富度">
            <select value={overrides.commentRichness ?? ''} onChange={(e) => patch({ commentRichness: (e.target.value || undefined) as CommentRichnessLevel | undefined })}>
              <option value="">默认</option>
              {RICHNESS.map((s) => <option key={s.id} value={s.id}>{s.title}</option>)}
            </select>
          </Field>
          <Field label="城市">
            <input list="qc-cities" value={overrides.city ?? ''} onChange={(e) => patch({ city: e.target.value || undefined })} />
            <datalist id="qc-cities">{(project?.cities ?? []).map((c) => <option key={c} value={c} />)}</datalist>
          </Field>
          <Field label="医生"><input value={overrides.doctor ?? ''} onChange={(e) => patch({ doctor: e.target.value || undefined })} /></Field>
          <Field label="必含词"><input value={overrides.mustInclude ?? ''} onChange={(e) => patch({ mustInclude: e.target.value || undefined })} /></Field>
          <Field label="禁用词"><input value={overrides.forbidden ?? ''} onChange={(e) => patch({ forbidden: e.target.value || undefined })} /></Field>
        </div>
      </details>

      <Button loading={busy} disabled={!presetId} onClick={() => void generate()}>生成文案</Button>
    </div>
  );
}
```

- [ ] **Step 2: 壳接入 ConfigTab**

编辑 `apps/web/src/pages/QuickChannelPage.tsx`:import `ConfigTab`,把 `{activeTab === 'config' && <div>[配置]</div>}` 换成:

```tsx
{activeTab === 'config' && (
  <ConfigTab
    project={project} opportunityId={opportunityId} presets={presets} presetId={presetId}
    overrides={overrides} busy={busy} setBusy={setBusy} fail={fail}
    setPresetId={setPresetId} setOverrides={setOverrides} onGenerated={onGenerated}
  />
)}
```

- [ ] **Step 3: typecheck + build**

Run: `cd apps/web && npm run typecheck && npm run build`
Expected: 0 error;build 成功。

- [ ] **Step 4: 全量测试回归**

Run: `cd apps/web && npm test`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/quick/ConfigTab.tsx apps/web/src/pages/QuickChannelPage.tsx
git commit -m "feat(web): config tab — preset + advanced overrides + generate"
```

---

### Task 7: `ResultTab` — 包一层 QuickResult + 再来一批/换选题

**Files:**
- Create: `apps/web/src/components/quick/ResultTab.tsx`
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(接入子标签,替换 result 占位)

**Interfaces:**
- Consumes: 壳 props/回调 `project`、`opportunityId`、`presetId`、`overrides`、`results`、`busy`、`setBusy`、`fail`、`onGenerated`、`goTo`;既有 `QuickResult` from `../QuickResult`;`autoApproveAndGenerate`、`quickCandidateFields`、`QuickCandidateView` from `../../lib/quick-generation`。
- Produces:
  ```ts
  export function ResultTab(props: Props): JSX.Element;
  ```

**说明:** 无 DOM 测试。ResultTab 只把壳的 `results` 喂给既有 `QuickResult` 组件(3 版标签页 + 逐卡复制 + 复制全部,上一版已测),并提供两个动作:①「再来一批」= 用同一 `{project,opportunityId,presetId,overrides}` 重跑 `autoApproveAndGenerate`,新候选经 `quickCandidateFields` 投影后交给 `onGenerated`(与 ConfigTab 的 generate 逻辑一致,复用同一编排,不新造后端);②「换个选题」= `goTo('topic')`。这两个动作正是 `QuickResult` 已有的 `onRegenerate` / `onPickAnotherTopic` 两个 props。

- [ ] **Step 1: 实现 ResultTab**

创建 `apps/web/src/components/quick/ResultTab.tsx`:

```tsx
import { QuickResult } from '../QuickResult';
import { autoApproveAndGenerate, quickCandidateFields, type QuickCandidateView } from '../../lib/quick-generation';
import type { SimpleSettingOverrides } from '../../lib/simple-generation';
import type { QuickTab } from '../../lib/quick-channel-state';
import type { Project } from '../../types';

interface Props {
  project: Project | null;
  opportunityId: string;
  presetId: string | undefined;
  overrides: SimpleSettingOverrides;
  results: QuickCandidateView[];
  setBusy: (b: boolean) => void;
  fail: (e: unknown, fallback: string) => void;
  onGenerated: (results: QuickCandidateView[]) => void;
  goTo: (tab: QuickTab) => void;
}

export function ResultTab({ project, opportunityId, presetId, overrides, results, setBusy, fail, onGenerated, goTo }: Props) {
  const regenerate = async () => {
    if (!project || !opportunityId) return;
    setBusy(true);
    try {
      const job = await autoApproveAndGenerate({ project, opportunityId, presetId, overrides });
      onGenerated((job.candidates ?? []).map(quickCandidateFields));
      setBusy(false);
    } catch (e) { fail(e, '生成失败'); }
  };

  return (
    <div className="qc-step">
      <QuickResult
        candidates={results}
        onRegenerate={() => void regenerate()}
        onPickAnotherTopic={() => goTo('topic')}
      />
    </div>
  );
}
```

- [ ] **Step 2: 壳接入 ResultTab**

编辑 `apps/web/src/pages/QuickChannelPage.tsx`:import `ResultTab`,把 `{activeTab === 'result' && <div>[结果]</div>}` 换成:

```tsx
{activeTab === 'result' && (
  <ResultTab
    project={project} opportunityId={opportunityId} presetId={presetId} overrides={overrides}
    results={results} setBusy={setBusy} fail={fail} onGenerated={onGenerated} goTo={goTo}
  />
)}
```

- [ ] **Step 3: typecheck + build**

Run: `cd apps/web && npm run typecheck && npm run build`
Expected: 0 error;build 成功。

- [ ] **Step 4: 全量测试回归**

Run: `cd apps/web && npm test`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/quick/ResultTab.tsx apps/web/src/pages/QuickChannelPage.tsx
git commit -m "feat(web): result tab — wrap QuickResult + regenerate/pick-another"
```

---

### Task 8: `HistoryTab` — 历史产出 查看/复制/导出(无删除)

**Files:**
- Create: `apps/web/src/components/quick/HistoryTab.tsx`
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(接入子标签,替换 history 占位)

**Interfaces:**
- Consumes: 壳 props/回调 `project`、`history`、`busy`、`setBusy`、`fail`、`setHistory`;`api.generations.list`、`api.generations.exportUrl` from `../../lib/api`;`quickCandidateFields` from `../../lib/quick-generation`;`Button`、`useToast` from `../Ui`;`useEffect`、`useState` from `react`。
- Produces:
  ```ts
  export function HistoryTab(props: Props): JSX.Element;
  ```

**说明:** 无 DOM 测试。HistoryTab 进入时(或项目变化时)拉 `api.generations.list(project.id)` → `setHistory`。每条历史(`GenerationJob`)显示 topic/状态/时间;可展开看候选(候选经 `quickCandidateFields` 投影,只显示白名单文案,绝不渲染审计字段)、复制标题+正文、导出(`exportUrl(job.id, candidate.id, 'markdown')` 用 `window.open` 下载)。**无删除按钮**(现有 API 无 DELETE,硬约束)。`exportUrl` 第三参可选 `'markdown'|'json'|'docx'|'pdf'`,本频道给一个「导出 Markdown」即可,保持简化。

- [ ] **Step 1: 实现 HistoryTab**

创建 `apps/web/src/components/quick/HistoryTab.tsx`:

```tsx
import { useEffect, useState } from 'react';
import { Button, useToast } from '../Ui';
import { api } from '../../lib/api';
import { quickCandidateFields } from '../../lib/quick-generation';
import type { GenerationJob, Project } from '../../types';

interface Props {
  project: Project | null;
  history: GenerationJob[];
  busy: boolean;
  setBusy: (b: boolean) => void;
  fail: (e: unknown, fallback: string) => void;
  setHistory: (h: GenerationJob[]) => void;
}

export function HistoryTab({ project, history, setBusy, fail, setHistory }: Props) {
  const toast = useToast();
  const [expanded, setExpanded] = useState<string | null>(null);
  const projectId = project?.id;

  useEffect(() => {
    if (!projectId) return;
    let cancelled = false;
    setBusy(true);
    api.generations.list(projectId)
      .then((res) => { if (!cancelled) { setHistory(res.items); setBusy(false); } })
      .catch((e) => { if (!cancelled) fail(e, '加载历史失败'); });
    return () => { cancelled = true; };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [projectId]);

  const copy = async (text: string) => {
    try { await navigator.clipboard.writeText(text); toast.push('已复制'); }
    catch { toast.push('复制失败，请手动选择文本', 'error'); }
  };

  return (
    <div className="qc-step">
      <ul className="qc-history-list">
        {history.map((job) => {
          const open = expanded === job.id;
          const views = open ? (job.candidates ?? []).map(quickCandidateFields) : [];
          return (
            <li key={job.id} className="qc-history-item">
              <div className="qc-project-row">
                <strong>{job.topic || '未命名选题'}</strong>
                <small className="qc-hint">· {job.status}{job.createdAt ? ` · ${new Date(job.createdAt).toLocaleString()}` : ''}</small>
                <Button variant="ghost" onClick={() => setExpanded(open ? null : job.id)}>{open ? '收起' : '查看'}</Button>
              </div>
              {open && views.map((v) => (
                <div key={v.id} className="qc-history-candidate">
                  <p><strong>{v.title}</strong></p>
                  <p className="quick-body">{v.body}</p>
                  <div className="qc-project-row">
                    <Button variant="ghost" onClick={() => void copy(`${v.title}\n\n${v.body}`)}>复制</Button>
                    <Button variant="ghost" onClick={() => window.open(api.generations.exportUrl(job.id, v.id, 'markdown'), '_blank')}>导出 Markdown</Button>
                  </div>
                </div>
              ))}
              {open && views.length === 0 && <p className="qc-hint">该次生成没有可展示的候选。</p>}
            </li>
          );
        })}
        {history.length === 0 && <li className="qc-hint">还没有历史产出</li>}
      </ul>
    </div>
  );
}
```

> 注:`QuickCandidateView` 的 `id` 来自 `quickCandidateFields` 投影(上一版已保留候选 id);`exportUrl(job.id, v.id, 'markdown')` 的第二参是候选 id。若投影后的 `id` 与后端候选 id 不一致,实现者需核对 `quickCandidateFields` 是否透传原候选 `id`(上一版实现应已透传);如未透传,改用 `job.candidates` 原始 id 与投影按序对齐。

- [ ] **Step 2: 壳接入 HistoryTab + CSS 补一条候选样式**

编辑 `apps/web/src/pages/QuickChannelPage.tsx`:import `HistoryTab`,把 `{activeTab === 'history' && <div>[历史]</div>}` 换成:

```tsx
{activeTab === 'history' && (
  <HistoryTab
    project={project} history={history} busy={busy} setBusy={setBusy} fail={fail} setHistory={setHistory}
  />
)}
```

在 `apps/web/src/styles.css` 末尾追加一条:

```css
.qc-history-candidate { border-top: 1px dashed var(--border); margin-top: 10px; padding-top: 10px; }
```

- [ ] **Step 3: typecheck + build**

Run: `cd apps/web && npm run typecheck && npm run build`
Expected: 0 error;build 成功。

- [ ] **Step 4: 全量测试回归**

Run: `cd apps/web && npm test`
Expected: 全绿。

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/quick/HistoryTab.tsx apps/web/src/pages/QuickChannelPage.tsx apps/web/src/styles.css
git commit -m "feat(web): history tab — list/view/copy/export (no delete)"
```

---

### Task 9: 收尾清理 + 全链路验证

**Files:**
- Modify: `apps/web/src/pages/QuickChannelPage.tsx`(移除 Task 3 占位阶段临时删掉后又需要的 import 核对;确认所有子标签均已接入、无残留占位 `<div>`)

**Interfaces:**
- Consumes: 全部前序任务产物。本任务不产出新接口,只做一致性核对与人工验证。

**说明:** 前八个任务逐个接入子标签时会反复编辑 `QuickChannelPage.tsx` 的 import 与占位块。本任务统一核对壳文件:五个 `activeTab === …` 分支全部指向真实子标签组件(无残留 `<div>[…]</div>` 占位),import 无缺失/无未使用,`tabProps` 变量若最终未被使用则删除(壳是逐个把 props 显式传给各子标签的,`tabProps` 聚合对象可能是死代码——核对并清理)。然后跑完整验证。

- [ ] **Step 1: 壳文件一致性核对**

Read `apps/web/src/pages/QuickChannelPage.tsx`,确认:
- 五个分支分别渲染 `ProjectKnowledgeTab` / `TopicTab` / `ConfigTab` / `ResultTab` / `HistoryTab`,无占位 `<div>`。
- import 齐全且无未使用(`Button`/`api`/`quickCandidateFields` 若壳内未直接使用则不 import——它们在子标签内使用)。
- 若 `tabProps` 聚合对象未被任何分支使用,删除该声明(壳是显式逐个传 props)。

- [ ] **Step 2: typecheck + build + test 全绿**

Run: `cd apps/web && npm run typecheck && npm run build && npm test`
Expected: 0 error;build 成功;测试全绿。

- [ ] **Step 3: 人工浏览器全链路冒烟**

启动 dev(API + web),在浏览器走一遍(对照 spec「验证」节):
1. 进 `/quick` → 只有「项目 & 知识」标签可点,其余灰显带 tooltip。
2. 新建项目 → 上传 .md/.txt → 「分析知识库」→ 自动跳到「选题」并点亮。
3. 选一个选题 → 自动跳到「配置」并点亮;展开高级设置改几项。
4. 「生成文案」→ 加载态 → 自动跳到「结果」;检查 3 版标签页、逐卡复制、复制全部。
5. 「再来一批」重跑;「换个选题」切回选题页。
6. 「历史」标签 → 列出本项目历次生成 → 展开查看候选、复制、导出 Markdown。
7. 切回已填标签(项目/选题/配置)确认状态不丢。
8. 重命名项目、换项目 → 确认下游(选题/结果)被清空。
9. 全程无任何审计字段(score/diagnostics/sources/validation);其他频道功能不受影响。

- [ ] **Step 4: 若有清理改动则 Commit**

```bash
git add apps/web/src/pages/QuickChannelPage.tsx
git commit -m "chore(web): quick-channel shell cleanup — remove placeholders / dead aggregate"
```

（若 Step 1 核对发现壳已干净、无改动，跳过本步。）

---
